# Exams Screen — CONTEXT.md

## Last Updated
Task M5 — `lib/ui/screens/school_dashboard/exams/event_detail_screen.dart` (new): `EventDetailScreen` (`StatelessWidget`) displays the list of `ScheduledPaper` items for a single exam event. Each paper is rendered by `_ScheduledPaperCard` (`StatefulWidget`), which initialises `PaperGenerationPhase`, `totalStudents`, and `generatedCount` from the passed-in `ScheduledPaper`, then starts a `Timer.periodic(5 s)` when `phase == generating`. The timer calls `paperService.getStudentPapersStatus(paperId: paper.scheduleId, accessToken: accessToken)` and updates `_phase`/`_total`/`_generated` via `setState`; the timer is cancelled once phase reaches `complete` or `failed`. `_PhaseChip` (`StatelessWidget`) renders a `Chip` in grey/amber/green/red with a `CircularProgressIndicator` for the generating state. A "Preview" `TextButton.icon` is shown only when phase is `complete` — currently a TODO stub pending Task F2 (`PaperPreviewPage` at `../academics/paper_preview_page.dart`). Empty-state widget: `Icon(Icons.event_available_rounded, size: 64)` + "No papers scheduled" text. Back button uses `Icons.chevron_left_rounded`. Card `BorderRadius` = 8.

Task C03 — `lib/database/tables/papers.dart`: added `IntColumn get timeAllowedMinutes => integer().nullable()()` and `TextColumn get customInstructions => text().nullable()()` at end of `Papers` class. `lib/database/database.dart`: schema version bumped 10 → **11**; `from < 11` migration adds `time_allowed_minutes INTEGER` and `custom_instructions TEXT` nullable columns to `papers` via `ALTER TABLE`. `lib/database/daos/exams_grades_dao.dart`: `createPaper` gains optional `int? timeAllowedMinutes` and `String? customInstructions` params — merges them into the `PapersCompanion` via `copyWith` and sets `payload.timeAllowedMinutes`/`payload.instructions` on the `CreatePaperPayload` proto when non-null; `updatePaper` gains the same two optional params — merges into `resolvedChanges` companion and sets on `UpdatePaperPayload` proto. `lib/sync/delta_writer.dart`: `_applyPapers` updated — both the NULL-paper (delete-then-insert) and non-NULL-paper (ON CONFLICT upsert) branches now include `time_allowed_minutes` and `custom_instructions` columns in the `INSERT INTO papers (...)` statement; the ON CONFLICT `DO UPDATE SET` clause also updates both new columns; values mapped from `row.hasTimeAllowedMinutes()` / `row.hasInstructions()`. `lib/ui/screens/school_dashboard/exams/create_paper_sheet.dart`: `_CreatePaperSheetState` gains `int? _timeAllowedMinutes` state field and `TextEditingController _instructionsCtrl`; `dispose()` disposes `_instructionsCtrl`; `_formatMinutes(int m)` helper added (formats minutes as "X hours Y minutes"); two new builder methods added: `_buildTimeAllowedRow` (numeric `TextFormField` with `FilteringTextInputFormatter.digitsOnly`, hints the formatted duration when a value is set) and `_buildInstructionsRow` (multiline `TextFormField`, `minLines: 2`, `maxLines: 5`, hint "Leave blank to use default instructions"); both rows inserted in `_buildContent` after the time picker row; `_save()` passes `timeAllowedMinutes: _timeAllowedMinutes` and `customInstructions: _instructionsCtrl.text.trim()` (null if empty) to `createPaper`.

Task C02 — `lib/models/paper_generation.dart`: `PaperPdf` gains `markingSchemeUrl` (`String?`) and `markingSchemeExpiry` (`DateTime?`) fields; `fromProto` reads them from `FinalizePaperResponse` via `hasMarkingSchemeUrl()`/`hasMarkingSchemeExpiry()`; `fromGetPdfProto` leaves them null (unchanged endpoint). `lib/ui/screens/school_dashboard/academics/paper_pdf_viewer.dart`: `downloadAndOpenPdf` gains optional `title` param (default `'Exam Paper'`) used in SnackBar messages; new `downloadAndOpenDirectUrl({url, context, title})` function added for downloading a PDF from a known URL without an API call (used for marking scheme). `lib/ui/screens/school_dashboard/academics/paper_generation_page.dart`: "Done" button now calls `Navigator.of(context).pop(_paperPdf)` so the detail page receives the `PaperPdf` result; conditional "View Marking Scheme" `_FinalizeActionButton` added in the success area (shown when `_paperPdf?.markingSchemeUrl != null`). `lib/ui/screens/school_dashboard/academics/paper_detail_page.dart`: `PaperPdf? _paperPdf` field added to `_PaperDetailPageState`; `_PaperHeader` widget gains optional `paperPdf` and `onPaperGenerated` params; Generate Paper navigation chains `.then()` to call `onPaperGenerated` with the returned `PaperPdf`; `_PaperHeaderState` gains `_openMarkingScheme(String url)` method and a conditional "View Marking Scheme" `IconButton` (`Icons.fact_check_outlined`, `cs.secondary`) in the action row after the print button.

Task C04 — `lib/models/paper_generation.dart`: `PaperQuestion` gains `final String? section` field (optional, `'A'`/`'B'`/`'C'`/`null`); `fromProto` reads `proto.hasSection() ? proto.section : null`. `lib/services/question_bank.dart`: new `setPaperQuestionSection` method added to `QuestionBankService` (wraps `SetPaperQuestionSection` RPC; `section: null` clears the assignment). `lib/ui/screens/school_dashboard/academics/paper_generation_page.dart`: section chip added to the question card header (between marks pill and Spacer); tapping opens `_SectionPickerSheet` (new `StatelessWidget` at bottom of file) with `None / Section A / B / C` choices; `_showSectionPicker` and `_setSectionForQuestion` methods added to `_PaperGenerationPageState`; `_setSectionForQuestion` does an optimistic update then calls `setPaperQuestionSection`, reverting on error with a SnackBar; `_saveEdit` updated to preserve `section` when reconstructing `PaperQuestion` after a text/marks edit.


## Directory
`lib/ui/screens/school_dashboard/exams/`

## Files

| File | Status | Description |
|---|---|---|
| `event_detail_screen.dart` | ✅ Active | `EventDetailScreen` — read-only detail view for a single exam event. Displays a list of `ScheduledPaper` cards with live `PaperGenerationPhase` polling (5-second `Timer.periodic` per generating card). `_ScheduledPaperCard` shows subject, grade, stream, date/time, duration, invigilator (or warning), and a `_PhaseChip`. Preview button is a TODO stub until Task F2 (`PaperPreviewPage` at `../academics/paper_preview_page.dart`) lands. |
| `exams_grades_screen.dart` | ✅ Active | Top-level entry point for Exams & Grades section. Contains `ExamsGradesScreen` (entry widget), `_ExamsShell` (navigation state: list → exam detail → paper detail), `_ExamGroupDetailView`, `_PaperDetailView`, `_CreatePaperSheet`, `_AddStreamForm`, `_AddGradeToExamForm`, plus many helper/picker widgets. ~9800 lines. |
| `paper_detail_page.dart` | ✅ Active | Paper detail page with grade entry, AI marking, scheme upload, and — as of Task 01 — **"Generate Paper" button (⚡ icon, visible when `canManage && isPending`) now navigates to `PaperGenerationPage`** via `Navigator.push(MaterialPageRoute(...))`. Previously showed a "coming soon" SnackBar. Import `paper_generation_page.dart` was added to wire this up. **Print button broadened (Task 03):** `_PaperHeaderState.build()` print button condition changed from `isDoneOrMarked` to `!isPending` so the "View / Print Paper" button (tooltip updated) is shown for `progress`, `done`, and `marked` statuses — not just `done`/`marked`. Unused `isDoneOrMarked` local variable removed. **Marking scheme button (Task C02):** `_PaperDetailPageState` gains `PaperPdf? _paperPdf` field and imports `paper_generation.dart`. `_PaperHeader` gains optional `paperPdf` (`PaperPdf?`) and `onPaperGenerated` (`void Function(PaperPdf)?`) params. The Generate Paper navigation now chains `.then((result) { if (result is PaperPdf ...) onPaperGenerated?.call(result); })` so the returned `_paperPdf` from finalization is stored in `_PaperDetailPageState`. `_PaperHeaderState` gains `_openMarkingScheme(String url)` method (calls `downloadAndOpenDirectUrl`) and a conditional `Icons.fact_check_outlined` `IconButton` (`cs.secondary`, 28×28) shown after the print button when `widget.paperPdf?.markingSchemeUrl != null`. |
| `paper_generation_page.dart` | ✅ Active | 3-step AI paper generation wizard: Step 0 = topic mark allocation (loads `CatalogDao.watchTopicsBySubjectAndGrade`), Step 1 = review/edit/regenerate questions (calls `QuestionBankService.generatePaper`, `regenerateQuestion`, `editPaperQuestion`), Step 2 = finalize + download PDF (calls `QuestionBankService.finalizePaper`, `getPaperPdf`). Fully implemented. Entry point: `PaperGenerationPage(schoolId, examId, subjectId, paperId?, grade, stream?, subjectName, examName)`. **UX improvements (Task 01 fix):** (1) Generation errors now show as inline `_GenerationErrorBanner` (red container above footer) instead of a SnackBar — `_generateError` state field, cleared on each new attempt, set via `_friendlyGenerateError(GrpcError)` which maps `failedPrecondition`/`notFound`/`unauthenticated` to human-readable strings. (2) Loading overlay (`Stack` + `Positioned.fill`, semi-transparent surface + spinner + "Generating questions…" label) covers the topic list while `_isGenerating` is true. (3) `addPostFrameCallback` accumulation fixed: `_lastSyncedTopics` field tracks the last synced topic list; the callback is only registered when `topicsChanged` is true (length or ID mismatch), preventing redundant `setState` calls on every stream rebuild. **Status auto-advance (Task 03):** `_finalize()` `Ok` case now calls `ExamsGradesDao(db).updatePaper(...)` with `PapersCompanion(status: Value(PaperStatus.progress), updated: Value(now))` after `finalizePaper` succeeds, advancing the paper from `pending` → `progress` so the print button appears on `paper_detail_page.dart` immediately after the wizard exits. The update is non-fatal (caught, logged via `debugPrint`). New imports added: `package:drift/drift.dart` (show `Value`), `database/daos/exams_grades_dao.dart`, `database/tables/enums.dart`. **Wizard state restore (Task 04):** `_tryRestoreExistingQuestions()` is called at the end of `initState`; it calls `QuestionBankService.getPaperQuestions` and, if questions are returned, calls `_buildQuestionTopicMap` then `setState` to set `_generatedQuestions` and advance `_currentStep` to `1` — skipping straight to the review step. On empty response or network error, the wizard stays on the allocation step (step 0). **Section assignment (Task C04):** Section chip added to each question card header in the review step (between the marks pill and the trailing action buttons). Chip shows `"Section A/B/C"` (using `cs.primaryContainer` background) or `"No section"` (using `cs.surfaceContainerHighest`). Tapping opens `_SectionPickerSheet` bottom sheet. `_setSectionForQuestion` applies an optimistic state update, calls `QuestionBankService.setPaperQuestionSection`, and reverts + shows a SnackBar on failure. `_saveEdit` now preserves `section` when reconstructing `PaperQuestion` after a text/marks/rubric edit. **Marking scheme button (Task C02):** "Done" button in the finalize step success area now calls `Navigator.of(context).pop(_paperPdf)` so the caller (`paper_detail_page.dart`) receives the `PaperPdf` result. A conditional "View Marking Scheme" `_FinalizeActionButton` (`Icons.fact_check_outlined`, `cs.secondary`/`cs.onSecondary`) is shown between the "Download / Print PDF" and "Done" buttons when `_paperPdf?.markingSchemeUrl != null`; it calls `downloadAndOpenDirectUrl`. |
| `paper_pdf_viewer.dart` | ✅ Active | Two top-level async functions: **`downloadAndOpenPdf`** — fetches a presigned PDF URL via `QuestionBankService.getPaperPdf`, downloads bytes via `HttpClient`, saves to temp dir, and opens with the system viewer (`xdg-open`/`open`/`start`). Now accepts an optional `title` param (default `'Exam Paper'`) used in SnackBar loading/error messages. On Android/iOS, opens the system share sheet via `Share.shareXFiles` (`share_plus` ^10.0.0). **`downloadAndOpenDirectUrl`** *(new, Task C02)* — same download/open logic but takes a direct `url` string without making a gRPC call first. Used for marking scheme PDFs where the URL is already known from `FinalizePaperResponse`. Accepts `title` param (default `'PDF'`). Used by `_PaperHeaderState._openMarkingScheme` and the marking scheme button in `paper_generation_page.dart`. |
| `exam_list_view.dart` | ✅ Active | `ExamsListView` — main exam list with search/filter toolbar. Accepts `schoolId`, `year`, `term`, `schoolContext`, `config`, `subjectNames`, `entry`, `onExamTap`. Uses `ExamsGradesDao.watchExamGroups()` → `Stream<List<ExamGroup>>`. **Stream caching (Task C3):** The Drift stream is cached in `_examStream` state field, initialized in `initState()`, and rebuilt in `didUpdateWidget()` only when `schoolId`, `year`, `term`, or `entry` change. Search query and type filter changes trigger `setState` but do NOT rebuild the stream — filtering is applied client-side via `_applyFilters()` on the snapshot data. This prevents re-subscription churn (unsubscribe→resubscribe with loading spinner flash) on every keystroke or filter toggle. Teacher scoping: teachers without `exams.read` permission only see exams they participate in (creator, invigilator, or subject teacher) via `teacherFilter` param. |
| `exam_creation_page.dart` | ✅ Active | Full-screen exam creation flow. |

## Key Exports / Entry Points

- **`ExamsGradesScreen`** — `StatelessWidget`, requires `SchoolContext`. Mounted from the dashboard shell under "Exams & Grades" (teacher) or "Academics → Exams" (owner/staff).
- **`EventDetailScreen`** — `StatelessWidget`. Entry: `EventDetailScreen(eventId, eventName, schoolId, papers: [...], eventType: 'exam', term: 1, year: 2025)`. Navigated to from the event list.

## Role-Based Access Guards

- **`ExamsGradesScreen.build()`** checks `schoolContext.currentEntry.value`:
  - `StudentEntry` or `GuardianEntry` → returns `_RestrictedAccessState` (lock icon + "Not available" message). Defense-in-depth — nav routing should also prevent these roles from reaching this screen.
  - All other roles (Owner, Teacher, Staff) → proceeds to term check and then `_ExamsShell`.

## Dependencies

- `SchoolContext` (from `lib/models/school_context.dart`) — provides `currentEntry`, `membership`, `permissions`
- `ActiveTermProvider` / `ActiveTermContext` — provides current term for exam queries
- `ExamsGradesDao` — `watchExamGroups(schoolId, year, term)`
- `CatalogDao`, `SubjectsDao`, `MembersDao` — supporting data for subjects, teachers
- `SchoolConfig` — grade/stream/curriculum configuration
- `ExamGroup` model — grouped exam data
- `ScheduledPaper`, `PaperGenerationPhase` (from `lib/models/event.dart`) — used by `EventDetailScreen`
- `paperService` global + `accessToken` global (from `lib/client.dart`) — used by `_ScheduledPaperCard` for polling

## Conventions

- Navigation within the exams section is managed by `_ExamsShell` via `_ExamsView` enum (`list`, `examDetail`, `paperDetail`).
- Permission checks (`_canCreateExam`, `_canEditExam`, `_canDeleteExam`, `_canMarkGrades`) use `SchoolContext.permissions.can(Resource.xxx, Action.yyy)`.
- **Teacher scoping (B03):** Teachers with `exams.update`/`exams.delete` can only edit/delete exams they created (`exam.teacher == teacherId`) or exams whose grades overlap with their `subject_teachers` assignments. Owners and staff with proper permissions can edit/delete any exam. The `_isTeacherExamOwnerOrAssigned` getter in `_ExamGroupDetailViewState` implements this guard.
- **`_canGradeContent` teacher fallback (C2):** In `paper_detail_page.dart`, `_canGradeContent` now mirrors `_canProgressStatus` — if the user lacks explicit `grades.read`/`grades.mark` RBAC permissions, a `TeacherEntry` user is still granted grade access if they created the exam (`_exam.teacher == userId`), are the paper's invigilator (`_paper.invigilator == userId`), or teach the paper's subject to its grade/stream (checked via `_teacherSubjects`). This ensures a teacher who can advance paper status can also enter grades.
- **Grade picker scoping (B03):** `_AddGradeToExamForm` and `_AddStreamForm` accept an optional `teacherAssignedGrades` parameter (`Set<int>?`). When non-null (i.e. for `TeacherEntry`), only grades in that set are shown in the grade picker. The set is derived from `_teacherSubjects` (loaded via `MembersDao.watchTeacherSubjectsForTerm`).
- Visual style follows `AGENT.md` §21: data-table rows, `AppTheme` border radii, dual box-shadow modals.