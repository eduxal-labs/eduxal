# ui/screens/school_dashboard/ — School Dashboard Context

> The school dashboard is the main app surface where users interact with school-scoped data.
> It is a responsive layout that adapts between desktop (sidebar navigation) and mobile (top tab navigation).

## Overview

This directory contains **1 shell screen file** and **8 subdirectories**, each representing a section of the school dashboard. All data displayed here is scoped to a specific school via `SchoolContext` and a specific academic term via `ActiveTermContext`.

## Entry Point

### `school_dashboard_screen.dart`
- **Status:** ✅ Complete
- **Widget:** `SchoolDashboardScreen` (StatefulWidget)
- **Purpose:** The responsive shell that hosts all school-scoped pages. Provides `SchoolContext` and `ActiveTermContext` to the widget tree. User menu avatar shows a badge with failed sync log count (via `LogsDao.watchFailedLogCount`). Notifications action navigates to `NotificationsPage`.
- **Layout:**
  - **Desktop (wide):** Fixed left navigation sidebar + content area. Sidebar shows nav items as icon+label rows with active indicator.
  - **Mobile (narrow):** Scrollable horizontal tabs at the top using a custom `_PillTabStrip` (icon-only, elevated, with shadow indicator).
- **Key responsibilities:**
  - Receives `SchoolMembership` + initial `MembershipEntry` from the home screen.
  - Creates `SchoolContext` with computed `SchoolPermissions`.
  - Creates `ActiveTermContext` from `TermsDao.watchTerms(schoolId)`.
  - Hosts the **Role Switcher** in the top AppBar (reads `SchoolContext.membership.entries`).
  - Hosts the **Term Selector** chip in the AppBar (reads `ActiveTermContext`).
  - Routes to content pages based on selected nav item.
  - Disposes `SchoolContext` and `ActiveTermContext` on pop.
- **Navigation items by role:**
  - **Owner:** Overview | Academics | Exams & Grades | Members | Finance | Announcements | Timetable | Roles
  - **Teacher:** Overview | My Classes | Exams & Grades | Timetable | Attendance | Announcements
  - **Staff:** Overview | Members | Finance | Attendance | Announcements
  - **Student:** Overview | Timetable | Grades | Attendance
  - **Guardian:** Overview | Attendance | Grades | Finance (read-only)
- **Dependencies:** `models/school_context.dart`, `models/school_permissions.dart`, `models/membership.dart`, `models/active_term_context.dart`, `client.dart` (DAOs, `logsDao`), `database/daos/terms_dao.dart`, `database/daos/school_scopes_dao.dart`, `ui/widgets/term_selector_chip.dart`, `ui/widgets/active_term_provider.dart`, `ui/screens/notifications/notifications_page.dart`

## Subdirectories

### `overview/`
| File | Widget | Status | Description |
|---|---|---|---|
| `overview_screen.dart` | `OverviewScreen` | ✅ Complete | Role-dispatched school dashboard landing page — the first page a user sees when entering a school dashboard. Switches layout based on `SchoolContext.currentEntry.role` via `ValueListenableBuilder<MembershipEntry>`. **Owner overview** (`_OwnerOverview`): `_SchoolIdentityCard` (school name, motto, establishment year, school icon), Quick Stats row (2×2 grid of `_StatCard` widgets: Students count via `MembersDao.watchStudents`, Teachers count via `MembersDao.watchTeachers`, Staff count via `MembersDao.watchStaff`, Classes count via `EnrollmentsDao.watchPopulatedClasses`), Current Term info card (`_TermInfoCard` — year/term label, date range, days remaining badge), Recent Announcements (last 3 via `AnnouncementsDao.watchAllAnnouncements`). **Teacher overview** (`_TeacherOverview`): `_WelcomeCard` (first name greeting + "Teacher" subtitle), Today's Schedule (`_TeacherTodaySchedule` — filters `TimetableDao.watchTeacherTimetable` to current day of week, shows `_TimetableSlotCard` per slot with grade/stream, subject, time range, color accent bar), Quick Stats (My Subjects count from `TeacherEntry.subjectCount`, My Exams count filtered from `ExamsGradesDao.watchExamsForTerm`), Recent Announcements (last 3 filtered by `AudienceBits.teachers`). **Staff overview** (`_StaffOverview`): `_WelcomeCard` (name + school name), Quick Stats (2×2 grid: Students count, Invoices count, Collection rate %, Pending invoices — all from `MembersDao.watchStudents` + `FinanceDao.watchTermFinanceSummary`), Recent Announcements (last 3 filtered by `AudienceBits.staff`). **Student overview** (`_StudentOverview`): `_WelcomeCard` (student name + "Student"), Enrollment info badge (`_StudentEnrollmentInfo` — grade/stream/ADM from `EnrollmentsDao.watchStudentEnrollment`), Today's Schedule (`_StudentTodaySchedule` — resolves enrollment then watches `TimetableDao.watchClassTimetable` for today, shows `_StudentTimetableSlotCard` with subject, teacher name, time), Recent Grades (`_StudentRecentGrades` — last 3 exams from `ExamsGradesDao.watchStudentGrades`, subject-level totals, score/total + percentage badge), Attendance summary (`_StudentAttendanceSummary` — `AttendanceDao.watchStudentAttendanceSummary`, stacked proportion bar + present/absent/leave dots + percentage), Recent Announcements (filtered by `AudienceBits.students`). **Guardian overview** (`_GuardianOverview`): `_WelcomeCard` (guardian name + "Guardian"), Ward info card (`_WardInfoCard` — ward name, ADM, initials avatar, enrollment badge from `EnrollmentsDao.watchStudentEnrollment`), Ward Attendance (reuses `_StudentAttendanceSummary`), Ward Recent Grades (reuses `_StudentRecentGrades`), Finance summary (`_GuardianFinanceSummary` — `FinanceDao.watchStudentFinanceSummary`, total invoiced / paid / balance with color-coded amounts), Recent Announcements (filtered by `AudienceBits.guardians`). **Shared widgets:** `_SchoolIdentityCard`, `_WelcomeCard`, `_TermInfoCard`, `_SectionTitle`, `_StatCard` (icon + value + label with tint color), `_AttendanceBar` (stacked 6px proportion bar + `_AttendanceDot` stats), `_PercentBadge` (tinted container with color-coded percentage), `_RecentAnnouncements` (last 3, supports owner mode or audience-filtered), `_EmptyCard` (icon + message placeholder), `_LoadingShimmer` (60px container with centered spinner), `_FinanceRow` (label/value row), `_WardInfoCard`, `_GuardianFinanceSummary`, `_TimetableSlotCard`, `_StudentTimetableSlotCard`. **Helpers:** `_currentDayOfWeek` (DateTime.weekday → DayOfWeek enum), `_fmtTime` (seconds since midnight → "HH:MM"), `_fmtDateFromSeconds` (BigInt seconds-since-epoch → "12 Jan 2025"), `_initials`, `_subjectColor` (15-color palette), `_pctColor` (green ≥70 / amber ≥40 / red <40), `_fmtCurrency` (comma-separated whole number). |

**Data sources:** `MembersDao.watchStudents(schoolId)`, `MembersDao.watchTeachers(schoolId)`, `MembersDao.watchStaff(schoolId)`, `EnrollmentsDao.watchPopulatedClasses(...)`, `EnrollmentsDao.watchStudentEnrollment(...)`, `TimetableDao.watchTeacherTimetable(...)`, `TimetableDao.watchClassTimetable(...)`, `ExamsGradesDao.watchExamsForTerm(...)`, `ExamsGradesDao.watchStudentGrades(...)`, `AttendanceDao.watchStudentAttendanceSummary(...)`, `FinanceDao.watchTermFinanceSummary(...)`, `FinanceDao.watchStudentFinanceSummary(...)`, `AnnouncementsDao.watchAllAnnouncements(...)`, `AnnouncementsDao.watchAnnouncementsForAudience(...)`
**Dependencies:** `database/daos/announcements_dao.dart`, `database/daos/attendance_dao.dart`, `database/daos/enrollments_dao.dart`, `database/daos/exams_grades_dao.dart`, `database/daos/finance_dao.dart`, `database/daos/members_dao.dart`, `database/daos/timetable_dao.dart`, `database/tables/enums.dart`, `models/active_term_context.dart`, `models/membership.dart`, `models/school_context.dart`, `ui/widgets/active_term_provider.dart`, `client.dart`

---

### `academics/`
| File | Widget | Status | Description |
|---|---|---|---|
| `academics_screen.dart` | `AcademicsScreen` | ✅ Complete | Grade/stream tree view. Scrollable list of grade cards from `SchoolConfig`. Each card shows grade label + stream chips. FAB to add grades (standardised to `FloatingActionButton.small` matching Roles page). Header simplified — Departments and Exams action links removed (Departments moved to Members tab, Exams is its own owner nav item). |
| `grade_detail_page.dart` | `GradeDetailPage` | ✅ Complete (scaffold) | Two-layer tab architecture. **Layer 1 — Stream Tabs:** scrollable `EduTabBar` with "Comparisons" + one tab per stream from `GradeConfig.streams`. **Layer 2 — Content Tabs:** scrollable `EduTabBar` with Students · Exams · Subjects · Attendance · Timetable · Lessons · Teachers (visible only when a specific stream is selected, hidden on Comparisons). **Comparisons tab** now wired to `ComparisonsTab` (from `tabs/comparisons_tab.dart`); other content areas are placeholder widgets ("coming soon"). Contextual `FloatingActionButton.small` appears on stream tabs only (hidden on Comparisons) with a bottom-sheet action picker: Add Student, Assign Class Teacher, Assign Subject Teacher. Uses `TickerProviderStateMixin` for two `TabController`s. Reads `ActiveTermProvider.of(context)` for year/term access. Exposes `_selectedStream`, `_selectedStreamCode`, `_selectedStreamName` getters for downstream use. Old `_FilterChip` class deleted entirely. **Deep-link params (Task 17b):** accepts optional `initialStreamIndex` (int?, 0=Comparisons, 1+=stream) and `initialContentTabIndex` (int?, 0=Students…3=Attendance…6=Teachers) — both clamped to valid range and wired into `TabController.initialIndex`. Used by `AttendanceScreen` to navigate directly to a stream's Attendance tab. **FAB — Add Student (Task 19):** "Add Student" action opens `_StudentEnrollSheet` (`DraggableScrollableSheet`-style bottom sheet, max 75% height). Searchable student picker via `MembersDao(db).searchStudents(schoolId, query)`. Each result row shows: `_initials` avatar, student name, ADM number, and enrollment status for the current term via `EnrollmentsDao(db).getStudentEnrollment(...)`. Three statuses: (a) "Not enrolled" (muted) — tap enrolls directly; (b) "Already enrolled" (primary tint, check icon, disabled) — student is in this exact class; (c) "Currently in {Grade} · {Stream}" (amber warning, swap icon) — tap shows a confirmation `AlertDialog` ("Transfer student?") before re-enrolling. Enrollment calls `EnrollmentsDao.enrollStudent(...)` which handles transfer (delete old + insert new) in one transaction. Shows snackbar confirmation on success. Sheet title: "Add Student" + subtitle "Enroll into {gradeLabel} · {streamName}". Private types: `_StudentEnrollSheet` (stateful), `_StudentEnrollCandidate` (model with `StudentsData`, `_EnrollStatus`, `statusLabel`), `_EnrollStatus` enum (`notEnrolled`, `alreadyHere`, `otherClass`). Uses `cache.currentUser?.user.id` for `accountId`. Grade labels resolved via `gradeLabelsFor(curriculumType)`. **FAB — Assign Class Teacher (Task 20):** "Assign Class Teacher" action opens `_ClassTeacherPickerSheet` (bottom sheet, max 75% height). Loads all teachers for the school via `MembersDao(db).watchTeachers(schoolId)` (first snapshot), resolves user data for each via `MembersDao.findUserById`, and checks the current active class teacher via `SubjectsDao(db).watchActiveClassTeacher(...)` (first snapshot). Teacher list is searchable by name (in-memory filter). Each row: `_initials` avatar (primary-tinted if active), teacher name, subtitle row with role + department info (or "Teacher" fallback) + "Current class teacher" label (primary tint) for the active teacher. Active class teacher row is disabled with a check icon. On tap: calls `SubjectsDao.assignClassTeacher(...)` which closes the previous assignment and inserts the new one in a single transaction (with log entries). Shows snackbar: "{name} assigned as class teacher for {gradeLabel} · {streamName}". Sheet title: "Assign Class Teacher" + subtitle "{gradeLabel} · {streamName}". Private types: `_ClassTeacherPickerSheet` (stateful), `_TeacherCandidate` (model with `TeachersData`, `UsersData`, `isCurrentClassTeacher`). Uses `cache.currentUser?.user.id` for `accountId`. Added import: `subjects_dao.dart`. **FAB — Assign Subject Teacher (Task 21):** "Assign Subject Teacher" action opens `_SubjectTeacherPickerSheet` (bottom sheet, max 75% height). **Two-step picker:** Step 0 lists all valid subjects for the grade's curriculum level (derived via inline grade-to-level mapping + `levelsFor` + `subjectLabel` from `curriculum_levels.dart`). Each subject row: book icon (primary-tinted if assigned), subject name, current teacher name or "Not assigned". All subjects are tappable (assigned subjects show current teacher but can be reassigned). Step 1: teacher picker (identical pattern to class teacher — `MembersDao.watchTeachers` + `findUserById`, searchable by name, currently assigned teacher shown with "Currently assigned" label and disabled). Back arrow returns to Step 0 (clears search + resets state). Grade-to-level mapping: CBC PP→0, Gr1–3→1, Gr4–6→2, Gr7–9→3, Gr10–12→4,5,6 union; 8-4-4 Std1–3→0, Std4–8→1, Form1–2→2, Form3–4→3. Current assignments loaded via `SubjectsDao.watchSubjectsForClass`, teacher user ID resolved via `SubjectsDao.getSubjectAssignment`. Assignment via `SubjectsDao.assignSubjectTeacher` (insert or update in transaction with log). Snackbar: "{subject} assigned to {teacher}". Added import: `curriculum_levels.dart`. Private types: `_SubjectTeacherPickerSheet` (stateful), `_SubjectCandidate` (model with `subjectIndex`, `subjectName`, `assignedTeacherName`). Reuses `_TeacherCandidate`. **All three FAB actions now fully implemented — no remaining stubs.** |
| `student_grade_page.dart` | `StudentGradePage` | ✅ Complete | Detail page for a single student within a grade/stream context. Opened from `StudentsTab` when a student row is tapped. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `streamName`, `studentAdm`, `curriculumType`, `schoolContext`. **Structure:** Scaffold with AppBar (reactive title from `MembersDao.watchStudent`), Student Header Card (compact info bar: `_StudentAvatar` radius 24 + name w500 14px + "ADM: {adm} · {streamName}" muted 12px + `_StatusChip` using `StudentStatus` enum — green Active, red Expelled, purple Graduated, blue Transferred, amber Withdrawn, muted Deleted), Performance Indicator Banner (slim color-coded strip: green "Improving" / red "Declining" / amber "Stable" / grey "Insufficient exam data" — trajectory computed reactively from `ExamsGradesDao.watchStudentGrades` by comparing averages of last two exams, delta threshold ±1.0), Content Tabs (`EduTabBar` with `isScrollable: true`, 4 tabs: Overview · Mastery · Exams · Attendance). **All tabs wired:** Overview → `StudentOverviewTab`, Mastery → `StudentMasteryTab`, Exams → `StudentExamsTab`, Attendance → `StudentAttendanceTab`. No remaining placeholders — `_buildPlaceholder` method removed. Uses `SingleTickerProviderStateMixin`. Private widgets: `_StudentAvatar` (file-based via `FileCache.studentImagePath`), `_StatusChip` (tinted container with `StudentStatus` switch). |
| `exam_detail_page.dart` | `ExamDetailPage` | ✅ Complete | Dedicated page for viewing a single exam's full details, reached from the `ExamsTab` in the grade detail page. Constructor params: `exam` (ExamWithPapers), `schoolId`, `year`, `term`, `grade`, `streamCode` (nullable), `streamName` (nullable), `curriculumType`, `schoolContext`. **Structure:** Scaffold with AppBar (back + exam type title), `_ExamSummaryCard` (always visible above tabs — type badge, personalized badge, grade-wide/stream badge, reactive grading status badge computed from papers stream: "Fully Graded" if all papers marked / "Partially Graded" if some / "Not Started" if none, date range with icon, teacher avatar + name), `EduTabBar` with 3 tabs: Papers · Grades · Performance, `TabBarView` with three tab widgets. **Papers tab** (`_PapersTab`): reactive `StreamBuilder<List<Paper>>` via `ExamsGradesDao.watchPapersForExam`, `ListView.builder` of `_PaperCard` widgets — each showing subject name + paper number, date/time range, `_PaperStatusChip` (Pending/In Progress/Done/Marked), chevron. On tap → navigates to `PaperDetailPage` via `MaterialPageRoute`, passing `paper`, `exam`, `schoolId`, `year`, `term`, `curriculumType`, `schoolContext`. `_PapersTab` now accepts `year`, `term`, and `schoolContext` params to forward to `PaperDetailPage`. Header count row. Empty state. **Grades tab** (`_GradesTab`): loads enrolled students via `ExamsGradesDao.getEnrolledStudents` + watches grades via `watchGradesForExam`. Builds student×subject grade map (prefers subject-level totals where paper==null). Pre-computes `_StudentGradeRow` objects (student, subjectGrades map, totalScore, totalPossible, percentage, hasGrades) via `_buildRows()` helper. **Sort toggle** (`_GradeSort` enum: `scoreDesc` default, `nameAsc`): compact toggle button above table ("By score" with leaderboard icon / "By name" with sort-alpha icon), tapping cycles between modes, rows re-sorted immediately via `setState`. **Desktop (≥600px): fixed first column + horizontally scrollable grade columns.** Left fixed column (200px): header "Student" + per-row name (12.5px w500, ellipsis) + "ADM {adm}" (10.5px muted). Scrollable columns: one per subject + Total + %. Subject headers abbreviated when >6 subjects via `_abbreviateSubject` (keeps first word + first letter of subsequent words, e.g. "Social Studies" → "Social S.", with `Tooltip` showing full name). Each subject cell uses `_GradeCell` widget: score/total on first line (12px, color-coded) + percentage on second line (10px, muted alpha 0.55, color-coded). Total cell: score/total (12px w500). % cell: tinted badge (borderRadius 4). Cells separated by thin borders. Row height 52px, header 42px. Empty "—" placeholder for ungraded cells (alpha 0.25). Student count shown in sort toggle row. Mobile (<600px): `ListView.builder` of `_MobileStudentGradeCard` (expandable) — collapsed shows name + ADM + percentage badge; expanded shows per-subject scores + total row with dividers. Uses `AutomaticKeepAliveClientMixin`. **Performance tab** (`_PerformanceTab`): one-shot load via `computeClassAnalytics` + `getEnrolledStudents` + `watchClassGrades.first`. Summary card (class average %, students graded / enrolled, subject count — large w300 text). **Grade distribution chart** (`_buildDistributionChart`) — aggregates `PaperAnalytics.distribution` across all subjects into one merged bucket map (0–39, 40–49, 50–59, 60–69, 70–79, 80–100), renders a `BarChart` (fl_chart) inside a bordered card with gradient red→green bars, touch tooltips showing bucket label + count. Subject performance bars (per-subject horizontal `LinearProgressIndicator` 6px, color-coded green ≥75 / amber ≥50 / red <50). Student ranking table (header + alternating rows: rank, name+ADM, score/total, percentage with 3px progress bar; **top 3 gold/silver/bronze tinting** — rank 1 gold `#FFD700`, rank 2 silver `#C0C0C0`, rank 3 bronze `#CD7F32` with subtle row bg tint + 2.5px left border accent + medal-colored rank number). Uses `AutomaticKeepAliveClientMixin`. Private widgets: `_StudentGradeRow` (pre-computed row model with student, subjectGrades, totalScore, totalPossible, percentage, hasGrades), `_GradeSort` enum (scoreDesc, nameAsc), `_GradeCell` (score/total + percentage sub-text, color-coded), `_ExamSummaryCard`, `_PapersTab`, `_PaperCard`, `_GradesTab`, `_MobileStudentGradeCard` (stateful expandable), `_PerformanceTab`, `_StudentRankRow`, badge widgets (`_TypeBadge`, `_OutlineBadge`, `_TintedBadge`, `_StatusBadge`, `_PaperStatusChip`). Shared helpers: `_buildLoading`, `_buildEmpty`, `_typeLabel`, `_typeColor`, `_pctColor`, `_fmtDate`, `_fmtTime`, `_fmtScore`, `_abbreviateSubject`. Uses `SingleTickerProviderStateMixin` for tab controller. |
| `paper_detail_page.dart` | `PaperDetailPage` | ✅ Complete | Dedicated page for viewing/managing a single paper within an exam, reached from `ExamDetailPage` when a paper card is tapped. Constructor params: `paper` (Paper), `exam` (ExamWithPapers), `schoolId`, `year`, `term`, `curriculumType`, `schoolContext`. **Structure:** Scaffold with AppBar (back + "{Subject} Paper {N}" title), body is a `StreamBuilder<List<GradeRow>>` via `ExamsGradesDao.watchGradesForPaper` + enrolled students loaded via `getEnrolledStudents`. **Sections (vertical ListView):** (1) **Paper Info Card** (`_PaperInfoCard`) — subject name + paper number header with `_PaperStatusChip`, scheduled time with clock icon, invigilator `UserAvatar` + name, exam type badge with optional "Personalized" outline chip. Card style: `surfaceContainerLowest` bg, `borderRadius: 8`, thin border. (2) **Status Advance Row** (`_PaperStatusRow`) — `_PaperStatusChip` + descriptive text ("Not yet started" / "Exam in progress" / "Exam completed, awaiting grading" / "Fully graded") + `_StatusAdvanceButton` (steps through Pending→Progress→Done→Marked via `ExamsGradesDao.updatePaper`, uses `AnimatedSaveButton`, permission-gated to TeacherEntry/OwnerEntry/StaffEntry). (3) **Analytics Section** (`_AnalyticsSection`, visible only when `status == PaperStatus.marked`) — computes `PaperAnalytics` in-memory from grade rows. Layout: responsive wide (>560px) row / narrow column. `_DonutChart` (80×80 `PieChart` with graded % center label + "{graded}/{total} graded" text + class average % + mean score stat rows) + `_DistributionChart` (6-bucket `BarChart` from fl_chart: 0–39, 40–49, 50–59, 60–69, 70–79, 80–100 with gradient red→green bars). (4) **Grade Entry Section** — "Grades" section label + responsive layout: Desktop (≥600px) → `_GradeSpreadsheet` (header row + `ListView.separated` of `_SpreadsheetRow` — adm badge, name, score `TextFormField` with inline save via `AnimatedSaveButton`, percentage badge, tab-to-next-row via `_focusNext`). Mobile (<600px) → `_GradeList` (tap-to-open `_MobileGradeEntrySheet` bottom sheet with score/total form fields, save button). Both save via `ExamsGradesDao.upsertGrade`. Empty state for no enrolled students. **Private widgets:** `_PaperInfoCard`, `_PaperStatusRow`, `_StatusAdvanceButton`, `_AnalyticsSection`, `_DonutChart`, `_StatRow`, `_DistributionChart`, `_GradeSpreadsheet`, `_SpreadsheetRow`, `_GradeList`, `_MobileGradeEntrySheet`, `_PaperStatusChip`, `_SectionLabel`. Shared helpers: `_buildLoading`, `_buildEmpty`, `_typeLabel`, `_typeColor`, `_pctColor`, `_fmtDate`, `_fmtTime`, `_fmtScore`, `_inputDeco`. Uses `client.dart` (`cache`) for account ID access. |

#### `academics/tabs/`
| File | Widget | Status | Description |
|---|---|---|---|
| `comparisons_tab.dart` | `ComparisonsTab` | ✅ Complete | Stream comparison UI. Calls `AcademicsDao(db).computeStreamComparison(...)` on init and shows: (1) **Summary Row** — horizontal scrollable row of 4 compact stat cards (Total Students, Streams, Best Performing, Most Improved); (2) **Stream Comparison Cards** — one card per stream with header (name + student count badge) and stats grid (2-col × 3-row): Overall Avg, Last Exam, Attendance, Mastery (each with thin color-coded 6px progress bar — green ≥75, amber ≥50, red <50), Trajectory (icon + label); (3) **Ranking Table** (2+ streams) — compact table ranking streams by `lastExamAverage` desc with columns: Rank, Stream, Students, Last Exam Avg, Overall Avg, Trajectory icon. Alternating row tints. Empty state for no streams. Loading spinner while computing. Constructor params: `schoolId`, `year`, `term`, `grade` (int), `streams` (List<GradeStream>). Re-loads on `didUpdateWidget` if key params change. |
| `students_tab.dart` | `StudentsTab` | ✅ Complete | Enrolled students list for a specific stream. Uses `AcademicsDao(db).watchStudentsForGrade(...)` reactive stream via `StreamBuilder`. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `streamName`, `curriculumType`, `schoolContext`. **UI:** (1) Header row with count badge ("24 students", muted 12px). (2) `ListView.builder` of card-based rows — each row: `_StudentAvatar` (radius 18, reads `FileCache.studentImagePath`), name (w500, 13.5px) + "ADM: {adm}" (muted, 11.5px), `_TrajectoryIcon` (16px, color-coded: green improving, red declining, amber stable, grey insufficient), `_AverageBadge` (compact tinted container with percentage or "—"). Item styling: `Material` with `surfaceContainerHighest` alpha 0.4, `borderRadius: 6`, padding `h:14 v:10`, bottom spacing 6px. (3) On tap → navigates to `StudentGradePage` via `MaterialPageRoute`, passing `schoolId`, `year`, `term`, `grade`, `streamCode`, `streamName`, `studentAdm`, `curriculumType`, `schoolContext`. (4) Empty state with `people_outline_rounded` icon. Uses `AutomaticKeepAliveClientMixin`. Re-builds stream on `didUpdateWidget` if key params change. Wired into `grade_detail_page.dart` replacing the Students placeholder via `_buildStudentsTab()` method. |
| `exams_tab.dart` | `ExamsTab` | ✅ Complete | Exam list with local in-memory filters for a specific stream within a grade. Uses `AcademicsDao(db).watchExamsForGradeStream(...)` reactive stream via `StreamBuilder`. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `streamName`, `curriculumType`, `schoolContext`. **SchoolConfig loading:** Loads `SchoolConfig` on `initState` via `db.select(db.settings)` + `SchoolConfig.fromJson(jsonDecode(row.data))` — needed to pass to `ExamCreationPage`. Tracks `_configLoaded` bool so FAB only appears once config is ready. **FAB:** `FloatingActionButton.small` (icon-only `Icons.add_rounded`, tooltip "New Exam") shown when `_canManage` (TeacherEntry/OwnerEntry/StaffEntry) and `_configLoaded`. Wrapped in `Scaffold(backgroundColor: Colors.transparent)`. Pushes `ExamCreationPage` via `Navigator.push(MaterialPageRoute(...))` with `preselectedGrade: widget.grade` and `preselectedStream: widget.streamCode` so the creation form opens with the current class already selected. **Filters:** Two horizontally scrollable chip rows using `_ExamFilterChip` (compact, `BorderRadius.circular(4)`, tinted when selected, animated container): (1) **Type filter** — All · Exam · Assignment · Assessment (single-select, maps to `ExamType` enum or null for All); (2) **Personalized filter** — All · Personalized · Standard (single-select, maps to `bool?`). Filters applied in-memory via `_applyFilters()`. **Exam Cards:** `ListView.builder` with header count row ("5 exams"). Each card: type badge (colored chip — primary for Exam, amber for Assignment, green for Assessment), optional "Personalized" outline chip, optional "Grade-wide" tinted chip (when `exam.stream == null`), date range ("01 Mar 2025 – 05 Mar 2025", 13px w400), teacher name ("Created by {name}", muted 12px), paper count badge ("{N} papers"). Card style: `surfaceContainerHighest` alpha 0.4, `borderRadius: 8`, padding 14, bottom spacing 8. On tap → navigates to `ExamDetailPage` via `MaterialPageRoute`, passing `exam`, `schoolId`, `year`, `term`, `grade`, `streamCode`, `streamName`, `curriculumType`, `schoolContext`. **Empty states:** context-aware — "No exams for {streamName}" when globally empty (with thin `OutlinedButton.icon` "Create Exam" per §21 when `_canManage && _configLoaded`), "No {type} {personalized} exams found" when filters yield zero results, with appropriate icons (`quiz_outlined` at 40px vs `filter_list_off_rounded`). Uses `AutomaticKeepAliveClientMixin`. Re-builds stream on `didUpdateWidget` if key params change. Wired into `grade_detail_page.dart` replacing the Exams placeholder via `_buildExamsTab()` method. **ExamCreationPage pre-selection:** `ExamCreationPage` now accepts optional `preselectedGrade` (int?) and `preselectedStream` (int?) params. When provided, `initState` pre-populates `_gradeSelections` with the grade checked and the stream selected (or "all streams" if `preselectedStream` is null). |
| `subjects_tab.dart` | `SubjectsTab` | ✅ Complete | Subject-teacher grid with mastery comparison bars for a specific stream within a grade. Uses `AcademicsDao(db).watchSubjectsForGrade(...)` reactive stream via `StreamBuilder`. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `streamName`, `curriculumType` (CurriculumType), `schoolContext`. **UI:** (1) Header row with count badge ("8 subjects", muted 12px). (2) `ListView.builder` of card-based rows — each card: subject name (resolved via `subjectLabel(curriculumType, subject.subject)` from `curriculum_levels.dart`, w500, 14px), teacher row (`UserAvatar` radius 14 + teacher name, 13px w400), mastery section with two horizontal `_MasteryBar` widgets side by side: "Stream Avg" and "Grade Avg" — each is a thin 6px `LinearProgressIndicator` with label prefix (68px width), percentage text (right-aligned), and color-coded fill (green ≥70, amber ≥40, red <40). **Delta indicator:** When both stream and grade mastery are non-null, shows a compact delta row: arrow icon + "+X.X% vs grade" in green if stream > grade, "-X.X% vs grade" in red if stream < grade, "Same as grade average" if delta < 0.05. Shows "No mastery data" muted text if both averages are null. Card style: `surfaceContainerHighest` alpha 0.4, `borderRadius: 8`, padding 14, bottom spacing 8. (3) Empty state with `menu_book_outlined` icon: "No subjects assigned to {streamName}". Uses `AutomaticKeepAliveClientMixin`. Re-builds stream on `didUpdateWidget` if key params change. Wired into `grade_detail_page.dart` replacing the Subjects placeholder via `_buildSubjectsTab()` method. |
| `teachers_tab.dart` | `TeachersTab` | ✅ Complete | Class teachers (active + history) and subject-teacher assignments for a specific stream within a grade. Uses three independent `SubjectsDao` streams: `watchActiveClassTeacher(...)`, `watchClassTeacherHistory(...)`, `watchSubjectsForClass(...)`. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `streamName`, `curriculumType` (CurriculumType), `schoolContext`. **UI Layout (vertical scroll, ListView, 3 sections):** (1) **Active Class Teacher** — section header "Class Teacher" + `StreamBuilder` on `watchActiveClassTeacher`. If active exists: prominent card with primary-tinted border (1px, alpha 0.2), `UserAvatar` radius 22 + teacher name (w500, 14px) + "Class Teacher" subtitle (12px muted) + "Since {formatted date}" (11px, days-since-epoch → "12 Jan 2024"). If none: inline empty placeholder with `person_outline_rounded` icon + "No class teacher assigned". (2) **Past Class Teachers** — `StreamBuilder` on `watchClassTeacherHistory`, filtered to entries where `end IS NOT NULL`. Hidden entirely if no past entries. Header: "Previous Class Teachers" + animated chevron toggle (`AnimatedRotation`). `AnimatedCrossFade` to expand/collapse (default collapsed). Each past entry: compact card (`surfaceContainerHighest` alpha 0.25), `UserAvatar` radius 16 + name (13px, alpha 0.7) + date range "{start} — {end}" (11px muted). (3) **Subject Teachers** — section header "Subject Teachers" + count badge (tinted `borderRadius: 4` container with count). `StreamBuilder` on `watchSubjectsForClass`. Each subject-teacher card: subject name on left (resolved via `subjectLabel`, 13.5px w500) + teacher avatar (radius 14) + name on right (13px w400, max width 120px constrained). Card style: `surfaceContainerHighest` alpha 0.4, `borderRadius: 8`, padding h:14 v:12, bottom spacing 6px. Empty state: `menu_book_outlined` icon + "No subjects assigned to {streamName}". Uses `AutomaticKeepAliveClientMixin`. Re-builds streams on `didUpdateWidget` if key params change. Date helper: `_formatDaysDate` (days-since-epoch → "12 Jan 2024" with year). Wired into `grade_detail_page.dart` replacing the Teachers placeholder via `_buildTeachersTab()` method. |
| `attendance_tab.dart` | `AttendanceTab` | ✅ Complete | Attendance marking tab for a specific stream within a grade — the primary attendance surface for teachers/owners within the grade detail page. Uses `AttendanceDao(db).watchClassAttendance(...)` and `watchDailyAttendanceSummaries(...)` reactive streams via `StreamBuilder`. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `streamName`, `curriculumType`, `schoolContext`. **UI Layout (vertical Column):** (1) **Date Selector Row** (`_DateSelectorRow`) — left/right arrow buttons (`_NavIconButton`) for day navigation, center tappable date label ("Mon, 12 Mar 2025" + "Today" subtitle when applicable), "Today" compact chip (`_CompactChip`) to jump back to today (hidden when already on today), history calendar toggle button (switches between list icon and calendar icon). Date picker via `showDatePicker` on date label tap. (2) **Weekly Mini Strip** (`_WeeklyMiniStrip`) — compact Mon–Sun row showing current week. Each day cell: abbreviated day name + day number + colored dot indicating marking status (green = fully marked, amber = partially marked, grey = no data). Tapping a day selects it. Selected day highlighted with primary tint. Today indicated with primary border. Future days disabled. Uses `watchDailyAttendanceSummaries` for dot color computation. (3) **Marking Body** (`_AttendanceMarkingBody`, stateful) — instant-save pattern (no draft mode). **Summary Strip** (`_SummaryStrip`): compact horizontal row of stat chips (Total, Present green, Absent red, Leave amber) + unmarked count badge (error container tint) when > 0. **Action Bar** (`_ActionBar`): "Mark All Present" button (`_ActionButton`, green tint) — calls `AttendanceDao.markClassAttendance` for all non-present students. Shows "Marking…" with hourglass during save. **Student List**: `ListView.builder` of `_StudentAttendanceTile` widgets — each tile: `AnimatedContainer` with status-tinted background (alpha 0.04), student name (13.5px w400) + "Adm: {adm}" (11px w300 muted), `_StatusToggleGroup` with 3 `_ToggleButton` widgets (P/A/L, 34×30, `borderRadius: 6`, filled active color with white text + shadow, transparent inactive). Each toggle immediately calls `AttendanceDao.markAttendance` (instant-save). (4) **History Calendar** (`_HistoryCalendar`, toggled via calendar icon) — monthly calendar view using `watchDailyAttendanceSummaries`. Month navigation (chevron buttons + month/year label). Day-of-week headers (Mon–Sun). Grid of `_CalendarDayCell` widgets with `_MarkingStatus` enum (fullyMarked → green, partiallyMarked → amber, notMarked → red muted, none → transparent). Today highlighted with primary border. Weekends and future days disabled. Tapping a day switches back to marking mode for that date. `_CalendarLegend` at bottom (Fully marked / Partial / Not marked). **Empty state**: `_EmptyState` with `people_outline_rounded` icon + "No students enrolled" + sub-text. Uses `AutomaticKeepAliveClientMixin`. Key difference from standalone `AttendanceScreen`: no class picker header (grade/stream provided by tab context), integrated history calendar toggle. Uses `cache.currentUser!.user.id` for `accountId`. Wired into `grade_detail_page.dart` replacing the Attendance placeholder via `_buildAttendanceTab()` method. |
| `timetable_tab.dart` | `TimetableTab` | ✅ Complete | Weekly schedule for a specific stream within a grade. Uses `TimetableDao(db).watchClassTimetable(...)` reactive stream via `StreamBuilder`. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `streamName`, `curriculumType` (CurriculumType), `schoolContext`. **Responsive layout:** Desktop (≥ 600px) → `_DesktopGrid`: weekly grid with `_GridRow` composites — time labels column (72px, `_TimeLabel` right-aligned, 10.5px tabular figures) + one column per active day (`_HeaderCell` day abbreviation header + `_SlotCell` per time slot). Each filled cell: subject name (via `subjectLabel`, 11.5px w500) + teacher name (10px muted), left color accent border (2.5px, deterministic subject color from 15-color palette), `surfaceContainerHighest` tinted bg. Empty cells: `_EmptyCell` with thin dashed border. Rows derived from unique `start` times, columns from active days (Mon–Sun, only days with data). Mobile (< 600px) → `_MobileDayPager`: horizontal day selector chips (`_DayChip`, compact animated containers, `borderRadius: 4`, tinted when selected) + day heading (full name) + vertical `ListView.separated` of `_MobileSlotCard` widgets — each showing time range ("08:00 – 08:40", 11.5px tabular figures, 88px fixed width) + subject name (13px w500) + teacher name (11.5px muted), left color accent border (3px). **Time formatting:** seconds since midnight → "HH:MM" 24-hour via `_fmtTime`. **Subject colors:** deterministic 15-color palette indexed by `subject % 15`. **Active days:** only days present in data are shown (Mon–Sun order). **Empty state:** centered column with `calendar_view_week_outlined` icon (52×52 container) + "No timetable configured for {streamName}". Uses `AutomaticKeepAliveClientMixin`. Re-builds stream on `didUpdateWidget` if key params change. Wired into `grade_detail_page.dart` replacing the Timetable placeholder via `_buildTimetableTab()` method. |
| `lessons_tab.dart` | `LessonsTab` | ✅ Complete | Lesson log for a specific stream within a grade. Uses `TimetableDao(db).watchClassTermLessons(...)` reactive stream via `StreamBuilder`. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `streamName`, `curriculumType` (CurriculumType), `schoolContext`. **UI Layout (vertical Column):** (1) **Summary Row** (`_SummaryRow`) — 3 compact `_StatChip` widgets: lesson count, distinct subjects count, distinct teachers count. Each chip: icon + value (12px w500) + label (11px muted), `surfaceContainerHighest` alpha 0.35 bg, `borderRadius: 4`, thin border. Stats are computed from the filtered list (react to subject filter). (2) **Subject Filter Chips** (`_SubjectFilterRow`) — horizontally scrollable `ListView` of `_FilterChip` widgets: "All" (null = no filter) + one chip per distinct subject in the data (sorted by code). Each chip: `AnimatedContainer`, `borderRadius: 4`, selected state uses primary tint bg + primary border + w500 text, unselected uses muted bg + muted border. Subject names resolved via `subjectLabel(curriculumType, code)` from `curriculum_levels.dart`. (3) **Grouped Lesson List** — `ListView.builder` with interleaved `_DateHeader` and `_LessonRow` items. Lessons grouped by `date` (days since epoch), sorted most recent first (DAO already orders by date desc). **`_DateHeader`:** formatted as "Monday, 12 March 2025" via `_formatDate(daysSinceEpoch)` — converts days-since-epoch to `DateTime` UTC and formats with full weekday + day + full month + year. 13px w500 muted text. **`_LessonRow`:** compact card with subject color indicator (3px vertical bar, deterministic 10-color palette indexed by `subject % 10`), subject name (13px w500) + teacher name (11.5px muted), `_TeacherAvatar` (28×28, `borderRadius: 4`, loads profile via `FileCache.get(FileCache.profilePath(userId))`, fallback initials). Card style: `surfaceContainerHighest` alpha 0.35, `borderRadius: 4`, thin border, bottom spacing 4px. **Empty states:** (a) No lessons at all: `menu_book_outlined` icon (52×52 container) + "No lessons recorded" + "No lessons recorded for {streamName}". (b) Filter yields zero results: `filter_list_off_outlined` icon + "No lessons for {subjectName}". Uses `AutomaticKeepAliveClientMixin`. Resets subject filter and re-builds stream on `didUpdateWidget` if key params change. Wired into `grade_detail_page.dart` replacing the Lessons placeholder via `_buildLessonsTab()` method. |

#### `academics/student_tabs/`
| File | Widget | Status | Description |
|---|---|---|---|
| `student_overview_tab.dart` | `StudentOverviewTab` | ✅ Complete | Overview tab for the Student Grade Page — "at a glance" view of a student's academic standing within a grade/stream context. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `studentAdm`, `curriculumType`. **UI Layout (vertical scroll, ListView):** (1) **Quick Stats Row** — 4 compact `_StatCard` widgets in a horizontal `SingleChildScrollView`: Subjects (count from `SubjectsDao.watchSubjectsForClass`), Exams Taken (distinct exam IDs from subject-level grade totals), Average Score (mean percentage across all subject-level grades, color-coded), Mastery (average mastery across all topics for current grade from `ExamsGradesDao.watchMasteryForStudent`, color-coded). Each card: 108px wide, icon + large value + label, `surfaceContainerHighest` alpha 0.4 bg, `borderRadius: 6`. (2) **Recent Exam Performance** — last 3 exams by created date. Groups grades by exam (prefers subject-level totals where `paper == null`). Each `_ExamRow`: subject summary (up to 3 names + "+N"), raw score / total, percentage badge (color-coded tinted container), `_ThinProgressBar` (3px). Empty state: "No exam results yet" with `assignment_outlined` icon. (3) **Subject Mastery Summary** — `_SubjectMasterySection` filters mastery to current grade, groups by subject, computes average score per subject. Each `_MasteryRow`: subject label (via `subjectLabel(curriculumType, index)`), 80px `_ThinProgressBar`, percentage text. Color-coded using mastery thresholds (≥80 green, ≥60 amber, ≥40 orange, <40 red). `Tooltip` message "See Mastery tab for details". Empty state: "No mastery data recorded yet". (4) **Attendance Summary** — `_AttendanceSummarySection` from `AttendanceDao.watchStudentAttendanceSummary`. Stacked proportion bar (6px, green/red/amber segments by flex), stats row with 3 `_AttendanceStat` widgets (dot + label + percentage) + total days count. Empty state: "No attendance records this term". **Data sources:** 4 independent streams (`_gradesStream`, `_masteryStream`, `_attendanceStream`, `_subjectsStream`) combined via nested `StreamBuilder` widgets. Uses `AutomaticKeepAliveClientMixin`. Re-builds streams on `didUpdateWidget` if key params change. Private widgets: `_SectionTitle`, `_StatCard`, `_ExamRow`, `_MasteryRow`, `_AttendanceSummarySection`, `_AttendanceStat`, `_ThinProgressBar`. Color helpers: `_percentColor` (≥70 green, ≥40 amber, <40 red), `_masteryColor` (≥80 green, ≥60 amber, ≥40 orange, <40 red). |
| `student_mastery_tab.dart` | `StudentMasteryTab` | ✅ Complete | Mastery tab for the Student Grade Page — shows topic-level mastery per subject with tappable subject cards. Constructor params: `schoolId`, `studentAdm`, `grade`, `curriculumType`. **Data source:** `ExamsGradesDao(db).watchMasteryForStudent(schoolId, studentAdm)` filtered to current grade, grouped by subject. **UI Layout:** (1) **Subject List** — `ListView.builder` with header count row ("{N} subjects"). Each `_SubjectCard`: subject name (via `subjectLabel(curriculumType, index)`, w500 13px) + percentage (color-coded), `_ThinProgressBar` (4px), topics count + chevron hint. Card style: `surfaceContainerHighest` alpha 0.4, `borderRadius: 6`, padding h:14 v:12, bottom spacing 6px. (2) **On tap** — responsive detail view: mobile (<600px) → `DraggableScrollableSheet` bottom sheet, desktop (≥600px) → `Dialog` (maxWidth 420, maxHeight 520). Both show: subject name header + overall mastery % badge (tinted container) + overall progress bar + topic count, then scrollable `_TopicRow` list. Each `_TopicRow`: topic index badge (28×28 tinted square), "Topic {N}" label, `_ThinProgressBar` (3px), percentage text. Sorted by topic index ascending. (3) **Empty state** — centered column: `psychology_outlined` icon (52×52 container), "No mastery data recorded yet" + sub-text. **Color coding (mastery):** ≥80% green `#4CAF50`, ≥60% amber `#FFC107`, ≥40% orange `#FF9800`, <40% red `#F44336`. Uses `AutomaticKeepAliveClientMixin`. Re-builds stream on `didUpdateWidget` if key params change. Private widgets: `_SubjectCard`, `_TopicDetailSheet`, `_TopicDetailDialog`, `_TopicRow`, `_ThinProgressBar`. Wired into `student_grade_page.dart` replacing Mastery placeholder. |
| `student_exams_tab.dart` | `StudentExamsTab` | ✅ Complete | Exams tab for the Student Grade Page — shows all exams the student has participated in with per-subject grades, paper breakdowns, and totals. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `studentAdm`, `curriculumType`. **Data source:** `ExamsGradesDao(db).watchStudentGrades(schoolId, studentAdm)` reactive stream, with exam metadata resolved via `ExamsGradesDao.getExam(examId)` cached in an in-memory map. **UI Layout:** `StreamBuilder<List<Grade>>` → groups grades by exam id → sorts exams by most recent grade created date descending → `FutureBuilder` resolves `Exam?` metadata for all exams → `ListView.separated` of `_ExamCard` widgets. **Each `_ExamCard`:** (1) **Header row** — exam type badge (colored chip: indigo Exam, teal Assignment, purple Assessment), scope chip ("Grade-wide" or "Stream"), date range ("12 Jan – 15 Jan" from `exam.start`/`exam.end` days-since-epoch). (2) **Subject grades** — separates subject-level totals (`paper == null`) from paper-level grades, sorted by subject index ascending. Each `_SubjectGradeRow`: subject name (via `subjectLabel`), score/total, percentage (color-coded), `_ThinProgressBar` (2.5px). If multiple papers exist for a subject, indented `_buildPaperRow` entries below (paper number, score/total, percentage — muted styling). (3) **Exam total row** — "Total" label + aggregated score/total + percentage badge (tinted container, color-coded). Card style: `surface` bg, `borderRadius: 6`, thin border. **Empty state:** centered column with `assignment_outlined` icon (52×52), "No exam results yet". Uses `AutomaticKeepAliveClientMixin`. Re-builds stream + clears exam cache on `didUpdateWidget` if key params change. Private widgets: `_ExamCard`, `_SubjectGradeRow`, `_ThinProgressBar`. Color helper: `_percentColor` (≥70 green, ≥40 amber, <40 red). Date helper: `_formatDaysDate` (days-since-epoch → "12 Jan"). Wired into `student_grade_page.dart` replacing Exams placeholder. |
| `student_attendance_tab.dart` | `StudentAttendanceTab` | ✅ Complete | Attendance tab for the Student Grade Page — shows the student's attendance record for the current term. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `studentAdm`. **Data sources:** Two independent streams: `AttendanceDao.watchStudentAttendanceHistory` (individual records ordered asc) and `AttendanceDao.watchStudentAttendanceSummary` (aggregate counts). **UI Layout (vertical scroll, ListView):** (1) **Summary Bar** (`_SummaryBar`) — container with stacked proportion bar (6px, green/red/amber segments by flex) + stats row: 3 `_SummaryStat` widgets (square dot + label + "N (X%)" count) for Present (green `#4CAF50`), Absent (red `#F44336`), Leave (amber `#FFA726`) + total days count right-aligned. Card style: `surfaceContainerHighest` alpha 0.4, `borderRadius: 6`, thin border. (2) **Month Calendar Heatmaps** (`_MonthCalendar`) — one per month in reverse chronological order. Each: month/year title, Mon–Sun day-of-week header (9.5px), grid of `_DayCell` widgets (24×24, `borderRadius: 3`). Cells color-coded by status: green bg Present, red bg Absent, amber bg Leave (alpha 0.15 light / 0.25 dark), muted text for weekends and unmarked days. Offset calculated from Monday-based weekday. (3) **"All Records" section header** — muted 12px w500 text. (4) **Month-Grouped Detail List** — `_MonthHeader` (full month name + year + count badge) followed by `_RecordRow` items in reverse chronological order within each month group. Each row: formatted date ("Mon, 12 Mar") + status chip (tinted container with square dot + label, `borderRadius: 3`). Row style: `surfaceContainerHighest` alpha 0.35, `borderRadius: 4`, thin border. **Month grouping:** `_groupByMonth` converts days-since-epoch to `DateTime`, groups by `(year, month)` record, sorts groups reverse chronologically. **Empty state:** centered column with `calendar_today_outlined` icon (52×52 container), "No attendance records this term" + sub-text. Uses `AutomaticKeepAliveClientMixin`. Re-builds streams on `didUpdateWidget` if key params change. Private widgets: `_SummaryBar`, `_SummaryStat`, `_MonthCalendar`, `_DayCell`, `_MonthHeader`, `_RecordRow`. Wired into `student_grade_page.dart` replacing Attendance placeholder; unused `_buildPlaceholder` method removed from `StudentGradePage`. |

**Data source:** `SettingsDao.watchSettings(schoolId)` → parses `settings.data` JSON → `SchoolConfig` → iterates `CurriculumConfig.grades`.
**Dependencies:** `models/school_config.dart` (`SchoolConfig`, `GradeConfig`, `GradeStream`, `kCbcGradeLabels`, `kEightFourFourGradeLabels`), `database/daos/settings_dao.dart`, `database/daos/academics_dao.dart`, `database/daos/members_dao.dart` (`MembersDao.watchStudent`), `database/daos/exams_grades_dao.dart` (`ExamsGradesDao`, `ExamWithPapers`, `PaperAnalytics`, `GradeRow`, `watchStudentGrades`, `watchGradesForExam`, `watchPapersForExam`, `computeClassAnalytics`, `getEnrolledStudents`, `watchClassGrades`), `database/daos/attendance_dao.dart` (`AttendanceDao`, `StudentAttendanceRecord`, `watchStudentAttendanceHistory`, `watchStudentAttendanceSummary`), `database/tables/enums.dart` (`ExamType`, `PaperStatus`, `StudentStatus`, `AttendanceStatus`), `database/tables/curriculum_subjects.dart` (`CurriculumType`), `models/grade_analytics.dart` (`StreamStats`, `Trajectory`, `SubjectTeacherEntry`), `models/curriculum_levels.dart` (`subjectLabel`), `models/active_term_context.dart`, `ui/widgets/active_term_provider.dart`, `ui/widgets/edu_tab_bar.dart`, `ui/widgets/user_avatar.dart`, `ui/theme/app_theme.dart`, `cache/file_cache.dart`

---

### `announcements/`
| File | Widget | Status | Description |
|---|---|---|---|
| `announcements_screen.dart` | `AnnouncementsScreen` | ✅ Complete | Chronological feed of school announcements. Owners/Staff can create announcements targeted by role or class. Feed filters by `SchoolContext.currentEntry` role for relevance. Compose FAB simplified to icon-only `FloatingActionButton.small` matching Roles page pattern; `_ElevatedFab` class removed. |

**Data source:** `AnnouncementsDao.watchAnnouncements(schoolId, ...)`
**Dependencies:** `database/daos/announcements_dao.dart`, `models/school_context.dart`

---

### `attendance/`
| File | Widget | Status | Description |
|---|---|---|---|
| `attendance_screen.dart` | `AttendanceScreen` | ✅ Complete | **Role dispatch:** Teacher/Owner → `_ClassPickerShell` (class picker list that navigates to `GradeDetailPage`); Guardian → `_GuardianAttendanceView` (read-only calendar history for ward); Student → `_GuardianAttendanceView` (read-only calendar history for self). **Teacher/Owner flow (Task 17b):** `_ClassPickerShell` loads `SchoolConfig` via `SettingsDao.getSettings` and watches populated classes via `EnrollmentsDao.watchPopulatedClasses`. Displays a header ("Attendance" + "Select a class to mark attendance") and a `_ClassList` of `_ClassCard` widgets. Each card shows: class icon (40×40 tinted square), grade label + stream name, `_TodayStatusBadge` (reactive `StreamBuilder` on `AttendanceDao.watchClassAttendance` for today — "Not marked" red / "X / Y marked" amber / "Fully marked" green / "No students" muted), chevron. On tap → resolves `GradeConfig` + `CurriculumType` + stream index from `SchoolConfig`, then navigates to `GradeDetailPage` via `MaterialPageRoute` with `initialStreamIndex = streamIndex + 1` and `initialContentTabIndex = 3` (Attendance). The old embedded marking UI (`_TeacherAttendanceShell`, `_AttendanceMarkingList`, `_AttendanceHeader`, `_ClassFilterChip`, `_SummaryStrip`, `_SummaryChip`, `_ActionBar`, `_ActionButton`, `_StudentAttendanceTile`, `_StatusToggleGroup`, `_ToggleButton`, `_EmptyStudentsState`) have been removed — attendance marking now lives exclusively in `AttendanceTab` inside the grade detail page. **Guardian/Student flow:** unchanged — `_GuardianAttendanceView` with `_GuardianSummaryCard` (rate circle + present/absent/leave stat pills) and `_AttendanceCalendar` (monthly heatmap grid with day cells color-coded by status, month navigation, legend). |

**Data source:** `EnrollmentsDao.watchPopulatedClasses(...)`, `AttendanceDao.watchClassAttendance(...)` (for today's status badges), `AttendanceDao.watchStudentAttendanceHistory(...)`, `AttendanceDao.watchStudentAttendanceSummary(...)`
**Dependencies:** `database/daos/attendance_dao.dart`, `database/daos/enrollments_dao.dart`, `database/daos/settings_dao.dart`, `database/tables/curriculum_subjects.dart`, `models/active_term_context.dart`, `models/school_config.dart`, `models/school_context.dart`, `models/membership.dart`, `ui/widgets/active_term_provider.dart`, `academics/grade_detail_page.dart`

---

### `exams/`
| File | Widget | Status | Description |
|---|---|---|---|
| `exams_grades_screen.dart` | `ExamsGradesScreen` | ✅ Complete | Hierarchical exam management: Term → ExamGroup → Grade/Stream → Paper → Grades/Mastery. Data-dense tables for desktop grading, list views for mobile. Analytics header with donut charts for class averages. **Exams list view** (`_ExamsListView`) uses `ExamGroup` model. Data source: `ExamsGradesDao.watchExamGroups()` → `Stream<List<ExamGroup>>`. `onExamTap` callback type: `ValueChanged<ExamGroup>`. `_ExamGroupCard` shows: exam type colored badge, personalized indicator, date range, participating grades as `_ClassChip` chips, subject count + grade count as `_MetaBadge` row. **`_ExamsShellState` (updated Task EX8):** `_selectedGroupKey: String?` tracks the group key for reactive lookup. `_selectedGroup: ExamGroup?` holds the latest snapshot (updated by stream). `_dao: ExamsGradesDao` added to shell for the reactive stream. `_ExamsView.examDetail` is wrapped in `StreamBuilder<List<ExamGroup>>` on `_dao.watchExamGroups()` — finds the matching group by `groupKey` on each emission, keeping the detail view reactive when grades are added, date ranges extended, etc. If the group disappears (deleted), automatically pops back to the list via `addPostFrameCallback`. `_openExam(ExamGroup)` sets both `_selectedGroup` and `_selectedGroupKey`. `_popToList()` clears both. `_openPaper(Paper, Exam)` captures the specific exam row for paper detail navigation. `_ExamsView.paperDetail` constructs `ExamWithPapers` bridge record from `_selectedExamRow` + `_selectedGroup.teacher` for `_PaperDetailView`. **`_ExamGroupDetailView` (updated Task EX8):** Accepts new `onDeleted` callback. Uses `TickerProviderStateMixin` for two `TabController`s. **Header:** custom `Row` layout with back button + exam type label + date range subtitle + **delete `IconButton`** (error-colored `Icons.delete_outline_rounded`, only shown when `_canManage`). Delete triggers `_confirmDeleteGroup()` which shows `_ConfirmDeleteDialog`, then loops through `group.examIds` calling `_dao.deleteExam()` for each, then calls `widget.onDeleted()` to pop back to list. **Grade tabs:** scrollable `EduTabBar` with one tab per `group.grades` entry (grade labels from `SchoolConfig`). Right side: `+` `IconButton` to add a grade (opens `_AddGradeToExamSheet` modal). **Stream sub-tabs:** shown below grade tabs only when current grade has >1 stream entry. Each tab labeled with stream name or "All Streams" for null stream. **Paper content area:** `StreamBuilder<List<Paper>>` on `_dao.watchPapersForExam(schoolId, currentStream.exam.id)`. Uses `LayoutBuilder` to switch between `_PaperTimetableGrid` (≥600px desktop) and `_PaperTimetableMobile` (<600px mobile), or `_EmptyPapersTimetableState` when empty. **FAB:** `_ExpandableFab` (expandable FAB with 3 option pills: Paper, Grade, Stream). Main FAB button has `heroTag: 'fab_exam_detail_main'`. Only shown when `_canManage`. **State:** `_gradeTabController`, `_selectedGradeIndex`, `_streamTabController`, `_selectedStreamIndex`. Grade tab changes reset stream index to 0 and rebuild stream tabs. `didUpdateWidget` reinitializes tabs when group changes. **Paper timetable helpers (Task EX5):** `_groupPapersByDate(List<Paper>)` groups papers by day component of start timestamp into `Map<DateTime, List<Paper>>`, sorted by start within each day. `_sortedPaperDates(Map)` returns sorted unique dates. `_uniquePaperStartTimes(List<Paper>)` returns sorted unique HH:MM start-time labels. `_paperAt(Map, DateTime, String)` finds paper at a given date + start-time label. `_fmtDayHeader(DateTime)` formats "Mon, 12 Jan". `_fmtDayColumn(DateTime)` formats "Mon 12" for column headers. **`_PaperTimetableGrid` (Task EX5, desktop ≥600px):** `SingleChildScrollView` grid modeled after `_DesktopGrid` in `timetable_tab.dart`. X-axis = dates (from paper data, not fixed weekdays), Y-axis = unique start times. Uses `_PaperGridRow` (72px time cell + expanded day cells), `_PaperHeaderCell` (date column headers), `_PaperTimeLabel` (left-edge time labels with tabular figures), `_PaperSlotBox` (filled cells: status-colored left border 2.5px + tinted background, subject name + paper number, time range), `_PaperEmptyCell` (subtle 1px border placeholder). **`_PaperTimetableMobile` (Task EX5, <600px):** `StatefulWidget` with horizontal `_PaperDayChip` selector at top (animated container, primary-tinted when selected, `BorderRadius.circular(4)`), day heading below, and `ListView.separated` of `_PaperSlotCard` cards for the selected day. State: `_grouped`, `_dates`, `_selectedDayIndex`. `didUpdateWidget` rebuilds groups and clamps index. **`_PaperSlotCard` (retained from Task EX3):** status-colored left border (2.5px) + tinted background via `_paperStatusColor()`, subject name + paper number, time range, `_StatusChip`, chevron. Used by mobile view. **`_PaperDayChip` (Task EX5):** compact day selector chip matching `_DayChip` pattern from timetable_tab — `AnimatedContainer`, sharp `BorderRadius.circular(4)`, primary-tinted when selected. **`_EmptyPapersTimetableState`:** centered empty state ("No papers scheduled"). **`_paperStatusColor(PaperStatus, ColorScheme)`:** top-level helper — pending=muted grey, progress=blue (#42A5F5), done=amber (#FFA726), marked=green (#66BB6A). **Deleted widgets (cumulative):** `_ExamDetailView`, `_ExamDetailViewState`, `_ExamSummaryCard`, `_InfoRow`, `_PaperCard`, `_EmptyPapersState`, `_ClassPerformanceSection`, `_ClassPerformanceSectionState`, `_StudentRankRow`, `_PaperListContent`, `_SubLabel`, `_IconActionButton`, `_SegmentedRow`, `_DatePickerField`, `_FieldLabel`, `_selectedGradeConfig` getter (unused), `_sameDay` static helper (unused), trivial `dispose()` override (unnecessary). **Retained:** `_PaperDetailView` (grades/scoring unchanged), `_PaperStatusRow`, `_StatusAdvanceButton` (used by `_PaperDetailView`), `_AnalyticsHeader`, `_DonutChart`, `_StatRow`, `_DistributionChart`, `_GradeSpreadsheet`, `_GradeList`, `_MobileGradeEntrySheet`, `_CreatePaperSheet` (for adding papers), `_ConfirmDeleteDialog` (used by delete exam group). **Imports:** `ui/widgets/edu_tab_bar.dart`, `models/exam_group.dart`. File compiles cleanly: 0 errors, 0 warnings (Tasks R11–R12). |
| `exam_creation_page.dart` | `ExamCreationPage` | ✅ Complete | Full-screen multi-grade exam creation form pushed via `Navigator.push`. Uses `TickerProviderStateMixin` (multiple tab controllers). Four sections: (1) **Exam Details** — type selector as slim inline chips (`_buildTypeSelector`), **inline date range picker** (`_buildDateRange`) with collapsible calendar (`_ExamRangeCalendar` using `AnimatedSize` instead of old `AnimatedCrossFade`; calendar month transitions use `AnimationController` with fade via `AnimatedBuilder`; sub-widgets renamed from `_Exam*` to `_Calendar*` prefix: `_CalendarMonthHeader`, `_CalendarChevronBtn`, `_CalendarWeekdayRow`, `_CalendarDayGrid`, `_CalendarOverflowCell`, `_CalendarDayCell`, `_BandPos`), personalized toggle. (2) **Grade Tabs** — scrollable `EduTabBar` with **only real grade tabs** (no synthetic "All" tab — removed in Task E1). Pre-selection from `preselectedGrade`/`preselectedStream`. `_hasSelection` simplified to `_gradeTabs.isNotEmpty`. (3) **Stream Sub-Tabs** — **two-level tab hierarchy: Grade → Stream** (redesigned in Task E2). Each grade with streams gets its own `TabController` stored in `Map<int, TabController> _streamTabControllers` and `Map<int, int> _activeStreamTabIndex`. Tab 0 = "All Streams" (shows all streams, one entry per stream), tabs 1..N = individual streams. `_buildStreamSection` returns an `EduTabBar` (scrollable, `padding: EdgeInsets.zero`) with "All Streams" + per-stream tabs. Pre-selection from `preselectedStream` sets `ctrl.index = streamIdx + 1`. **Removed**: `_StreamChip` widget class, `_streamSelections` map, `_toggleStreamChip` method, `_buildStreamChips` method, `_loadSubjectsForGrade` method — all replaced by `_onStreamTabChanged(int grade)` which loads subjects for the active stream (or all streams when "All Streams" is active). `_onGradeTabChanged` delegates to `_onStreamTabChanged`. **`_selectedClasses` redesigned (Tasks E1+E2):** uses `_activeStreamTabIndex[grade]` instead of `_streamSelections`. When index == 0 ("All Streams"), returns all streams individually (one entry per stream). When index > 0, returns only the specific stream. When a grade has no streams, returns `(grade, null)`. All stream tab controllers are disposed in `dispose()`. (4) **Paper Timetable** — dynamic timetable-style scheduler with **per-stream timetable rendering** (Task E3). `_buildTimetableSections` rewritten to be stream-tab-aware: when a specific stream tab is active (index > 0), shows only that stream's timetable with no header; when "All Streams" tab is active (index == 0), shows one timetable section per stream with stream name as header label; when a grade has no streams, shows a single headerless timetable. No longer iterates `_selectedClasses` — instead reads `_activeStreamTabIndex` directly. Per-stream paper configuration — each stream has independent `_PaperSlot` lists keyed by `"$grade:$streamCode"`. **"Copy to all" feature** (Task E4): when viewing a specific stream's timetable (stream tab index > 0) and the stream has at least one paper slot, a "Copy to all" `_MiniTextButton` (icon: `Icons.copy_all`) appears in the header actions row. `_copyPapersToAllStreams(int grade, int sourceStreamCode)` copies the source stream's paper slots to all other streams of the same grade — each target stream gets independent copies (new `_PaperSlot.id` values). Subject codes are copied as-is, but invigilators are remapped to the teacher assigned to that subject in the target stream (looked up from `_classSubjects[targetKey]`). Slots for subjects that don't exist in the target stream are skipped. Slots without a subject are copied without remapping. `_isSpecificStreamActive(int grade)` helper returns `true` when the grade has multiple streams and a specific stream tab (index > 0) is active. The copy uses whatever subjects are already loaded — the user can navigate to each target stream to trigger loading and verify. **`_loadSubjectsForClass` simplified** (Task E3): removed union-of-streams fallback (old `else` branch that iterated all streams when `stream == null`). Now always loads per individual stream via `SubjectsDao.getSubjectsForClass()`; for grades without streams, returns empty list. **Removed helpers** (Task E3): `_gradeLabelText` and `_streamLabelText` — dead code after `_buildTimetableSections` rewrite (grade is clear from grade tab, stream name comes directly from `stream.name`). **New data model**: `_PaperSlot` (id, date, startTime, endTime, subjectCode, invigilatorId, computed `duration` getter) replaces old `_PaperSchedule`. State: `Map<String, List<_PaperSlot>> _paperSlots` keyed by `"$grade:$stream"`. Teachers loaded eagerly via `MembersDao.watchTeachers()` + `findUserById()` join into `List<({TeachersData teacher, UsersData user})> _teachers`. **UI structure**: `_buildTimetableForClass` renders header row with `_MiniTextButton` actions ("Auto-fill" when empty, "Clear" when populated, "Copy to all" when specific stream active and slots non-empty), then `_EmptyTimetableHint` or day-grouped slot cards. Slots grouped by day via `_DayColumn` widget (day header with dot + day name + date + paper count badge, then `_PaperSlotCard` per slot). **`_PaperSlotCard`**: time range row with two `_InlineTimePicker` chips + duration badge (`Xmin` in indigo, or "invalid" in error color) + remove button; subject selector (`_SubjectDropdown` — tappable row opening `showModalBottomSheet` with `_PickerOption` list, auto-sets invigilator from subject's teacher on selection); invigilator selector (`_InvigilatorDropdown` — tappable row opening `_InvigilatorPickerSheet` with search field + `_PickerOption` list). **`_InlineTimePicker`**: tappable chip that opens `_TimePickerSheet` bottom sheet with modern wheel-style pickers (`_WheelColumn` using `ListWheelScrollView.useDelegate`, hour 0–23 + minute in 5-min increments), quick-tap preset chips (`_TimePresetChip` for 8:00–16:00), "Done" button returns `TimeOfDay`. All use `FontFeature.tabularFigures()` for monospaced digits. **`_AddPaperRow`**: horizontally scrollable day chips at bottom of each class section — tap to add a new paper to that day. Each chip shows day name + date + existing paper count badge + "+" icon. New slots auto-chain start time from previous slot's end time and auto-assign first unassigned subject. **`_autoPopulateSlots`**: fills slots for all subjects across exam days (3 per day max, 2hr each starting at 08:00). **Date range changes clear all paper slots** (prevents orphaned slots outside range). **Save logic**: iterates `_selectedClasses` (which returns all streams when "All Streams" active), looks up per-stream `_paperSlots[key]`, skips slots with `subjectCode == null`, builds `PapersCompanion` with `invigilator` from `slot.invigilatorId ?? teacherId`. **Footer**: thin divider + Cancel text + `_ExamConfirmButton` (brand indigo, shadow, loading spinner). **Deleted old widgets**: `_PaperSchedule`, `_PaperTimetableRow`, `_TimetableDateChip`, `_TimetableTimeChip`, `_paperSchedules` map, `_StreamChip`. **Added new widgets**: `_PaperSlot`, `_MiniTextButton`, `_EmptyTimetableHint`, `_DayColumn`, `_PaperSlotCard`, `_InlineTimePicker`, `_TimePickerSheet`, `_WheelColumn`, `_TimePresetChip`, `_SubjectDropdown`, `_InvigilatorDropdown`, `_InvigilatorPickerSheet`, `_PickerOption`, `_AddPaperRow`. **Added imports**: `members_dao.dart`. **Removed imports**: `flutter/services.dart` (unnecessary). Top-level helpers: `_sameDay`, `_generateId`, `_kMonths`, `_kDayNames`, `_fmtDate`, `_fmtDateFull`, `_fmtTime`, `_kFullMonthNames`. Follows §21 UI guidelines. |

**Data source:** `ExamsGradesDao.watchExamGroups(...)`, `ExamsGradesDao.watchPapersForExam(...)`, `ExamsGradesDao.watchGradesForPaper(...)`, `ExamsGradesDao.computeClassAnalytics(...)`, `ExamsGradesDao.getEnrolledStudents(...)`, `ExamsGradesDao.createExamBatch(...)`, `SubjectsDao.getSubjectsForTerm(...)`, `MembersDao.watchTeachers(...)`, `MembersDao.findUserById(...)`
**Dependencies:** `database/daos/exams_grades_dao.dart`, `database/daos/subjects_dao.dart`, `database/daos/members_dao.dart`, `models/active_term_context.dart`, `models/school_context.dart`, `models/school_config.dart`, `models/curriculum_levels.dart`, `models/membership.dart`, `models/exam_group.dart`, `ui/widgets/animated_save_button.dart`, `ui/widgets/edu_tab_bar.dart`

---

### `finance/`
| File | Widget | Status | Description |
|---|---|---|---|
| `finance_screen.dart` | `FinanceScreen` | ✅ Complete | Financial management: fee structures, invoicing, payments, discounts. Overview cards with totals. Owner/Staff can define fees, generate invoices, record payments. Guardian gets read-only view of invoices/payments/balances per ward. "New Fee" FAB simplified to icon-only `FloatingActionButton.small` matching Roles page pattern; `_ElevatedFab` class removed. |

**Data source:** `FinanceDao.watchFees(...)`, `FinanceDao.watchInvoices(...)`, `FinanceDao.watchPayments(...)`, `FinanceDao.watchDiscounts(...)`
**Dependencies:** `database/daos/finance_dao.dart`, `models/active_term_context.dart`, `models/school_context.dart`

---

### `members/`
| File | Widget | Status | Description |
|---|---|---|---|
| `members_page.dart` | `MembersPage` | ✅ Complete | Unified members page with 6 tabs (Departments, Owners, Teachers, Staff, Students, Guardians) using `EduTabBar`. Context-aware FAB adapts action per active tab (standardised to `FloatingActionButton.small` matching Roles page). Each tab is a reactive list from the corresponding Drift table joined with `users`. Departments tab includes full department management: list view, detail screen with 3 left-aligned scrollable tabs (**All**, **Teachers**, **Staff**), create/delete actions. The "All" tab (`_DeptAllMemberList`) shows a combined list of teachers and staff with role-tag chips ("Teacher"/"Staff") and remove buttons. Per-tab inline assign buttons have been removed; a single `FloatingActionButton.small` opens a searchable assign modal (`_AssignMemberSearchSheet`) with a text search field (name/phone), Teachers/Staff filter toggle chips (`_DeptFilterChip`), and tap-to-assign items. **Owner detail** is a responsive modal (`_OwnerInfoSheet`) — bottom sheet on mobile (< 600 px) and a slide-in side sheet on desktop (≥ 600 px); "Remove from school" action verified with full error handling (null account ID snackbar, try/catch with failure snackbar). **Teacher detail** is a responsive modal (`_TeacherInfoSheet`, now a `StatefulWidget`) — bottom sheet on mobile (< 600 px) and a slide-in side sheet on desktop (≥ 600 px), matching the owner pattern. Displays avatar, name, phone, email, status dot+label, detail rows (role, department, hired date), **two collapsible assignment sections** (Class Teacher Assignments and Subject Assignments), and a compact `_ActionIconGroup` row of icon buttons: Edit (opens edit bottom sheet with role/department/hired date fields), Resign, Restore, Transfer, Fired, Retired (each directly sets `TeacherStatus` and closes sheet), and Remove (confirmation dialog). The **Class Teacher Assignments** section uses `StreamBuilder` on `MembersDao.watchClassTeacherAssignments()` and displays each assignment as a compact `_AssignmentChip` showing "Grade X · SY (Year TZ)". The **Subject Assignments** section uses `StreamBuilder` on `MembersDao.watchTeacherSubjects()`, groups subjects by `(year, term, grade, stream)`, and displays each group as a header with subject name chips below. Both sections are collapsible via `_buildCollapsibleSection` (tap-to-expand with `AnimatedCrossFade` + `AnimatedRotation` chevron). Subject names are resolved from the school's curriculum type loaded via `SettingsDao.getSettings()` → `SchoolConfig` → `subjectLabel()`, and grade labels via `gradeLabelsFor()`. Assignment creation UI is **deferred** to a follow-up task — currently read-only display only. The old full-page `_TeacherDetailPage`/`_TeacherDetailPageState` and the `_showStatusSheet` bottom sheet with choice chips have been removed. `_ActionIconGroup` and `_ActionIconItem` are new shared widgets — a `Wrap` of small 36×36 bordered icon buttons inside a tinted container, reusable by other detail sheets. `_AssignmentChip` is a new shared compact chip widget for displaying assignment labels. **Staff detail** is a responsive modal (`_StaffInfoSheet`, `StatelessWidget`) — bottom sheet on mobile (< 600 px) and a slide-in side sheet on desktop (≥ 600 px), matching the teacher/owner pattern. Displays avatar, name, phone, email, status dot+label, detail rows (role, department, ID number), and a compact `_ActionIconGroup` row of icon buttons: Edit (opens edit bottom sheet with role/department/ID number fields), Resign, Restore, Transfer, Fired, Retired (each directly sets `StaffStatus` and closes sheet), and Remove (confirmation dialog). The old full-page `_StaffDetailPage`/`_StaffDetailPageState` and the separate `_showStatusSheet` bottom sheet with choice chips have been removed. Both teacher and staff detail sheets use `MemberManagementService` for all mutations. **Student detail** is now a responsive modal (`StudentDetailSheet`) — `_StudentRow` opens a `DraggableScrollableSheet` bottom sheet on mobile (< 600 px) and a slide-in side sheet (420 px wide) on desktop (≥ 600 px), matching the teacher/staff/owner pattern. `_StudentDetailStub` has been removed. **Guardian creation** uses a searchable student picker (`_StudentPickerSheet`, StatefulWidget) — supports search-as-you-type by name or admission number via `MembersDao.searchStudents()`, replacing the old pre-fetched non-searchable list. **Guardians tab** now shows **unique guardians** — one row per distinct user via `MembersDao.watchUniqueGuardians(schoolId)` — with ward count displayed in the subtitle (e.g. "2 wards"). `_UniqueGuardianRow` replaces the old `_GuardianRow`. **Guardian detail** is a responsive modal (`_GuardianWardsSheet`) — bottom sheet on mobile (< 600 px) and a slide-in side sheet on desktop (≥ 600 px) — showing guardian header (avatar, name, phone, email, status indicator) and a reactive ward list via `MembersDao.watchGuardianWards(schoolId, userId)`. Each ward (`_WardItem`) displays student name, relationship · role subtitle, and inline edit (relationship/role dropdown sheet) and unlink (confirmation dialog) icon buttons. The old `_GuardianRow`, `_GuardianResolved`, `_GuardianDetailPage`, and `_GuardianDetailPageState` have been removed. Uses `MemberManagementService` for mutations. `_MemberDetailStub` has been removed (Task 13). |
| `student_detail_page.dart` | `StudentDetailSheet`, `StudentDetailPage` | ✅ Complete | **`StudentDetailSheet`** is the primary widget — a responsive modal that works as bottom-sheet (with `DraggableScrollableSheet` + drag handle), side-sheet (slide-in from right, 420 px wide, left-rounded corners), or embedded (inside a `Scaffold` via `StudentDetailPage` thin wrapper for backward-compatible push-route usage). Features a **compact horizontal header** row: smaller avatar (radius 28), name + "ADM: X" in a column, `_StatusChip`, and a `Row` of `_ActionIcon` icon buttons (Edit, Change Status — conditional on active status, Delete with error-tinted color) replacing the old `PopupMenuButton` — all in a single `Row`. The `_onMenuAction` string-switch method has been removed; each `_ActionIcon.onTap` calls `_showEditSheet`, `_showStatusSheet`, or `_confirmDelete` directly. `_ActionIcon` is a new private widget: `Tooltip` → `Material` → `InkWell` → `Icon` (size 18, padding 7, borderRadius 6). **Left-aligned scrollable tabs** via `EduTabBar(isScrollable: true, padding: EdgeInsets.only(left: 16, ...))` with 4 tabs: **Info**, **Performance**, **Guardians**, **Subscriptions** (renamed from "Plans"). Tab content is unchanged: Info (gender, DOB, admitted date, status, linked user details, documents), Performance (grades by exam + mastery by subject), Guardians (reactive list + add guardian button), Subscriptions (reactive subscription list + subscribe button). `StudentDetailPage` is now a thin `StatelessWidget` wrapper that embeds `StudentDetailSheet` inside a `Scaffold`. Uses `MemberManagementService` for student mutations and `PlansDao` for subscription operations. |

**Data source:** `MembersDao.watchOwners(schoolId)`, `watchTeachers(...)`, `watchStaff(...)`, `watchAllStudents(...)` (all statuses), `watchUniqueGuardians(schoolId)` (unique guardian list with ward counts), `watchGuardianWards(schoolId, userId)` (ward list for a specific guardian), `watchGuardians(...)`, `watchStudent(schoolId, adm)`, `MembersDao.searchStudents(schoolId, query)` (student picker for guardian linking), `MembersDao.watchClassTeacherAssignments(schoolId, teacherUserId)` (teacher class assignments), `MembersDao.watchTeacherSubjects(schoolId, teacherUserId)` (teacher subject assignments), `DepartmentsDao.watchDepartments(schoolId)`, `ExamsGradesDao.watchStudentGrades(...)`, `ExamsGradesDao.watchMasteryForStudent(...)`, `ExamsGradesDao.getExam(...)`, `PlansDao.watchStudentSubscriptions(...)`, `PlansDao.getActivePlans()`, `PlansDao.createSubscription(...)`, `PlansDao.updateSubscriptionStatus(...)`, `SettingsDao.getSettings(schoolId)`
**Dependencies:** `database/daos/members_dao.dart`, `database/daos/departments_dao.dart`, `database/daos/exams_grades_dao.dart`, `database/daos/plans_dao.dart`, `database/daos/settings_dao.dart`, `database/tables/curriculum_subjects.dart`, `models/curriculum_levels.dart`, `models/school_config.dart`, `ui/widgets/edu_tab_bar.dart`, `ui/widgets/member_creation/` (all creation panels), `services/members.dart` (`MemberCreationService`), `services/member_management.dart` (`MemberManagementService`), `ui/widgets/user_avatar.dart`, `client.dart`

---

### `roles/`
| File | Widget | Status | Description |
|---|---|---|---|
| `school_roles_screen.dart` | `SchoolRolesScreen` | ✅ Complete | Single list of role cards for this school. Each card shows role name, description, permission count badge. FAB to create new role. |
| `school_role_detail_screen.dart` | `SchoolRoleDetailScreen` | ✅ Complete | Full push-route role detail page with 2 tabs (`EduTabBar`): Permissions (toggle chips grouped by resource) and Assigned Users (list of users with this role + assign/unassign actions). |

**Data source:** `SchoolScopesDao.watchSchoolRoles(schoolId)`, `SchoolScopesDao.watchSchoolScopes(schoolId, ...)`
**Dependencies:** `database/daos/school_scopes_dao.dart`, `ui/widgets/edu_tab_bar.dart`, `models/school_context.dart`

---

### `timetable/`
| File | Widget | Status | Description |
|---|---|---|---|
| `timetable_screen.dart` | `TimetableScreen` | ✅ Complete | Timetable rules setup + viewing. Desktop: weekly grid view. Mobile: day-by-day pager/timeline. Rules configuration form for owners. "Generate Timetable" button is a stub (shows snackbar — algorithm pending). |

**Data source:** `TimetableDao.watchTimetable(...)`, `TimetableDao.watchLessons(...)`
**Dependencies:** `database/daos/timetable_dao.dart`, `models/active_term_context.dart`, `models/school_context.dart`

## Key Provided Context Objects

These objects are created in `SchoolDashboardScreen` and provided to all child pages:

| Object | Type | Provided via | Scope |
|---|---|---|---|
| `SchoolContext` | `SchoolContext` | `InheritedWidget` or similar | Entire school dashboard session |
| `ActiveTermContext` | `ActiveTermContext` | `ActiveTermProvider` (InheritedNotifier) | Entire school dashboard session |

All child screens access these via:
```dart
final schoolCtx = SchoolContext.of(context); // or similar lookup
final termCtx = ActiveTermProvider.of(context);
```

## Role Switcher

The role switcher widget in the AppBar reads `SchoolContext.membership.entries` and calls `SchoolContext.switchEntry(entry)` on tap. When `SchoolContext.canSwitch` is `false` (single entry), the switcher is hidden.

Switching entry:
- Does NOT recompute `SchoolPermissions` (constant for session).
- Does NOT trigger a new page — the dashboard content rebuilds in place via `ValueListenableBuilder<MembershipEntry>` on `SchoolContext.currentEntry`.
- Entry-sensitive streams (lessons filtered by teacher, grades for a guardian's ward, etc.) re-subscribe based on the new entry.

## Term Selector

The term selector chip in the AppBar reads `ActiveTermContext.currentTermLabel` and opens a picker showing `ActiveTermContext.allTerms`. Selecting a term calls `ActiveTermContext.setTerm(term)`. All term-sensitive data streams rebuild via `ValueListenableBuilder<Term?>` on `ActiveTermContext.termNotifier`.

When `ActiveTermContext.hasTerms` is `false`, the dashboard shows a blank state with a "Create First Term" CTA for owners (via `NoTermsBlankState` widget).

## Dependencies

- **Depends on:** `models/school_context.dart`, `models/school_permissions.dart`, `models/membership.dart`, `models/active_term_context.dart`, `models/school_config.dart`, `models/curriculum_levels.dart`, `database/daos/` (multiple DAOs — including `AttendanceDao`, `ExamsGradesDao`, `SubjectsDao` for student tabs), `services/members.dart`, `ui/widgets/` (shared components), `cache/file_cache.dart`, `client.dart`
- **Depended on by:** `home/home_screen.dart` (navigates to `SchoolDashboardScreen`)

## Conventions

- All screens in subdirectories receive `SchoolContext` and `ActiveTermContext` from the shell — they never create their own.
- All data queries are scoped by `schoolId` from `SchoolContext.membership.school.id` and `year`/`term` from `ActiveTermContext.currentTerm`.
- Navigation within the dashboard uses the shell's routing (selected nav item index), not `Navigator.push` — except for detail pages (grade detail, role detail) which use push routes.
- Owner detail uses a responsive modal (`_OwnerInfoSheet`) — bottom sheet on mobile, side sheet on desktop. Remove owner action has full error handling (null account snackbar + try/catch with failure snackbar). Teacher detail uses a responsive modal (`_TeacherInfoSheet`) — bottom sheet on mobile, side sheet on desktop — with grouped icon action buttons (`_ActionIconGroup`/`_ActionIconItem`) replacing the old full-page `_TeacherDetailPage` and separate status-change bottom sheet. Staff detail uses a responsive modal (`_StaffInfoSheet`) — bottom sheet on mobile, side sheet on desktop — with grouped icon action buttons (`_ActionIconGroup`/`_ActionIconItem`) replacing the old full-page `_StaffDetailPage` and separate status-change bottom sheet; matches the teacher pattern exactly. Student detail navigates to `StudentDetailPage` (in separate file `student_detail_page.dart`) with 4 tabs — the old `_StudentDetailStub` has been removed from `members_page.dart`. Guardian creation uses a searchable student picker (`_StudentPickerSheet`) with search-as-you-type via `MembersDao.searchStudents()`. Guardian list now shows **unique guardians** (one row per user via `watchUniqueGuardians`) with ward count badge. Guardian detail is a responsive modal (`_GuardianWardsSheet`) — bottom sheet on mobile, side sheet on desktop — showing a reactive ward list with per-ward edit and unlink actions. The old `_GuardianRow`, `_GuardianResolved`, `_GuardianDetailPage`, and `_GuardianDetailPageState` have been removed. `_MemberDetailStub` has been removed — all member types now have dedicated detail sheets/modals.
- `PhoneFirstPanel` footer replaced: the old full-width `_CtaButton` (indigo, label-based) has been removed and replaced with a compact right-aligned footer row containing a plain "Cancel" text button and a `_ConfirmIconButton` (green tick icon, 36×36, `AppTheme.brandGreen`). The `ctaLabel` parameter remains on `PhoneFirstPanel` but is now unused by the button.
- `PhoneFirstPanel` now supports **duplicate detection at lookup time** via an optional `checkAlreadyExists` callback (`Future<bool> Function(UsersData user)?`). When a phone number resolves to an existing user, the callback is invoked to check if that user already holds the target role at the school. If duplicate is detected: an `_isDuplicate` flag is set, the `alreadyExistsMessage` is shown via `_ErrorBanner`, and the confirm button is disabled. The flag resets when the phone input changes. All four phone-first panels (`add_owner_panel.dart`, `add_teacher_panel.dart`, `add_staff_panel.dart`, `add_guardian_panel.dart`) pass `checkAlreadyExists` using the corresponding `MembersDao` existence check (`ownerExists`, `teacherExists`, `staffExists`, `guardianExists`).
- The "Generate Timetable" button is a stub — displays a snackbar, algorithm not implemented.
- `EduTabBar` now left-aligns the tab strip when `isScrollable: true` — the strip container is wrapped in `Align(alignment: Alignment.centerLeft)` so it hugs its content width instead of stretching full-width. The `MembersPage` tab bar uses `padding: EdgeInsets.only(left: 16, top: 8, bottom: 8)` for flush left alignment. This fix applies globally to all `EduTabBar` (and `EduTabBarBottom`) instances with `isScrollable: true`.
- **Card-based member list items:** `_MemberListView` now adds horizontal padding (`left: 16, right: 16, top: 8, bottom: 80`) and wraps each item in `Padding(bottom: 6)` for consistent spacing. `_BaseTile` uses a tinted `Material` background (`surfaceContainerHighest` with alpha 0.4 dark / 0.3 light`), `borderRadius: 6`, inner padding `horizontal: 14, vertical: 10`, and subtitle alpha reduced to `0.55` — matching the Departments tab card style exactly. All member tabs (Owners, Teachers, Staff, Students, Guardians) inherit this style via `_BaseTile`.
- **Department delete button:** Each department list item now has a delete icon (`Icons.delete_outline_rounded`, size 16, error color at 0.45 alpha) between the text content and the chevron. Tapping it shows an `AlertDialog` confirmation ("Delete \"{name}\"?" / "This department will be permanently removed.") with Cancel and Delete actions. On confirmation, calls `DepartmentsDao.deleteDepartment(schoolId, name, accountId: userId)` using the active account from `cache.currentUser`. The `_confirmDeleteDepartment` method lives in `_DepartmentsTabState`.
- **Department detail redesign (Task 06):** `_DepartmentDetailScreen` now has 3 tabs (All, Teachers, Staff) using `EduTabBarBottom` with `isScrollable: true` and left-aligned padding. The "All" tab uses `_DeptAllMemberList` — a dual-`StreamBuilder` widget combining teacher and staff streams into a single list with role-tag chips and per-item remove buttons. Per-tab "Assign" buttons removed from `_DeptMemberList` (the `onAssign` parameter no longer exists); replaced by a single `FloatingActionButton.small` on the Scaffold. The FAB opens `_AssignMemberSearchSheet` — a stateful bottom sheet with a search `TextField` (name/phone), Teachers/Staff toggle via `_DeptFilterChip` widgets, and a filtered list of unassigned members. Tapping an item assigns them to the department and closes the sheet. `_DeptMemberList` count header simplified to plain text (no longer a row with assign button).
- **Academics — Stream tabs + Content tabs pattern:** `GradeDetailPage` uses a two-layer tab architecture. Layer 1 (Stream Tabs): "Comparisons" + one tab per stream. Layer 2 (Content Tabs, visible only on a specific stream): Students · Exams · Subjects · Attendance · Timetable · Lessons · Teachers. Both layers use `EduTabBar` with `isScrollable: true`.
- **Academics — Back button convention:** All push-route pages in academics (`GradeDetailPage`, `StudentGradePage`, `ExamDetailPage`, `PaperDetailPage`) use `Icons.chevron_left_rounded` (size 24), title `fontSize: 15, fontWeight: w500, color: cs.onSurface`, and `elevation: 0, scrolledUnderElevation: 0, backgroundColor: cs.surface`. **Exams shell back buttons:** All in-shell back buttons in `exams_grades_screen.dart` (`_ExamGroupDetailView` header and `_PaperDetailView` via `_SectionHeader`) use `Icons.chevron_left` (size 22) — not `Icons.arrow_back`. The `_SectionHeader` widget renders `leadingAction!.icon` at size 22. Do NOT use `Icons.arrow_back` anywhere in the exams shell.
- **Academics — FAB action sheet pattern:** `GradeDetailPage` FAB is context-sensitive per content tab. Single-action tabs (Students, Exams, Subjects, Timetable, Lessons) execute the action directly on tap. Multi-action tabs (Teachers: Assign Class Teacher + Assign Subject Teacher) open a bottom sheet action picker. Attendance tab has no FAB (marking is inline). FAB animates in/out via `ScaleTransition` when switching to/from Comparisons tab, and bounces subtly when switching between content tabs. Create Exam sheet (`_CreateExamFromGradeSheet`) pre-fills grade and stream from context.
- **Academics — Attendance marking is embedded:** Attendance marking lives exclusively in `AttendanceTab` inside the grade detail page. The standalone `AttendanceScreen` for teacher/owner roles redirects to `GradeDetailPage` with `initialContentTabIndex = 3`.
- **Academics — Shared widgets used:** `ThinProgressBar` (`ui/widgets/thin_progress_bar.dart`) for percentage bars, `TrajectoryIndicator` (`ui/widgets/trajectory_indicator.dart`) for stream/student trend display, `StatBadge` (`ui/widgets/stat_badge.dart`) for compact stat chips. Color utilities from `core/academic_utils.dart` (`percentageColor`, `masteryColor`, `formatPercent`, `formatScore`, `formatDateFromDays`).
- **Academics — Empty state consistency:** All tabs use centered column pattern: 52×52 rounded container (radius 10, `surfaceContainerHighest` alpha 0.5) + icon (22px, `onSurfaceVariant` alpha 0.3) + 18px gap + title (13.5px, w500, `onSurface`) + 5px gap + subtitle (12px, w400, `onSurfaceVariant` alpha 0.5). Loading states: centered 20×20 `CircularProgressIndicator(strokeWidth: 2)` with `onSurfaceVariant` alpha 0.4.
- **Academics — Ranking animation:** Comparisons tab ranking table uses staggered fade-in + slide animation (50ms delay per row, 200ms duration, `Curves.easeOut`). Top-ranked stream gets a subtle gold tint (`#FFA726` at 6–8% alpha).

## Last Updated
- Tasks R11–R12 — `exams_grades_screen.dart`: **R11 — FAB heroTag verification.** Confirmed `FloatingActionButton.small` in `_ExpandableFabState` already carries `heroTag: 'fab_exam_detail_main'`. No additional FAB instances exist in the file — no changes needed. **R12 — Diagnostics pass.** Ran `dart analyze` and resolved all 5 issues: (1) Removed unused `_selectedGradeConfig` getter (`unused_element` warning in `_AddGradeToExamFormState`). (2) Removed unused `_sameDay` static method (`unused_element` warning in `_GradeDateTrigger`-related class). (3) Removed unnecessary `dispose()` override that only called `super.dispose()` (`unnecessary_overrides` info). (4+5) Added curly braces to two bare-`return` `if` statements in `_loadSubjectsForStream` methods across two different state classes (`curly_braces_in_flow_control_structures` info). File now: **0 errors, 0 warnings**. All tasks R01–R12 complete. `TASKS.md` cleared (header only).
- Task R10 — `exams_grades_screen.dart`: Dark mode + aesthetic polish pass. All modals and overlays audited for dark/light correctness: `_AddGradeToExamDialog`, `_AddGradeToExamSheet`, `_AddStreamToExamDialog`, `_AddStreamToExamSheet`, `_CreatePaperSheet` dialog/sheet, `_SubjectMenuOverlayWidget`, `_StreamDropdownOverlay`, `_StreamSelectorOverlayWidget`. Verified: all use `isDark ? Color(0xFF18222E) : cs.surface` for sheet backgrounds and `isDark ? Color(0xFF1E2A3A) : cs.surface` for overlay/dropdown backgrounds. All `AnimatedContainer` transitions at 140ms. All `borderRadius` values at 4 (chips), 8 (cards/inputs), 12 (modal containers) per §21. All `InkWell` and `GestureDetector` tap targets use `NoSplash.splashFactory` where applicable. File compiles cleanly: 0 errors, 5 pre-existing infos/warnings.
- Task R09 — exams_grades_screen.dart: Deleted dead code blocks: Block A (_ClassPerformanceSection, _ClassPerformanceSectionState, _StudentRankRow + "Analytics header — donut + bar chart" banner), Block B (_SubLabel, _IconActionButton, _SegmentedRow, _DatePickerField, _FieldLabel), Block C (_ExamSummaryCard, _InfoRow, _PaperCard + "Summary cards" banner), Block D (_EmptyPapersState). Fixed 3× `(_, __)` → `(_, _)` in separatorBuilder callbacks (_PaperTimetableMobileState, _GradeSpreadsheetState, _GradeListState). File now: 0 errors, 5 pre-existing infos/warnings.
- Task R07 — `exams_grades_screen.dart`: **Wired `_showAddStreamModal` with adaptive modal and full stream configuration form.** `_ExamGroupDetailViewState._showAddStreamModal(context)` dispatches to `showDialog` (desktop ≥600px) or `showModalBottomSheet` (mobile). **`_AddStreamToExamDialog`** (StatelessWidget): transparent `Dialog` → `ConstrainedBox(maxWidth: 520)` → `Container(Color(0xFF18222E) dark / cs.surface light, BorderRadius.circular(12), dual box-shadow)` → `_AddStreamForm`. **`_AddStreamToExamSheet`** (StatelessWidget): bottom sheet with `BorderRadius.vertical(top: Radius.circular(16))`. **`_AddStreamForm`** (StatefulWidget): single-step form for adding missing streams to an existing grade in the exam. **Key data logic:** `_gradesWithMissing` getter — finds grades already in the exam group that still have ≥1 stream missing from the group; `_missingStreamsForGrade(int grade)` — cross-references config `StreamConfig.code` list against `ExamGradeEntry.streams` to find absent stream codes. **State:** `_selectedGradeIndex` (int), `_activeStreamCode` (int?), `_paperSlots` (Map<int?, List<_GradePaperSlot>>), `_subjects` (Map<int?, List<...>>), `_loadingSubjects` map, `_teachers` list, `_teachersLoaded`, `_saving`. **UI structure:** drag handle (sheet only) → header ("Add Stream" + grade label badge + close icon) → optional multi-grade selector chips (hidden when only one grade has missing streams) → stream dropdown trigger (macOS-style, indigo-tinted border when selected, "N set" badge) → `_GradePaperSlotCard` list for active stream → footer (Cancel + `_GradeConfirmButton`). **Stream dropdown trigger:** `GestureDetector` with `_streamTriggerKey` (GlobalKey) inserts an `OverlayEntry` via `_toggleStreamOverlay()`. **`_StreamDropdownOverlay`** (StatefulWidget): `FadeTransition` + `ScaleTransition` (0.95→1.0, 120ms, `Curves.easeOutCubic`) → `Material` container (`Color(0xFF1E2A3A)` dark / `cs.surface` light, `BorderRadius.circular(6)`, 1px border, dual box-shadow). Menu items: 36px tall, selected=indigo w500 + check icon; divider + "Auto-fill all streams" special action (indigo, `Icons.bolt_rounded`). **Auto-fill:** `_autoFillStream(grade, streamCode)` fills slots using 3-per-day/2hr/08:00-start algorithm (same as `_AddGradeToExamFormState`). `_autoFillAllStreams()` calls it for every missing stream of the current grade. **Save logic:** iterates `_currentMissingStreams`, builds `ExamBatchEntry` per stream using template from first existing stream exam row (`templateExam`), calls `widget.dao.createExamBatch(entries, accountId)`. Drift reactivity refreshes parent `StreamBuilder`. **Empty state:** `Icons.check_circle_outline_rounded` + "All streams included" text + Close. File compiles cleanly: 0 errors, 0 warnings (14 pre-existing info/warning items — none new from R07).
- Task R06 — `exams_grades_screen.dart`: **Wired `_showAddGradeModal` with full two-step adaptive modal.** `_ExamGroupDetailViewState._showAddGradeModal(context)` now dispatches to `showDialog` (desktop ≥600px) or `showModalBottomSheet` (mobile) instead of the previous stub. **`_AddGradeToExamDialog`** (StatelessWidget): `Dialog(backgroundColor: transparent, elevation: 0)` → `ConstrainedBox(maxWidth: 520)` → `Container` with dark palette (`Color(0xFF18222E)` dark / `cs.surface` light), `BorderRadius.circular(12)`, dual box-shadow matching `_CreateTermDialog`. Passes `membersDao: MembersDao(db)` and `subjectsDao: SubjectsDao(db)` to the shared form. **`_AddGradeToExamSheet`** (StatelessWidget): bottom sheet with `BorderRadius.vertical(top: Radius.circular(16))`, same bg colors, wraps the same shared form. Both pass `isSheet: bool` to the form. **`_AddGradeToExamForm`** (StatefulWidget, `SingleTickerProviderStateMixin`) — two-step flow driven by `int _step` (0 = grade+stream selection, 1 = paper timetable): **Step 1 state:** `int? _selectedGrade`, `Set<int?> _selectedStreams`. Available grades computed from `SchoolConfig.curricula` excluding grades already in `group.participatingGrades`. **Step 1 UI:** header ("Add Grade", 15px w600 + dynamic subtitle + close icon); `_GradeStepDots` indicator; `_GradeOptionRow` list (36px `AnimatedContainer`, indigo-tinted bg + border when selected, stream count badge, `NoSplash`); `_StreamToggleRow` list (32px, custom 16×16 checkbox `BorderRadius.circular(4)`, indigo checked, white check icon) — only shown when selected grade has streams; footer with Cancel `TextButton` + `_GradeNextButton` (indigo fill 36px, `Icons.chevron_right`, disabled when no grade selected or still loading). Empty state when all grades already included: `Icons.check_circle_outline` (32px, indigo 40%) + "All grades included" text + Close button. **Step 2 state:** `int? _activeStreamCode`, `Map<int?, List<_GradePaperSlotCard>> _paperSlots`, `Set<int?> _configuredStreams`, `Map<int?, List<Subject>> _subjects`, `Map<int?, bool> _loadingSubjects`, `List<({TeachersData teacher, UsersData user})> _teachers`, `bool _teachersLoaded`. `_goToStep2()` pre-loads subjects for all selected streams via `_loadSubjectsForStream(grade, streamCode)` and activates the first stream via `_step2ActivateStream()`. `_step2ActivateStream(streamCode)` sets `_activeStreamCode` and auto-fills paper slots (3/day, 2hr, 08:00 start) via `_autoFillStream()` if not yet populated. **Step 2 UI:** header subtitle dynamically shows active stream name + paper count; stream selector trigger (`_SubjectTrigger`-style, opens `_StreamSelectorOverlay`) when multiple streams; per-stream `_GradePaperSlotCard` list + `_GradeAddPaperButton` (dashed `CustomPaint` border, 40px, `Icons.add_rounded`); footer with `IconButton(Icons.chevron_left)` left (goes back to step 1) + Cancel + `_GradeConfirmButton` (green, 36×36, check icon) right. **`_GradePaperSlotCard`** (StatefulWidget): card bg `Color(0xFF1A2536)` dark / `surfaceContainerHighest alpha 0.5` light, `BorderRadius.circular(8)`, 1px `outlineVariant` border. Header row: subject name (resolved via `_subjectLabel`) + remove `IconButton`. Body: `_SubjectTrigger` → `_SubjectMenuOverlay`; `_PaperDateTrigger` → `_PaperSingleCalendar` (`AnimatedCrossFade`); inline `_TimeField` trigger → `AnimatedCrossFade` → `_InlineTimeConfigurator` (reused from `_CreatePaperSheet`); duration badge + date label. State: `_timeOpen`, `_durationMinutes`, 4 `FixedExtentScrollController`s. **`_GradePaperSlot`** model: `id`, `date`, `startTime`, `endTime`, `subjectCode`, `invigilatorId`, computed `duration` getter. **`_GradeStepDots`**: two 8×8 circles (active = filled indigo, inactive = outlined indigo), `AnimatedContainer` 140ms. **`_GradeNextButton`**: indigo fill, 36px height, `Icons.chevron_right`, disabled at 0.4 opacity. **`_GradeAddPaperButton`**: `CustomPaint` `_DashedBorderPainter` (5px dash / 4px gap), 40px height, `Icons.add_rounded` (16px, indigo). **Save logic:** iterates `_configuredStreams`, builds `ExamBatchEntry` per stream using `_streamPaperSlots[streamCode]`, invigilator falls back to `accountId`, calls `widget.dao.createExamBatch(entries: entries, accountId: accountId)`. Drift reactivity auto-refreshes the parent `StreamBuilder<List<ExamGroup>>` on `watchExamGroups`. **Step animation:** `AnimatedSwitcher` (200ms, `Curves.easeOutCubic`) switches between step 1 and step 2 content. Dialog `maxWidth` widened from 440 to 520 to accommodate paper slot cards. File compiles cleanly: 0 errors, 0 warnings.
- Task R04 — `exams_grades_screen.dart`: **Replaced `_PaperContentArea` placeholder list with responsive timetable views.** `_PaperContentArea.build()` now uses `LayoutBuilder` to switch between `_PaperTimetableGrid` (≥600px, desktop) and `_PaperTimetableMobile` (<600px, mobile) instead of the old `ListView.separated`. **New top-level helper functions added:** `_groupPapersByDate(List<Paper>)` — groups papers by day (midnight-anchored `DateTime`), sorts within each day by start; `_sortedPaperDates(Map<DateTime,List<Paper>>)` — sorted unique dates; `_uniquePaperStartTimes(List<Paper>)` returns `List<({String start, String end})>` of sorted unique start/end HH:MM pairs; `_paperAt(Map, DateTime, String)` finds paper at date+startTime label; `_fmtDayHeader(DateTime)` → "Mon, 12 Jan"; `_fmtDayColumn(DateTime)` → "Mon 12". **New widget classes:** `_PaperTimetableGrid` (StatelessWidget, desktop) — `SingleChildScrollView` (vertical) containing `SingleChildScrollView` (horizontal) with `IntrinsicWidth Column`; header row via `_PaperGridHeaderRow` + data rows via `_PaperGridRow` (one per unique start time). `_PaperGridHeaderRow` — 72px gutter placeholder + `_PaperHeaderCell` per date. `_PaperHeaderCell` — 140px wide, "Mon 12" (fontSize 11, w500, onSurfaceVariant), center-aligned. `_PaperGridRow` — `_PaperTimeLabel` (72px) + `_PaperSlotBox` or `_PaperEmptyCell` (140px each) per date. `_PaperTimeLabel` — right-aligned two-line column: start (10.5px, w500, alpha 0.65) + end (9.5px, w400, alpha 0.4), both with `FontFeature.tabularFigures()`. `_PaperSlotBox` — tappable 140px cell: status-colored 2.5px left border + 6% alpha tinted bg + 1px outlineVariant border; subject name + paper label (12px, w500), time range (9.5px, w400, tabular), invigilator ("Inv: {name}", 10px, alpha 0.5). `_PaperEmptyCell` — 140px, 56px min-height, 1px outlineVariant border at alpha 0.12. `_PaperTimetableMobile` (StatefulWidget, mobile) — state: `_grouped`, `_dates`, `_selectedDayIndex`; `didUpdateWidget` rebuilds groups + clamps index. Column: horizontal `SingleChildScrollView` of `_PaperDayChip` selectors + day heading (13px, w500) + `Expanded ListView.separated` of `_PaperSlotCard`. `_PaperDayChip` — `AnimatedContainer` 140ms, `BorderRadius.circular(4)`, selected = cs.primary fill + cs.onPrimary text; unselected = transparent + outlineVariant border at 50% alpha. **`_PaperStatusLegend`** (StatelessWidget, new) — compact `Wrap` of 4 color-dot + label pairs (8×8 dot, BorderRadius.circular(2), fontSize 10.5, w400, muted). Inserted above paper content area in `_ExamGroupDetailViewState.build()` when `_currentStreamEntry != null`. File compiles cleanly: 0 errors, 5 expected warnings (dead widgets `_SegmentedRow`, `_ExamSummaryCard`, `_PaperCard`, `_EmptyPapersState`, `_generateId`).
- Tasks R02 + R03 — `exams_grades_screen.dart`: **R02 — Reactive shell rebuild.** `_ExamsShellState` fields updated: removed `ExamWithPapers? _selectedExam`, added `String? _selectedGroupKey`, `Exam? _selectedExamRow`, `late final ExamsGradesDao _dao` (initialized in `initState`). `_openExam(ExamGroup)` now sets `_selectedGroup` + `_selectedGroupKey` only (no more bridge record). `_openPaper(Paper, Exam)` now accepts a second `Exam` parameter and stores `_selectedExamRow`. `_popToList()` clears `_selectedGroup`, `_selectedGroupKey`, `_selectedExamRow`, `_selectedPaper`. `_popToExam()` also clears `_selectedExamRow`. `_ExamsView.examDetail` is now wrapped in `StreamBuilder<List<ExamGroup>>` on `_dao.watchExamGroups()` — finds the matching group by `_selectedGroupKey` on each emission, keeping `_selectedGroup` up-to-date reactively; if group disappears, calls `_popToList()` via `addPostFrameCallback`. `_ExamsView.examDetail` now renders `_ExamGroupDetailView` (not `_ExamDetailView`). `_ExamsView.paperDetail` constructs `ExamWithPapers` bridge inline from `_selectedExamRow!` + `_selectedGroup!.teacher` + `const []` papers. **R03 — New `_ExamGroupDetailView` widget.** `StatefulWidget` with `TickerProviderStateMixin`. Constructor params: `group, schoolId, year, term, config, entry, onBack, onPaperTap (void Function(Paper, Exam)), onDeleted`. State fields: `_gradeTabController` (TabController), `_selectedGradeIndex`, `_streamTabController` (TabController?), `_selectedStreamIndex`, `_dao` (ExamsGradesDao), `_membersDao` (MembersDao), `_teacherNames` (Map<String,String>). `initState` initializes both DAOs, grade tab controller with listener, calls `_rebuildStreamTabs()` + `_loadTeacherNames()`. `didUpdateWidget` reinitializes grade tab controller and rebuilds stream tabs when group changes. `dispose` disposes both tab controllers. `_onGradeTabChanged` updates `_selectedGradeIndex`, resets `_selectedStreamIndex`, calls `_rebuildStreamTabs()`. `_rebuildStreamTabs` disposes old `_streamTabController` and creates a new one only when current grade has `streams.length > 1`. `_loadTeacherNames` collects all invigilator user IDs from all papers in the group, resolves via `_membersDao.findUserById()`, stores in `_teacherNames`. `_currentStreamEntry` getter returns `group.grades[_selectedGradeIndex].streams[_selectedStreamIndex]` (clamped), or null if grades empty. `_canManage` getter returns true for `TeacherEntry`, `OwnerEntry`, `StaffEntry`. `_confirmDeleteGroup` shows `_ConfirmDeleteDialog`, then loops `group.examIds` calling `_dao.deleteExam()` for each, then calls `widget.onDeleted()`. **Header:** custom `Row` with leading `IconButton(Icons.chevron_left, size: 22)` (NOT `Icons.arrow_back`), `Expanded` column with type label (fontSize 15, w500) + year/term subtitle (fontSize 12, w400, `onSurfaceVariant`), trailing delete `IconButton(Icons.delete_outline_rounded, size: 18, cs.error alpha 0.6)` shown only when `_canManage`. **Grade tabs:** `EduTabBar(isScrollable: true, padding: EdgeInsets.fromLTRB(16, 6, 16, 4))` with one tab per grade. **Stream sub-tabs:** `EduTabBar(isScrollable: true, padding: EdgeInsets.fromLTRB(16, 2, 16, 4))` shown only when `streamTabs.isNotEmpty && _streamTabController != null`. **Paper content area:** `_currentStreamEntry == null` → `_EmptyPapersTimetableState`; otherwise `_PaperContentArea`. **New `_PaperContentArea` widget** (StatelessWidget): takes `streamEntry, schoolId, config, teacherNames, dao, canManage, onPaperTap`. Uses `StreamBuilder<List<Paper>>` on `dao.watchPapersForExam(schoolId, streamEntry.exam.id)`. Empty → `_EmptyPapersTimetableState`. Non-empty → `ListView.separated` of `_PaperSlotCard` items. **New `_PaperSlotCard` widget** (StatelessWidget): status-colored left border 2.5px + tinted bg (status color at 6% alpha) + thin 1px `outlineVariant` border on remaining 3 sides. Shows subject name (13px, w500), paper number label if > 1, time range (11.5px, w400, `onSurfaceVariant`), invigilator name ("Inv: {name}", 10.5px, alpha 0.6), `_StatusChip` + chevron on right. **New `_EmptyPapersTimetableState` widget** (StatelessWidget): centered 52×52 container (radius 10, `surfaceContainerHighest` alpha 0.5) + `Icons.event_note_outlined` (22px, alpha 0.3) + "No papers scheduled" (13.5px, w500) + subtitle (12px, w400, alpha 0.5). **New top-level helper functions:** `_paperStatusColor(PaperStatus, ColorScheme)` — pending=`onSurfaceVariant alpha 0.3`, progress=`Color(0xFF42A5F5)`, done=`Color(0xFFFFA726)`, marked=`Color(0xFF66BB6A)`; `_paperStatusLabel(PaperStatus)` — Pending/In Progress/Done/Marked. **Deleted:** `_ExamDetailView`, `_ExamDetailViewState` (replaced by `_ExamGroupDetailView`). **Dead `_CreateExamSheet` + `_CreateExamSheetState`** also removed in this batch (was previously missed in R01). **Added imports:** `database/daos/members_dao.dart`, `ui/widgets/edu_tab_bar.dart`. File compiles cleanly: 0 errors, 6 expected warnings (dead widgets `_SegmentedRow`, `_ExamSummaryCard`, `_PaperCard`, `_EmptyPapersState`, `_paperStatusLabel`, `_generateId` — all retained for R04–R09 use).
- Task R01 — `exams_grades_screen.dart`: Replaced `_ExamsListView` data source with `ExamGroup` model. `_buildStream()` now calls `_dao.watchExamGroups(...)` returning `Stream<List<ExamGroup>>`. `onExamTap` callback type changed from `ValueChanged<ExamWithPapers>` to `ValueChanged<ExamGroup>`. Replaced `_ExamCard` with `_ExamGroupCard` — transparent container with thin 1px `outlineVariant` border, `BorderRadius.circular(4)`, `NoSplash` inkwell; Row 1: type badge (w500) + date range; Row 2: `Wrap` of `_ClassChip` per grade; Row 3: `_MetaBadge` row (subject count, grade count, optional "Custom" if personalized). Removed `onDelete` + `_confirmDelete` from list view (delete moved to detail). `_showCreateExam` now calls `Navigator.of(context).push(MaterialPageRoute(...))` to `ExamCreationPage` (replaces old `_CreateExamSheet` bottom-sheet call). `_EmptyExamsState` simplified: removed `onCreate` callback and CTA button — subtitle reads "Tap + to create your first exam." Shell's `_openExam` updated to accept `ExamGroup`; bridge record constructed for old `_ExamDetailView` (temporary, replaced in R02). Added `_selectedGroup: ExamGroup?` field to `_ExamsShellState`. Added imports: `models/exam_group.dart`, `exam_creation_page.dart`. File compiles with 0 errors, 1 expected warning (`_selectedGroup` unused — will be used in R02).
- Tasks 23-26 -- Four fixes/features in `paper_detail_page.dart`:
  (23) Button label fix: `_PaperActionBarState._buttonConfig` for `PaperStatus.done` label changed from `'Graded'` to `'Grade'` (imperative command).
  (24) Persist answer submissions: New `paper_submissions` client-only Drift table (`PaperSubmissions`, `@DataClassName('PaperSubmissionData')`) stores local file paths for submitted answer images. `_GradeSpreadsheetState.initState` and `_GradeListState.initState` now call `_loadPersistedSubmissions()` which reads `dao.getSubmissionsForPaper()` and seeds `_submissions` map on page open. `_AnswerSubmissionSheet` gains required `dao: ExamsGradesDao` param; `_addPhotos` persists each new path via `dao.insertSubmission()`; `_removePhoto` captures `removedPath` before mutation and calls `dao.deleteSubmission()`. DB layer: `PaperSubmissions` registered in `AppDatabase` (schemaVersion->5, `onUpgrade from < 5` creates table); added to `ExamsGradesDao` `@DriftAccessor` tables plus 5 new methods (getSubmissionsForStudent, getSubmissionsForPaper, insertSubmission, deleteSubmission, clearSubmissionsForStudent). build_runner regenerated.
  (25) Analytics count fix: `_AnalyticsSection._compute()` now filters `gradeRows` to `actuallyGraded = gradeRows.where((r) => r.grade.score > 0).toList()`. `gradedStudents`, `averageScore`, `averagePercent` all computed from `actuallyGraded`. Distribution loop also iterates `actuallyGraded` only -- zero-score shell rows excluded from donut chart percentage and count display.
  (26) Per-student Quick Grade button: When `submissionCount > 0`, a 28x28 indigo-tinted (`Color(0xFF6366F1).withValues(alpha: 0.12)`) `GestureDetector`-wrapped container with `Icons.auto_fix_high` (15px, `Color(0xFF6366F1)`) and tooltip 'Quick-grade with AI' appears to the LEFT of the submit-photos button. Desktop: added to `_SpreadsheetRowState.build` via two new required params on `_SpreadsheetRow`: `isQuickGrading: bool` and `onQuickGradeTap: VoidCallback`. Mobile: inline button added to `_GradeListState` item builder before the submit button. Both `_GradeSpreadsheetState` and `_GradeListState` gain `Map<int, bool> _quickGrading` field and `_quickGrade(int adm)` async method -- generates random score (55-100), calls `dao.upsertGrade`, updates `_controllers[adm]?.text` (desktop only), triggers row flash. Button disabled during `_aiMarking`. File compiles cleanly: 0 errors, 0 warnings.
- Task 22 — Bug fix in `exams_grades_screen.dart`: paper timetable slot status colour was not updating after navigating back from paper detail page. Three-part fix: (1) `_PaperTimetableMobileState.initState` now initialises `_grouped`/`_dates` directly (no `setState`) and `_rebuildGroups()` is only called from `didUpdateWidget`; `_rebuildGroups()` wraps all mutations in `setState(...)` so the widget tree repaints on every emission. (2) `didUpdateWidget` guards the `_rebuildGroups()` call with a `_papersEqual()` check comparing `status`, `subject`, and `paper` fields — avoids unnecessary rebuilds when the list reference changes but content is identical. (3) The `StreamBuilder<List<Paper>>` in `_ExamGroupDetailViewState.build()` now carries `key: ValueKey('papers_${currentStream.exam.id}')` so Flutter creates a fresh builder when switching between grade/stream tabs. `_PaperTimetableGrid` (StatelessWidget) confirmed correct — purely prop-driven, no internal caching. File compiles cleanly: 0 errors, 0 warnings.
- Task 21 — Bug fix in `paper_detail_page.dart`: `_PaperDetailPageState` gains a `_paperStream` (`Stream<Paper?>`) initialized in `initState` via `ExamsGradesDao.watchPaper(...)`. The `build` body is wrapped in an outer `StreamBuilder<Paper?>` that derives `currentPaper` (falling back to `widget.paper` if null). `_PaperActionBar`, `_PaperInfoCard`, `_GradeSpreadsheet`, and `_GradeList` all receive `currentPaper` so status chips and action buttons update immediately after `_advance()` writes to the DB, without requiring page re-entry. Added `watchPaper` to `ExamsGradesDao` (see daos/CONTEXT.md). Both files compile cleanly: 0 errors, 0 warnings.
- Task 20 — Bug fix: `_subjectLabel` in `exams_grades_screen.dart` now uses `config.curricula` to determine the school's curriculum before looking up a subject code. Previously it always tried CBC first, causing subject code collisions (e.g. code 55 resolving to the wrong subject for an 8-4-4 school). Fix: reads `config.curricula.map((c) => c.type).toSet()`, tries only `CbcSubject.values` when the set contains `CurriculumType.cbc`, only `EightFourFourSubject.values` when it contains `CurriculumType.eightFourFour`, and falls back to trying both only when the config is empty. No other changes — all 6 call sites already pass `config`. File compiles cleanly: 0 errors, 0 warnings.
- Task 18 — Grade entry button and flash animation added to both exams_grades_screen.dart and paper_detail_page.dart. Desktop: _SpreadsheetRow converted from StatelessWidget to StatefulWidget (_SpreadsheetRowState). New required params: flashController (AnimationController) and onGradeButtonTap (VoidCallback). Pencil icon button (Icons.edit_outlined, size 14) appears right of score input when paperStatus is done or marked and canGrade is true. On tap: focuses field and selects all text; if already focused, triggers save. AnimatedBuilder wraps the row with indigo flash (Color(0xFF6366F1) alpha*0.08) over 400ms (forward+reverse) on successful save. _GradeSpreadsheetState gains TickerProviderStateMixin and Map<int,AnimationController> _flashControllers (init in _initFromMap, disposed in dispose). _saveRow triggers flash after DAO write. Mobile: _GradeListState gains TickerProviderStateMixin and Map<int,AnimationController> _flashControllers (init in initState, new dispose override). Grade badge pill: if graded shows score/total in green-tinted bordered container (brandGreen alpha 0.10 bg, 0.25 border, radius 4); if not graded shows "Not graded" in muted text. cardContent wrapped in AnimatedBuilder for flash. _MobileGradeEntrySheet gains optional onSaved callback called before Navigator.pop on successful save. Both files compile cleanly: 0 errors, 0 warnings.
- Task 19 — AI "Mark with AI" button added to `paper_detail_page.dart`. New `_AiPhase` enum (idle, analyzing, assigning, done) and `_AiMarkButton` StatelessWidget (shared between desktop and mobile, `fullWidth` param). **`_GradeSpreadsheetState` additions:** `_aiMarking` (bool), `_aiPhase` (_AiPhase), `_shimmerCtrl` (AnimationController 1200ms, looping), `_progressCtrl` (AnimationController 2000ms), `_aiMarkedCount` (int), `_hasSubmissions`/`_submissionCount` getters, `_runAiMarking()` async method (4-phase sequence). Desktop: `_AiMarkButton` rendered right-aligned above the header row inside an `Align`. **`_GradeListState` additions:** same fields + `_runAiMarking()`. Mobile: `_AiMarkButton` rendered as full-width card (`fullWidth: true`) above the `Material` list card inside a `Column`. **Both:** `_shimmerCtrl`/`_progressCtrl` initialized in `initState` and disposed in `dispose`. `canGrade` passed as `widget.canGrade && !_aiMarking` to `_SpreadsheetRow`; submit tap and grade entry tap disabled during AI marking. **`_AiMarkButton` button behavior:** indigo→violet `LinearGradient` (`Color(0xFF6366F1)` to `Color(0xFF8B5CF6)`), `BorderRadius.circular(4)`, `Icons.auto_awesome` icon (15px white) + label; `ShaderMask` shimmer animation during analyzing phase (looping gradient); progress bar (2px `LinearProgressIndicator`, indigo, `ClipRRect` radius 1) below button during marking + assigning; subtitle text "{N} papers submitted" / "No papers submitted" (10.5px, muted alpha 0.5); disabled state at 0.4 opacity with tooltip; done state shows `Icons.check` + "Marked N papers" for 2 seconds. **Grade generation:** `(55 + Random().nextInt(46)).toDouble()`, range 55–100. Each saved grade triggers row flash via `_flashControllers`. Button conditionally visible when `paper.status == PaperStatus.done && widget.canGrade`. File compiles cleanly: 0 errors, 0 warnings.
- Task 17 — Added per-student answer submission UI to both `exams_grades_screen.dart` and `paper_detail_page.dart`. **`_GradeSpreadsheetState`** gains `Map<int, List<String>> _submissions` (adm → local file paths) and `_openSubmissionSheet(context, student)` method. **`_GradeListState`** gains the same fields. **`_SpreadsheetRow`** gains two new required params: `paperStatus: PaperStatus` and `submissionCount: int`, plus `onSubmitTap: VoidCallback`. When `paperStatus == PaperStatus.done`, a `GestureDetector`-wrapped `Icons.upload_file_outlined` (muted, 16px) or `Icons.check_circle` (green, 16px) icon button appears to the left of the save button; name column gains an inline green `📄 N` badge when `submissionCount > 0`. **`_GradeList`** mobile view: same submit icon button replaces the `>` chevron when `paper.status == PaperStatus.done`; name row gains inline submission count badge. **`_AnswerSubmissionSheet`** (new `StatefulWidget`) added to both files: modal bottom sheet showing student name header, 3-column portrait-ratio image grid with per-thumbnail delete buttons + page-number labels, dashed-border "Add Photos" button (`image_picker.pickMultiImage(imageQuality: 85)`), and a "Done" button. Images saved to `{appDir}/submissions/{schoolId}/{examId}/{subject}_{paper}/{adm}/N.jpg`. `_DashedBorderPainter` added to `paper_detail_page.dart` (already existed in `exams_grades_screen.dart`). Imports added to both files: `dart:io`, `package:image_picker/image_picker.dart`, `package:path_provider/path_provider.dart`. `image_picker: ^1.2.1` was already present in `pubspec.yaml`. Both files compile cleanly: 0 errors, 0 warnings.
- Task 16 — Replaced `_PaperStatusRow` + `_StatusAdvanceButton` with unified `_PaperActionBar` in both files. Both compile cleanly.
- Task 15 — Updated timetable grid and mobile list in `exams_grades_screen.dart` to show start+end times and invigilator name. **`_uniquePaperStartTimes()`** changed from `List<String>` to `List<({String start, String end})>` — captures both labels per unique start time. **`_PaperTimeLabel`** updated to two-line layout: start (10.5px, w500, alpha 0.65) + end (9.5px, w400, alpha 0.4). **`_PaperSlotBox`** (desktop) and **`_PaperSlotCard`** (mobile) both accept `Map<String, String> teacherNames` and show `Inv: {name}` below the time range. **`_PaperTimetableGrid`** and **`_PaperTimetableMobile`** forward `teacherNames` to child slot widgets. **`_ExamGroupDetailViewState`** gains `_membersDao`, `_teacherNames`, and `_loadTeacherNames()` called in `initState`. Added import: `members_dao.dart`. File compiles cleanly: 0 errors, 0 warnings.
- Task 12 — Replaced dual `_TimeChipPicker` bottom-sheet time pickers with a single inline expandable time field in three places: (1) `_CreatePaperSheetState` in `exams_grades_screen.dart`, (2) `_GradePaperSlotCardState` in `exams_grades_screen.dart`, and (3) `_PaperSlotCardState` in `exam_creation_page.dart`. **`exams_grades_screen.dart` changes:** Added `_timePickerOpen` (bool), `_durationMinutes` (int, default 120), and four `FixedExtentScrollController` fields (`_startHourCtrl`, `_startMinCtrl`, `_durHourCtrl`, `_durMinCtrl`) to `_CreatePaperSheetState`. Added `_recomputeEndTime()` helper (derives `_endTime` from `_startTime + _durationMinutes`). Added `dispose()` override for the 4 controllers. `_validateTimes()` simplified to check `_durationMinutes > 0`. TIME section in `_buildContent()` now uses `_TimeField` trigger + `AnimatedCrossFade` → `_InlineTimeConfigurator` body. **Deleted widget classes:** `_TimeChipPicker`, `_PaperTimePickerSheet`, `_PaperTimePickerSheetState`, `_PaperWheelColumn`, `_PaperTimePresetChip`. **New widget classes:** `_TimeField` (StatelessWidget — animated trigger chip showing "HH:MM · XhYYm", animated chevron, animated border color when open), `_InlineTimeConfigurator` (StatefulWidget — two-column layout with Start Time wheels on left and Duration wheels on right, vertical divider separator, quick duration preset chips row at bottom), `_CompactWheelColumn` (StatelessWidget — compact `ListWheelScrollView` with 32px itemExtent, 1.5 diameterRatio, 1.1 magnification, indigo highlight band), `_DurationPresetChip` (StatelessWidget — compact chip for duration presets ["30m", "1h", "1h 30m", "2h", "2h 30m", "3h"]). `_GradePaperSlotCardState` updated similarly: added `_timePickerOpen`, `_durationMinutes`, 4 controllers, `initState`/`dispose`/`_onStartTimeChanged`/`_onDurationChanged`/`_recomputeEndTime` helpers. Time row replaced with animated `_TimeField`-style inline trigger + `AnimatedCrossFade` → `_InlineTimeConfigurator`. Duration badge in footer row now uses `_fmtDurationBadge()` helper. Removed `_fmtTimeOfDay` top-level function (no longer referenced). **`exam_creation_page.dart` changes:** Converted `_PaperSlotCard` from `StatelessWidget` to `StatefulWidget` (`_PaperSlotCardState`). Added `_timePickerOpen`, `_durationMinutes`, 4 controllers, `initState`/`dispose`/`_onStartTimeChanged`/`_onDurationChanged` to `_PaperSlotCardState`. Top row now shows inline animated time trigger (same pattern as `_TimeField`) instead of two `_InlineTimePicker` chips + duration badge + arrow. `AnimatedCrossFade` below top row expands to `_SlotInlineTimeConfigurator` (new StatefulWidget — same layout as `_InlineTimeConfigurator` but uses existing `_WheelColumn` and `_TimePresetChip` widgets from the file). **Deleted widget classes:** `_InlineTimePicker`, `_TimePickerSheet`, `_TimePickerSheetState`. Removed `_fmtTime(TimeOfDay)` top-level function (no longer referenced). Both files compile cleanly: 0 errors, 0 warnings.
- Task 13 — Redesigned `_AddStreamFormState` in `exams_grades_screen.dart` — replaced checkbox stream toggles with a dropdown-based stream selector and per-stream paper configuration. **State changes:** removed `_streamChecked` map; added `_activeStreamCode` (int?), `_streamPaperSlots` (Map<int, List<_GradePaperSlot>>), `_configuredStreams` (Set<int>). **New helpers:** `_activateStream(int)` — activates a stream and populates its paper slots from subjects if not yet populated; `_autoFillAllStreams()` — auto-fills all missing streams using the 3-per-day/2hr algorithm with cross-stream invigilator conflict detection; `_buildTeacherSchedule({int? excludeStream})` — builds a teacher schedule map for conflict detection; `_buildSlotsForStream(int, {required teacherSchedule})` — creates paper slots for a single stream respecting the invigilator schedule; `_addSlot(int)`, `_removeSlot(int, String)`, `_canSave()`. **New overlay widget classes:** `_StreamSelectorOverlay` (static helper, same pattern as `_SubjectMenuOverlay`) and `_StreamSelectorOverlayWidget` (StatefulWidget, FadeTransition + ScaleTransition 0.95→1.0, 120ms, Curves.easeOutCubic). Menu layout: "Auto-fill all streams" special row at top (indigo, bolt icon) + separator + per-stream items showing "checked configured" (green badge) or "· not set" (muted text). **UI changes:** replaced STREAMS label + `_buildStreamToggles()` with a STREAM selector trigger and `_buildStreamSelector()` (styled like `_SubjectTrigger`, layers icon, shows "N set" badge when streams configured); below it PAPERS section shows `_buildActiveStreamPapers()` (paper slot cards + add button for the active stream). **Save logic:** iterates `_configuredStreams`, builds `ExamBatchEntry` per configured stream using `_streamPaperSlots`. **`_AddGradeToExamFormState` aligned (partial Task 14 prep):** removed `_step2TabCtrl`, `_step2TabIndex`, `_activeStreamKeys`; added `_activeStreamCode`, `_configuredStreams`; replaced `_buildStep2Tabs()` + `_buildPaperSlots()` with `_initStep2State()` + refactored `_buildPaperSlots(List<int>)` (now takes explicit stream keys, adds cross-stream invigilator conflict detection); `_buildStep2Body()` replaced tab layout with stream dropdown + per-stream paper config; added `_buildStep2StreamSelector()`, `_step2ActivateStream()`, `_step2AutoFillAll()`, `_buildStep2ActiveStreamPapers()`, `_step2Subtitle()` helpers; header subtitle dynamically shows active stream name + paper count. **Deleted:** `_buildStreamToggles()`, `_buildSubjectsPreview()` in `_AddStreamFormState`; `_buildStep2Tabs()`, `_buildStreamPaperList()` in `_AddGradeToExamFormState`. File compiles cleanly: 0 errors, 0 warnings.
- Task 11 — Combined multi-paper checkbox and paper number selector into a single inline row with drumroll picker in `exams_grades_screen.dart`. **Removed:** `_PaperFieldLabel(label: PAPER NUMBER)` section label and its `SizedBox(height: 5)` spacer. Removed `_paperNumberCtrl` `TextEditingController` field, its `initState` init, and `dispose` call. Removed conditional 80px `TextField`. **Deleted:** `_PaperMultiToggleRow` (replaced by `_PaperNumberRow`). **Added:** `_PaperNumberRow` StatefulWidget (42px `AnimatedContainer`, checkbox + label left, `_PaperNumberWheel` right via `AnimatedSize`). **Added:** `_PaperNumberWheel` (`ListWheelScrollView.useDelegate` 44x30px, items 1-2-3, `FixedExtentScrollPhysics`, center highlight band). `_save()` reads `_paperNumber` directly. File compiles cleanly: 0 errors, 0 warnings.
- Task 10 — Replaced all subject picker bottom sheets with macOS-style inline overlay dropdowns. **`exams_grades_screen.dart`:** (1) Deleted `_SubjectPickerSheet` class (old `showModalBottomSheet`-based picker). (2) Added `_SubjectMenuOverlay` static helper class with `show()` method — measures trigger `RenderBox` position/size, inserts an `OverlayEntry` containing `_SubjectMenuOverlayWidget`. (3) `_SubjectMenuOverlayWidget` is a `StatefulWidget` with `SingleTickerProviderStateMixin` — animates in with `FadeTransition` + `ScaleTransition` (0.95→1.0, 120ms, `Curves.easeOutCubic`). Menu container: `Color(0xFF1E2A3A)` dark / `cs.surface` light, `BorderRadius.circular(6)`, 1px `outlineVariant` border, dual box-shadow (20px diffuse + 4px tight). Items: 36px tall, `fontSize: 12.5`, selected=indigo w500 + check icon 13px, unselected=onSurface w400. Separators: `Divider(height: 1)` with `outlineVariant` alpha. Hover: `indigo.withValues(alpha: 0.06)`. Max height: `min(items * 36 + 8, 240)`. Width matches trigger. Position: below trigger (or above if insufficient space). Dismiss: full-screen transparent `GestureDetector` barrier. (4) Updated `_CreatePaperSheetState._showSubjectPicker` — now calls `_SubjectMenuOverlay.show()` instead of `showModalBottomSheet`. (5) Updated `_GradePaperSlotCardState._showSubjectPicker` — same overlay approach, callback updates slot and calls `onChanged()`. **`exam_creation_page.dart`:** (1) Replaced `_SubjectDropdown._showPicker` bottom sheet with overlay-based approach — measures trigger via `findRenderObject()`, inserts `OverlayEntry` containing `_SubjectDropdownOverlay`. (2) Added `_SubjectDropdownOverlay` `StatefulWidget` with `SingleTickerProviderStateMixin` — same animation pattern as `_SubjectMenuOverlayWidget`. Includes "None" option (first item, 36px) + subject items (44px to fit subtitle/teacher name). `_buildMenuItem` helper renders each row with optional subtitle line. Max height 280px. All three subject pickers now use consistent macOS-style inline dropdown overlays instead of bottom sheets. File compiles cleanly: 0 errors, 0 warnings.
- Task 09 — Dead code cleanup and analyzer pass on `exams_grades_screen.dart`. **Removed:** `_FieldLabel` class (unused `StatelessWidget` — the pre-existing `unused_element` warning from Tasks 03–08). **Fixed:** 5 `unnecessary_underscores` info-level warnings — replaced `(_, __)` with `(_, _)` in all `separatorBuilder` / `builder` callbacks (`_ExamsListViewState` L297, `_ExpandableFabState` L3871, `_PaperTimetableMobileState` L4414, `_GradeSpreadsheetState` L5504, `_GradeListState` L5770). **Verified:** `_showAddPaper`, `_showAddGradeModal`, `_showAddStreamModal` all have live call sites in `_ExpandableFab`. **Verified:** No dead imports. `dart analyze` result: **0 issues found**. File compiles cleanly: 0 errors, 0 warnings.
- Task 06 — Redesigned _AddGradeToExamForm in exams_grades_screen.dart into a two-step flow. Step 1 (grade + stream selection): unchanged from Task 03. Footer: Cancel + Next (_GradeNextButton: indigo fill, 36px, chevron_right, disabled when no selection or loading). Step 2 (paper timetable): auto-populates _GradePaperSlot list(s) via 3-per-day/2hr algorithm, then shows editable per-slot cards. Multiple streams: EduTabBar stream tabs at top. Each _GradePaperSlotCard: subject name header + remove button; _SubjectTrigger opens _SubjectPickerSheet; _PaperDateTrigger opens inline AnimatedCrossFade _PaperSingleCalendar; two _TimeChipPicker chips (START, END); duration badge + date label. Card style: isDark Color(0xFF1A2536) / surfaceContainerHighest 0.5, border outlineVariant, radius 8. Add Paper: CustomPaint _DashedBorderPainter (5px/4px), 40px, add_rounded icon. Step 2 footer: Back (chevron_left) left, Cancel + _GradeConfirmButton right. Step indicator: _GradeStepDots — two 8x8 circles (active=filled indigo, inactive=outlined), AnimatedContainer 200ms. Step animation: FadeTransition (120ms in / 180ms out). Dialog maxWidth widened from 440 to 520. Save uses _paperSlots map, invigilator falls back to accountId. Validation: _validateStep2(). New model: _GradePaperSlot. New widgets: _GradeStepDots, _GradeNextButton, _GradePaperSlotCard, _GradeAddPaperButton, _DashedBorderPainter. State additions: _step, _paperSlots, _step2TabCtrl, _activeStreamKeys, _stepAnimCtrl; uses SingleTickerProviderStateMixin. File compiles cleanly: 0 errors, 1 pre-existing unused_element warning (_FieldLabel).
- Task 05 — Implemented _AddStreamModal in exams_grades_screen.dart. _showAddStreamModal is now fully wired (showDialog desktop >=600px, showModalBottomSheet mobile). Architecture: _AddStreamDialog (desktop, max-width 440px, BorderRadius.circular(12), dark-mode-aware box-shadow) + _AddStreamSheet (mobile, BorderRadius.vertical top 16) + shared _AddStreamForm stateful widget. Data logic: _buildAvailableGrades() cross-references widget.group.grades (each ExamGradeEntry.streams → set of in-exam streamCodes) against SchoolConfig curricula to find missing streams per grade. Skips grades not in the exam and grades with no streams. Auto-selects the grade and pre-checks all streams when only one grade has missing streams (via initState + postFrameCallback). Grade selector hidden when only one grade has missing streams (skipped directly to stream toggles). Subjects preview filters by currently-checked streams only. Save logic: builds ExamBatchEntry per checked stream with correct stream code, streams subjects from subjectsDao per stream, auto-schedules papers (3 per day, 2hr each, 08:00 start). Empty state: check_circle_outline + "All streams included" + "All streams are already included in this exam." text. Reuses existing _GradeOptionRow, _StreamToggleRow, _GradeFieldLabel, _GradeSheetDivider, _GradeConfirmButton widgets. File compiles cleanly: 0 errors, 1 pre-existing unused_element warning (_FieldLabel).
- Task 03 — Redesigned grade modal in exams_grades_screen.dart. Architecture: refactored into _AddGradeToExamDialog (desktop, max-width 440px, BorderRadius.circular(12), box-shadow), _AddGradeToExamSheet (mobile, BorderRadius.vertical top 16), and shared _AddGradeToExamForm. _showAddGradeModal is now adaptive (showDialog desktop >=600px, showModalBottomSheet mobile). Form UI: (A) drag handle sheet-only; (B) polished header with title/subtitle/close button + divider; (C) GRADE section label + _GradeOptionRow (36px AnimatedContainer, indigo selected, unselected slate fill, stream count badge, NoSplash hover); (D) STREAMS section + _StreamToggleRow (32px, custom 16x16 checkbox BorderRadius.circular(4), indigo checked, white check icon); (E) PAPERS (AUTO-FILLED) section + bordered container BorderRadius.circular(8), indigo-tinted chips BorderRadius.circular(6); (F) footer divider + Cancel text + _GradeConfirmButton (36x36 brandGreen BorderRadius.circular(8) green shadow animated spinner/check); (G) empty state (check_circle_outline 32px indigo 40% + All grades included text + Close). New widgets: _AddGradeToExamDialog, _AddGradeToExamSheet (now StatelessWidget wrappers), _AddGradeToExamForm, _AddGradeToExamFormState, _GradeOptionRow, _StreamToggleRow, _GradeFieldLabel, _GradeSheetDivider, _GradeConfirmButton. Deleted: old _AddGradeToExamSheet StatefulWidget, _AddGradeToExamSheetState, _AddGradeRow. File compiles cleanly: 0 errors, 0 warnings.
- Task 02 — Replaced dual FAB+IconButton pattern in `_ExamGroupDetailView` with a single `_ExpandableFab` widget. **Removed:** (1) The `FloatingActionButton.small` in the `Scaffold.floatingActionButton` slot that only triggered "Add Paper"; (2) The `IconButton` with `Icons.add_rounded` trailing the grade tabs row that triggered "Add Grade". **Added:** `_ExpandableFab` — a new `StatefulWidget` placed in the `Scaffold.floatingActionButton` slot, always shown when `_canManage` is true. It manages an `AnimationController` (200ms, `Curves.easeOutCubic`) that drives: (a) a 45° rotation of the `+` icon (becomes `×` when open) via `RotationTransition`, (b) staggered slide-up + fade-in for 3 option pills (50ms stagger per pill) via per-option `SlideTransition`+`FadeTransition`. When expanded, a semi-transparent scrim (`Colors.black` at 15% alpha, animated) is rendered via `Positioned.fill` and dismisses the menu on tap. **Three option pills** (bottom-to-top: Paper, Grade, Stream) rendered as `_FabOptionPill` widgets: `BorderRadius.circular(8)`, 1px `outlineVariant` border (alpha 0.30 dark / 0.45 light), `BoxShadow` (blurRadius 8, offset Offset(0,3)), icon size 16 + label fontSize 12.5 w500, padding `horizontal: 12, vertical: 9`, disabled state uses `onSurfaceVariant` at 35% alpha. Pills positioned via `Positioned(bottom: 56+8+(i*44))` above the FAB. **Paper pill** is disabled (`enabled: false`) when `_currentStreamEntry == null`. **Stream pill** calls `_showAddStreamModal(context)` — a new stub method (TODO: Task 05). **Grade tabs row** now uses `EduTabBar` as the sole child of `Expanded` (no trailing `IconButton`), with `padding: EdgeInsets.fromLTRB(16, 6, 16, 8)`. Empty-state hint text updated from "Tap + next to the grade tabs to add a grade." to "Tap + to add a grade." File compiles cleanly: 0 errors, 0 warnings.
- Task EX8 — Full integration pass and cleanup on `exams_grades_screen.dart`. **(1) Reactive detail view:** `_ExamsShellState` now wraps `_ExamsView.examDetail` in a `StreamBuilder<List<ExamGroup>>` on `_dao.watchExamGroups()`. Finds the matching group by `_selectedGroupKey` on each emission, keeping `_selectedGroup` up-to-date. Adding grades, extending date ranges, or any mutation that re-emits the stream automatically refreshes the detail view. If the group is deleted externally, `addPostFrameCallback` pops back to the list. Added `_dao: ExamsGradesDao` and `_selectedGroupKey: String?` fields to `_ExamsShellState`. **(2) Delete exam group:** Added `_confirmDeleteGroup()` method to `_ExamGroupDetailViewState` — shows `_ConfirmDeleteDialog` (reused existing widget), then loops `group.examIds` calling `_dao.deleteExam()` for each, then calls `widget.onDeleted()` to pop. Delete button is an `IconButton` with `Icons.delete_outline_rounded` (error-colored) in the detail view header row, visible only when `_canManage`. Added `onDeleted` callback to `_ExamGroupDetailView` constructor. Detail header replaced from `_SectionHeader` to custom `Row` layout to accommodate the delete button alongside back button + title + subtitle. **(3) Dead code cleanup:** Removed `_SubLabel`, `_IconActionButton`, `_SegmentedRow`, `_DatePickerField` — all unused since earlier refactors. `_ConfirmDeleteDialog` retained (now used by delete). **(4) Verification:** All navigation flows verified (list → detail → paper detail → back transitions). Empty states verified: no exams → `_EmptyExamsState`, no papers → `_EmptyPapersTimetableState`, no grades to add → message in `_AddGradeToExamSheet`. §21 compliance verified: `BorderRadius.circular(4)`, w300/w400 body, w500 headings, thin 1px borders, dark mode aware. `ExamWithPapers` typedef retained in DAO for backward compatibility (used by `overview_screen.dart`, `exams_tab.dart`, `exam_detail_page.dart`, `paper_detail_page.dart`, `academics_dao.dart`). File compiles cleanly: 0 errors, 0 warnings.
- Task EX7 — Rebuilt `_CreatePaperSheet` in `exams_grades_screen.dart` with redesigned paper creation UX. **Constructor change:** added `examGroup` (`ExamGroup`) parameter; `_showAddPaper` in `_ExamGroupDetailViewState` updated to pass `widget.group`. **State changes:** replaced `DateTime _startTime`/`_endTime` with `DateTime? _selectedDate` (date only), `TimeOfDay _startTime` (default 08:00), `TimeOfDay _endTime` (default 10:00), `bool _outOfRange`, `String? _timeError`. Added computed getters `_examStartDate`/`_examEndDate` for display formatting. **UI redesign:** (1) Single date picker via new `_DateChipPicker` widget — tappable container showing selected date or "Select date" placeholder, opens `showDatePicker` constrained to ±30 days around exam range (allows out-of-range selection for detection). (2) Time range via two `_TimeChipPicker` widgets in a `Row` with "–" separator — each shows label + formatted time, opens `_PaperTimePickerSheet` (wheel-style bottom sheet with `_PaperWheelColumn` hour 0–23 + minute in 5-min increments + `_PaperTimePresetChip` quick presets 8:00–16:00, matching `exam_creation_page.dart` pattern but with local widget names to avoid conflicts). (3) Out-of-range detection via `_checkOutOfRange()` — compares selected date's days-since-epoch against `examGroup.start`/`end`. Warning UI: amber-tinted `Container` (radius 4) with `Icons.warning_amber_rounded`, descriptive text showing exam range, right-aligned "Extend Exam Period" `TextButton`. (4) `_extendExamRange()` calls `ExamsGradesDao.updateExamGroupDateRange()` to widen the exam's date range to include the selected date, then clears `_outOfRange` and shows snackbar. (5) Time validation via `_validateTimes()` — checks end > start, sets `_timeError` inline message. (6) Save button disabled when `_selectedDate == null`. Save logic builds `startDt`/`endDt` from `_selectedDate` + `_startTime`/`_endTime`, converts to seconds-since-epoch `BigInt`. **New helper widgets (local to file):** `_DateChipPicker` (date container + showDatePicker), `_TimeChipPicker` (time container + opens `_PaperTimePickerSheet`), `_PaperTimePickerSheet` / `_PaperTimePickerSheetState` (wheel time picker with presets), `_PaperWheelColumn` (ListWheelScrollView with selection highlight), `_PaperTimePresetChip` (compact preset chip). **New helper function:** `_fmtTimeOfDay(TimeOfDay)` for formatting `TimeOfDay` as "HH:MM". All new widgets follow §21 guidelines: `BorderRadius.circular(4)`, thin 1px borders, light font weights, dark mode aware.
- Task EX6 — Implemented `_AddGradeToExamSheet` modal bottom sheet in `exams_grades_screen.dart`. Wired `_showAddGradeModal` in `_ExamGroupDetailViewState` to open the new sheet. **`_AddGradeToExamSheet`:** `StatefulWidget` modal that lets users add a new grade (and auto-generated papers) to an existing exam group. UI: drag handle, "Add Grade" title, radio-style grade selector listing grades from `SchoolConfig` not already in `group.participatingGrades`, stream checkboxes (all checked by default) when selected grade has streams, subjects preview section showing auto-detected subjects as chips with "N subjects will be auto-scheduled (3 per day, 2 hrs each)" summary, right-aligned "Add Grade" `FilledButton.tonal` (radius 4). Empty state shown when all grades already participate. Subjects loaded via `SubjectsDao.getSubjectsForTerm()` filtered per grade+stream. Save uses `ExamsGradesDao.createExamBatch()` (not `addGradeToExamGroup` which has a paper-matching bug) — builds `ExamBatchEntry` per selected stream with pre-generated exam IDs, auto-populates `PapersCompanion` list using same algorithm as `_autoPopulateSlots` (3 papers/day, 2hr each, starting 08:00). Drift reactivity auto-refreshes the parent `StreamBuilder` on `watchExamGroups`. **`_AddGradeRow`:** radio-button style row widget with `Icons.radio_button_checked`/`unchecked`, primary-tinted background when selected, `BorderRadius.circular(4)`. Both widgets placed between `_ExamGroupDetailViewState` and the paper timetable helpers section.
- Task EX5 — Replaced `_PaperListContent` with responsive paper timetable. `_ExamGroupDetailViewState.build()` paper content area now uses `LayoutBuilder` to switch between `_PaperTimetableGrid` (≥600px) and `_PaperTimetableMobile` (<600px). **Desktop grid** (`_PaperTimetableGrid`): `SingleChildScrollView` with `_PaperGridRow` rows (modeled after `_GridRow` in `timetable_tab.dart`). Columns = dates derived from papers (not fixed weekdays), rows = unique start times. Sub-widgets: `_PaperHeaderCell` (date column headers with "Mon 12" format), `_PaperTimeLabel` (HH:MM left-edge labels with tabular figures), `_PaperSlotBox` (filled slot: status-colored left border 2.5px + tinted background, subject name + paper number, time range in 9.5px), `_PaperEmptyCell` (56px min-height, subtle 1px border). **Mobile pager** (`_PaperTimetableMobile`): `StatefulWidget` with `_PaperDayChip` horizontal selector (animated, primary-tinted selected state, `BorderRadius.circular(4)`), day heading, `ListView.separated` of existing `_PaperSlotCard` cards. State tracks `_grouped` map, `_dates` list, `_selectedDayIndex`. `didUpdateWidget` rebuilds groups and clamps index. **Helpers added:** `_groupPapersByDate` (groups papers by day, sorts within each day), `_sortedPaperDates` (sorted unique dates), `_uniquePaperStartTimes` (sorted HH:MM labels), `_paperAt` (finds paper at date+time), `_fmtDayHeader` ("Mon, 12 Jan"), `_fmtDayColumn` ("Mon 12"). **Deleted:** `_PaperListContent` class (replaced by the two responsive views). `_PaperSlotCard` retained (used by mobile view).
- Task EX4 — Added `_paperStatusLabel(PaperStatus)` top-level helper function (returns human-readable labels: Pending, In Progress, Done, Marked) and `_PaperStatusLegend` widget (compact horizontal `Wrap` of color-dot + label pairs for all 4 `PaperStatus` values). Legend uses 8×8 square dots with `BorderRadius.circular(2)`, `fontSize: 10.5`, `fontWeight: w400`, muted `onSurfaceVariant` text. Inserted `const _PaperStatusLegend()` in `_ExamGroupDetailView.build()` between the stream sub-tabs and the paper timetable content area (inside the main `Column`). Both helpers placed immediately after existing `_paperStatusColor` function.
- Task EX2 — Rewrote `_ExamsListView` to display grouped exams using `ExamGroup` model instead of `ExamWithPapers`. Data source changed from `ExamsGradesDao.watchExamsForTerm()` to `ExamsGradesDao.watchExamGroups()` returning `Stream<List<ExamGroup>>`. `onExamTap` callback type changed from `ValueChanged<ExamWithPapers>` to `ValueChanged<ExamGroup>`. Replaced `_ExamCard` with `_ExamGroupCard` — shows exam type badge, personalized indicator, date range, participating grades as `_ClassChip` chips, subject count + grade count badges. No teacher name, no delete button, no paper count on card (delete moves to detail view). Added import for `models/exam_group.dart`.
- Task EX3 — Replaced `_ExamDetailView` with `_ExamGroupDetailView`. New detail view uses `TickerProviderStateMixin` for grade tabs (`EduTabBar`) and stream sub-tabs. Grade tabs show one tab per `ExamGradeEntry` in the group; `+` button next to tabs (placeholder for EX6 "Add Grade" modal). Stream sub-tabs appear only when current grade has >1 stream. Paper content area uses `StreamBuilder<List<Paper>>` on `watchPapersForExam` for the current stream's exam ID. Papers displayed via `_PaperListContent` (day-grouped list) with `_PaperSlotCard` cards showing status-colored left border, subject name, time range, and `_StatusChip`. `_paperStatusColor` top-level helper added: pending=grey, progress=blue, done=amber, marked=green. `_EmptyPapersTimetableState` for empty state. `_ExamsShellState` updated: removed `_selectedExam: ExamWithPapers?`, added `_selectedExamRow: Exam?`; `_openExam` no longer bridges to `ExamWithPapers`; `_openPaper` now takes `(Paper, Exam)` to capture the specific exam row for paper detail navigation. `_PaperDetailView` receives a bridge `ExamWithPapers` constructed from `_selectedExamRow` + `_selectedGroup.teacher`. Deleted: `_ExamDetailView`, `_ExamDetailViewState`, `_ExamSummaryCard`, `_InfoRow`, `_PaperCard`, `_EmptyPapersState`, `_ClassPerformanceSection`, `_ClassPerformanceSectionState`, `_StudentRankRow`. Added import: `ui/widgets/edu_tab_bar.dart`.
- Task E6 — Dead code cleanup and final verification for exam creation page per-stream redesign (Tasks E1–E5). Confirmed all previously-identified dead code already removed by prior tasks: `_StreamChip` widget class (removed in E2), `_loadSubjectsForGrade` method (removed in E2), `_streamSelections` field (removed in E2), `_activeGradeTabIndex == 0` "All" tab logic (removed in E1). Ran analyzer on `exam_creation_page.dart` — zero errors, zero warnings. No unused imports, variables, or methods found. CONTEXT.md `exam_creation_page.dart` entry already fully reflects the new two-level tab architecture (Grade → Stream sub-tabs with "All Streams" index 0), per-stream paper configuration keyed by `"$grade:$streamCode"`, "Copy to all streams" feature, and all widget/method removals. No code changes required — cleanup task is a verification-only pass.
- Task E5 — Verified save logic and auto-populate work correctly with per-stream paper configuration. `_autoPopulateSlots(classKey, grade)` and `_addPaperSlot(classKey, date, grade)` are always called with stream-specific keys (from `_buildTimetableForClass` which receives concrete stream codes from `_buildTimetableSections`). `_save` iterates `_selectedClasses` (which returns all streams individually when "All Streams" tab is active), looks up per-stream `_paperSlots[_classKey(cls.grade, cls.stream)]` for each — keys are always `"$grade:$streamCode"` for grades with streams, never `"$grade:null"`. Each `ExamBatchEntry` gets a unique `examId` via `_generateId()`, correct `stream` value from `cls.stream`, and its own paper list from the per-stream slot map. Invigilator on each paper uses `slot.invigilatorId ?? teacherId` — stream-correct after Task E4's copy-with-remap. Exams without papers are valid (papers can be added later from the detail view), so no new save validation was needed. Empty paper slot lists handled gracefully (the `for (final slot in slots)` loop simply doesn't execute). No code changes required — all paths confirmed correct after Tasks E1–E4.
- Task E4 — added "Copy to all" feature to `exam_creation_page.dart`. New `_copyPapersToAllStreams(int grade, int sourceStreamCode)` method copies paper slots from the active stream to all other streams of the same grade, remapping invigilators to each target stream's assigned teacher and skipping subjects not present in the target. New `_isSpecificStreamActive(int grade)` helper. "Copy to all" `_MiniTextButton` (icon: `Icons.copy_all`) appears in `_buildTimetableForClass` header row when a specific stream tab is active and slots are non-empty.
- Task E3 — rewrote `_buildTimetableSections` to render per-stream timetables based on active stream tab index (specific stream → single timetable, "All Streams" → one section per stream with name header, no streams → single headerless timetable). Simplified `_loadSubjectsForClass` by removing union-of-streams fallback. Removed unused `_gradeLabelText` and `_streamLabelText` helpers.
- Task E2 — added per-grade stream sub-tabs with `EduTabBar`, removed `_StreamChip` / `_streamSelections` / `_loadSubjectsForGrade`, switched to `TickerProviderStateMixin`
Task E1 — Removed synthetic "All" grade tab from `exam_creation_page.dart`. `_gradeTabs` now contains only real grade entries (no `_GradeTabEntry(label: 'All', grade: null, curriculum: null)`). `_hasSelection` simplified to `_gradeTabs.isNotEmpty`. `_selectedClasses` redesigned: when a grade has streams and no specific selection, returns all streams individually (one entry per stream) instead of a single null-stream entry; when specific streams are selected, returns only those; when no streams, returns single `(grade, null)`. Removed `_activeGradeTabIndex == 0` branch from `_selectedClasses` and `_hasSelection`. Removed `if (_activeGradeTabIndex > 0)` guard from stream chips in build method — stream chips now always show when the current grade has streams. Added empty-guard to `_buildStreamSection` for safety when `_gradeTabs` is empty. `preselectedGrade` index search unchanged (already correct without "All" tab since `indexWhere` matches real grades directly).
Timetable Redesign — Complete rewrite of `exam_creation_page.dart` paper scheduling section. Replaced per-subject checkbox list (`_PaperSchedule`, `_PaperTimetableRow`, `_TimetableDateChip`, `_TimetableTimeChip`) with dynamic timetable-style builder. New `_PaperSlot` model stores id, date, startTime, endTime, subjectCode, invigilatorId. Papers organized by day (`_DayColumn`) with individual slot cards (`_PaperSlotCard`) containing inline time pickers (modern wheel-style `_TimePickerSheet` bottom sheet with `ListWheelScrollView` + preset chips), subject dropdown (`_SubjectDropdown` with bottom sheet picker), and invigilator dropdown (`_InvigilatorDropdown` with searchable bottom sheet `_InvigilatorPickerSheet`). Schools can add 3, 4, or any number of papers per day with varying durations. Auto-fill distributes subjects across exam days. Calendar improved: `AnimatedCrossFade` replaced with `AnimatedSize` for smoother expansion, month transitions use fade animation via `AnimationController`, calendar sub-widgets renamed from `_Exam*` to `_Calendar*` prefix. `MembersDao` added as dependency for teacher loading. All `showDatePicker`/`showTimePicker` system dialogs eliminated.
Task E-R6 — Polish pass on `exam_creation_page.dart`. Audited all sections against §21 UI guidelines and dark mode requirements. Changes: (1) `_PaperTimetableRow` `borderRadius` reduced from `8` to `4` (sharp corners per §21); (2) `_PaperTimetableRow` border alpha now dark-mode-aware (`isDark ? 0.10 : 0.08` when included, `isDark ? 0.07 : 0.05` when excluded). Verified: section headers use correct `letterSpacing: 1.0`, `fontSize: 11`, `fontWeight: w600`; whitespace ≥28px between major sections, ≥12px between subsections; all `withValues(alpha:)` calls have appropriate dark/light variants; all `AnimatedContainer` durations 120–150ms; `ListView` scrolls within `Expanded` column; personalized toggle positioned between date range and grade tabs; `_showError` snackbar validation works; AppBar has no `actions`, only leading close icon + title; all imports used, no dead imports; file compiles with 0 errors (4 benign unused-optional-parameter warnings on `_PaperSchedule` constructor — intentional, fields use defaults and are mutated post-construction).
Task E-R5 — Redesigned `exam_creation_page.dart` form footer: removed AppBar `actions` (old "Create" `TextButton`), restructured body from flat `ListView` to `Column(Expanded(ListView) + footer)`. Footer has thin divider + right-aligned Cancel/Create Exam buttons. Added `_ExamConfirmButton` widget (brand indigo fill, shadow, loading spinner, disabled state). Updated `_hasSelection` getter for tab-aware logic. ListView bottom padding reduced from 40 to 24.
Task E-R4 — Restructured `exam_creation_page.dart` form layout: replaced checkbox-based grade/stream selector with tabbed `EduTabBar` grade tabs ("All" + per-grade) and `_StreamChip` filter chips; replaced "Set up papers now" toggle + flat subject checkboxes + single time range with per-subject paper timetable (`_PaperSchedule` model, `_PaperTimetableRow` with individual date/time pickers). State changed to `SingleTickerProviderStateMixin` with `TabController`. Old models/methods/widgets deleted (`_GradeSelection`, `_CompactCheck`, `_SubjectRow`, `_PaperTimeRow`, `_TimeTap`, etc.). Save logic updated to read from `_paperSchedules` map.
Task E-R3 — Replaced separate date pickers with inline collapsible date range calendar in `exam_creation_page.dart`, matching `create_term_modal.dart` pattern. `_startDate`/`_endDate` changed to nullable, old `_DateField`/`_fmtDate` removed, calendar helper widgets added with `_Exam` prefix.
Task E-R2 — Exam type selector redesigned to slim inline chips in `exam_creation_page.dart`.
Task E-R1 — Simplified `_EmptyExamsState` in `exams_grades_screen.dart`. Removed `onCreate` callback and `OutlinedButton.icon` CTA button — users create exams exclusively via the FAB. Icon size reduced from 40 to 36, padding reduced from 40 to 32. Subtitle updated to reference FAB. Constructor simplified to single `canCreate` param. Call site updated to remove `onCreate` argument.
Task E8 — Aesthetic polish pass on `exams_grades_screen.dart` to align with §21 (clean, minimal, thin, modern). Changes: (1) **`_ExamCard`** — replaced `Material(elevation: 2)` with `Container` + thin 1px border (`cs.outlineVariant.withValues(alpha: 0.4)`), `BorderRadius.circular(4)`, `Material(color: transparent)` + `InkWell` inside for tap ripple, reduced internal padding from 16 to 14, type badge `fontWeight` reduced from `w600` to `w500`, teacher name `fontWeight` reduced from `w500` to `w400`, fixed bracket nesting (Container→Material→InkWell→Padding→Column); (2) **`_EmptyExamsState`** — already correct from Task E4 (40px icon, `cs.outlineVariant`, thin `OutlinedButton.icon`, `BorderRadius.circular(4)`, `fontSize: 13`, `fontWeight: w400`); (3) **`_EmptyPapersState`** — replaced heavy container + `TextButton.icon` with thin `Container` (sharp `BorderRadius.circular(4)`, 1px border), 40px `cs.outlineVariant` icon, thin `OutlinedButton.icon` "Add Paper" (`fontSize: 13`, `fontWeight: w400`, `BorderRadius.circular(4)`, 1px border); (4) **`_SectionHeader`** — title `fontSize` reduced from 16 to 15, `fontWeight: w500` preserved, subtitle `fontSize: 12`, `fontWeight: w400`, `cs.onSurfaceVariant`; (5) **`_MetaBadge`** — wrapped in `Container` with `cs.surfaceContainerHighest.withValues(alpha: 0.4)` background, `BorderRadius.circular(4)`, padding `horizontal: 7, vertical: 3`, `fontSize` reduced from 12 to 11, icon size 13, `fontWeight: w400`; (6) Fixed broken `_MetaBadge` widget (duplicate nested build methods from previous conflicting edits removed). File compiles cleanly: 0 errors, 2 pre-existing unused_element warnings (`_SegmentedRow`, `_generateId`).
Task E7 — Deleted dead code `_CreateExamSheet` and `_CreateExamSheetState` from `exams_grades_screen.dart`. These classes (~300 lines) were the old bottom-sheet exam creation flow, fully replaced by `ExamCreationPage` (full-screen page) in Tasks E3/E4/E6. No other code referenced `_CreateExamSheet` (private class, only used within the same file). `_CreatePaperSheet` retained — still used by `_ExamDetailView` for adding papers to existing exams. Shared utility widgets (`_SegmentedRow`, `_DatePickerField`, `_FieldLabel`, `_ConfirmDeleteDialog`, etc.) retained per task spec. File compiles cleanly (0 errors, 2 unused_element warnings for `_SegmentedRow` and `_generateId` which are no longer referenced but kept intentionally).
Task E5 — Updated `_ExamDetailView` in `exams_grades_screen.dart`: wrapped body in `Scaffold` with `FloatingActionButton.small` for "Add Paper" (replacing header action button), removed unused `action` parameter from `_SectionHeader`. Polished `_ExamSummaryCard` (sharp `BorderRadius.circular(4)`, thin 1px border, no elevation), `_PaperCard` (sharp corners, thin border via `Material` shape, `elevation: 0`, softened delete icon color), and `_CreatePaperSheet` (top radius 4, tighter padding, compact right-aligned `FilledButton.tonal`).
Task E3 — Created `exam_creation_page.dart` (`ExamCreationPage`) in `exams/`. Full-screen multi-grade exam creation form pushed via `Navigator.push`, replacing the cramped `_CreateExamSheet` bottom sheet. Three logical sections in a single scrollable `Form` wrapped in `ListView`: (1) **Exam Details** — `ExamType` segmented row (Exam/Assignment/Assessment), start/end date pickers (`_DateField`), personalized checkbox toggle; (2) **Grade & Stream Selection** — per-curriculum subheaders (CBC / 8-4-4), grade chips (`Wrap` of tappable `AnimatedContainer`s with `BorderRadius.circular(4)`), expandable stream checkboxes per selected grade with "All streams" toggle, state model `Map<int, _GradeSelection>` (allStreams bool + selectedStreams set); (3) **Papers Setup** (collapsible, off by default) — switch toggle loads subjects per selected class via `SubjectsDao.getSubjectsForClass()`, union across all streams for `stream: null` selections, subject checkbox rows (`_SubjectRow`: 40px height, subject name + teacher name), per-class paper time range pickers (`_PaperTimeRow` with `_TimeTap` widgets opening `showTimePicker`). Save builds `List<ExamBatchEntry>` (one per grade+stream combo, with optional `PapersCompanion` list) and calls `ExamsGradesDao.createExamBatch()`. AppBar: close icon leading, "Create" `TextButton` trailing (disabled when no selection, shows `CircularProgressIndicator(strokeWidth: 1.5)` while saving). Follows §21 UI guidelines: sharp corners, thin 1px borders, light font weights (`w400`/`w500`), generous whitespace. Private helper widgets: `_CompactCheck`, `_SubjectRow`, `_DateField`, `_PaperTimeRow`, `_TimeTap`. Utility functions: `_fmtDate`, `_generateId`.
Task 07 (Phase 3) — Redesigned student edit sheet (`_showEditSheet`) in `student_detail_page.dart`: (1) Replaced `showDatePicker` dialog with `InlineCalendar` widget (inline expandable calendar, `hint: 'Date of Birth'`, `icon: Icons.cake_outlined`); (2) Replaced full-width `FilledButton` "Save" with compact right-aligned `TextButton.icon` (check icon + "Save" label, `BorderRadius.circular(4)`, horizontal padding 16); (3) Added entrance animation — entire form `Column` wrapped in `TweenAnimationBuilder<double>` (0→1, 250ms easeOut) applying `Opacity` + `Transform.translate` (12px slide-up); (4) Tightened gender `ChoiceChip`s — added `visualDensity: VisualDensity.compact`, `padding: EdgeInsets.symmetric(horizontal: 4)`, changed `borderRadius` from `circular(4)` to `circular(6)`; (5) Reduced vertical spacing from 20px to 16px between sections. Added import for `inline_calendar.dart`.
Task 06 (Phase 3) — Redesigned student detail header in `student_detail_page.dart`. Replaced `PopupMenuButton` with a `Row` of `_ActionIcon` widgets (Edit, Change Status, Delete) — always-visible small icon buttons matching the teacher/staff detail pattern. Removed the `_onMenuAction` string-switch method; each icon calls `_showEditSheet`, `_showStatusSheet`, or `_confirmDelete` directly. Added `_ActionIcon` private widget class (Tooltip → Material → InkWell → Icon, size 18, padding 7, borderRadius 6).
Task 05 (Phase 2) — Fixed student image display in members list and student detail page. `_StudentAvatar` in `members_page.dart` now uses `FutureBuilder<File?>` with `FileCache.get(FileCache.studentImagePath(schoolId, adm))` to load cached student images, falling back to initials if no cached file exists. `_StudentAvatarLarge` in `student_detail_page.dart` updated with the same pattern (radius 28, fontSize 15). Added `dart:io` and `cache/file_cache.dart` imports to both files.
Task 04 (Phase 1) — Wired notifications action in School Dashboard's `_UserMenuAnchor`: (1) `_handleAction` notifications case now navigates to `NotificationsPage(accountId:)` instead of no-op `break`; (2) `_UserMenuAnchorState.build()` wraps `UserAvatar` in `StreamBuilder<int>` on `logsDao.watchFailedLogCount(...)` with red badge (same pattern as System Dashboard Task 03). Added import for `notifications_page.dart`.
Task 50 — Comprehensive update: added academics-specific conventions (stream tabs + content tabs pattern, back button convention, FAB action sheet pattern, attendance embedding, shared widgets, empty state consistency, ranking animation). Updated all existing entries to reflect Tasks 37–49 completion.
Task 31 — Exam Detail Page — Performance Tab. Enhanced `_PerformanceTab` in `exam_detail_page.dart` with: (1) **Grade distribution bar chart** (`_buildDistributionChart`) — aggregates `PaperAnalytics.distribution` across all subjects into one merged bucket map (0–39 through 80–100), renders a `BarChart` (fl_chart) inside a bordered card with gradient red→green bars and touch tooltips; (2) **Gold/silver/bronze tinting** for top 3 ranked students — rank 1 gold (#FFD700), rank 2 silver (#C0C0C0), rank 3 bronze (#CD7F32) with subtle row background tint (alpha 0.06 light/0.08 dark) + 2.5px left border accent + medal-colored rank number text. Added imports: `dart:math` as math, `package:fl_chart/fl_chart.dart`.
Task 29 — Exam Detail Page — Grade Table Desktop View. Enhanced `_buildDesktopTable` in `_GradesTab` with: (1) fixed first column (200px, Student Name + ADM) that stays in place while subject columns scroll horizontally; (2) `_GradeCell` widget showing score/total on first line + percentage in muted sub-text below, both color-coded; (3) `_GradeSort` enum and sort toggle button above table (cycles between "By score" descending and "By name" alphabetical); (4) `_abbreviateSubject` helper for compact column headers when >6 subjects (first word + initials of rest, with Tooltip for full name); (5) `_StudentGradeRow` pre-computed row model (student, subjectGrades, totalScore, totalPossible, percentage, hasGrades) used by both sorting and rendering. Table uses custom `Row` layout (fixed left + `SingleChildScrollView` horizontal right) instead of `DataTable`. Cell dimensions: 90px per subject, 100px total, 72px percentage. Thin border separators throughout.
Task 22 — Overview Nav Item — Created `overview/overview_screen.dart` with `OverviewScreen` widget. Role-dispatched school dashboard landing page replacing the default placeholder. Five role-specific overview layouts: Owner (school identity card + quick stats grid + current term info + recent announcements), Teacher (welcome card + today's schedule from timetable + quick stats + recent announcements), Staff (welcome card + quick stats with finance summary + recent announcements), Student (welcome card + enrollment badge + today's schedule + recent grades + attendance summary + recent announcements), Guardian (welcome card + ward info card + ward attendance + ward grades + finance summary + recent announcements). Wired into `_buildContentPanel` in `school_dashboard_screen.dart` under `item.label == 'Overview'`. Added import for `overview/overview_screen.dart`. Created `overview/` directory. Added `overview/` section to CONTEXT.md.
Academics — Grade Detail Page Overhaul Task 21 — Implemented "Assign Subject Teacher" FAB action on `GradeDetailPage`. Replaced stub snackbar with `_showAssignSubjectTeacherSheet()` which opens `_SubjectTeacherPickerSheet` bottom sheet (max 75% height). Two-step picker: Step 0 lists all valid subjects for the grade's curriculum level (via `levelsFor` + `subjectLabel` from `curriculum_levels.dart`), showing current assignment status. Step 1 shows teacher picker (same pattern as class teacher). Grade-to-level mapping via inline helpers (CBC: PP→0, Gr1–3→1, Gr4–6→2, Gr7–9→3, Gr10–12→4,5,6 union; 8-4-4: Std1–3→0, Std4–8→1, Form1–2→2, Form3–4→3). Current assignments loaded via `SubjectsDao.watchSubjectsForClass`. Assignment via `SubjectsDao.assignSubjectTeacher`. Back arrow returns to step 0. Snackbar: "{subject} assigned to {teacher}". Added import: `curriculum_levels.dart`. New private types: `_SubjectTeacherPickerSheet`, `_SubjectTeacherPickerSheetState`, `_SubjectCandidate`. Reuses `_TeacherCandidate`. All three FAB actions now fully implemented.
Academics — Grade Detail Page Overhaul Task 20 — Implemented "Assign Class Teacher" FAB action on `GradeDetailPage`. Replaced stub snackbar with `_showAssignClassTeacherSheet()` which opens `_ClassTeacherPickerSheet` bottom sheet (max 75% height). Loads all teachers for the school via `MembersDao.watchTeachers` + resolves user data via `MembersDao.findUserById` + checks active class teacher via `SubjectsDao.watchActiveClassTeacher`. Searchable by name (in-memory filter). Each row: avatar (primary-tinted if active), name, role/department subtitle, "Current class teacher" label for active. Active teacher row disabled with check icon. Assignment via `SubjectsDao.assignClassTeacher` (closes previous + inserts new in transaction with log entries). Snackbar confirmation on success. Added import: `subjects_dao.dart`. New private types: `_ClassTeacherPickerSheet`, `_ClassTeacherPickerSheetState`, `_TeacherCandidate`.
Academics — Grade Detail Page Overhaul Task 18 — Added `LessonsTab` (`academics/tabs/lessons_tab.dart`) with subject filter chips, date-grouped lesson log, summary stats, and teacher avatars. Wired into `grade_detail_page.dart` via `_buildLessonsTab()` replacing the Lessons placeholder.
Task 17b — Standalone Attendance Nav Item — Redirect to Academics. Rewrote `AttendanceScreen` teacher/owner flow: replaced embedded marking UI with `_ClassPickerShell` class picker list that navigates to `GradeDetailPage` with `initialStreamIndex` and `initialContentTabIndex` params (Attendance tab pre-selected). Removed all old marking widgets (`_TeacherAttendanceShell`, `_AttendanceMarkingList`, etc.) — marking now lives exclusively in `AttendanceTab` inside the grade detail page. Guardian/student read-only view unchanged. Updated `GradeDetailPage` constructor to accept `initialStreamIndex` (int?) and `initialContentTabIndex` (int?) — both clamped and wired into `TabController.initialIndex`.
Task 15 — Created `academics/tabs/teachers_tab.dart` (`TeachersTab`) with three sections: Active Class Teacher (prominent card with primary-tinted border, avatar, name, "Since" date), Past Class Teachers (collapsible list with `AnimatedCrossFade`, default collapsed, chevron toggle), Subject Teachers (list with count badge, subject name on left, teacher avatar + name on right). Uses `SubjectsDao` streams: `watchActiveClassTeacher`, `watchClassTeacherHistory`, `watchSubjectsForClass`. Wired into `grade_detail_page.dart` replacing the Teachers placeholder via `_buildTeachersTab()` method. Added import for `tabs/teachers_tab.dart`.
Task 14 — Added `paper_detail_page.dart` entry, updated `exam_detail_page.dart` description (paper navigation wired).
Academics — Grade Detail Page Overhaul Task 13 — Added `exam_detail_page.dart` (`ExamDetailPage`) with Papers/Grades/Performance tabs. Updated `exams_tab.dart` navigation from placeholder snackbar to `MaterialPageRoute` push to `ExamDetailPage`.
Academics — Grade Detail Page Overhaul Task 12 — Created `student_attendance_tab.dart` (`StudentAttendanceTab`) in `academics/student_tabs/`. Full attendance tab with: summary bar (stacked proportion bar + Present/Absent/Leave stats with counts and percentages + total days), month calendar heatmaps (Mon–Sun grid with color-coded day cells per status), and month-grouped detail list (date-formatted rows with status chips). Wired into `student_grade_page.dart` replacing the last Attendance placeholder. Removed unused `_buildPlaceholder` method from `StudentGradePage`. Updated `student_grade_page.dart` status from "Complete (scaffold)" to "Complete". All 4 student tabs now fully wired — no remaining placeholders.
Task 11 — Added `student_exams_tab.dart` (`StudentExamsTab`) to `academics/student_tabs/`. Wired into `student_grade_page.dart` replacing Exams placeholder. Only Attendance tab remains as placeholder.
Task 10 — Created `academics/student_tabs/student_mastery_tab.dart` with `StudentMasteryTab` widget. Subject-level mastery list with tappable cards that open a topic-level detail view. Mobile (<600px) uses `DraggableScrollableSheet` bottom sheet; desktop (≥600px) uses a `Dialog` (maxWidth 420, maxHeight 520). Each subject card shows name + overall mastery % + thin progress bar + topic count. Topic detail shows per-topic rows with index badge, "Topic {N}" label, progress bar, and percentage. Color-coded mastery thresholds (≥80 green, ≥60 amber, ≥40 orange, <40 red). Wired into `student_grade_page.dart` replacing the Mastery placeholder. Updated `student_grade_page.dart` description in this CONTEXT.md.
Task 09 — Created `academics/student_tabs/student_overview_tab.dart` with `StudentOverviewTab` widget. Four-section overview: Quick Stats Row (subjects, exams taken, average score, mastery), Recent Exam Performance (last 3 exams with scores and progress bars), Subject Mastery Summary (per-subject mastery bars), Attendance Summary (stacked bar + stats). Wired into `student_grade_page.dart` replacing the Overview placeholder. Added `student_tabs/` directory section to this CONTEXT.md.
Task 08 — Added `StudentGradePage` (`academics/student_grade_page.dart`). Shell with reactive student header card, trajectory banner (computed from last two exams via `ExamsGradesDao.watchStudentGrades`), and 4 content tabs (Overview, Mastery, Exams, Attendance — all placeholder). Wired `StudentsTab._onStudentTap` to navigate to `StudentGradePage` via `MaterialPageRoute`. Updated dependencies to include `MembersDao`, `StudentStatus`, `FileCache`.
Task 05 (Academics — Grade Detail Page Overhaul) — Created `academics/tabs/students_tab.dart` with `StudentsTab` widget. Uses `AcademicsDao.watchStudentsForGrade()` reactive stream via `StreamBuilder`. Shows header count badge, `ListView.builder` of card-based student rows (avatar via `FileCache.studentImagePath`, name, ADM, trajectory icon, average % badge). Empty state with `people_outline_rounded` icon. Tap placeholder for `StudentGradePage` (Task 08). Wired into `grade_detail_page.dart` via `_buildStudentsTab()` method replacing the Students placeholder. Added `students_tab.dart` import.- Task 04 — Redesigned `_CreatePaperSheet` in `exams_grades_screen.dart`. **Removed:** `_DateChipPicker` (called `showDatePicker` dialog — eliminated entirely). **Replaced with inline calendar:** `_PaperDateTrigger` (styled like `_DateRangeTrigger` — indigo-tinted border when selected, icon badge, animated chevron, `AnimatedContainer` with box-shadow) + `_PaperSingleCalendar` (self-contained single-date calendar: month header with `<` `>` chevrons, weekday row, day grid with selected=filled indigo circle, today=thin indigo border, in-exam-range days get a subtle indigo tint, auto-collapses via `AnimatedCrossFade`). **Dual-mode container:** desktop (≥`kMobileBreakpoint`) shows a centered `Dialog` (maxWidth 440px, `BorderRadius.circular(12)`, dark-mode-aware box-shadow identical to `_CreateTermDialog`); mobile shows bottom sheet with `BorderRadius.vertical(top: Radius.circular(16))`. **Header:** drag handle (sheet only), title New Paper (fontSize 15, w600), subtitle showing grade·stream context, close `IconButton`. **Subject selector:** replaced `DropdownButtonFormField` with `_SubjectTrigger` (styled like `_DateRangeTrigger`: fill color, indigo-tinted border when selected, `Icons.menu_book_outlined` badge 24×24) that opens `_SubjectPickerSheet` (bottom sheet with scrollable list, selected item highlighted with indigo + check icon). **Multi-paper toggle:** replaced plain `Checkbox` row with `_PaperMultiToggleRow` (`AnimatedContainer` 36px, custom 16×16 checkbox BorderRadius.circular(4), indigo checked — same pattern as `_StreamToggleRow`). Paper number input is a compact 80px wide `TextField` with styled border. **Time pickers:** `_TimeChipPicker` border updated from `BorderRadius.circular(4)` to `BorderRadius.circular(8)`, fill from `surfaceContainerHighest 0.45/0.5` to `isDark ? Color(0xFF1E2C3C) : surfaceContainerHighest 0.55`, border from `cs.outline 0.08` to `cs.outlineVariant isDark?0.25:0.4`. **Warning/error banner:** new `_PaperWarningBanner` widget — amber variant for out-of-range date (with inline Extend action link), red variant for time validation error — replaces raw `Text` and old amber `Container`. **Footer:** thin divider + Cancel text + `_GradeConfirmButton` (reused green checkmark button). **State:** added `_dateCalendarOpen`, `_paperNumberCtrl` (`TextEditingController`), removed `_formKey`. **`_showAddPaper`:** now adaptive — `showDialog` on desktop (≥`kMobileBreakpoint`), `showModalBottomSheet` on mobile. **New widgets added:** `_PaperFieldLabel`, `_PaperSheetDivider`, `_SubjectTrigger`, `_SubjectPickerSheet`, `_PaperMultiToggleRow`, `_PaperDateTrigger`, `_PaperSingleCalendar`, `_PaperWarningBanner`. **Deleted widgets:** `_DateChipPicker`. File compiles cleanly: 0 errors, 1 unused_element warning (`_FieldLabel` — intentional, cleanup deferred to Task 09).
- Task 08 — Dark-mode color audit pass on `exams_grades_screen.dart`. All modals/sheets from Tasks 03–06 verified against the canonical palette. Three deviations corrected: (1) `_GradeStepDots._dot` `AnimatedContainer` duration changed from 200ms to 140ms (standard app transition duration); (2) `_PaperTimePickerSheet` background changed from `Color(0xFF1A2332)` to `Color(0xFF18222E)` (canonical dark sheet bg); (3) `_PaperTimePresetChip` unselected border changed from `cs.outline.withValues(alpha: 0.08)` to `cs.outlineVariant.withValues(alpha: isDark ? 0.25 : 0.4)` (matches canonical default border values). Additionally, `_PaperTimePickerSheet` container was given a top `BorderSide` (`Color(0xFF2A3848)` dark / `cs.outlineVariant alpha 0.5` light) to match all other bottom sheets. All `InkWell` widgets confirmed to use `splashFactory: NoSplash.splashFactory`. All `AnimatedContainer` transitions confirmed at 140ms. All `borderRadius` values confirmed at 6 (chips), 8 (inputs/cards), or 12 (modal containers). `_GradePaperSlotCard` inner card uses `Color(0xFF1A2536)` intentionally (nested content card, slightly differentiated from sheet bg). File compiles with 0 errors, 1 pre-existing unused_element warning (`_FieldLabel` — cleanup deferred to Task 09).
- Task R05 — `exams_grades_screen.dart`: **Added `_ExpandableFab` widget and wired it into `_ExamGroupDetailViewState`.** `_ExamGroupDetailViewState.build()` restructured from `Column` to `Stack` — the column (header + tabs + content) fills the stack, with a `Positioned(right: 12, bottom: 16)` `_ExpandableFab` overlaid when `_canManage`. **New `_ExpandableFab` StatefulWidget** (`SingleTickerProviderStateMixin`): `AnimationController _ctrl` (200ms, `Curves.easeOutCubic`), `bool _open`. Three staggered per-option `Animation<double> _fades` + `Animation<Offset> _slides` with 80ms stagger (reverse index = topmost animates first). Layout: `SizedBox(160×stackH)` → `Stack(bottomRight)` with: transparent `GestureDetector` tap-outside-to-close overlay when open; three `_FabOptionPill` widgets at `Positioned(bottom: pillBottom(i))` (Paper i=0, Grade i=1, Stream i=2), each wrapped in `FadeTransition` + `SlideTransition`; main `FloatingActionButton.small(heroTag: "fab_exam_detail_main")` at bottom-right with `AnimatedBuilder` rotating `+` icon 45° when open. **New `_FabOptionPill` StatelessWidget**: `GestureDetector` → `Container(padding: horizontal 12, vertical 9, decoration: BoxDecoration(color: cs.surface, borderRadius: circular(8), border: 1px outlineVariant alpha 0.30 dark/0.45 light, boxShadow: blurRadius 8, Offset(0,3)))` → `Row` of `Icon(16px)` + `Text(12.5px, w500)`. Disabled state: `onSurfaceVariant` at 35% alpha, `GestureDetector.onTap = null`. Three pill configs: Paper (`Icons.note_add_outlined`, disabled when `paperEnabled == false`), Grade (`Icons.school_outlined`, always enabled), Stream (`Icons.account_tree_outlined`, always enabled). **New methods on `_ExamGroupDetailViewState`:** `_showAddPaper(context)` — reads `_currentStreamEntry` + current `gradeEntry`, calls `showModalBottomSheet` with `_CreatePaperSheet`; `_showAddGradeModal(context)` — stub (TODO R06); `_showAddStreamModal(context)` — stub (TODO R07). File compiles cleanly: 0 errors, 5 pre-existing warnings.
