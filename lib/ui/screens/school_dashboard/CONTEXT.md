# ui/screens/school_dashboard/ — School Dashboard Context

> The school dashboard is the main app surface where users interact with school-scoped data.
> It is a responsive layout that adapts between desktop (sidebar navigation) and mobile (top tab navigation).

## Overview

This directory contains **1 shell screen file** and **8 subdirectories**, each representing a section of the school dashboard. All data displayed here is scoped to a specific school via `SchoolContext` and a specific academic term via `ActiveTermContext`.

## Entry Point

### `school_dashboard_screen.dart`
- **Status:** ✅ Complete
- **Widget:** `SchoolDashboardScreen` (StatefulWidget)
- **Purpose:** The responsive shell that hosts all school-scoped pages. Provides `SchoolContext` and `ActiveTermContext` to the widget tree. User menu avatar shows a badge with failed sync log count (via `LogsDao.watchFailedLogCount`). Notifications action navigates to `NotificationsPage`.
- **Layout:** Uses a single `Scaffold` with a `_LayoutMode` enum (`full`, `rail`, `mobile`) that drives which navigation chrome is shown. A `LayoutBuilder` computes `mode` inline on each constraint change and passes it directly to `_buildLayout` — no state field, no `addPostFrameCallback`, no `setState`. The `_buildLayout` method uses a **unified tree structure** for all layout modes: `Scaffold → SafeArea → Column → [mobile-only top bar, mobile-only pill tabs] → Expanded(Row → [navigationChrome, wrappedContent])`. The `content` widget always appears at the **exact same depth** in the Element tree regardless of mode — this prevents state loss when crossing the 600px breakpoint. `navigationChrome` is `_FullSidebar` for full mode, `_IconRail` for rail mode, or `SizedBox.shrink()` for mobile. `wrappedContent` uses `_wrapSidebarContent` (padded `ClipRRect` card) for full/rail and raw `Expanded(child: content)` for mobile. (Unified in Task 01; previously three separate subtrees caused element teardown at breakpoints.)
  - **Full (≥1200px):** `_FullSidebar` (232px labelled sidebar) + content area wrapped in padded `ClipRRect` card.
  - **Rail (600–1200px):** `_IconRail` (64px icon-only rail with tooltips) + same wrapped content area.
  - **Mobile (<600px):** `_TabLayoutTopBar` + `_UnifiedMobileTabBar` (text-only, tinted background strip with `surfaceContainerHighest` alpha, uses `TabBar` widget with `TabController` and `BoxDecoration` indicator for a single smooth sliding animation — eliminates the dual-ripple issue from the old custom `GestureDetector` + `AnimatedContainer` approach; takes a `TabController controller` parameter instead of `selectedIndex` + `onTabSelected`; the `_buildTab` helper method was removed; ≤4 tabs use `Expanded` children, >4 tabs use scrollable `TabBar` with 80px fixed-width tabs) + content area directly (no `TabBarView` — content driven by `_selectedIndex` for persistence). The old `_SimpleTabBar` (teacher-only, solid `cs.primary` fill) and `LoopingTabStrip` (infinite-looping `ListView.builder` with icons) have been removed and replaced by this single unified widget for all roles.
- **Key responsibilities:**
  - Receives `SchoolMembership` + initial `MembershipEntry` from the home screen.
  - Creates `SchoolContext` with computed `SchoolPermissions`. Note: `_initializeSession()` now uses `parsePermissions` from `core/permission_parser.dart` instead of inline `jsonDecode` + `Permissions.fromJson` with a silent `catch`. For System and Super users, `_initializeSession()` also queries system-scoped scopes (`scopes.school IS NULL`) and unions those permissions with school-scoped permissions (per AGENT.md §17: "system + school roles merge"). New import: `database/tables/enums.dart` (for `UserLevel`).
  - Creates `ActiveTermContext` from `TermsDao.watchTerms(schoolId)`.
  - Hosts the **Role Switcher** in the top AppBar (reads `SchoolContext.membership.entries`).
  - Hosts the **Term Selector** chip in the AppBar (reads `ActiveTermContext`).
  - Routes to content pages based on selected nav item.
  - Disposes `SchoolContext` and `ActiveTermContext` on pop.
- **Navigation items by role:**
  - **Owner:** Overview | Academics | Exams | Members | Finance | Announcements | Timetable | Roles
  - **Teacher:** Overview | Academics | Exams | Timetable (core 4, always visible) | Announcements (always visible) | (permission-gated: Members, Finance, Roles)
  - **Staff:** Overview | Announcements (always visible) | (permission-gated: Academics [classes.read OR attendance.read/mark OR students.read], Exams [exams.read], Members [checks all 5 member resources: departments, owners, teachers, staff, students — expanded in Task A1], Finance, Timetable [classes.read], Roles [roles.read], Settings [schools.update])
  - **Student:** Overview | Grades | Timetable | Finance | Announcements
  - **Guardian:** Overview | Progress | Timetable | Finance | Announcements
- **Dependencies:** `models/school_context.dart`, `models/school_permissions.dart`, `models/membership.dart`, `models/active_term_context.dart`, `client.dart` (DAOs, `logsDao`), `database/daos/terms_dao.dart`, `database/daos/school_scopes_dao.dart`, `database/tables/enums.dart` (for `UserLevel`), `ui/widgets/term_selector_chip.dart`, `ui/widgets/active_term_provider.dart`, `ui/screens/notifications/notifications_page.dart` (note: `ui/widgets/looping_tab_strip.dart` import removed — `LoopingTabStrip` no longer used here)

## Subdirectories

### `overview/`
| File | Widget | Status | Description |
|---|---|---|---|
| `overview_screen.dart` | `OverviewScreen` | ✅ Complete | Role-dispatched school dashboard landing page — the first page a user sees when entering a school dashboard. Switches layout based on `SchoolContext.currentEntry.role` via `ValueListenableBuilder<MembershipEntry>`. Delegates to role-specific overview files below. |
| `overview_shared.dart` | (shared widgets) | ✅ Complete | Shared widgets used across role overviews: `WelcomeCard`, `SectionTitle`, `StaggeredList`, `EmptyCard`, `LoadingShimmer`, `RecentAnnouncements`, helpers (`currentDayOfWeek`, `fmtTime`, `fmtDateFromSeconds`). |
| `owner_overview.dart` | `OwnerOverview` | ✅ Complete | Owner overview: `_SchoolIdentityCard` (school name, motto, establishment year), Quick Stats (students/teachers/staff/classes counts), Current Term info card, Recent Announcements. |
| `teacher_overview.dart` | `TeacherOverview` | ✅ Complete | Teacher overview: Welcome card, Today's Attendance, Next Class countdown, Class Chips, Today's Schedule, Quick Stats, Upcoming Exams, Recent Announcements. |
| `staff_overview.dart` | `StaffOverview` | ✅ Complete | Staff overview: Welcome card, permission-gated Quick Stats (students/invoices/collection/pending), Recent Announcements, "no roles assigned" empty state. |
| `student_overview.dart` | `StudentOverview` | ✅ Complete | Student overview: Welcome card, Today's Attendance, Next Class countdown (`StudentNextClass` — public, reused by guardian), Enrollment info, Today's Schedule (`StudentTodaySchedule` — public), Recent Grades (`StudentRecentGrades` — public), Upcoming Exams (`StudentUpcomingExams` — public, reused by guardian), Attendance Summary (`StudentAttendanceSummary` — public), Recent Announcements. |
| `guardian_overview.dart` | `GuardianOverview` | ✅ Complete | Guardian overview: Welcome card, Ward attendance status, **Next Class countdown** (reuses `StudentNextClass` from `student_overview.dart`), Ward info card (`_WardInfoCard`), Unenrolled ward banner, Quick Stats (`_GuardianQuickStats`), Today's Schedule (reuses `StudentTodaySchedule`), Attendance Summary (reuses `StudentAttendanceSummary`), Recent Grades (reuses `StudentRecentGrades`), **Upcoming Exams** (reuses `StudentUpcomingExams`), Finance summary (`_GuardianFinanceSummary`), Recent Announcements. |

**Data sources:** `MembersDao.watchStudents(schoolId)`, `MembersDao.watchTeachers(schoolId)`, `MembersDao.watchStaff(schoolId)`, `EnrollmentsDao.watchPopulatedClasses(...)`, `EnrollmentsDao.watchStudentEnrollment(...)`, `TimetableDao.watchTeacherTimetable(...)`, `TimetableDao.watchClassTimetable(...)`, `ExamsGradesDao.watchExamsForTerm(...)`, `ExamsGradesDao.watchStudentGrades(...)`, `AttendanceDao.watchStudentAttendanceSummary(...)`, `FinanceDao.watchTermFinanceSummary(...)`, `FinanceDao.watchStudentFinanceSummary(...)`, `AnnouncementsDao.watchAllAnnouncements(...)`, `AnnouncementsDao.watchAnnouncementsForAudience(...)`
**Dependencies:** `database/daos/announcements_dao.dart`, `database/daos/attendance_dao.dart`, `database/daos/enrollments_dao.dart`, `database/daos/exams_grades_dao.dart`, `database/daos/finance_dao.dart`, `database/daos/members_dao.dart`, `database/daos/timetable_dao.dart`, `database/tables/enums.dart`, `models/active_term_context.dart`, `models/membership.dart`, `models/permissions.dart`, `models/school_permissions.dart`, `models/school_context.dart`, `ui/widgets/active_term_provider.dart`, `client.dart`

---

### `academics/`
| File | Widget | Status | Description |
|---|---|---|---|
| `academics_screen.dart` | `AcademicsScreen` | ✅ Complete | Grade/stream tree view. Scrollable list of grade cards from `SchoolConfig`. Each card shows grade label + stream chips. FAB to add grades (standardised to `FloatingActionButton.small` matching Roles page). Header simplified — Departments and Exams action links removed (Departments moved to Members tab, Exams is its own owner nav item). **Teacher scoping (B01):** When the current entry is a `TeacherEntry`, the grade/stream tree is filtered to only show grades/streams where the teacher has assignments via `class_teachers` or `subject_teachers` for the active term. Implementation uses nested `StreamBuilder<List<ClassTeacher>>` and `StreamBuilder<List<SubjectTeacher>>` (reactive queries on `db.classTeachers` and `db.subjectTeachers` filtered by school/year/term/teacher) inside the main `StreamBuilder<List<SchoolStream>>`. Distinct `(grade, stream)` pairs from both assignment tables are collected into a `Set<String>` of `"$grade|$stream"` keys, then `allStreams` is filtered to `visibleStreams` containing only matching pairs. The tree body is extracted into a `_buildTreeBody(visibleStreams, allStreams, ...)` helper method — `visibleStreams` drives the grade tree display while `allStreams` (full school-wide list) is passed to `_showAddGradeSheet` so that grade creation (if permitted) still knows which grades already exist. Non-teacher entries bypass the filtering and see all grades/streams. Import added: `package:drift/drift.dart` (with `hide Column` to avoid Flutter conflict). |
| `grade_detail_page.dart` | `GradeDetailPage` | ✅ Complete (scaffold) | Two-layer tab architecture. **Layer 1 — Stream Tabs:** scrollable `EduTabBar` with "Comparisons" + one tab per stream from `GradeConfig.streams`. **Layer 2 — Content Tabs:** scrollable `EduTabBar` with Students · Exams · Subjects · Attendance · Timetable · Lessons · Teachers (visible only when a specific stream is selected, hidden on Comparisons). **Comparisons tab** now wired to `ComparisonsTab` (from `tabs/comparisons_tab.dart`); other content areas are placeholder widgets ("coming soon"). Contextual `FloatingActionButton.small` appears on stream tabs only (hidden on Comparisons) with a bottom-sheet action picker: Add Student, Assign Class Teacher, Assign Subject Teacher. Uses `TickerProviderStateMixin` for two `TabController`s. Reads `ActiveTermProvider.of(context)` for year/term access. Exposes `_selectedStream`, `_selectedStreamCode`, `_selectedStreamName` getters for downstream use. Old `_FilterChip` class deleted entirely. **Deep-link params (Task 17b):** accepts optional `initialStreamIndex` (int?, 0=Comparisons, 1+=stream) and `initialContentTabIndex` (int?, 0=Students…3=Attendance…6=Teachers) — both clamped to valid range and wired into `TabController.initialIndex`. Used by `AttendanceScreen` to navigate directly to a stream's Attendance tab. **FAB — Add Student (Task 19):** "Add Student" action opens `_StudentEnrollSheet` (`DraggableScrollableSheet`-style bottom sheet, max 75% height). Searchable student picker via `MembersDao(db).searchStudents(schoolId, query)`. Each result row shows: `_initials` avatar, student name, ADM number, and enrollment status for the current term via `EnrollmentsDao(db).getStudentEnrollment(...)`. Three statuses: (a) "Not enrolled" (muted) — tap enrolls directly; (b) "Already enrolled" (primary tint, check icon, disabled) — student is in this exact class; (c) "Currently in {Grade} · {Stream}" (amber warning, swap icon) — tap shows a confirmation `AlertDialog` ("Transfer student?") before re-enrolling. Enrollment calls `EnrollmentsDao.enrollStudent(...)` which handles transfer (delete old + insert new) in one transaction. Shows snackbar confirmation on success. Sheet title: "Add Student" + subtitle "Enroll into {gradeLabel} · {streamName}". Private types: `_StudentEnrollSheet` (stateful), `_StudentEnrollCandidate` (model with `StudentsData`, `_EnrollStatus`, `statusLabel`), `_EnrollStatus` enum (`notEnrolled`, `alreadyHere`, `otherClass`). Uses `cache.currentUser?.user.id` for `accountId`. Grade labels resolved via `gradeLabelsFor(curriculumType)`. **FAB — Assign Class Teacher (Task 20):** "Assign Class Teacher" action opens `_ClassTeacherPickerSheet` (bottom sheet, max 75% height). Loads all teachers for the school via `MembersDao(db).watchTeachers(schoolId)` (first snapshot), resolves user data for each via `MembersDao.findUserById`, and checks the current active class teacher via `SubjectsDao(db).watchActiveClassTeacher(...)` (first snapshot). Teacher list is searchable by name (in-memory filter). Each row: `_initials` avatar (primary-tinted if active), teacher name, subtitle row with role + department info (or "Teacher" fallback) + "Current class teacher" label (primary tint) for the active teacher. Active class teacher row is disabled with a check icon. On tap: calls `SubjectsDao.assignClassTeacher(...)` which closes the previous assignment and inserts the new one in a single transaction (with log entries). Shows snackbar: "{name} assigned as class teacher for {gradeLabel} · {streamName}". Sheet title: "Assign Class Teacher" + subtitle "{gradeLabel} · {streamName}". Private types: `_ClassTeacherPickerSheet` (stateful), `_TeacherCandidate` (model with `TeachersData`, `UsersData`, `isCurrentClassTeacher`). Uses `cache.currentUser?.user.id` for `accountId`. Added import: `subjects_dao.dart`. **FAB — Assign Subject Teacher (Task 21):** "Assign Subject Teacher" action opens `_SubjectTeacherPickerSheet` (bottom sheet, max 75% height). **Two-step picker:** Step 0 lists all valid subjects for the grade's curriculum level (derived via inline grade-to-level mapping + `levelsFor` + `subjectLabel` from `curriculum_levels.dart`). Each subject row: book icon (primary-tinted if assigned), subject name, current teacher name or "Not assigned". All subjects are tappable (assigned subjects show current teacher but can be reassigned). Step 1: teacher picker (identical pattern to class teacher — `MembersDao.watchTeachers` + `findUserById`, searchable by name, currently assigned teacher shown with "Currently assigned" label and disabled). Back arrow returns to Step 0 (clears search + resets state). Grade-to-level mapping: CBC PP→0, Gr1–3→1, Gr4–6→2, Gr7–9→3, Gr10–12→4,5,6 union; 8-4-4 Std1–3→0, Std4–8→1, Form1–2→2, Form3–4→3. Current assignments loaded via `SubjectsDao.watchSubjectsForClass`, teacher user ID resolved via `SubjectsDao.getSubjectAssignment`. Assignment via `SubjectsDao.assignSubjectTeacher` (insert or update in transaction with log). Snackbar: "{subject} assigned to {teacher}". Added import: `curriculum_levels.dart`. Private types: `_SubjectTeacherPickerSheet` (stateful), `_SubjectCandidate` (model with `subjectIndex`, `subjectName`, `assignedTeacherName`). Reuses `_TeacherCandidate`. **All three FAB actions now fully implemented — no remaining stubs.** |
| `student_grade_page.dart` | `StudentGradePage` | ✅ Complete | Detail page for a single student within a grade/stream context. Opened from `StudentsTab` when a student row is tapped. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `streamName`, `studentAdm`, `curriculumType`, `schoolContext`. **Structure:** Scaffold with AppBar (reactive title from `MembersDao.watchStudent`), Student Header Card (compact info bar: `_StudentAvatar` radius 24 + name w500 14px + "ADM: {adm} · {streamName}" muted 12px + `_StatusChip` using `StudentStatus` enum — green Active, red Expelled, purple Graduated, blue Transferred, amber Withdrawn, muted Deleted), Performance Indicator Banner (slim color-coded strip: green "Improving" / red "Declining" / amber "Stable" / grey "Insufficient exam data" — trajectory computed reactively from `ExamsGradesDao.watchStudentGrades` by comparing averages of last two exams, delta threshold ±1.0), Content Tabs (`EduTabBar` with `isScrollable: true`, 4 tabs: Overview · Mastery · Exams · Attendance). **All tabs wired:** Overview → `StudentOverviewTab`, Mastery → `StudentMasteryTab`, Exams → `StudentExamsTab`, Attendance → `StudentAttendanceTab`. No remaining placeholders — `_buildPlaceholder` method removed. Uses `SingleTickerProviderStateMixin`. Private widgets: `_StudentAvatar` (file-based via `FileCache.studentImagePath`), `_StatusChip` (tinted container with `StudentStatus` switch). |
| `exam_detail_page.dart` | `ExamDetailPage` | ✅ Complete | Dedicated page for viewing a single exam's full details, reached from the `ExamsTab` in the grade detail page. Constructor params: `exam` (ExamWithPapers), `schoolId`, `year`, `term`, `grade`, `streamCode` (nullable), `streamName` (nullable), `curriculumType`, `schoolContext`. **Structure:** Scaffold with AppBar (back + exam type title), `_ExamSummaryCard` (always visible above tabs — type badge, personalized badge, grade-wide/stream badge, reactive grading status badge computed from papers stream: "Fully Graded" if all papers marked / "Partially Graded" if some / "Not Started" if none, date range with icon, teacher avatar + name), `EduTabBar` with 3 tabs: Papers · Grades · Performance (**`isScrollable: true`** — tabs hug left at natural width, no longer forced full-width), `TabBarView` with three tab widgets. **Papers tab** (`_PapersTab`): reactive `StreamBuilder<List<Paper>>` via `ExamsGradesDao.watchPapersForExam`. Inside the builder a `LayoutBuilder` switches on width: **Desktop (≥600px)** renders `_PapersCrossTable` — a unified cross-table matrix where rows = unique `(grade, stream)` pairs derived from the papers, columns = unique exam dates, and cells show the paper's subject name + paper number with a status-colored left-accent border (2.5px) + tinted background. Multiple papers on the same date are stacked vertically within one cell. Empty cells show a thin-border placeholder. Tapping any cell navigates to `PaperDetailPage`. **Mobile (<600px)** retains the original date-grouped flat list of `_PaperTimetableCard` widgets with optional stream badges. On tap → navigates to `PaperDetailPage` via `MaterialPageRoute`, passing `paper`, `exam`, `schoolId`, `year`, `term`, `curriculumType`, `schoolContext`. Header count row and empty state unchanged. **Grades tab** (`_GradesTab`): accepts optional `streamCode` param (forwarded from parent `ExamDetailPage.streamCode`), loads enrolled students via `ExamsGradesDao.getEnrolledStudents(stream: streamCode)` — **now correctly filters by stream** instead of showing all students in the grade. Watches grades via `watchGradesForExam`. Builds student×subject grade map (prefers subject-level totals where paper==null). Pre-computes `_StudentGradeRow` objects (student, subjectGrades map, totalScore, totalPossible, percentage, hasGrades) via `_buildRows()` helper. **Sort toggle** (`_GradeSort` enum: `scoreDesc` default, `nameAsc`): compact toggle button above table ("By score" with leaderboard icon / "By name" with sort-alpha icon), tapping cycles between modes, rows re-sorted immediately via `setState`. **Desktop (≥600px): fixed first column + horizontally scrollable grade columns.** Left fixed column (200px): header "Student" + per-row name (12.5px w500, ellipsis) + "ADM {adm}" (10.5px muted). Scrollable columns: one per subject + Total + %. Subject headers abbreviated when >6 subjects via `_abbreviateSubject` (keeps first word + first letter of subsequent words, e.g. "Social Studies" → "Social S.", with `Tooltip` showing full name). Each subject cell uses `_GradeCell` widget: score/total on first line (12px, color-coded) + percentage on second line (10px, muted alpha 0.55, color-coded). Total cell: score/total (12px w500). % cell: tinted badge (borderRadius 4). Cells separated by thin borders. Row height 52px, header 42px. Empty "—" placeholder for ungraded cells (alpha 0.25). Student count shown in sort toggle row. Mobile (<600px): `ListView.builder` of `_MobileStudentGradeCard` (expandable) — collapsed shows name + ADM + percentage badge; expanded shows per-subject scores + total row with dividers. Uses `AutomaticKeepAliveClientMixin`. **Performance tab** (`_PerformanceTab`): accepts optional `streamCode` param (forwarded from parent), **now correctly filters enrolled students by stream**. One-shot load via `computeClassAnalytics` + `getEnrolledStudents(stream: streamCode)` + `watchClassGrades.first`. **Redesigned with 3-insight views** controlled by `_selectedInsight` state (0=Overview, 1=Subjects, 2=Rankings). **Insight selector strip** at top — 3 compact tappable chips with icons (`insights_rounded`, `menu_book_rounded`, `emoji_events_rounded`) and `AnimatedContainer` selection state. **Overview insight** (`_buildOverviewInsight`): hero metrics row (3 `_buildMetricTile` containers — class average % color-coded, graded/enrolled count, spread = highest−lowest), stacked horizontal distribution bar (`_buildDistribution` — 6 color segments red→emerald for score buckets 0–39/40–49/50–59/60–69/70–79/80–100, counts overlaid in white, `Tooltip` per segment, `Wrap` legend below), key insights card (`_buildStrengthsWeaknesses` — strongest subject with trending_up icon + weakest with trending_down, separated by thin divider, only shown when ≥2 subjects). **Subjects insight** (`_buildSubjectsInsight`): `ListView.builder` of per-subject cards sorted by average descending — subject name + avg% color-coded, 6px `LinearProgressIndicator`, graded/total count + avg score subtitle. **Rankings insight** (`_buildRankingsInsight`): `ListView.builder` with header row + data rows using data-table style (thin 0.5px dividers, no cards per row), top-3 medal accents (gold/silver/bronze left border 2.5px + trophy icon replacing rank number), percentage in tinted badge (borderRadius 4). **No longer uses `fl_chart`** — `BarChart` replaced by custom stacked horizontal bar. `dart:math` import removed. Old methods deleted: `_buildSummaryCard`, `_buildSubjectBar`, `_buildDistributionChart`, `_buildRankingTable`, `_buildSectionLabel`, `_medalTint`. Uses `AutomaticKeepAliveClientMixin`. `AnimatedSwitcher` (200ms) for smooth transitions between insight views. Private widgets: `_StudentGradeRow` (pre-computed row model with student, subjectGrades, totalScore, totalPossible, percentage, hasGrades), `_GradeSort` enum (scoreDesc, nameAsc), `_GradeCell` (score/total + percentage sub-text, color-coded), `_ExamSummaryCard`, `_PapersTab`, `_PaperTimetableCard`, `_PapersCrossTable` (Task EX-01 — desktop cross-table: rows=streams, cols=dates, cells=status-colored paper cards stacked vertically; `_buildCell` helper renders each paper; row labels show stream name from `streamNames` map), `_GradesTab`, `_MobileStudentGradeCard` (stateful expandable), `_PerformanceTab`, `_StudentRankRow`, badge widgets (`_TypeBadge`, `_OutlineBadge`, `_TintedBadge`, `_StatusBadge`, `_PaperStatusChip`). Shared helpers: `_buildLoading`, `_buildEmpty`, `_typeLabel`, `_typeColor`, `_pctColor`, `_fmtDate`, `_fmtTime`, `_fmtScore`, `_abbreviateSubject`, `_fmtDayHeader` (Task EX-01 — "Mon, 12 Jan" format), `_groupExamPapersByDate` (Task EX-01 — groups papers by calendar day), `_sortedExamPaperDates` (Task EX-01 — sorted unique dates list). Uses `SingleTickerProviderStateMixin` for tab controller. |
| `paper_detail_page.dart` | `PaperDetailPage` | ✅ Complete | Dedicated page for viewing/managing a single paper within an exam, reached from `ExamDetailPage` when a paper card is tapped. Constructor params: `paper` (Paper), `exam` (ExamWithPapers), `schoolId`, `year`, `term`, `curriculumType`, `schoolContext`. **Structure:** Scaffold with AppBar (back + "{Subject} Paper {N}" title), body is a `StreamBuilder<List<GradeRow>>` via `ExamsGradesDao.watchGradesForPaper` + enrolled students loaded via `getEnrolledStudents`. **Top-level AI state (Task 05):** `_PaperDetailPageState` holds AI marking state fields (`_aiMarking`, `_aiPhase`, `_aiMarkedCount`, `_aiProgress`) and a `_runAiMarking()` method that delegates to the active grade widget via `GlobalKey<_GradeSpreadsheetState>` or `GlobalKey<_GradeListState>`. `_hasUnmarkedSubmissions` getter checks child state via GlobalKey. `_AiPhase` enum (`idle`, `analyzing`, `assigning`, `done`) defined at file scope. **Permission split (Bug Group 3 fix):** The old single `_canManage` getter has been replaced by two separate getters: **`_canProgressStatus`** (exam-level management — status progression, delete, edit invigilator, scheme management; for teachers grants access if they created the exam, are the invigilator, have `Resource.exams`/`Action.update` permission, OR teach this subject to this class) and **`_canGradeContent`** (subject-specific — grade entry, answer sheet submission, AI marking; for teachers only grants access if they teach the specific subject to the paper's grade/stream). Owners and staff get both unconditionally. Import added: `models/permissions.dart` (with `hide Action` on `package:flutter/material.dart` to avoid conflict). **Sections (vertical ListView):** (1) **Paper Header** (`_PaperHeader`, stateful) — unified widget replacing the former `_PaperInfoCard`, `_PaperActionBar`, and `_AnalyticsSection`/`_CompactBarChart`. Now accepts three permission props: `canEdit` (`_canProgressStatus`), `canManage` (`_canProgressStatus`), and `canGrade` (`_canGradeContent`). The scheme/marking-scheme section is gated by `canGrade` (subject-specific), while status advancement and delete buttons remain gated by `canManage` (exam-level). Single rounded container (`surfaceContainerLowest` bg, `borderRadius: kCardRadius`, thin border) containing 6 rows: **Row 1** subject name + paper number + `_PaperStatusChip`; **Row 2** date/time with day-of-week ("Mon, 15 Jan 2025 · 08:00 – 10:00") via `_kDayAbbr`; **Row 3** exam type badge + optional "Personalized" chip; **Row 4** invigilator `UserAvatar` (radius 14) + name + edit icon (always visible when `canEdit` — no longer gated on `status == pending`); **Row 5** (conditional, shown when `gradeRows.isNotEmpty`) donut chart (`_DonutPainter` CustomPainter, 80×80, 10px stroke, 6 color segments for score distribution 0–49/50–59/60–69/70–79/80–89/90–100, center text = graded count) + stats column (graded/total + average% with color-coded text); **Row 6** bottom action row — delete icon button (canManage && pending, left) + spacer + unified action circle button (right, **4 priority states — Task 05**): (a) dirty-save → orange border + save icon, calls `onSaveAllGrades`; (b) AI marking in progress (`aiProgress != null`) → `_RadialFillPainter` radial fill with percentage text (indigo `#6366F1`); (c) has unmarked submissions → indigo circle border + `Icons.auto_fix_high`, calls `onAiMark`; (d) normal advance → arc-progress + next-status icon / fully-marked green check. Animation controllers (`_arcCtrl`, `_scaleCtrl`, `_flashCtrl`) and status methods (`_arcFraction`, `_statusColor`, `_statusIcon`, `_statusActionLabel`, `_nextStatus`, `_advance`, `_deletePaper`) moved from old `_PaperActionBar`. Analytics computation (`_computeAnalytics`, `_avgColor`) moved from old `_AnalyticsSection._compute`. (2) **Grade Entry Section** — "Grades" section label + responsive layout: Desktop (≥600px) → `_GradeSpreadsheet` (header row + `ListView.separated` of `_SpreadsheetRow`, `canGrade: _canGradeContent`). Mobile (<600px) → `_GradeList` (`canGrade: _canGradeContent`). Both save via `ExamsGradesDao.upsertGrade`. Both accept AI callbacks (`onAiPhaseChanged`, `onAiProgressChanged`, `onAiMarkedCountChanged`) wired to parent state. Both expose public `runAiMarking()` method and `hasUnmarkedSubmissions` getter (callable via GlobalKey). Empty state for no enrolled students. **Deleted widgets (Task 03):** `_PaperInfoCard`, `_PaperActionBar`, `_AnalyticsSection`, `_CompactBarChart`. **Deleted widgets (Task 04):** `_MiniArcIndicator`. **Deleted widgets (Task 05):** `_AiMarkButton` — AI marking now triggered exclusively from `_PaperHeader` unified action circle. **Deleted getters (Bug Group 3):** `_canManage` — fully replaced by `_canProgressStatus` + `_canGradeContent`. **`_SpreadsheetRow` simplified (Task 04):** removed `onGradeButtonTap`, `onQuickGradeTap`, `isQuickGrading` params; removed submission count badge next to name; removed edit pencil icon; removed AI quick-grade button; upload/submit button now always shows `upload_file_outlined` with green circle count badge overlay when `submissionCount > 0`. **Mobile `_GradeList` (Task 04):** `_openStudentActionSheet` reduced to 2 actions (Submit Answer Sheets + Enter Grade); removed "Quick Grade with AI" and "View Submissions (N)" rows; mobile row widget removed inline submission count badge next to student name. **BUG-010 compliance (UI polish pass):** `_MobileGradeEntrySheet` now wraps its content in `EduSheet(title: student.name)` for proper BUG-010 compliance (self-contained sheet with background, handle, title, keyboard padding); layout compacted with tighter padding, reduced gaps, and compact input decorations; save button replaced with two icon buttons (cancel + green save) per §21 guidelines. `_openStudentActionSheet` now wraps its content in `EduSheet(title: student.name)` + `SafeArea(top: false)` for proper BUG-010 compliance. **New private widgets:** `_PaperHeader`, `_DonutPainter`, `_DonutSegment`, `_RadialFillPainter` (Task 05). **Kept:** `_PaperStatusChip`, `_ArcProgressPainter`, `_GradeSpreadsheet`, `_SpreadsheetRow`, `_GradeList`, `_MobileGradeEntrySheet`, `_SectionLabel`, `_AnswerSubmissionSheet`, `_ActionSheetRow`. Shared helpers: `_buildLoading`, `_buildEmpty`, `_typeLabel`, `_typeColor`, `_pctColor`, `_fmtDate`, `_fmtTime`, `_fmtScore`, `_inputDeco`, `_kDayAbbr`, `_months`. Uses `client.dart` (`cache`) for account ID access. Uses `models/permissions.dart` for `Resource`/`Action` enums in `_canProgressStatus`. **Question bank integration (Tasks 11–18):** `_PaperHeader` row 6 gains two additional action buttons: (1) **Generate Paper** (`auto_awesome_rounded` icon, Tooltip "Generate Paper", 28×28, shown when `canManage && isPending`) — navigates to `PaperGenerationPage`; (2) **Print Paper** (`print_rounded` icon, Tooltip "Print Paper", 28×28, shown when status is done/marked) — calls `downloadAndOpenPdf()` from `paper_pdf_viewer.dart`, shows 16×16 spinner during download via `_printBusy` state. **`MarkingStatusIndicator`** widget (from `ui/widgets/marking_status_indicator.dart`) is rendered above the grades section, shown conditionally. Wired with `onComplete` callback that triggers grade list reload. **View Breakdown button** in grade rows: both `_GradeSpreadsheet` (desktop) and `_GradeList` (mobile) show a `quiz_outlined` icon button on AI-graded students that opens `QuestionGradesSheet` via `showEduSheet`, passing student, subject, paper, and grade details. New imports: `marking_status_indicator.dart`, `paper_pdf_viewer.dart`, `question_grades_sheet.dart`. |
| `question_grades_sheet.dart` | `QuestionGradesSheet` | ✅ Complete | Bottom sheet / desktop dialog showing per-question AI marking breakdown for a student's paper. Launched from grade spreadsheet/list rows via `showEduSheet`. Constructor params: `school`, `exam`, `student` (int), `subject` (int), `paper` (int?, optional), `studentName`, `overallScore` (double), `totalMarks` (int). **Structure:** Self-contained `EduSheet` wrapper (BUG-010 compliant — own background, handle, title "Marking Breakdown — {studentName}", keyboard padding). Body is a `FutureBuilder` on `questionBankService.getQuestionGrades(...)` called in `initState`. **States:** Loading → centered `CircularProgressIndicator` (20×20, strokeWidth 2). Error → error icon + "Failed to load breakdown" + error message + "Retry" `TextButton.icon`. Empty → `quiz_outlined` icon (48×48 container) + "No marking breakdown available." Data → `ListView` with `_OverallScoreBar` + list of `_QuestionCard` widgets separated by 8px gap. **`_OverallScoreBar`:** `AppTheme.nestedBg` container, `kCardRadius` corners, 12px padding. Shows "{score} / {total}" (20px w500) + percentage badge (color-coded: green ≥70%, amber ≥50%, red <50%, `kChipRadius` corners, tinted bg) + 4px `LinearProgressIndicator` with rounded ends (color-coded fill). **`_QuestionCard` (stateful):** `AppTheme.nestedBg` container, `kCardRadius` corners. Header: "Q{n}" primary-tinted badge (`kChipRadius`) + "{awarded}/{total}" marks badge (color-coded) + `feedback_outlined` icon (16px, when feedback present). Question text: 13px w400, truncated to 3 lines, expandable on tap via `AnimatedCrossFade` (200ms). AI feedback: 13px w300 italic, `onSurfaceVariant` alpha 0.7. Rubric results: `_RubricTable` with data-table style rows separated by `AppTheme.tableRowDivider`. Each `_RubricRow`: `Icons.check_circle_rounded` green (16px) for satisfied / `Icons.cancel_rounded` red (16px) for unsatisfied + criterion text (12.5px w400) + "{awarded}/{available}" (12px w500). **Private widgets:** `_OverallScoreBar`, `_QuestionCard`/`_QuestionCardState`, `_RubricTable`, `_RubricRow`. **Helpers:** `_pctColor` (≥70 green, ≥50 amber, <50 red), `_fmtScore` (integer or 1 decimal). **Dependencies:** `client.dart` (`questionBankService`, `accessToken`), `models/question_grade.dart` (`QuestionGradeDetail`, `RubricResult`), `models/result.dart` (`Result`, `Ok`, `Err`), `ui/theme/app_theme.dart`, `ui/widgets/edu_sheet.dart`. |
| `paper_generation_page.dart` | `PaperGenerationPage` | ✅ Complete | Multi-step wizard (3 steps) for AI paper generation. Constructor params: `schoolId`, `examId`, `subjectId`, `paperId` (nullable), `grade`, `stream` (nullable), `subjectName`, `examName`. **Page state:** `_currentStep` (0=allocate, 1=review, 2=finalize), `_totalMarks` (default 80), `_allocations` (List<TopicAllocation>), `_generatedQuestions` (List<PaperQuestion>, filled after generate), `_paperPdf` (PaperPdf?, filled after finalize), `_isGenerating` (bool), `_isFinalizing` (bool), `_questionTopicMap` (Map<String, int> — paperQuestionId→topicId for regeneration). **AppBar** — back chevron (`Icons.chevron_left_rounded`), title "Generate Paper" (w400, 17px), trailing `_StepDots` (●○○ — animated dots, filled for current/past, muted for future). Back button behavior is step-aware: step 1 → step 0, step 2 → step 1, step 0 → pop page. **Step 0 — Topic Allocation:** (1) Context row — subject name · exam name (13px w400 muted). (2) Total marks input — label "Total marks" (w500, 13px) + 100px `TextField` (number input, max 4 digits, `kCardRadius` border). (3) Topic list — `StreamBuilder<List<Topic>>` on `catalogDao.watchTopicsBySubjectAndGrade(subjectId, grade)`. Each `_TopicRow`: topic name (w400, 14px) + 60px marks `TextField` (number input, `kChipRadius` border, tabular figures). Data-table style: rows separated by `AppTheme.tableRowDivider`, 48px height. `_syncAllocations()` method preserves existing mark values when topic list updates. (4) Sticky footer (`_AllocationFooter`, stateful) — "Allocated: {sum} / {total}" with `RichText` color coding (green when sum==total, red when sum>total, amber when sum<total) + "Generate" button with `auto_awesome_rounded` icon. Button has press animation (scale 0.95→1.0, 100ms), enabled only when sum==total AND total>0, shows 16×16 spinner during generation. On generate: calls `questionBankService.generatePaper(...)`, advances to step 1. Empty state for no topics. **Step 1 — Review & Edit Questions:** Scrollable list of `_ReviewQuestionCard` widgets (one per generated question). Each card: "Q{order}" primary-tinted badge + marks pill badge + topic name muted label + question text (expandable via `AnimatedCrossFade`) + rubric criteria list with marks badges + action buttons. **Actions per question:** Regenerate (`Icons.refresh_rounded`, calls `questionBankService.regenerateQuestion(...)` with topic context from `_questionTopicMap`, replaces question in list with loading state), Edit (opens `_EditPaperQuestionSheet` via `showEduSheet` — editable text + marks + rubric, calls `questionBankService.editPaperQuestion(...)`), Remove (removes from list with confirmation). Sticky footer: question count + total marks summary + "Continue to Finalize" button (enabled when ≥1 question). **Step 2 — Finalize:** Summary view of the paper: subject/exam context, total questions, total marks, list of question previews (read-only, compact). "Finalize & Generate PDF" button (primary, `auto_awesome_rounded` icon, 60s timeout) — calls `questionBankService.finalizePaper(...)` with ordered `paperQuestionIds`. On success: stores `PaperPdf` result, shows success state with PDF URL + "Open PDF" button (calls `downloadAndOpenPdf`). On error: shows error SnackBar, stays on step 2 for retry. `_isFinalizing` state drives loading spinner. **Private widgets:** `_StepDots`, `_TopicRow`, `_AllocationFooter`/`_AllocationFooterState`, `_ReviewQuestionCard`, `_EditPaperQuestionSheet`, `_FinalizeFooter`. **Dependencies:** `client.dart` (`catalogDao`, `questionBankService`, `accessToken`), `database/database.dart` (`Topic`), `models/question.dart` (`RubricCriterion`, `QuestionImage`), `models/paper_generation.dart` (`TopicAllocation`, `PaperQuestion`, `PaperPdf`), `models/result.dart` (`Result`, `Ok`, `Err`), `ui/theme/app_theme.dart`, `ui/widgets/edu_sheet.dart`, `paper_pdf_viewer.dart` (`downloadAndOpenPdf`). |

| `paper_pdf_viewer.dart` | `downloadAndOpenPdf()` | ✅ Complete | Top-level async function (not a widget) for downloading and opening a finalized paper PDF. Params: `school`, `exam`, `subject`, `paper?`, `grade`, `stream?`, `accessToken`, `context`. Flow: (1) shows persistent loading SnackBar with 16×16 spinner + "Downloading PDF…"; (2) calls `questionBankService.getPaperPdf(...)` to get presigned URL; (3) downloads PDF bytes via `dart:io` `HttpClient`; (4) saves to temp directory (`getTemporaryDirectory()`) with deterministic filename `paper_{exam}_{subject}[_{paper}].pdf`; (5) opens with system viewer: `xdg-open` (Linux), `open` (macOS), `start` (Windows), or shows saved path SnackBar (Android/iOS). Error handling: dismisses loading SnackBar and shows error SnackBar on any failure (gRPC error, HTTP error, IO error). **Dependencies:** `dart:io` (`HttpClient`, `File`, `Process`, `Platform`), `path_provider` (`getTemporaryDirectory`), `client.dart` (`questionBankService`), `models/result.dart` (`Ok`, `Err`). |

#### `academics/tabs/`
| File | Widget | Status | Description |
|---|---|---|---|
| `comparisons_tab.dart` | `ComparisonsTab` | ✅ Complete | Stream comparison UI. Calls `AcademicsDao(db).computeStreamComparison(...)` on init and shows: (1) **Summary Row** — horizontal scrollable row of 4 compact stat cards (Total Students, Streams, Best Performing, Most Improved); (2) **Stream Comparison Cards** — one card per stream with header (name + student count badge) and stats grid (2-col × 3-row): Overall Avg, Last Exam, Attendance, Mastery (each with thin color-coded 6px progress bar — green ≥75, amber ≥50, red <50), Trajectory (icon + label); (3) **Ranking Table** (2+ streams) — compact table ranking streams by `lastExamAverage` desc with columns: Rank, Stream, Students, Last Exam Avg, Overall Avg, Trajectory icon. Alternating row tints. Empty state for no streams. Loading spinner while computing. Constructor params: `schoolId`, `year`, `term`, `grade` (int), `streams` (List<GradeStream>). Re-loads on `didUpdateWidget` if key params change. |
| `students_tab.dart` | `StudentsTab` | ✅ Complete | Enrolled students list for a specific stream. Uses `AcademicsDao(db).watchStudentsForGrade(...)` reactive stream via `StreamBuilder`. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `streamName`, `curriculumType`, `schoolContext`. **UI:** (1) Header row with count badge ("24 students", muted 12px). (2) `ListView.builder` of card-based rows — each row: `_StudentAvatar` (radius 18, reads `FileCache.studentImagePath`), name (w500, 13.5px) + "ADM: {adm}" (muted, 11.5px), `_TrajectoryIcon` (16px, color-coded: green improving, red declining, amber stable, grey insufficient), `_AverageBadge` (compact tinted container with percentage or "—"). Item styling: `Material` with `surfaceContainerHighest` alpha 0.4, `borderRadius: 6`, padding `h:14 v:10`, bottom spacing 6px. (3) On tap → navigates to `StudentGradePage` via `MaterialPageRoute`, passing `schoolId`, `year`, `term`, `grade`, `streamCode`, `streamName`, `studentAdm`, `curriculumType`, `schoolContext`. (4) Empty state with `people_outline_rounded` icon. Uses `AutomaticKeepAliveClientMixin`. Re-builds stream on `didUpdateWidget` if key params change. Wired into `grade_detail_page.dart` replacing the Students placeholder via `_buildStudentsTab()` method. |
| `exams_tab.dart` | `ExamsTab` | ✅ Complete | Exam list with local in-memory filters for a specific stream within a grade. Uses `AcademicsDao(db).watchExamsForGradeStream(...)` reactive stream via `StreamBuilder`. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `streamName`, `curriculumType`, `schoolContext`. **SchoolConfig loading:** Loads `SchoolConfig` on `initState` via `db.select(db.settings)` + `SchoolConfig.fromJson(jsonDecode(row.data))` — needed to pass to `ExamCreationPage`. Tracks `_configLoaded` bool so FAB only appears once config is ready. **FAB:** `FloatingActionButton.small` (icon-only `Icons.add_rounded`, tooltip "New Exam") shown when `_canManage` (TeacherEntry/OwnerEntry/StaffEntry) and `_configLoaded`. Wrapped in `Scaffold(backgroundColor: Colors.transparent)`. Pushes `ExamCreationPage` via `Navigator.push(MaterialPageRoute(...))` with `preselectedGrade: widget.grade` and `preselectedStream: widget.streamCode` so the creation form opens with the current class already selected. **Filters:** Two horizontally scrollable chip rows using `_ExamFilterChip` (compact, `BorderRadius.circular(4)`, tinted when selected, animated container): (1) **Type filter** — All · Exam · Assignment · Assessment (single-select, maps to `ExamType` enum or null for All); (2) **Personalized filter** — All · Personalized · Standard (single-select, maps to `bool?`). Filters applied in-memory via `_applyFilters()`. **Exam Cards:** `ListView.builder` with header count row ("5 exams"). Each `_ExamRow` card: type badge (colored chip — primary for Exam, amber for Assignment, green for Assessment), **exam name as primary text** (13.5px w500, `exam.name`, maxLines 1 ellipsis), date range as secondary text (11px w400 muted, `cs.onSurfaceVariant` alpha 0.6, with calendar icon), optional "Personalized" outline chip, paper count badge ("{N}p"). Card style: colored accent bar left edge, `AnimatedContainer` with hover/press states tinted by type color, `borderRadius: kCardRadius`, padding 10. On tap → navigates to `ExamDetailPage` via `MaterialPageRoute`, passing `exam`, `schoolId`, `year`, `term`, `grade`, `streamCode`, `streamName`, `curriculumType`, `schoolContext`. **Empty states:** context-aware — "No exams for {streamName}" when globally empty (with thin `OutlinedButton.icon` "Create Exam" per §21 when `_canManage && _configLoaded`), "No {type} {personalized} exams found" when filters yield zero results, with appropriate icons (`quiz_outlined` at 40px vs `filter_list_off_rounded`). Uses `AutomaticKeepAliveClientMixin`. Re-builds stream on `didUpdateWidget` if key params change. Wired into `grade_detail_page.dart` replacing the Exams placeholder via `_buildExamsTab()` method. **ExamCreationPage pre-selection:** `ExamCreationPage` now accepts optional `preselectedGrade` (int?) and `preselectedStream` (int?) params. When provided, `initState` pre-populates `_gradeSelections` with the grade checked and the stream selected (or "all streams" if `preselectedStream` is null). |
| `subjects_tab.dart` | `SubjectsTab` | ✅ Complete | Subject-teacher grid with mastery comparison bars for a specific stream within a grade. Uses `AcademicsDao(db).watchSubjectsForGrade(...)` reactive stream via `StreamBuilder`. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `streamName`, `curriculumType` (CurriculumType), `schoolContext`. **UI:** (1) Header row with count badge ("8 subjects", muted 12px). (2) `ListView.builder` of card-based rows — each card: subject name (resolved via `subjectLabel(curriculumType, subject.subject)` from `curriculum_levels.dart`, w500, 14px), teacher row (`UserAvatar` radius 14 + teacher name, 13px w400), mastery section with two horizontal `_MasteryBar` widgets side by side: "Stream Avg" and "Grade Avg" — each is a thin 6px `LinearProgressIndicator` with label prefix (68px width), percentage text (right-aligned), and color-coded fill (green ≥70, amber ≥40, red <40). **Delta indicator:** When both stream and grade mastery are non-null, shows a compact delta row: arrow icon + "+X.X% vs grade" in green if stream > grade, "-X.X% vs grade" in red if stream < grade, "Same as grade average" if delta < 0.05. Shows "No mastery data" muted text if both averages are null. Card style: `surfaceContainerHighest` alpha 0.4, `borderRadius: 8`, padding 14, bottom spacing 8. (3) Empty state with `menu_book_outlined` icon: "No subjects assigned to {streamName}". Uses `AutomaticKeepAliveClientMixin`. Re-builds stream on `didUpdateWidget` if key params change. Wired into `grade_detail_page.dart` replacing the Subjects placeholder via `_buildSubjectsTab()` method. |
| `teachers_tab.dart` | `TeachersTab` | ✅ Complete | Class teachers (active + history) and subject-teacher assignments for a specific stream within a grade. Uses three independent `SubjectsDao` streams: `watchActiveClassTeacher(...)`, `watchClassTeacherHistory(...)`, `watchSubjectsForClass(...)`. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `streamName`, `curriculumType` (CurriculumType), `schoolContext`. **UI Layout (vertical scroll, ListView, 3 sections):** (1) **Active Class Teacher** — section header "Class Teacher" + `StreamBuilder` on `watchActiveClassTeacher`. If active exists: prominent card with primary-tinted border (1px, alpha 0.2), `UserAvatar` radius 22 + teacher name (w500, 14px) + "Class Teacher" subtitle (12px muted) + "Since {formatted date}" (11px, days-since-epoch → "12 Jan 2024"). If none: inline empty placeholder with `person_outline_rounded` icon + "No class teacher assigned". (2) **Past Class Teachers** — `StreamBuilder` on `watchClassTeacherHistory`, filtered to entries where `end IS NOT NULL`. Hidden entirely if no past entries. Header: "Previous Class Teachers" + animated chevron toggle (`AnimatedRotation`). `AnimatedCrossFade` to expand/collapse (default collapsed). Each past entry: compact card (`surfaceContainerHighest` alpha 0.25), `UserAvatar` radius 16 + name (13px, alpha 0.7) + date range "{start} — {end}" (11px muted). (3) **Subject Teachers** — section header "Subject Teachers" + count badge (tinted `borderRadius: 4` container with count). `StreamBuilder` on `watchSubjectsForClass`. Each subject-teacher card: subject name on left (resolved via `subjectLabel`, 13.5px w500) + teacher avatar (radius 14) + name on right (13px w400, max width 120px constrained). Card style: `surfaceContainerHighest` alpha 0.4, `borderRadius: 8`, padding h:14 v:12, bottom spacing 6px. Empty state: `menu_book_outlined` icon + "No subjects assigned to {streamName}". Uses `AutomaticKeepAliveClientMixin`. Re-builds streams on `didUpdateWidget` if key params change. Date helper: `_formatDaysDate` (days-since-epoch → "12 Jan 2024" with year). Wired into `grade_detail_page.dart` replacing the Teachers placeholder via `_buildTeachersTab()` method. |
| `attendance_tab.dart` | `AttendanceTab` | ✅ Complete | Attendance marking tab for a specific stream within a grade — the primary attendance surface for teachers/owners within the grade detail page. Uses `AttendanceDao(db).watchClassAttendance(...)` and `watchDailyAttendanceSummaries(...)` reactive streams via `StreamBuilder`. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `streamName`, `curriculumType`, `schoolContext`. **`didUpdateWidget` override (Bug Group 2 fix):** `_AttendanceTabState` now overrides `didUpdateWidget` to re-resolve `_canMark` when `streamCode`, `grade`, `year`, or `term` props change. On change: resets `_loadingCanMark = true` and `_canMark = false`, then re-runs `_resolveCanMark()`. This prevents stale permission state when `AutomaticKeepAliveClientMixin` keeps the state alive across stream tab switches (previously a teacher landing on stream A as non-class-teacher would retain `_canMark = false` even after switching to stream B where they ARE class teacher). **Diagnostic logging:** `_resolveCanMark()` now includes `debugPrint` logging for `OwnerEntry` (always true) and `TeacherEntry` (logs userId, grade, streamCode, and resolved canMark result). **UI Layout (vertical Column):** (1) **Date Selector Row** (`_DateSelectorRow`) — left/right arrow buttons (`_NavIconButton`) for day navigation, center tappable date label ("Mon, 12 Mar 2025" + "Today" subtitle when applicable), "Today" compact chip (`_CompactChip`) to jump back to today (hidden when already on today), history calendar toggle button (switches between list icon and calendar icon). Date picker via `showDatePicker` on date label tap. (2) **Weekly Mini Strip** (`_WeeklyMiniStrip`) — compact Mon–Sun row showing current week. Each day cell: abbreviated day name + day number + colored dot indicating marking status (green = fully marked, amber = partially marked, grey = no data). Tapping a day selects it. Selected day highlighted with primary tint. Today indicated with primary border. Future days disabled. Uses `watchDailyAttendanceSummaries` for dot color computation. (3) **Marking Body** (`_AttendanceMarkingBody`, stateful) — instant-save pattern (no draft mode). **Summary Strip** (`_SummaryStrip`): compact horizontal row of stat chips (Total, Present green, Absent red, Leave amber) + unmarked count badge (error container tint) when > 0. **Action Bar** (`_ActionBar`): "Mark All Present" button (`_ActionButton`, green tint) — calls `AttendanceDao.markClassAttendance` for all non-present students. Shows "Marking…" with hourglass during save. **Student List**: `ListView.builder` of `_StudentAttendanceTile` widgets — each tile: `AnimatedContainer` with status-tinted background (alpha 0.04), student name (13.5px w400) + "Adm: {adm}" (11px w300 muted), `_StatusToggleGroup` with 3 `_ToggleButton` widgets (P/A/L, 34×30, `borderRadius: 6`, filled active color with white text + shadow, transparent inactive). Each toggle immediately calls `AttendanceDao.markAttendance` (instant-save). (4) **History Calendar** (`_HistoryCalendar`, toggled via calendar icon) — monthly calendar view using `watchDailyAttendanceSummaries`. Month navigation (chevron buttons + month/year label). Day-of-week headers (Mon–Sun). Grid of `_CalendarDayCell` widgets with `_MarkingStatus` enum (fullyMarked → green, partiallyMarked → amber, notMarked → red muted, none → transparent). Today highlighted with primary border. Weekends and future days disabled. Tapping a day switches back to marking mode for that date. `_CalendarLegend` at bottom (Fully marked / Partial / Not marked). **Empty state**: `_EmptyState` with `people_outline_rounded` icon + "No students enrolled" + sub-text. Uses `AutomaticKeepAliveClientMixin`. Key difference from standalone `AttendanceScreen`: no class picker header (grade/stream provided by tab context), integrated history calendar toggle. Uses `cache.currentUser!.user.id` for `accountId`. Wired into `grade_detail_page.dart` replacing the Attendance placeholder via `_buildAttendanceTab()` method. |
| `timetable_tab.dart` | `TimetableTab` | ✅ Complete | Weekly timetable in two layout modes: **single-stream** (stream tab → Timetable content tab) and **all-streams cross-table** (All tab → Timetable sub-tab). Both share the same matrix visual design. **New optional param `streams` (List<GradeStream>?):** when provided and `streamCode` is null, renders the all-streams matrix; otherwise renders single-stream matrix. **Column descriptor model:** sealed class `_ColDesc` with two subtypes: `_TimeCol(start, end)` (a lesson slot) and `_BreakCol` (a gap between consecutive slots). `_buildColumns(entries)` produces the ordered list — inserts a `_BreakCol` wherever `maxEnd[start[i]] < start[i+1]`. **Single-stream matrix (`_SingleStreamMatrix`, desktop ≥ 600px):** Row axis = active days (Mon–Sun), Column axis = time slots. Header row shows "HH:MM – HH:MM" labels (10.5px w500, tabular figures) for `_TimeCol` entries and a muted "Break" pill for `_BreakCol` entries. Each day row: 72px right-aligned day label (11px w500) + one cell per column. `_SlotCell` (filled): subject name (11.5px w500) + teacher name (10px muted), 2.5px left accent border (deterministic 15-color palette), tinted bg, `minHeight: 52`. `_EmptyCell` (no entry at that slot): thin 1px border placeholder. `_BreakCell` (break period): muted `· · ·` filler, 50px wide. Wrapped in dual `SingleChildScrollView` (vertical outer, horizontal inner). **All-streams matrix (`_AllStreamsMatrix`, desktop ≥ 600px):** Same column structure as single-stream. Row axis = Day section header + one sub-row per stream. Day section: full-width muted strip (`_dayFullLabels` label, `borderRadius: 4`, `surfaceContainerHighest` tinted). Below the day strip: one `_buildStreamRow` per stream — 80px stream name label on the left, then `_SlotCell` / `_EmptyCell` / `_BreakCell` per column. Uses `_entryForStream(entries, streamCode, day, startTime)` for cell lookup. **Mobile single-stream (`_MobileDayPager`):** Day selector chips + day heading + `ListView.separated` of `_MobileSlotCard` widgets (time range 88px fixed + subject + teacher, left accent border 3px). Unchanged from prior implementation. **Mobile all-streams (`_MobileAllStreamsPager`):** Same day-chip selector + day heading. Below: `ListView.separated` with one section per stream — stream name label (12px w500 muted) + `_MobileSlotCard` per entry, or "No lessons" placeholder when stream has no slots that day. **Shared helpers:** `_fmtTime` (seconds → "HH:MM"), `_subjectColor` (15-color palette, `subject % 15`), `_activeDays` (Mon→Sun order, only days in data), `_uniqueStartTimes` (sorted unique starts), `_entryAt` (single-stream lookup by day+start), `_entryForStream` (all-streams lookup by streamCode+day+start), `_buildColumns` (produces `_ColDesc` list with break detection). **Width constants:** `_kDayLabelW = 72`, `_kStreamLabelW = 80`, `_kTimeColW = 110`, `_kBreakColW = 50`, `_kColGap = 3`. **Empty state:** `calendar_view_week_outlined` icon + "No timetable configured for {streamName}" (unchanged). **Data source:** `TimetableDao(db).watchClassTimetable(schoolId, year, term, grade, stream: streamCode)` — when `streamCode` is null, the DAO returns all entries for the grade across every stream (no change to DAO needed). Uses `AutomaticKeepAliveClientMixin`. Re-builds stream on `didUpdateWidget` if `schoolId`/`year`/`term`/`grade`/`streamCode` change. Wired into `grade_detail_page.dart` via `_buildTimetableTab()` (single stream) and `_buildAllTimetableTab()` (all streams, passes `streams: widget.grade.streams`). |
| `lessons_tab.dart` | `LessonsTab` | ✅ Complete | Lesson log for a specific stream within a grade. Uses `TimetableDao(db).watchClassTermLessons(...)` reactive stream via `StreamBuilder`. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `streamName`, `curriculumType` (CurriculumType), `schoolContext`. **UI Layout (vertical Column):** (1) **Summary Row** (`_SummaryRow`) — 3 compact `_StatChip` widgets: lesson count, distinct subjects count, distinct teachers count. Each chip: icon + value (12px w500) + label (11px muted), `surfaceContainerHighest` alpha 0.35 bg, `borderRadius: 4`, thin border. Stats are computed from the filtered list (react to subject filter). (2) **Subject Filter Chips** (`_SubjectFilterRow`) — horizontally scrollable `ListView` of `_FilterChip` widgets: "All" (null = no filter) + one chip per distinct subject in the data (sorted by code). Each chip: `AnimatedContainer`, `borderRadius: 4`, selected state uses primary tint bg + primary border + w500 text, unselected uses muted bg + muted border. Subject names resolved via `subjectLabel(curriculumType, code)` from `curriculum_levels.dart`. (3) **Grouped Lesson List** — `ListView.builder` with interleaved `_DateHeader` and `_LessonRow` items. Lessons grouped by `date` (days since epoch), sorted most recent first (DAO already orders by date desc). **`_DateHeader`:** formatted as "Monday, 12 March 2025" via `_formatDate(daysSinceEpoch)` — converts days-since-epoch to `DateTime` UTC and formats with full weekday + day + full month + year. 13px w500 muted text. **`_LessonRow`:** compact card with subject color indicator (3px vertical bar, deterministic 10-color palette indexed by `subject % 10`), subject name (13px w500) + teacher name (11.5px muted), `_TeacherAvatar` (28×28, `borderRadius: 4`, loads profile via `FileCache.get(FileCache.profilePath(userId))`, fallback initials). Card style: `surfaceContainerHighest` alpha 0.35, `borderRadius: 4`, thin border, bottom spacing 4px. **Empty states:** (a) No lessons at all: `menu_book_outlined` icon (52×52 container) + "No lessons recorded" + "No lessons recorded for {streamName}". (b) Filter yields zero results: `filter_list_off_outlined` icon + "No lessons for {subjectName}". Uses `AutomaticKeepAliveClientMixin`. Resets subject filter and re-builds stream on `didUpdateWidget` if key params change. Wired into `grade_detail_page.dart` replacing the Lessons placeholder via `_buildLessonsTab()` method. |

#### `academics/student_tabs/`
| File | Widget | Status | Description |
|---|---|---|---|
| `student_overview_tab.dart` | `StudentOverviewTab` | ✅ Complete | Overview tab for the Student Grade Page — "at a glance" view of a student's academic standing within a grade/stream context. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `studentAdm`, `curriculumType`. **UI Layout (vertical scroll, ListView):** (1) **Quick Stats Row** — 4 compact `_StatCard` widgets in a horizontal `SingleChildScrollView`: Subjects (count from `SubjectsDao.watchSubjectsForClass`), Exams Taken (distinct exam IDs from subject-level grade totals), Average Score (mean percentage across all subject-level grades, color-coded), Mastery (average mastery across all topics for current grade from `ExamsGradesDao.watchMasteryForStudent`, color-coded). Each card: 108px wide, icon + large value + label, `surfaceContainerHighest` alpha 0.4 bg, `borderRadius: 6`. (2) **Recent Exam Performance** — last 3 exams by created date. Groups grades by exam (prefers subject-level totals where `paper == null`). Each `_ExamRow`: subject summary (up to 3 names + "+N"), raw score / total, percentage badge (color-coded tinted container), `_ThinProgressBar` (3px). Empty state: "No exam results yet" with `assignment_outlined` icon. (3) **Subject Mastery Summary** — `_SubjectMasterySection` filters mastery to current grade, groups by subject, computes average score per subject. Each `_MasteryRow`: subject label (via `subjectLabel(curriculumType, index)`), 80px `_ThinProgressBar`, percentage text. Color-coded using mastery thresholds (≥80 green, ≥60 amber, ≥40 orange, <40 red). `Tooltip` message "See Mastery tab for details". Empty state: "No mastery data recorded yet". (4) **Attendance Summary** — `_AttendanceSummarySection` from `AttendanceDao.watchStudentAttendanceSummary`. Stacked proportion bar (6px, green/red/amber segments by flex), stats row with 3 `_AttendanceStat` widgets (dot + label + percentage) + total days count. Empty state: "No attendance records this term". **Data sources:** 4 independent streams (`_gradesStream`, `_masteryStream`, `_attendanceStream`, `_subjectsStream`) combined via nested `StreamBuilder` widgets. Uses `AutomaticKeepAliveClientMixin`. Re-builds streams on `didUpdateWidget` if key params change. Private widgets: `_SectionTitle`, `_StatCard`, `_ExamRow`, `_MasteryRow`, `_AttendanceSummarySection`, `_AttendanceStat`, `_ThinProgressBar`. Color helpers: `_percentColor` (≥70 green, ≥40 amber, <40 red), `_masteryColor` (≥80 green, ≥60 amber, ≥40 orange, <40 red). |
| `student_mastery_tab.dart` | `StudentMasteryTab` | ✅ Complete | Mastery tab for the Student Grade Page — shows topic-level mastery per subject with tappable subject cards. Constructor params: `schoolId`, `studentAdm`, `grade`, `curriculumType`. **Data source:** `ExamsGradesDao(db).watchMasteryForStudent(schoolId, studentAdm)` filtered to current grade, grouped by subject. **UI Layout:** (1) **Subject List** — `ListView.builder` with header count row ("{N} subjects"). Each `_SubjectCard`: subject name (via `subjectLabel(curriculumType, index)`, w500 13px) + percentage (color-coded), `_ThinProgressBar` (4px), topics count + chevron hint. Card style: `surfaceContainerHighest` alpha 0.4, `borderRadius: 6`, padding h:14 v:12, bottom spacing 6px. (2) **On tap** — responsive detail view: mobile (<600px) → `DraggableScrollableSheet` bottom sheet, desktop (≥600px) → `Dialog` (maxWidth 420, maxHeight 520). Both show: subject name header + overall mastery % badge (tinted container) + overall progress bar + topic count, then scrollable `_TopicRow` list. Each `_TopicRow`: topic index badge (28×28 tinted square), "Topic {N}" label, `_ThinProgressBar` (3px), percentage text. Sorted by topic index ascending. (3) **Empty state** — centered column: `psychology_outlined` icon (52×52 container), "No mastery data recorded yet" + sub-text. **Color coding (mastery):** ≥80% green `#4CAF50`, ≥60% amber `#FFC107`, ≥40% orange `#FF9800`, <40% red `#F44336`. Uses `AutomaticKeepAliveClientMixin`. Re-builds stream on `didUpdateWidget` if key params change. Private widgets: `_SubjectCard`, `_TopicDetailSheet`, `_TopicDetailDialog`, `_TopicRow`, `_ThinProgressBar`. Wired into `student_grade_page.dart` replacing Mastery placeholder. |
| `student_exams_tab.dart` | `StudentExamsTab` | ✅ Complete | Exams tab for the Student Grade Page — shows all exams the student has participated in with per-subject grades, paper breakdowns, and totals. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `studentAdm`, `curriculumType`. **Data source:** `ExamsGradesDao(db).watchStudentGrades(schoolId, studentAdm)` reactive stream, with exam metadata resolved via `ExamsGradesDao.getExam(examId)` cached in an in-memory map. **UI Layout:** `StreamBuilder<List<Grade>>` → groups grades by exam id → sorts exams by most recent grade created date descending → `FutureBuilder` resolves `Exam?` metadata for all exams → `ListView.separated` of `_ExamCard` widgets. **Each `_ExamCard`:** (1) **Header row** — exam type badge (colored chip: indigo Exam, teal Assignment, purple Assessment), scope chip ("Grade-wide" or "Stream"), date range ("12 Jan – 15 Jan" from `exam.start`/`exam.end` days-since-epoch). (2) **Subject grades** — separates subject-level totals (`paper == null`) from paper-level grades, sorted by subject index ascending. Each `_SubjectGradeRow`: subject name (via `subjectLabel`), score/total, percentage (color-coded), `_ThinProgressBar` (2.5px). If multiple papers exist for a subject, indented `_buildPaperRow` entries below (paper number, score/total, percentage — muted styling). (3) **Exam total row** — "Total" label + aggregated score/total + percentage badge (tinted container, color-coded). Card style: `surface` bg, `borderRadius: 6`, thin border. **Empty state:** centered column with `assignment_outlined` icon (52×52), "No exam results yet". Uses `AutomaticKeepAliveClientMixin`. Re-builds stream + clears exam cache on `didUpdateWidget` if key params change. Private widgets: `_ExamCard`, `_SubjectGradeRow`, `_ThinProgressBar`. Color helper: `_percentColor` (≥70 green, ≥40 amber, <40 red). Date helper: `_formatDaysDate` (days-since-epoch → "12 Jan"). Wired into `student_grade_page.dart` replacing Exams placeholder. |
| `student_attendance_tab.dart` | `StudentAttendanceTab` | ✅ Complete | Attendance tab for the Student Grade Page — shows the student's attendance record for the current term. Constructor params: `schoolId`, `year`, `term`, `grade`, `streamCode`, `studentAdm`. **Data sources:** Two independent streams: `AttendanceDao.watchStudentAttendanceHistory` (individual records ordered asc) and `AttendanceDao.watchStudentAttendanceSummary` (aggregate counts). **UI Layout (vertical scroll, ListView):** (1) **Summary Bar** (`_SummaryBar`) — container with stacked proportion bar (6px, green/red/amber segments by flex) + stats row: 3 `_SummaryStat` widgets (square dot + label + "N (X%)" count) for Present (green `#4CAF50`), Absent (red `#F44336`), Leave (amber `#FFA726`) + total days count right-aligned. Card style: `surfaceContainerHighest` alpha 0.4, `borderRadius: 6`, thin border. (2) **Month Calendar Heatmaps** (`_MonthCalendar`) — one per month in reverse chronological order. Each: month/year title, Mon–Sun day-of-week header (9.5px), grid of `_DayCell` widgets (24×24, `borderRadius: 3`). Cells color-coded by status: green bg Present, red bg Absent, amber bg Leave (alpha 0.15 light / 0.25 dark), muted text for weekends and unmarked days. Offset calculated from Monday-based weekday. (3) **"All Records" section header** — muted 12px w500 text. (4) **Month-Grouped Detail List** — `_MonthHeader` (full month name + year + count badge) followed by `_RecordRow` items in reverse chronological order within each month group. Each row: formatted date ("Mon, 12 Mar") + status chip (tinted container with square dot + label, `borderRadius: 3`). Row style: `surfaceContainerHighest` alpha 0.35, `borderRadius: 4`, thin border. **Month grouping:** `_groupByMonth` converts days-since-epoch to `DateTime`, groups by `(year, month)` record, sorts groups reverse chronologically. **Empty state:** centered column with `calendar_today_outlined` icon (52×52 container), "No attendance records this term" + sub-text. Uses `AutomaticKeepAliveClientMixin`. Re-builds streams on `didUpdateWidget` if key params change. Private widgets: `_SummaryBar`, `_SummaryStat`, `_MonthCalendar`, `_DayCell`, `_MonthHeader`, `_RecordRow`. Wired into `student_grade_page.dart` replacing Attendance placeholder; unused `_buildPlaceholder` method removed from `StudentGradePage`. |

**Data source:** `SettingsDao.watchSettings(schoolId)` → parses `settings.data` JSON → `SchoolConfig` → iterates `CurriculumConfig.grades`.
**Dependencies:** `models/school_config.dart` (`SchoolConfig`, `GradeConfig`, `GradeStream`, `kCbcGradeLabels`, `kEightFourFourGradeLabels`), `database/daos/settings_dao.dart`, `database/daos/academics_dao.dart`, `database/daos/members_dao.dart` (`MembersDao.watchStudent`), `database/daos/exams_grades_dao.dart` (`ExamsGradesDao`, `ExamWithPapers`, `PaperAnalytics`, `GradeRow`, `watchStudentGrades`, `watchGradesForExam`, `watchPapersForExam`, `computeClassAnalytics`, `getEnrolledStudents`, `watchClassGrades`), `database/daos/attendance_dao.dart` (`AttendanceDao`, `StudentAttendanceRecord`, `watchStudentAttendanceHistory`, `watchStudentAttendanceSummary`), `database/tables/enums.dart` (`ExamType`, `PaperStatus`, `StudentStatus`, `AttendanceStatus`), `database/tables/curriculum_subjects.dart` (`CurriculumType`), `models/grade_analytics.dart` (`StreamStats`, `Trajectory`, `SubjectTeacherEntry`), `models/curriculum_levels.dart` (`subjectLabel`), `models/active_term_context.dart`, `ui/widgets/active_term_provider.dart`, `ui/widgets/edu_tab_bar.dart`, `ui/widgets/user_avatar.dart`, `ui/theme/app_theme.dart`, `cache/file_cache.dart`

---

### `announcements/`
| File | Widget | Status | Description |
|---|---|---|---|
| `announcements_screen.dart` | `AnnouncementsScreen` | ✅ Complete | Chronological feed of school announcements. Owners/Staff can create announcements targeted by role or class. Feed filters by `SchoolContext.currentEntry` role for relevance. Compose FAB simplified to icon-only `FloatingActionButton.small` matching Roles page pattern; `_ElevatedFab` class removed. `_RoleFeed` and `_AdminFeed` now load real `SchoolConfig` reactively from `CatalogDao.watchAllStreamsForSchool` via shared `_buildConfigFromStreams()` helper (same pattern as attendance/exams/timetable screens) instead of using `SchoolConfig.defaults()`. |

**Data source:** `AnnouncementsDao.watchAnnouncements(schoolId, ...)`, `CatalogDao.watchAllStreamsForSchool(schoolId)`
**Dependencies:** `database/daos/announcements_dao.dart`, `database/daos/catalog_dao.dart`, `database/daos/enrollments_dao.dart`, `database/tables/curriculum_subjects.dart`, `models/school_config.dart`, `models/school_context.dart`

---

### `attendance/` — **DELETED (Task G3)**
> The standalone Attendance page was removed in Task G3. Attendance marking for Owner/Teacher/Staff is accessed via Academics → Grade Detail Page → Attendance tab (`academics/tabs/attendance_tab.dart`). Attendance history for Guardian/Student is accessed via the Progress page → Attendance tab. The `attendance/` directory and `attendance_screen.dart` no longer exist.

---

### `my_classes/` — **DELETED (Task G7)**
> The My Classes page was removed in Task G7. It was redundant because the teacher's Overview page already displays class teacher assignments and subjects taught. The `my_classes/` directory and `my_classes_screen.dart` no longer exist. The `'My Classes'` nav item was removed from the teacher's core items in `_itemsForRole()`, from `_kAcademicNavLabels`, and from `_buildContentPanel()`. The import for `my_classes_screen.dart` was also removed.

---

### `exams/`
| File | Widget | Status | Description |
|---|---|---|---|
| `exams_grades_screen.dart` | `ExamsGradesScreen` | ✅ Complete | Hierarchical exam management: Term → ExamGroup → Grade/Stream → Paper → Grades/Mastery. Data-dense tables for desktop grading, list views for mobile. Analytics header with donut charts for class averages. **Exams list view** (`_ExamsListView`) uses `ExamGroup` model. Data source: `ExamsGradesDao.watchExamGroups()` → `Stream<List<ExamGroup>>`. `onExamTap` callback type: `ValueChanged<ExamGroup>`. `_ExamGroupCard` shows: exam type colored badge, personalized indicator, date range, participating grades as `_ClassChip` chips, subject count + grade count as `_MetaBadge` row. **`_ExamsShellState` (updated Task EX8):** `_selectedGroupKey: String?` tracks the group key for reactive lookup. `_selectedGroup: ExamGroup?` holds the latest snapshot (updated by stream). `_dao: ExamsGradesDao` added to shell for the reactive stream. `_ExamsView.examDetail` is wrapped in `StreamBuilder<List<ExamGroup>>` on `_dao.watchExamGroups()` — finds the matching group by `groupKey` on each emission, keeping the detail view reactive when grades are added, date ranges extended, etc. If the group disappears (deleted), automatically pops back to the list via `addPostFrameCallback`. `_openExam(ExamGroup)` sets both `_selectedGroup` and `_selectedGroupKey`. `_popToList()` clears both. `_openPaper(Paper, Exam)` captures the specific exam row for paper detail navigation. `_ExamsView.paperDetail` constructs `ExamWithPapers` bridge record from `_selectedExamRow` + `_selectedGroup.teacher` for `_PaperDetailView`. **`_ExamGroupDetailView` (updated Task EX-01):** Accepts new `onDeleted` callback. Uses `TickerProviderStateMixin` for two `TabController`s. **Header:** custom `Row` layout with back button + exam type label + date range subtitle + **delete `IconButton`** (error-colored `Icons.delete_outline_rounded`, only shown when `_canManage`). Delete triggers `_confirmDeleteGroup()` which shows `_ConfirmDeleteDialog`, then loops through `group.examIds` calling `_dao.deleteExam()` for each, then calls `widget.onDeleted()` to pop back to list. **Responsive paper area (Task EX-01):** Below the header, an `Expanded > LayoutBuilder` drives the layout split: **Desktop (≥600px):** Grade tabs and stream sub-tabs are entirely replaced by `_ExamGroupCrossTable` — a unified cross-table matrix where rows = each grade+stream combination present in `group.grades[].streams[]` and columns = unique exam dates. All paper data is read directly from `widget.group` (reactive via parent `StreamBuilder<List<ExamGroup>>`; no extra DAO calls). Multiple papers on the same date are stacked vertically within one cell. **Mobile (<600px):** Grade tabs (`_gradeTabController`) + stream sub-tabs (`_streamTabController`, shown only when >1 stream entries) + `_PaperContentArea` (which itself switches between `_PaperTimetableGrid` ≥600px and `_PaperTimetableMobile` <600px via inner `LayoutBuilder`) are retained exactly as before. **Grade tabs:** scrollable `EduTabBar` with one tab per `group.grades` entry (grade labels from `SchoolConfig`). Right side: `+` `IconButton` to add a grade. **Stream sub-tabs:** reactive from `watchStreamsWithPapersForGrade`. **FAB:** `_ExpandableFab` (expandable FAB with 3 option pills: Paper, Grade, Stream). Main FAB button has `heroTag: 'fab_exam_detail_main'`. Only shown when `_canManage`. **State:** `_gradeTabController`, `_selectedGradeIndex`, `_streamTabController`, `_selectedStreamIndex`. Grade tab changes reset stream index to 0 and rebuild stream tabs. `didUpdateWidget` reinitializes tabs when group changes. **Paper timetable helpers (Task EX5):** `_groupPapersByDate(List<Paper>)` groups papers by day component of start timestamp into `Map<DateTime, List<Paper>>`, sorted by start within each day. `_sortedPaperDates(Map)` returns sorted unique dates. `_uniquePaperStartTimes(List<Paper>)` returns sorted unique HH:MM start-time labels. `_paperAt(Map, DateTime, String)` finds paper at a given date + start-time label. `_fmtDayHeader(DateTime)` formats "Mon, 12 Jan". `_fmtDayColumn(DateTime)` formats "Mon 12" for column headers. **`_ExamGroupCrossTable` (Task EX-01, desktop ≥600px):** Unified cross-table driven by `widget.group`. Builds a `List<_CrossRow>` (anonymous record: `grade`, `streamCode`, `label`, `exam`, `papers`) from `group.grades[].streams[]`. Columns = unique dates from `_groupPapersByDate` across all papers. Each data row uses `IntrinsicHeight` so all cells in a row match the tallest. Status legend (`_PaperStatusLegend`) shown above matrix. Tapping any cell calls `onPaperTap(paper, exam, grade)` → navigates to `PaperDetailPage`. **`_CrossTableHeaderRow`:** date label row (corner spacer 140px + one 152px `SizedBox` per date with `_fmtDayHeader` text). **`_CrossTableRowLabel`:** left-edge label cell (140px) — grade+stream name in `surfaceContainerHighest` tinted container with thin outline border, `borderRadius: kChipRadius`. Empty cells use `_PaperEmptyCell`. Filled cells reuse `_PaperSlotBox` (status-colored left border 2.5px + tinted background, subject name + paper number, time range). **`_PaperTimetableGrid` (Task EX5, used inside `_PaperContentArea` on mobile path ≥600px):** `SingleChildScrollView` grid. X-axis = dates, Y-axis = unique start times. Uses `_PaperGridRow` (72px time cell + expanded day cells), `_PaperHeaderCell`, `_PaperTimeLabel`, `_PaperSlotBox`, `_PaperEmptyCell`. **`_PaperTimetableMobile` (Task EX5, <600px):** `StatefulWidget` with horizontal `_PaperDayChip` selector at top, day heading below, and `ListView.separated` of `_PaperSlotCard` cards for the selected day. State: `_grouped`, `_dates`, `_selectedDayIndex`. `didUpdateWidget` rebuilds groups and clamps index. **`_PaperSlotCard` (retained from Task EX3):** status-colored left border (2.5px) + tinted background via `_paperStatusColor()`, subject name + paper number, time range, `_StatusChip`, chevron. Used by mobile view. **`_PaperDayChip` (Task EX5):** compact day selector chip — `AnimatedContainer`, sharp `BorderRadius.circular(4)`, primary-tinted when selected. **`_EmptyPapersTimetableState`:** centered empty state ("No papers scheduled"). **`_paperStatusColor(PaperStatus, ColorScheme)`:** top-level helper — pending=muted grey, progress=blue (#42A5F5), done=amber (#FFA726), marked=green (#66BB6A). **New widgets (Task EX-01):** `_ExamGroupCrossTable`, `_CrossTableHeaderRow`, `_CrossTableRowLabel`. **Deleted widgets (cumulative):** `_ExamDetailView`, `_ExamDetailViewState`, `_ExamSummaryCard`, `_InfoRow`, `_PaperCard`, `_EmptyPapersState`, `_ClassPerformanceSection`, `_ClassPerformanceSectionState`, `_StudentRankRow`, `_PaperListContent`, `_SubLabel`, `_IconActionButton`, `_SegmentedRow`, `_DatePickerField`, `_FieldLabel`, `_selectedGradeConfig` getter (unused), `_sameDay` static helper (unused), trivial `dispose()` override (unnecessary). **Retained:** `_PaperDetailView` (grades/scoring unchanged), `_PaperStatusRow`, `_StatusAdvanceButton` (used by `_PaperDetailView`), `_AnalyticsHeader`, `_DonutChart`, `_StatRow`, `_DistributionChart`, `_GradeSpreadsheet`, `_GradeList`, `_MobileGradeEntrySheet`, `_CreatePaperSheet` (for adding papers), `_ConfirmDeleteDialog` (used by delete exam group). **Imports:** `ui/widgets/edu_tab_bar.dart`, `models/exam_group.dart`. File compiles cleanly: 0 errors, 0 warnings. |
| `exam_creation_page.dart` | `ExamCreationPage` | ✅ Complete | Full-screen multi-grade exam creation form pushed via `Navigator.push`. Uses `TickerProviderStateMixin` (multiple tab controllers). Four sections: (1) **Exam Details** — type selector as slim inline chips (`_buildTypeSelector`), **inline date range picker** (`_buildDateRange`) with collapsible calendar (`_ExamRangeCalendar` using `AnimatedSize` instead of old `AnimatedCrossFade`; calendar month transitions use `AnimationController` with fade via `AnimatedBuilder`; sub-widgets renamed from `_Exam*` to `_Calendar*` prefix: `_CalendarMonthHeader`, `_CalendarChevronBtn`, `_CalendarWeekdayRow`, `_CalendarDayGrid`, `_CalendarOverflowCell`, `_CalendarDayCell`, `_BandPos`), personalized toggle. (2) **Grade Tabs** — scrollable `EduTabBar` with **only real grade tabs** (no synthetic "All" tab — removed in Task E1). Pre-selection from `preselectedGrade`/`preselectedStream`. `_hasSelection` simplified to `_gradeTabs.isNotEmpty`. (3) **Stream Sub-Tabs** — **two-level tab hierarchy: Grade → Stream** (redesigned in Task E2). Each grade with streams gets its own `TabController` stored in `Map<int, TabController> _streamTabControllers` and `Map<int, int> _activeStreamTabIndex`. Tab 0 = "All Streams" (shows all streams, one entry per stream), tabs 1..N = individual streams. `_buildStreamSection` returns an `EduTabBar` (scrollable, `padding: EdgeInsets.zero`) with "All Streams" + per-stream tabs. Pre-selection from `preselectedStream` sets `ctrl.index = streamIdx + 1`. **Removed**: `_StreamChip` widget class, `_streamSelections` map, `_toggleStreamChip` method, `_buildStreamChips` method, `_loadSubjectsForGrade` method — all replaced by `_onStreamTabChanged(int grade)` which loads subjects for the active stream (or all streams when "All Streams" is active). `_onGradeTabChanged` delegates to `_onStreamTabChanged`. **`_selectedClasses` redesigned (Tasks E1+E2):** uses `_activeStreamTabIndex[grade]` instead of `_streamSelections`. When index == 0 ("All Streams"), returns all streams individually (one entry per stream). When index > 0, returns only the specific stream. When a grade has no streams, returns `(grade, null)`. All stream tab controllers are disposed in `dispose()`. (4) **Paper Timetable** — dynamic timetable-style scheduler with **per-stream timetable rendering** (Task E3). `_buildTimetableSections` rewritten to be stream-tab-aware: when a specific stream tab is active (index > 0), shows only that stream's timetable with no header; when "All Streams" tab is active (index == 0), shows one timetable section per stream with stream name as header label; when a grade has no streams, shows a single headerless timetable. No longer iterates `_selectedClasses` — instead reads `_activeStreamTabIndex` directly. Per-stream paper configuration — each stream has independent `_PaperSlot` lists keyed by `"$grade:$streamCode"`. **"Copy to all" feature** (Task E4): when viewing a specific stream's timetable (stream tab index > 0) and the stream has at least one paper slot, a "Copy to all" `_MiniTextButton` (icon: `Icons.copy_all`) appears in the header actions row. `_copyPapersToAllStreams(int grade, int sourceStreamCode)` copies the source stream's paper slots to all other streams of the same grade — each target stream gets independent copies (new `_PaperSlot.id` values). Subject codes are copied as-is, but invigilators are remapped to the teacher assigned to that subject in the target stream (looked up from `_classSubjects[targetKey]`). Slots for subjects that don't exist in the target stream are skipped. Slots without a subject are copied without remapping. `_isSpecificStreamActive(int grade)` helper returns `true` when the grade has multiple streams and a specific stream tab (index > 0) is active. The copy uses whatever subjects are already loaded — the user can navigate to each target stream to trigger loading and verify. **`_loadSubjectsForClass` simplified** (Task E3): removed union-of-streams fallback (old `else` branch that iterated all streams when `stream == null`). Now always loads per individual stream via `SubjectsDao.getSubjectsForClass()`; for grades without streams, returns empty list. **Removed helpers** (Task E3): `_gradeLabelText` and `_streamLabelText` — dead code after `_buildTimetableSections` rewrite (grade is clear from grade tab, stream name comes directly from `stream.name`). **New data model**: `_PaperSlot` (id, date, startTime, endTime, subjectCode, invigilatorId, computed `duration` getter) replaces old `_PaperSchedule`. State: `Map<String, List<_PaperSlot>> _paperSlots` keyed by `"$grade:$stream"`. Teachers loaded eagerly via `MembersDao.watchTeachers()` + `findUserById()` join into `List<({TeachersData teacher, UsersData user})> _teachers`. **UI structure**: `_buildTimetableForClass` renders header row with `_MiniTextButton` actions ("Auto-fill" when empty, "Clear" when populated, "Copy to all" when specific stream active and slots non-empty), then `_EmptyTimetableHint` or day-grouped slot cards. Slots grouped by day via `_DayColumn` widget (day header with dot + day name + date + paper count badge, then `_PaperSlotCard` per slot). **`_PaperSlotCard`**: time range row with two `_InlineTimePicker` chips + duration badge (`Xmin` in indigo, or "invalid" in error color) + remove button; subject selector (`_SubjectDropdown` — tappable row opening `showModalBottomSheet` with `_PickerOption` list, auto-sets invigilator from subject's teacher on selection); invigilator selector (`_InvigilatorDropdown` — tappable row opening `_InvigilatorPickerSheet` with search field + `_PickerOption` list). **`_InlineTimePicker`**: tappable chip that opens `_TimePickerSheet` bottom sheet with modern wheel-style pickers (`_WheelColumn` using `ListWheelScrollView.useDelegate`, hour 0–23 + minute in 5-min increments), quick-tap preset chips (`_TimePresetChip` for 8:00–16:00), "Done" button returns `TimeOfDay`. All use `FontFeature.tabularFigures()` for monospaced digits. **`_AddPaperRow`**: horizontally scrollable day chips at bottom of each class section — tap to add a new paper to that day. Each chip shows day name + date + existing paper count badge + "+" icon. New slots auto-chain start time from previous slot's end time and auto-assign first unassigned subject. **`_autoPopulateSlots`**: fills slots for all subjects across exam days (3 per day max, 2hr each starting at 08:00). **Date range changes clear all paper slots** (prevents orphaned slots outside range). **Save logic**: iterates `_selectedClasses` (which returns all streams when "All Streams" active), looks up per-stream `_paperSlots[key]`, skips slots with `subjectCode == null`, builds `PapersCompanion` with `invigilator` from `slot.invigilatorId ?? teacherId`. **Footer**: thin divider + Cancel text + `_ExamConfirmButton` (brand indigo, shadow, loading spinner). **Deleted old widgets**: `_PaperSchedule`, `_PaperTimetableRow`, `_TimetableDateChip`, `_TimetableTimeChip`, `_paperSchedules` map, `_StreamChip`. **Added new widgets**: `_PaperSlot`, `_MiniTextButton`, `_EmptyTimetableHint`, `_DayColumn`, `_PaperSlotCard`, `_InlineTimePicker`, `_TimePickerSheet`, `_WheelColumn`, `_TimePresetChip`, `_SubjectDropdown`, `_InvigilatorDropdown`, `_InvigilatorPickerSheet`, `_PickerOption`, `_AddPaperRow`. **Added imports**: `members_dao.dart`. **Removed imports**: `flutter/services.dart` (unnecessary). Top-level helpers: `_sameDay`, `_generateId`, `_kMonths`, `_kDayNames`, `_fmtDate`, `_fmtDateFull`, `_fmtTime`, `_kFullMonthNames`. Follows §21 UI guidelines. |

**Data source:** `ExamsGradesDao.watchExamGroups(...)`, `ExamsGradesDao.watchPapersForExam(...)`, `ExamsGradesDao.watchGradesForPaper(...)`, `ExamsGradesDao.computeClassAnalytics(...)`, `ExamsGradesDao.getEnrolledStudents(...)`, `ExamsGradesDao.createExamBatch(...)`, `SubjectsDao.getSubjectsForTerm(...)`, `MembersDao.watchTeachers(...)`, `MembersDao.findUserById(...)`
**Dependencies:** `database/daos/exams_grades_dao.dart`, `database/daos/subjects_dao.dart`, `database/daos/members_dao.dart`, `models/active_term_context.dart`, `models/school_context.dart`, `models/school_config.dart`, `models/curriculum_levels.dart`, `models/membership.dart`, `models/exam_group.dart`, `ui/widgets/animated_save_button.dart`, `ui/widgets/edu_tab_bar.dart`

---

### `finance/`
| File | Widget | Status | Description |
|---|---|---|---|
| `finance_screen.dart` | `FinanceScreen` | ✅ Complete | Financial management: fee structures, invoicing, payments, discounts. Overview cards with totals. Owner/Staff can define fees, generate invoices, record payments. Guardian/Student gets fully read-only view of invoices/payments/balances per ward (or self). **Task A03:** `_GuardianFinanceView` is now purely read-only — the "Pay" `FloatingActionButton.extended` and `_GuardianPaymentSheet` (which called `FinanceDao.recordPayment()` with `SyncAction.createPayment`) have been removed entirely. `bottomPadding` is unconditionally `0.0`. The `_showGuardianPaymentSheet` function is also deleted. Guardians and students cannot create, edit, or delete any financial records. **Task A05:** `StudentEntry` now explicitly matched in the `FinanceScreen.build()` switch — reuses `_GuardianFinanceView` with the student's own ADM and name for a read-only finance view. The `_ =>` wildcard fallback (which incorrectly routed students to `_OwnerFinanceShell`) has been removed; the switch is now exhaustive over all 5 `MembershipEntry` subtypes. "New Fee" FAB simplified to icon-only `FloatingActionButton.small` matching Roles page pattern; `_ElevatedFab` class removed. Fee creation modal (`_CreateFeeSheet`) uses inline error text (`_gradeError` state variable) for grade selection validation instead of `SnackBar` — error clears on grade chip tap; catch block uses `clearSnackBars()` before showing error to prevent stacking. |

**Data source:** `FinanceDao.watchFees(...)`, `FinanceDao.watchInvoices(...)`, `FinanceDao.watchPayments(...)`, `FinanceDao.watchDiscounts(...)`
**Dependencies:** `database/daos/finance_dao.dart`, `models/active_term_context.dart`, `models/school_context.dart`

---

### `members/`
| File | Widget | Status | Description |
|---|---|---|---|
| `members_page.dart` | `MembersPage` | ✅ Complete | Unified members page with 6 tabs (Departments, Owners, Teachers, Staff, Students, Guardians) using `EduTabBar`. Context-aware FAB adapts action per active tab (standardised to `FloatingActionButton.small` matching Roles page). Each tab is a reactive list from the corresponding Drift table joined with `users`. **Task U05 — Data-table migration:** All 6 member tabs converted from `_BaseTile` card-based rows (Material + surfaceContainerHighest alpha background + borderRadius: 6) to flat data-table-style rows separated by `AppTheme.tableRowDivider`. `_MemberListView` replaced by `_FlatMemberList` (`ListView.separated` with `AppTheme.tableRowDivider`). `_BaseTile` and `_MemberTile` replaced by `_UserDataRow` (StatefulWidget, for user-based tabs: Owners/Teachers/Staff/Guardians) and `_FlatRow` (StatefulWidget, for student rows). Both implement `MouseRegion` + `AnimatedContainer` hover (`cs.primary alpha 0.04`, 100ms) + `NoSplash` `InkWell` — matching the `EduDataTable` pattern. Avatar radius reduced from 20 → 16 on all tabs. Desktop: `_InlineActions` — per-row 28×28 icon buttons that fade in on row hover (`_InlineActionButton` with `AnimatedOpacity`). Mobile: `_MobileActions` — `Icons.more_vert` (18px) opens a bottom sheet with action rows (same pattern as `EduDataTable._MobileMenuButton`). **Departments tab** migrated to `EduDataTable<Department>` directly (synchronous data), with a 32×32 domain icon badge + name + description. Desktop actions: View (`Icons.open_in_new_rounded`) + Delete (`Icons.delete_outline_rounded`, destructive). **Owners tab** actions: View + Remove (`Icons.person_remove_outlined`, destructive) — `_OwnerRow` gains `_confirmRemoveOwner()` calling `MemberManagementService.removeOwner()`. **Teachers tab** actions: View + Edit (both open `_TeacherInfoSheet`) + Remove — `_TeacherRow` gains `_confirmRemove()`. **Staff tab** actions: View + Edit + Remove — `_StaffRow` gains `_confirmRemove()`. **Students tab** actions: View + Edit (both open `StudentDetailSheet`) + Delete (calls `MemberManagementService.changeStudentStatus(StudentStatus.deleted)`) — `_StudentRow` gains `_confirmDelete()`. **Guardians tab** actions: View only — tap still opens `_GuardianWardsSheet`. All confirmation dialogs use `AppTheme.kCardRadius` and match §21 typography. `_RowAction` class added as a lightweight action descriptor (icon, label, onTap, isDestructive). `models/result.dart` imported for `Err<void, MemberActionError>` pattern matching. Departments tab includes full department management: list view, detail screen with 3 left-aligned scrollable tabs (**All**, **Teachers**, **Staff**), create/delete actions. The "All" tab (`_DeptAllMemberList`) shows a combined list of teachers and staff with role-tag chips ("Teacher"/"Staff") and remove buttons. Per-tab inline assign buttons have been removed; a single `FloatingActionButton.small` opens a searchable assign modal (`_AssignMemberSearchSheet`) with a text search field (name/phone), Teachers/Staff filter toggle chips (`_DeptFilterChip`), and tap-to-assign items. **Owner detail** is a responsive modal (`_OwnerInfoSheet`) — bottom sheet on mobile (< 600 px) and a slide-in side sheet on desktop (≥ 600 px); "Remove from school" action uses `MemberManagementService.removeOwner()` with self-removal guard (Task A4) — consistent with `_OwnerRow._confirmRemoveOwner`; `Result<void, MemberActionError>` pattern matching for error handling. **Teacher detail** is a responsive modal (`_TeacherInfoSheet`, now a `StatefulWidget`) — bottom sheet on mobile (< 600 px) and a slide-in side sheet on desktop (≥ 600 px), matching the owner pattern. Displays avatar, name, phone, email, status dot+label, detail rows (role, department, hired date), **two collapsible assignment sections** (Class Teacher Assignments and Subject Assignments), and a compact `_ActionIconGroup` row of icon buttons: Edit (opens edit bottom sheet with role/department/hired date fields), Resign, Restore, Transfer, Fired, Retired (each directly sets `TeacherStatus` and closes sheet), and Remove (confirmation dialog). The **Class Teacher Assignments** section uses `StreamBuilder` on `MembersDao.watchClassTeacherAssignments()` and displays each assignment as a compact `_AssignmentChip` showing "Grade X · SY (Year TZ)". The **Subject Assignments** section uses `StreamBuilder` on `MembersDao.watchTeacherSubjects()`, groups subjects by `(year, term, grade, stream)`, and displays each group as a header with subject name chips below. Both sections are collapsible via `_buildCollapsibleSection` (tap-to-expand with `AnimatedCrossFade` + `AnimatedRotation` chevron). Subject names are resolved from the school's curriculum type loaded via `SettingsDao.getSettings()` → `SchoolConfig` → `subjectLabel()`, and grade labels via `gradeLabelsFor()`. Assignment creation UI is **deferred** to a follow-up task — currently read-only display only. The old full-page `_TeacherDetailPage`/`_TeacherDetailPageState` and the `_showStatusSheet` bottom sheet with choice chips have been removed. `_ActionIconGroup` and `_ActionIconItem` are new shared widgets — a `Wrap` of small 36×36 bordered icon buttons inside a tinted container, reusable by other detail sheets. `_AssignmentChip` is a new shared compact chip widget for displaying assignment labels. **Staff detail** is a responsive modal (`_StaffInfoSheet`, `StatelessWidget`) — bottom sheet on mobile (< 600 px) and a slide-in side sheet on desktop (≥ 600 px), matching the teacher/owner pattern. Displays avatar, name, phone, email, status dot+label, detail rows (role, department, ID number), and a compact `_ActionIconGroup` row of icon buttons: Edit (opens edit bottom sheet with role/department/ID number fields), Resign, Restore, Transfer, Fired, Retired (each directly sets `StaffStatus` and closes sheet), and Remove (confirmation dialog). The old full-page `_StaffDetailPage`/`_StaffDetailPageState` and the separate `_showStatusSheet` bottom sheet with choice chips have been removed. Both teacher and staff detail sheets use `MemberManagementService` for all mutations. **Student detail** is now a responsive modal (`StudentDetailSheet`) — `_StudentRow` opens a `DraggableScrollableSheet` bottom sheet on mobile (< 600 px) and a slide-in side sheet (420 px wide) on desktop (≥ 600 px), matching the teacher/staff/owner pattern. `_StudentDetailStub` has been removed. **Guardian creation** uses a searchable student picker (`_StudentPickerSheet`, StatefulWidget) — supports search-as-you-type by name or admission number via `MembersDao.searchStudents()`, replacing the old pre-fetched non-searchable list. **Guardians tab** now shows **unique guardians** — one row per distinct user via `MembersDao.watchUniqueGuardians(schoolId)` — with ward count displayed in the subtitle (e.g. "2 wards"). `_UniqueGuardianRow` replaces the old `_GuardianRow`. **Guardian detail** is a responsive modal (`_GuardianWardsSheet`) — bottom sheet on mobile (< 600 px) and a slide-in side sheet on desktop (≥ 600 px) — showing guardian header (avatar, name, phone, email, status indicator) and a reactive ward list via `MembersDao.watchGuardianWards(schoolId, userId)`. Each ward (`_WardItem`) displays student name, relationship · role subtitle, and inline edit (relationship/role dropdown sheet) and unlink (confirmation dialog) icon buttons. The old `_GuardianRow`, `_GuardianResolved`, `_GuardianDetailPage`, and `_GuardianDetailPageState` have been removed. Uses `MemberManagementService` for mutations. `_MemberDetailStub` has been removed (Task 13). |
| `student_detail_page.dart` | `StudentDetailSheet`, `StudentDetailPage` | ✅ Complete | **`StudentDetailSheet`** is the primary widget — a responsive modal that works as bottom-sheet (with `DraggableScrollableSheet` + drag handle), side-sheet (slide-in from right, 420 px wide, left-rounded corners), or embedded (inside a `Scaffold` via `StudentDetailPage` thin wrapper for backward-compatible push-route usage). Features a **compact horizontal header** row: smaller avatar (radius 28), name + "ADM: X" in a column, `_StatusChip`, and a `Row` of `_ActionIcon` icon buttons (Edit, Change Status — conditional on active status, Delete with error-tinted color) replacing the old `PopupMenuButton` — all in a single `Row`. The `_onMenuAction` string-switch method has been removed; each `_ActionIcon.onTap` calls `_showEditSheet`, `_showStatusSheet`, or `_confirmDelete` directly. `_ActionIcon` is a new private widget: `Tooltip` → `Material` → `InkWell` → `Icon` (size 18, padding 7, borderRadius 6). **Left-aligned scrollable tabs** via `EduTabBar(isScrollable: true, padding: EdgeInsets.only(left: 16, ...))` with 4 tabs: **Info**, **Performance**, **Guardians**, **Subscriptions** (renamed from "Plans"). Tab content is unchanged: Info (gender, DOB, admitted date, status, linked user details, documents), Performance (grades by exam + mastery by subject), Guardians (reactive list + add guardian button), Subscriptions (reactive subscription list + subscribe button). `StudentDetailPage` is now a thin `StatelessWidget` wrapper that embeds `StudentDetailSheet` inside a `Scaffold`. Uses `MemberManagementService` for student mutations and `PlansDao` for subscription operations. |

**Data source:** `MembersDao.watchOwners(schoolId)`, `watchTeachers(...)`, `watchStaff(...)`, `watchAllStudents(...)` (all statuses), `watchUniqueGuardians(schoolId)` (unique guardian list with ward counts), `watchGuardianWards(schoolId, userId)` (ward list for a specific guardian), `watchGuardians(...)`, `watchStudent(schoolId, adm)`, `MembersDao.searchStudents(schoolId, query)` (student picker for guardian linking), `MembersDao.watchClassTeacherAssignments(schoolId, teacherUserId)` (teacher class assignments), `MembersDao.watchTeacherSubjects(schoolId, teacherUserId)` (teacher subject assignments), `DepartmentsDao.watchDepartments(schoolId)`, `ExamsGradesDao.watchStudentGrades(...)`, `ExamsGradesDao.watchMasteryForStudent(...)`, `ExamsGradesDao.getExam(...)`, `PlansDao.watchStudentSubscriptions(...)`, `PlansDao.getActivePlans()`, `PlansDao.createSubscription(...)`, `PlansDao.updateSubscriptionStatus(...)`, `SettingsDao.getSettings(schoolId)`
**Dependencies:** `database/daos/members_dao.dart`, `database/daos/departments_dao.dart`, `database/daos/exams_grades_dao.dart`, `database/daos/plans_dao.dart`, `database/daos/settings_dao.dart`, `database/tables/curriculum_subjects.dart`, `models/curriculum_levels.dart`, `models/membership.dart` (`OwnerEntry` — used in `_computeVisibleTabs` owner bypass), `models/result.dart`, `models/school_config.dart`, `models/school_context.dart`, `ui/widgets/edu_data_table.dart`, `ui/widgets/edu_tab_bar.dart`, `ui/widgets/member_creation/` (all creation panels), `ui/theme/app_theme.dart`, `services/members.dart` (`MemberCreationService`), `services/member_management.dart` (`MemberManagementService`), `ui/widgets/user_avatar.dart`, `client.dart`

---

### `roles/`
| File | Widget | Status | Description |
|---|---|---|---|
| `_role_helpers.dart` | (helpers) | ✅ Complete | Resource groupings, action colour/icon maps, `parsePermissions()`, `serialisePermissions()`, `countPermissions()`, `popcount()`. **BUG-012 fix (Task 03):** `parsePermissions()` rewritten to handle all three storage formats encountered in the wild: (1) standard JSON objects/flat-map via `Permissions.fromJson`, (2) seeder's binary-int JSON array (`[5,2,0,...]`) via `Permissions.fromBlob`, (3) base64-encoded strings via base64 decode → UTF-8 JSON or raw binary blob. Each attempt is logged via `debugPrint`. Added imports: `dart:typed_data` (`Uint8List`). |
| `school_roles_screen.dart` | `SchoolRolesScreen` | ✅ Complete | Single list of role rows (data-table style) for this school. **Task B4:** Owner gets full RBAC bypass on create/edit/delete — `canCreate`, `canEdit`, `canDelete` now include `isOwner` check (`entry is OwnerEntry`) to solve chicken-and-egg problem where a new owner has no scopes and therefore cannot create the first role. New import: `models/membership.dart` (`OwnerEntry`). **Task D2:** Defense-in-depth permission guard added at the top of `_SchoolRolesBodyState.build()`. Non-owner users without `Resource.roles` / `Action.read` permission now see a centered lock icon + "Access restricted" + "You don't have permission to view roles." message instead of the roles list. Matches the pattern used in `SchoolSettingsScreen`. **Task G5 (Bug Sweep):** Delete button hidden from UI — roles currently only support hard delete (purge) via `rolesDao.deleteRole()`. Both `_deleteRole` and `_purgeRole` called the same DAO method, which was confusing. Now only Purge is shown (Super users only, `_isSuper` gate). `_deleteRole` retained as empty stub with TODO for future soft-delete. `canDelete` variable removed (was unused after button removal). **Task A09:** Removed `|| entry is OwnerEntry` from `canCreate`, `canEdit` (previously also `canDelete`). **Task U06:** `_RoleCard` replaced by `_RoleRow` (StatefulWidget). Row pattern: 52px min-height `ConstrainedBox`, `MouseRegion`+`AnimatedContainer` hover (`cs.primary alpha 0.04`), `NoSplash` `InkWell`. Left: role name (13px w500) + description (12px muted, 1-line ellipsis). Right: permission count badge (`BorderRadius.circular(AppTheme.kChipRadius)`, primary-tinted bg + color when `hasPerms`, muted when zero) + desktop inline edit button (hover-only, 28×28, `Icons.edit_outlined`) or mobile three-dot (`Icons.more_vert`, opens bottom sheet with Edit action) + `Icons.chevron_right_rounded` (16px muted). Rows separated by `AppTheme.tableRowDivider`. `ListView.builder` padding changed from `fromLTRB(20,16,20,88)` to `fromLTRB(0,8,0,88)`. Empty state updated to standard 52×52 pattern (radius 10, `surfaceContainerHighest alpha 0.5`, icon 22px `alpha 0.3`, title 13.5px w500, subtitle 12px w400 `alpha 0.5`). FAB `FloatingActionButton.small` retained. Role creation modal (`_RoleFormSheet`) adaptive: desktop `Dialog` (maxWidth 440, `BorderRadius.circular(12)`, dual box-shadow, `AppTheme.modalBg`); mobile bottom sheet. Modal includes inline permissions section: collapsible resource rows (36px) that expand to a `Wrap` of 28×28 icon action chips. Permissions serialised as JSON via `_localSerialisePermissions` and stored in `roles.permissions`. |
| `school_role_detail_screen.dart` | `SchoolRoleDetailScreen` | ✅ Complete | Full push-route role detail page with 2 tabs (`EduTabBar`): Permissions (toggle chips grouped by resource) and Assigned Users (list of users with this role + assign/unassign actions). **Diagnostic logging:** `_PermissionsTabState._save()`, `_resetFromRole`, and `didUpdateWidget` now include `debugPrint` diagnostic logging for permission parsing, reset, and widget-update flows. **Timestamp precision fix:** `updated` field in save now uses milliseconds since epoch (`DateTime.now().millisecondsSinceEpoch`) instead of seconds, matching the `accounts` table convention for `bigint` timestamp columns. |

**Data source:** `SchoolScopesDao.watchSchoolRoles(schoolId)`, `SchoolScopesDao.watchSchoolScopes(schoolId, ...)`
**Dependencies:** `database/daos/school_scopes_dao.dart`, `ui/widgets/edu_tab_bar.dart`, `models/school_context.dart`, `models/membership.dart` (`OwnerEntry`)

---

### `settings/`
| File | Widget | Status | Description |
|---|---|---|---|
| `school_settings_screen.dart` | `SchoolSettingsScreen` | ✅ Complete | Owner-facing school profile & settings editor. Full-page form (rendered inside dashboard content area, not a sheet) for editing school identity, contact details, location, and logo. **Permission guard (E01):** Defense-in-depth guard at the top of `build()` — blocks access for non-`OwnerEntry` users who lack `Resource.schools` / `Action.update` permission. Shows a centered lock icon + "Access restricted" message. **Task D2:** Matching defense-in-depth guard added at the top of `_save()` — early-returns for non-`OwnerEntry` users without `Resource.schools` / `Action.update`, preventing unauthorized writes even if the UI guard is somehow bypassed. `material.dart` import updated to `hide Action` to resolve conflict with `permissions.dart`. New imports: `models/membership.dart` (`OwnerEntry`), `models/permissions.dart` (`Resource`, `Action`). **Fields:** School name (required), motto, phone, email, domain, county (required, searchable picker sheet), established year. **Logo:** `ImagePicker` → preview + pick/clear buttons, cached at `{appDir}/schools/{schoolId}/logo` via `FileCache.saveBytes`. **Save:** `AnimatedSaveButton` in header row, dirty-tracking across all fields + logo. Writes via `SchoolsDao.updateSchoolDetails()` (auto-creates `SyncAction.updateSchool` log entry) + `SchoolsDao.logLogoChange()` for logo. Shows SnackBar on success, error banner on failure. **Layout:** Responsive — constrained to 560px max-width on desktop, full-width on mobile. Three sections (Identity, Contact, Location & Details) in `_SectionContainer` cards with `_SectionHeader` labels matching account screen pattern. **County picker:** `_CountyPickerSheet` via `showEduSheet` — searchable list of 47 Kenya counties. **Private widgets:** `_ErrorBanner`, `_SectionHeader`, `_SectionContainer`, `_FormField`, `_CountyPickerRow`, `_LogoSection`, `_CountyPickerSheet`/`_CountyPickerSheetState`. |
| `mpesa_config_screen.dart` | `MpesaConfigScreen` | ✅ Complete | Owner-facing M-Pesa Daraja API configuration. Pushed route from settings screen. **Permission guard (D1):** Defense-in-depth via `_hasPermission` getter — checks `entry is OwnerEntry || perms.can(Resource.schools, Action.update)`. Applied in `build()` (returns `RestrictedAccessState`), `_save()` (early return), `_delete()` (early return). `material.dart` import updated to `hide Action`. New imports: `models/membership.dart`, `models/permissions.dart`, `../exams/exams_shared.dart` (`RestrictedAccessState`). |

**Data source:** `SchoolsDao.updateSchoolDetails(schoolId, SchoolsCompanion, accountId)`, `SchoolsDao.logLogoChange(schoolId, accountId)`, `FileCache.saveBytes(bytes, relativePath)`, `FileCache.get(relativePath)`, `FileCache.logoPath(schoolId)`
**Dependencies:** `cache/file_cache.dart`, `client.dart` (`cache`, `schoolsDao`), `database/database.dart` (`SchoolsData`, `SchoolsCompanion`), `database/tables/curriculum_subjects.dart` (`KenyaCounty`), `models/school_context.dart`, `models/membership.dart` (`OwnerEntry`), `models/permissions.dart` (`Resource`, `Action`), `ui/theme/app_theme.dart`, `ui/widgets/animated_save_button.dart`, `ui/widgets/edu_sheet.dart`

---

### `timetable/`
| File | Widget | Status | Description |
|---|---|---|---|
| `timetable_screen.dart` | `TimetableScreen` | ✅ Complete | Timetable rules setup + viewing. Owner/admin view has **2 tabs** (Timetable · Lessons) via `EduTabBar`. **Timetable tab:** school-wide cross-matrix displaying ALL classes (all grades × all streams) for the active term via `_SchoolWideMatrixTab`. Row axis: Day → Grade → Stream (three-level hierarchy). Column axis: time slots with "Break" filler cells for gaps. Desktop (≥600px): fully scrollable cross-matrix, left-axis pinned, columns scroll horizontally — rendered by `_SWDesktopMatrix`. Mobile (<600px): day-chip selector + horizontally-scrollable matrix for the selected day — rendered by `_SWMobileView` + `_SWMobileViewState`. Cells show subject name (w500, `onSurface`) + teacher name (w400, `onSurfaceVariant @ 0.6`) with coloured left-accent border (deterministic from `_kSubjectColors` via `_colorForSubject`). Empty slots: subtle border-only `_SWEmptyCell`. Break columns: muted `_SWBreakCell`. Grade sub-headers: slightly more subtle than day headers, both full-width strips inside the horizontal scroll. **Lessons tab (Task 03):** redesigned tri-view. `_LessonsTab` is now `StatefulWidget` with `config: SchoolConfig` param. Enum `_LessonView { log, teachers, coverage }` drives an `AnimatedSwitcher(180ms FadeTransition)`. Stats bar (`_LessonStatsBar` + `_LessonStatChip`): 3-chip row (total lessons, teacher count, subject count). View switcher (`_LessonViewSwitcher` + `_LessonViewChip`): animated chips for Log/Teachers/Coverage. **Log view** (`_LessonLogView`): date-grouped newest-first; `_LessonDayHeader` (day label via `_lessonDayLabel` + count badge); `_LessonLogRow` (coloured left accent bar + subject/teacher + grade·stream badge via `_lessonClassLabel`). **Teachers view** (`_LessonTeachersView` / `_LessonTeachersViewState`): expandable accordion sorted by lesson count desc; initial circle avatar; `AnimatedCrossFade` expand with `_TeacherLessonSubRow` sub-rows. **Coverage view** (`_LessonCoverageView`): per-subject lesson count with `AnimatedContainer` progress bars relative to max. **Empty state:** `_LessonsEmptyState`. **Removed:** `_LessonDateHeader`, `_LessonRow`, `_EmptyLessonsState`. **Helpers added:** `_lessonDayLabel(int)`, `_lessonClassLabel(int, int, SchoolConfig)`. Teacher/student/guardian/staff views remain single-view (no tabs). **FAB:** `_GenerateFab` is `FloatingActionButton.small` (green, `RoundedRectangleBorder(radius: 8)`) — opens the wizard via `showTimetableWizardDialog`. Saved rules loaded from `FileCache.loadTimetableRules` on open; persisted back via `FileCache.saveTimetableRules` inside the wizard's `_save()`. On successful generation, `_OwnerTimetableShellState._runGeneration` runs `compute(runTimetableGenerator, input)` and bulk-inserts via `TimetableDao.insertSlots`. **Wizard entry point — `showTimetableWizardDialog` (TW-01):** Top-level function. Desktop (≥`kMobileBreakpoint`): `showDialog` wrapped in `Dialog(transparent) → ConstrainedBox(maxWidth:520) → Container(modalBg, border, shadow) → ClipRRect → _TimetableWizard`. Mobile: `showModalBottomSheet(isScrollControlled:true) → Padding(viewInsets) → Container(maxHeight:88vh, topRadius:kModalRadius, border-top)`. **4-stage generation wizard (`_TimetableWizard` / `_TimetableWizardState`, TW-01):** Plain `Column` — NO Scaffold, NO AppBar. Layout: `_buildHeader` + `Divider` + `Flexible(AnimatedSwitcher body, 180ms fade)` + `Divider` + `_buildFooter`. `_buildHeader`: mobile drag handle (36×3 px, rounded, `outlineVariant@0.45`) + Row(Expanded Column(stage title 14pt w500 + "Step N of 4" 11pt muted) + `_WizardStepDots(totalSteps:4)` + close `Icons.close` 17px). `_buildFooter`: Back `_WizardTextButton` (left, hidden on Stage 0) + Spacer + Save `_WizardTextButton` (always visible on all stages, with loading spinner) + Next/Review `_WizardFilledButton` (hidden on Stage 3 — Generate button lives inside stage body instead). **Stage mapping:** Stage 0 = "Day & Slot Setup" (`_Stage0DaysSlots` — TW-02); Stage 1 = "Teacher Constraints" (`_Stage1TeacherConstraints` — TW-03); Stage 2 = "Subject Constraints" (`_Stage2SubjectConstraints` — TW-03); Stage 3 = "Review & Generate" (`_Stage3Generate` — TW-04). **`_save()` (TW-01):** Async — calls `FileCache.saveTimetableRules` with `_saving` loading state, pops with `_RulesSheetResult(shouldGenerate:false)`. `_completeWithGeneration()` pops with `shouldGenerate:true` — called from "Apply Timetable →" in `_SuccessPanel`. **Stage 0 — Day & Slot Setup (`_Stage0DaysSlots` / `_Stage0DaysSlotsState`, TW-02):** Full compact redesign. `SingleChildScrollView → Padding(all:20) → Column(spacing:16)`. Section A: `_SectionLabel('School Days')` + `SingleChildScrollView(horizontal)` + `ToggleButtons` (7 children Mon–Sun, indices 1–7, `kCardRadius` border-radius, primary fill 10–15% alpha, 44×36 min constraints); `_toggleDay()` prevents deselecting last day. `_DayChip` class retained — still used in `_ConstraintEntryFormState` until TW-07. Section B: 48px tappable `InkWell → Container` row (nestedBg, kCardRadius border) — sun icon + "Starts at" text + primary-tint time chip — calls `showDrumTimePicker` (custom drum picker dialog, TW-05). Thin `Divider(0.5px)`. Section C: `_SectionLabel('Slot Sequence')` + lesson-count badge chip + card-wrapped slot list: empty state = `Container(nestedBg, kCardRadius, border)` with `view_timeline_outlined` icon + title + subtitle; non-empty = `Container(modalBg, kCardRadius, border) → ListView.separated(AppTheme.tableRowDivider) → _SlotRowTile` rows (TW-05). Section D: two `_AddSlotButton` widgets side-by-side ("+ Add Lesson" green, "+ Add Break" amber) — each calls `_promptAdd()` which shows `_DurationPickerDialog`. State fields: `_activeDays`, `_slots`, `_dayStart` — all initialised from `widget.rules` in `initState`. `_notify()` calls `widget.onChanged` with updated `TimetableRules`. Break slots advance the time cursor in `_rows()` but are NEVER written to the DB — only `GeneratedSlot` entries from `TimetableGenerator` are. **Stage 1 — Teacher Constraints (`_Stage1TeacherConstraints`, TW-03):** `SingleChildScrollView → Padding(all:20) → Column`. `_SearchField` (38px) filters teacher list. `ListView.builder(shrinkWrap:true)` of `_ConstraintEntityRow` — 44px expandable row with `AnimatedCrossFade` showing `_ConstraintChipRow` entries + "Add constraint" `InkWell`. `_showConstraintEntry()` opens `_ConstraintEntryForm` in a `showDialog`. **Stage 2 — Subject Constraints (`_Stage2SubjectConstraints`, TW-03):** Identical pattern for subjects. **Stage 3 — Review & Generate (`_Stage3Generate` / `_Stage3GenerateState`, TW-04):** `_computeConflicts()` called when advancing from Stage 2. Body is `SingleChildScrollView → Padding(all:20) → Column(crossAxisAlignment:stretch, spacing:16)`. Contains: `_SummarySection` (if no conflicts) OR `_ConflictSection` (if conflicts exist), then `_GenerateSection`. `_SummarySection`: compact `Wrap(spacing:8, runSpacing:8)` of four `_StatChip` widgets showing Days, Slots/Day, Teacher rules, Subject rules counts. `_StatChip`: `nestedBg` container with `kCardRadius` border, Row of value (16pt w500 `onSurface`) + label (11pt w400 `onSurfaceVariant@0.6`). `_ConflictSection`: `_SectionLabel('Conflicts Detected')` + red count badge (`error@0.12` bg) + `ListView.separated(shrinkWrap, NeverScrollableScrollPhysics)` of redesigned `_ConflictCard`s. `_ConflictCard` redesign: `nestedBg` container with `error@0.3` border; teacher row (person icon 14px muted + "Teacher: {name}" 12.5pt + `_ConstraintTypeBadge`); subject row (book icon 14px muted + "Subject: {name}" 12.5pt + `_ConstraintTypeBadge`); `_SectionLabel('Which takes priority?')` + Row of two `Expanded _PriorityChip` widgets. `_ConstraintTypeBadge`: small inline badge, error tint for Block / primary tint for Require. `_PriorityChip`: 36px `AnimatedContainer(140ms)` chip using `cs.primary` (not `brandGreen`) as active color, `kChipRadius` border, text 12pt w500. `_GenerateSection`: `Column(crossAxisAlignment:stretch, spacing:12)` switching between: `_GenerateButton` (idle — full-width `FilledButton.icon`, `brandGreen`, `minimumSize: Size(∞, 44)`, `auto_awesome_rounded` icon), `_GeneratingIndicator` (centred `Column`: `CircularProgressIndicator(strokeWidth:2, color:cs.primary)` + `AnimatedSwitcher(FadeTransition)` cycling status text every 1.5s via `Timer.periodic` in `_Stage3GenerateState`; messages: "Analysing subjects…", "Building slot matrix…", "Resolving constraints…", "Optimising schedule…"), `_SuccessPanel` (`brandGreen@0.08` container, `brandGreen@0.3` border: check icon + "Timetable ready!" 13.5pt w500 + "{N} slots across {D} days · {ms}ms" 12pt muted + Row: "Regenerate" TextButton muted + "Apply Timetable →" `FilledButton brandGreen`), `_FailurePanel` (`error@0.06` container, `error@0.3` border: error icon + "Could not generate" 13.5pt w500 error + reason text 12pt muted maxLines:4 + right-aligned "Try Again" `OutlinedButton`). `_Stage3GenerateState` no longer uses `TickerProviderStateMixin` — timer replaced by `Timer? _statusTimer` started/stopped in `didUpdateWidget`, cancelled in `dispose`. **Removed (TT-03):** old `_TimetableRulesPage`, legacy helper classes. |

**Widgets added TT-02:** `_SchoolWideMatrixTab`, `_SWDesktopMatrix`, `_SWMobileView` / `_SWMobileViewState`, `_SWSlotCell`, `_SWEmptyCell`, `_SWBreakCell`, `_SWDayChip`. Helpers: `_SwColDesc` / `_SwTimeCol` / `_SwBreakCol`, `_swActiveDays()`, `_swBuildColumns()`, `_swEntryAt()`, `_swTotalWidth()`. Constants: `_kSwStreamLabelW = 100`, `_kSwTimeColW = 110`, `_kSwBreakColW = 50`, `_kSwColGap = 3.0`.

**Widgets added TT-03:** `_TimetableWizardPage` / `_TimetableWizardPageState` (now renamed — see TW-01), `_WizardStepDots`, `_RulesSheetResult`, `_WizardTeacher`, `_WizardSubject`, `_ConflictPair`. Stage widgets: `_Stage0SlotBuilder` / `_Stage0SlotBuilderState` (**removed TW-02**), `_Stage1ActiveDays` (**removed TW-02**), `_Stage2TeacherConstraints` / `_Stage2TeacherConstraintsState` (now renamed — see TW-01), `_Stage3SubjectConstraints` / `_Stage3SubjectConstraintsState` (now renamed — see TW-01), `_Stage4Generate` / `_Stage4GenerateState` (now renamed — see TW-01), `_ConflictCard` (**redesigned TW-04**). Shared wizard sub-widgets: `_SlotRowTile` (**redesigned TW-02** compact 48px row; **redesigned TW-05** as StatefulWidget with hover/press animations and left accent bar), `_ExpandableFab` (**removed TW-02**), `_MiniActionFab` (**removed TW-02**), `_WizardDayChip` (**removed TW-02**), `_WizardSection` (**removed TW-04**), `_WizardRuleRow` (**removed TW-04**), `_WizardStepper` (**removed TW-02**), `_WizardStepBtn` (**removed TW-02**), `_EntityConstraintCard` (**removed TW-03**), `_ConstraintTile` (**removed TW-03**), `_ConstraintSheet` / `_ConstraintSheetState` (**removed TW-03**), `_ConstraintTypeChip` (**removed TW-03**). Top-level helpers: `_showConstraintSheet` (**removed TW-03**), `showDrumTimePicker` (**added TW-05**), `_fmtTime`, `_subjectLabel`, `_kWizDayFull` (**removed TW-03**), `_kWizDayShort`. Drum picker widgets: `_DrumTimePicker` / `_DrumTimePickerState` (**added TW-05**), `_AmPmChip` (**added TW-05**).

**Changes in TW-01:** Renamed `_TimetableWizardPage` / `_TimetableWizardPageState` → `_TimetableWizard` / `_TimetableWizardState`. Renamed stage classes: `_Stage2TeacherConstraints` → `_Stage1TeacherConstraints`, `_Stage3SubjectConstraints` → `_Stage2SubjectConstraints`, `_Stage4Generate` → `_Stage3Generate` (and their State classes). Added: `showTimetableWizardDialog` (top-level function), `_Stage0DaysSlots` / `_Stage0DaysSlotsState` (stub combining old stages 0+1), `_WizardTextButton`, `_WizardFilledButton`. Removed Scaffold from wizard — replaced with plain `Column` (`_buildHeader` + `Flexible` body + `_buildFooter`). `_save()` now async with `FileCache.saveTimetableRules` + `_saving` loading state. `_openRulesSheet` updated to use `showTimetableWizardDialog` and no longer calls `FileCache.saveTimetableRules` directly. Stage count: 5 → 4. `_WizardStepDots` `totalSteps` updated to 4 at all call sites.

**Changes in TW-02:** Replaced `_Stage0DaysSlots` / `_Stage0DaysSlotsState` stub (which delegated to `_Stage0SlotBuilder` + `_Stage1ActiveDays`) with the full compact redesign. Stage 0 now has state fields `_activeDays`, `_slots`, `_dayStart` and owns all day/slot logic directly — no sub-widgets. Deleted dead classes: `_Stage0SlotBuilder` / `_Stage0SlotBuilderState`, `_Stage1ActiveDays`, `_ExpandableFab`, `_MiniActionFab`, `_WizardDayChip`, `_WizardStepper`, `_WizardStepBtn` (total ~990 lines removed). Added new Stage 0 helpers: `_DayChip` (compact 34–36px animated chip, `AnimatedContainer` 140ms), `_SlotRowTile` (redesigned — compact 48px row: number badge + type chip + time range + spacer + duration + 32×32 delete icon), `_AddSlotButton` (38px outlined inline button, no FAB), `_DurationPickerDialog` / `_DurationPickerDialogState` (compact modal-style dialog matching create_term_modal aesthetic — preset chips + custom TextField), `_SectionLabel` (uppercase 10.5pt w600 label with optional trailing widget).

**Changes in TW-03:** Compact redesign of Stage 1 (Teacher) and Stage 2 (Subject) constraint stages. **Deleted:** `_EntityConstraintCard`, `_ConstraintTile`, `_ConstraintSheet` / `_ConstraintSheetState`, `_ConstraintTypeChip`, `_showConstraintSheet` (bottom-sheet pattern), `_kWizDayFull` constant (unused after sheet removal). **Added:** `_SearchField` (38px inline search bar — `Container(height:38, nestedBg, kCardRadius border)` + search icon + `TextField` + clear button); `_ConstraintEntityRow` (replaces `_EntityConstraintCard` — transparent `Column`, 44px `InkWell` header row with `AnimatedRotation` chevron, `AnimatedCrossFade` expansion to `nestedBg` container showing constraint rows + "Add constraint" 40px `InkWell` row); `_ConstraintChipRow` (replaces `_ConstraintTile` — 40px `SizedBox`, `horizontal:14` padding, type chip `error@0.10`/`primary@0.10` bg, day chips and slot chips with `onSurfaceVariant@0.08` bg, `Spacer`, 32×32 `GestureDetector` delete icon); `_ConstraintEntryForm` / `_ConstraintEntryFormState` (replaces `_ConstraintSheet` — `SingleChildScrollView → Column(min)`: header row (title 14pt w500 + entityName 12pt muted + close icon) + `Divider(0.5px)` + `Padding(20)` body (Type: two `Expanded AnimatedContainer` chips Block=`error` tint / Require=`primary` tint at 38px + 14pt gap, Days: `_DayChip` wrap, Slots: `InkWell AnimatedContainer` two-line chips "Slot N" + "HH:MM–HH:MM" 10pt muted) + `Divider(0.5px)` + footer (`fromLTRB(20,10,20,14)` Row: Cancel TextButton + Spacer + `AnimatedContainer` Add button `brandGreen`/`brandGreen@0.35` disabled)). Validation: type + ≥1 day + ≥1 slot required. **Modified:** Both constraint state classes now hold `_searchCtrl TextEditingController` (disposed in `dispose()`); `_add()` refactored from async (calling `_showConstraintSheet`) to sync (accepting explicit `days, slotIndices, isBlock` params); `_showConstraintEntry(entityId, entityName)` added as an instance method that calls `showDialog` containing `_ConstraintEntryForm` (maxWidth 400, `modalBg`, `kModalRadius`, `modalShadow`). Build layout changed from `Column + Expanded(ListView.separated)` to `SingleChildScrollView → Padding(all:20) → Column` with shrinkWrap `ListView.builder`.

**Changes in TW-04:** Full redesign of Stage 3 (Review & Generate). **Footer update:** `_buildFooter` now always shows the Save `_WizardTextButton` (on all 4 stages); the Next/Review `_WizardFilledButton` is hidden only on Stage 3 (condition changed from `!isLastStage` to `_stage < 3`). **Stage 3 body redesign:** `SingleChildScrollView → Padding(all:20) → Column(crossAxisAlignment:stretch, spacing:16)`. Replaces old multi-branch return (separate layouts for generating/success/failure/idle) with a unified layout that always shows a summary or conflict section plus a generate section. **`_SummarySection`:** compact `Wrap(spacing:8, runSpacing:8)` of `_StatChip` widgets (Days, Slots/Day, Teacher rules, Subject rules). **`_StatChip`:** `nestedBg` container, `kCardRadius` border, Row: value (16pt w500 `onSurface`) + spacer + label (11pt w400 `onSurfaceVariant@0.6`). **`_ConflictSection`:** `_SectionLabel('Conflicts Detected')` + red count badge (`error@0.12` bg, 11pt w600 error text) + `ListView.separated(shrinkWrap, NeverScrollableScrollPhysics)`. **`_ConflictCard` redesign:** `nestedBg` container with `error@0.3` border; teacher row (Icon `person_outline_rounded` 14px `onSurfaceVariant@0.45` + "Teacher: {name}" 12.5pt w400 + `_ConstraintTypeBadge`); subject row (Icon `book_outlined` 14px muted + "Subject: {name}" 12.5pt w400 + `_ConstraintTypeBadge`); `SizedBox(10)` + `_SectionLabel('Which takes priority?')` + `SizedBox(6)` + Row of two `Expanded _PriorityChip` widgets. **`_ConstraintTypeBadge`:** new inline badge widget — `error@0.10` bg for Block / `primary@0.10` bg for Require, text 10.5pt w500. **`_PriorityChip`:** 36px tall `AnimatedContainer(140ms)`, `cs.primary` as active color (not `brandGreen`), `kChipRadius`, text 12pt w500, `maxLines:1 overflow:ellipsis`. **`_GenerateSection`:** `Column(crossAxisAlignment:stretch, spacing:12)` switching among: `_GenerateButton` (when `result==null && !generating`), `_GeneratingIndicator` (when `generating`), `_SuccessPanel` (when `GeneratorSuccess`), `_FailurePanel` (when `GeneratorFailure`). **`_GenerateButton`:** `FilledButton.icon(auto_awesome_rounded 16px)`, `brandGreen` bg, `minimumSize: Size(∞, 44)`, 13.5pt w500. **`_GeneratingIndicator`:** centred `Column`: `CircularProgressIndicator(strokeWidth:2, color:cs.primary)` + `AnimatedSwitcher(300ms, FadeTransition)` cycling status text. **`_SuccessPanel`:** `brandGreen@0.08` tinted container, `brandGreen@0.3` border, `check_circle_outline_rounded` green icon + "Timetable ready!" 13.5pt w500 + "{N} slots across {D} days · {ms}ms" 12pt muted + Row(end): "Regenerate" `TextButton` muted + "Apply Timetable →" `FilledButton brandGreen`. **`_FailurePanel`:** `error@0.06` tinted container, `error@0.3` border, `error_outline_rounded` icon + "Could not generate" 13.5pt w500 `cs.error` + reason 12pt `onSurfaceVariant` maxLines:4 + right-aligned "Try Again" `OutlinedButton`. **Animation change:** `_Stage3GenerateState` drops `TickerProviderStateMixin` and `AnimationController _pulseCtrl`; replaced by `Timer? _statusTimer` (started in `didUpdateWidget` when `generating` becomes true via `Timer.periodic(1500ms)`, cancelled when `generating` stops or in `dispose`). **Deleted:** `_WizardSection`, `_WizardRuleRow` (no longer needed — Stage 3 summary replaced by `_SummarySection`/`_StatChip`).

**Changes in TW-05:** Stage 0 cohesive day selector, custom drum time picker, and slot row redesign. **Section A (day selector):** `Wrap` of `_DayChip` widgets replaced by `SingleChildScrollView(horizontal)` + `ToggleButtons` (7 children Mon–Sun, `kCardRadius` border-radius, primary fill at 10–15% alpha, 44×36 min constraints). `_DayChip` class is NOT deleted — still used in `_ConstraintEntryFormState` until TW-07. **Section B (start time):** `showTimePicker` replaced by `showDrumTimePicker` — a custom top-level function that opens a `Dialog(transparent) → ConstrainedBox(maxWidth:280) → Container(modalBg, radius:8, border, modalShadow) → ClipRRect → _DrumTimePicker`. **`_DrumTimePicker` / `_DrumTimePickerState`:** two `ListWheelScrollView.useDelegate(FixedExtentScrollPhysics, diameterRatio:1.5, perspective:0.003)` drums for hour (1–12, looping) and minute (0–59, looping), each 72×120 with a `cs.primary@0.08` center-highlight `Container(height:40, radius:6)`. AM/PM column with two `_AmPmChip` widgets. Footer: `_WizardTextButton('Cancel')` + `_WizardFilledButton('Confirm')`. `_toTimeOfDay()` converts `_hour`/`_minute`/`_isPm` to `TimeOfDay`. **`_AmPmChip`:** `GestureDetector → AnimatedContainer(140ms)` 32×28, `primary@0.15`/`nestedBg` bg, border width 1.0/0.5, `primary`/`onSurfaceVariant@0.5` text. **Section C (slot list):** Simple `Center(Text)` empty state replaced by `Container(nestedBg, kCardRadius, border)` with `view_timeline_outlined` icon + "No slots yet" + subtitle. Non-empty divider-separated list replaced by `Container(modalBg, kCardRadius, border) → ListView.separated(shrinkWrap, NeverScrollableScrollPhysics, AppTheme.tableRowDivider)`. **`_rows()` method added to `_Stage0DaysSlotsState`:** returns `List<({int i, String range, int dur, SlotType type})>` by walking `_slots` with `cursor = _dayStart.hour*3600 + _dayStart.minute*60`, formatting each slot's start/end via `_fmtTime`. **`_SlotRowTile` redesigned (StatelessWidget → StatefulWidget + `SingleTickerProviderStateMixin`):** `MouseRegion`+`GestureDetector` for hover/press state; `AnimationController(100ms forward, 150ms reverse)` + `Tween(1.0→0.97)` `ScaleTransition`; `AnimatedContainer(150ms)` bg and border; left accent bar (`AnimatedContainer(150ms)` width 3→4 on hover/press, `brandGreen@0.7`/breakColor@0.7` color); `IntrinsicHeight Row` with accent bar + `Expanded(Padding(fromLTRB(12,10,12,10)) Row)`: number badge (20×20 `surfaceContainerHighest`/`kChipRadius`) + type chip (Lesson=`brandGreen@0.12` / Break=`#FFA726@0.12`) + `Expanded(Text timeRange 12.5pt w400)` + duration text (11pt w400 `onSurfaceVariant@0.55`) + `GestureDetector(close_rounded 16px)` delete.

**Changes in TW-06:** Stages 1+2 members-style search bar and entity rows. **Deleted:** `_SearchField` (38px `Container(nestedBg, kCardRadius border)` + Row(icon + `TextField` + clear button) — replaced with inline `ValueListenableBuilder<TextEditingValue>` + `TextField` pattern matching `_FlatMemberList` from `members_page.dart`); `_ConstraintEntityRow` (`StatelessWidget` — transparent `Column`, 44px `InkWell` header, `AnimatedCrossFade` expansion, "Add constraint" 40px `InkWell` row — replaced with `_WizardEntityRow`). **Added:** `_WizardEntityRow` (`StatefulWidget`, `SingleTickerProviderStateMixin`) — `ScaleTransition(1.0→0.98, 100ms/150ms)`, `MouseRegion`+`GestureDetector` for hover/press state, `AnimatedContainer(150ms)` bg (idle/hover/press primary tints: @0.04–0.06/@0.08–0.12/@0.13–0.18), 0.5px border (idle `outline@0.08`, hover/press `primary@0.25`), `ClipRRect(kCardRadius)`, `IntrinsicHeight Row`: 3→4px left accent bar (rounded left corners, `primary@0.7→1.0`) + `Expanded Padding(fromLTRB:12,10,12,10) Row`: 28×28 leading icon container (`kChipRadius`, hover `primary@0.12`/idle `surfaceContainerHighest`, icon 14px) + `SizedBox(10)` + `Expanded Column(start,min)` (name 13pt w500 `onSurface` + subtitle 11.5pt w400 `onSurfaceVariant@0.55`) + `Spacer` + `AnimatedRotation` chevron (0→0.25 turns, 180ms); `AnimatedCrossFade(180ms, sizeCurve:easeInOut)` expanded section: `Container(margin:left:3, nestedBg, border-left:3px primary@0.2)` wrapping `expandedContent`. **Modified:** Both `_Stage1TeacherConstraintsState.build()` and `_Stage2SubjectConstraintsState.build()` updated — `_SearchField(...)+SizedBox(12)` replaced with `Padding(bottom:4)→SizedBox(38)→ValueListenableBuilder<TextEditingValue>→TextField` (filled, `kCardRadius` borders, `prefixIcon` search 18px, reactive `suffixIcon` clear 16px, `enabledBorder 0.5px outlineVariant`, `focusedBorder 1px primary@0.5`) + `SizedBox(8)`; `_ConstraintEntityRow(...)` return replaced with `Padding(bottom:8)→_WizardEntityRow(name, subtitle:"N constraint(s)"/"No constraints", icon:person_outline/book_outlined, isExpanded, onTap, expandedContent: Column[...constraints.expand(c=>[_ConstraintChipRow, tableRowDivider]), Padding(fromLTRB:14,8,14,10)→OutlinedButton.icon("Add Constraint")])`. **Stage 1** uses `Icons.person_outline_rounded`; **Stage 2** uses `Icons.book_outlined`. "Add constraint" `InkWell` row removed — replaced with `OutlinedButton.icon` (`primary` foreground, `primary@0.4` border 0.8px, `kCardRadius` shape, 12.5pt w500, 34px min height, `shrinkWrap` tapTargetSize).

**Widgets added Task 02:** `_GenerateLessonsFab` (StatelessWidget — `FloatingActionButton.small`, `auto_awesome_rounded`, green, shown when timetable exists), `_GeneratedLesson` (in-memory data class — holds all lesson fields including mutable `teacherId`/`teacherName`; `toCompanion()` converts to `LessonsCompanion`), `showGenerateLessonsDialog` (top-level function — desktop: centred `Dialog(maxWidth:480)`; mobile: `showModalBottomSheet(88% height)`), `_GenerateLessonsDialog` / `_GenerateLessonsDialogState` (two-step dialog — Step 0: scope picker Today/This Week; Step 1: preview + edit + save; `_generate()` calls `TimetableDao.getTermTimetableForDays` and maps entries to `_GeneratedLesson` list; `_save()` calls `TimetableDao.saveLessons` + shows `SnackBar`), `_ScopeOptionCard` / `_ScopeOptionCardState` (animated scope selection card — `AnimationController(100ms/150ms)` + `ScaleTransition(1.0→0.96)` + `AnimatedContainer(140ms)` bg/border transitions; selected: `primary@0.08` fill + `primary` border 1.5px; unselected: `nestedBg` fill + `borderColor` 1px), `_PreviewDateHeader` (date header row in preview list — uses `formatDateFromDays`), `_PreviewLessonItem` (preview list row — subject colour dot, subject/teacher/time text, grade·stream badge, edit icon opens `_showSubstitutePickerDialog`; `_gradeStreamLabel()` resolves label via `gradeLabelsFor` + config streams), `_showSubstitutePickerDialog` (top-level function — desktop: `Dialog(maxWidth:380)`; mobile: `showModalBottomSheet(70% height)`), `_SubstitutePickerDialog` / `_SubstitutePickerDialogState` (teacher substitution picker — `_load()` scans `allLessons` for teachers teaching the same subject, deduplicates by `teacherId`, computes overlap conflicts; list shows avatar initial + name + Current/Conflict/Available badge; selecting a conflict-free candidate mutates `lesson.teacherId`/`lesson.teacherName` and calls `onChanged`).

**Data source:** `TimetableDao.watchSchoolWideTimetable(...)` (Timetable tab), `TimetableDao.watchAllLessons(...)` (Lessons tab), `TimetableDao.getTermTimetableForDays(...)` (lesson generation), `TimetableDao.saveLessons(...)` (lesson save), `TimetableDao.getSubjectTeachersForTerm(...)` (wizard teacher/subject loading), `db.select(db.users)` (teacher name resolution), `CatalogDao.getSubjects()` (subject name resolution)
**Dependencies:** `database/daos/timetable_dao.dart`, `database/daos/catalog_dao.dart`, `models/active_term_context.dart`, `models/school_context.dart`, `models/school_config.dart`, `models/timetable_rules.dart`, `services/timetable_generator.dart`, `cache/file_cache.dart`, `ui/widgets/edu_tab_bar.dart`, `core/academic_utils.dart` (`formatDateFromDays`)

## Key Provided Context Objects

These objects are created in `SchoolDashboardScreen` and provided to all child pages:

| Object | Type | Provided via | Scope |
|---|---|---|---|
| `SchoolContext` | `SchoolContext` | `InheritedWidget` or similar | Entire school dashboard session |
| `ActiveTermContext` | `ActiveTermContext` | `ActiveTermProvider` (InheritedNotifier) | Entire school dashboard session |

All child screens access these via:
```dart
final schoolCtx = SchoolContext.of(context); // or similar lookup
final termCtx = ActiveTermProvider.of(context);
```

## Role Switcher

The role switcher widget in the AppBar reads `SchoolContext.membership.entries` and calls `SchoolContext.switchEntry(entry)` on tap. When `SchoolContext.canSwitch` is `false` (single entry), the switcher is hidden.

Switching entry:
- Does NOT recompute `SchoolPermissions` (constant for session).
- Does NOT trigger a new page — the dashboard content rebuilds in place via `ValueListenableBuilder<MembershipEntry>` on `SchoolContext.currentEntry`.
- Entry-sensitive streams (lessons filtered by teacher, grades for a guardian's ward, etc.) re-subscribe based on the new entry.
- The content area `KeyedSubtree` uses a **role-dependent key** (`ValueKey('dashboard-content-${currentEntry.role}')`) so that switching roles (e.g. Owner → Teacher) tears down and rebuilds the entire content subtree, eliminating stale state. Switching entries within the same role (e.g. Guardian ward A → ward B) preserves the key and thus scroll position / tab state.

## Term Selector

The term selector chip in the AppBar reads `ActiveTermContext.currentTermLabel` and opens a picker showing `ActiveTermContext.allTerms`. Selecting a term calls `ActiveTermContext.setTerm(term)`. All term-sensitive data streams rebuild via `ValueListenableBuilder<Term?>` on `ActiveTermContext.termNotifier`.

When `ActiveTermContext.hasTerms` is `false`, the dashboard shows a blank state with a "Create First Term" CTA for owners (via `NoTermsBlankState` widget).

## Dependencies

- **Depends on:** `models/school_context.dart`, `models/school_permissions.dart`, `models/membership.dart`, `models/active_term_context.dart`, `models/school_config.dart`, `models/curriculum_levels.dart`, `database/daos/` (multiple DAOs — including `AttendanceDao`, `ExamsGradesDao`, `SubjectsDao` for student tabs), `services/members.dart`, `ui/widgets/` (shared components), `cache/file_cache.dart`, `client.dart`
- **Depended on by:** `home/home_screen.dart` (navigates to `SchoolDashboardScreen`)

## Conventions

- All screens in subdirectories receive `SchoolContext` and `ActiveTermContext` from the shell — they never create their own.
- All data queries are scoped by `schoolId` from `SchoolContext.membership.school.id` and `year`/`term` from `ActiveTermContext.currentTerm`.
- Navigation within the dashboard uses the shell's routing (selected nav item index), not `Navigator.push` — except for detail pages (grade detail, role detail) which use push routes.
- Owner detail uses a responsive modal (`_OwnerInfoSheet`) — bottom sheet on mobile, side sheet on desktop. Remove owner action has full error handling (null account snackbar + try/catch with failure snackbar). Teacher detail uses a responsive modal (`_TeacherInfoSheet`) — bottom sheet on mobile, side sheet on desktop — with grouped icon action buttons (`_ActionIconGroup`/`_ActionIconItem`) replacing the old full-page `_TeacherDetailPage` and separate status-change bottom sheet. Staff detail uses a responsive modal (`_StaffInfoSheet`) — bottom sheet on mobile, side sheet on desktop — with grouped icon action buttons (`_ActionIconGroup`/`_ActionIconItem`) replacing the old full-page `_StaffDetailPage` and separate status-change bottom sheet; matches the teacher pattern exactly. Student detail navigates to `StudentDetailPage` (in separate file `student_detail_page.dart`) with 4 tabs — the old `_StudentDetailStub` has been removed from `members_page.dart`. Guardian creation uses a searchable student picker (`_StudentPickerSheet`) with search-as-you-type via `MembersDao.searchStudents()`. Guardian list now shows **unique guardians** (one row per user via `watchUniqueGuardians`) with ward count badge. Guardian detail is a responsive modal (`_GuardianWardsSheet`) — bottom sheet on mobile, side sheet on desktop — showing a reactive ward list with per-ward edit and unlink actions. The old `_GuardianRow`, `_GuardianResolved`, `_GuardianDetailPage`, and `_GuardianDetailPageState` have been removed. `_MemberDetailStub` has been removed — all member types now have dedicated detail sheets/modals.
- `PhoneFirstPanel` footer replaced: the old full-width `_CtaButton` (indigo, label-based) has been removed and replaced with a compact right-aligned footer row containing a plain "Cancel" text button and a `_ConfirmIconButton` (green tick icon, 36×36, `AppTheme.brandGreen`). The `ctaLabel` parameter remains on `PhoneFirstPanel` but is now unused by the button.
- `PhoneFirstPanel` now supports **duplicate detection at lookup time** via an optional `checkAlreadyExists` callback (`Future<bool> Function(UsersData user)?`). When a phone number resolves to an existing user, the callback is invoked to check if that user already holds the target role at the school. If duplicate is detected: an `_isDuplicate` flag is set, the `alreadyExistsMessage` is shown via `_ErrorBanner`, and the confirm button is disabled. The flag resets when the phone input changes. All four phone-first panels (`add_owner_panel.dart`, `add_teacher_panel.dart`, `add_staff_panel.dart`, `add_guardian_panel.dart`) pass `checkAlreadyExists` using the corresponding `MembersDao` existence check (`ownerExists`, `teacherExists`, `staffExists`, `guardianExists`).
- The "Generate Timetable" button is a stub — displays a snackbar, algorithm not implemented.
- `EduTabBar` now left-aligns the tab strip when `isScrollable: true` — the strip container is wrapped in `Align(alignment: Alignment.centerLeft)` so it hugs its content width instead of stretching full-width. The `MembersPage` tab bar uses `padding: EdgeInsets.only(left: 16, top: 8, bottom: 8)` for flush left alignment. This fix applies globally to all `EduTabBar` (and `EduTabBarBottom`) instances with `isScrollable: true`.
- **Flat data-table member rows (Task U05):** `_BaseTile` card-based pattern removed. All 6 member tabs now use flat rows separated by `AppTheme.tableRowDivider`. `_MemberListView` replaced by `_FlatMemberList` (`ListView.separated`, padding `top: 4, bottom: 80`). `_BaseTile` and `_MemberTile` replaced by `_UserDataRow` (StatefulWidget for user-based rows: Owners/Teachers/Staff/Guardians) and `_FlatRow` (StatefulWidget for student rows). Both use `MouseRegion` + `AnimatedContainer` hover + `NoSplash InkWell`. Desktop inline actions (`_InlineActions` / `_InlineActionButton`) fade in on hover; mobile three-dot (`_MobileActions`) opens a bottom sheet. Departments tab uses `EduDataTable<Department>`. Avatar radius 16 on all tabs.
- **Department delete button:** Each department list item now has a delete icon (`Icons.delete_outline_rounded`, size 16, error color at 0.45 alpha) between the text content and the chevron. Tapping it shows an `AlertDialog` confirmation ("Delete \"{name}\"?" / "This department will be permanently removed.") with Cancel and Delete actions. On confirmation, calls `DepartmentsDao.deleteDepartment(schoolId, name, accountId: userId)` using the active account from `cache.currentUser`. The `_confirmDeleteDepartment` method lives in `_DepartmentsTabState`.
- **Department detail redesign (Task 06):** `_DepartmentDetailScreen` now has 3 tabs (All, Teachers, Staff) using `EduTabBarBottom` with `isScrollable: true` and left-aligned padding. The "All" tab uses `_DeptAllMemberList` — a dual-`StreamBuilder` widget combining teacher and staff streams into a single list with role-tag chips and per-item remove buttons. Per-tab "Assign" buttons removed from `_DeptMemberList` (the `onAssign` parameter no longer exists); replaced by a single `FloatingActionButton.small` on the Scaffold. The FAB opens `_AssignMemberSearchSheet` — a stateful bottom sheet with a search `TextField` (name/phone), Teachers/Staff toggle via `_DeptFilterChip` widgets, and a filtered list of unassigned members. Tapping an item assigns them to the department and closes the sheet. `_DeptMemberList` count header simplified to plain text (no longer a row with assign button).
- **Academics — Stream tabs + Content tabs pattern:** `GradeDetailPage` uses a two-layer tab architecture. Layer 1 (Stream Tabs): "Comparisons" + one tab per stream. Layer 2 (Content Tabs, visible only on a specific stream): Students · Exams · Subjects · Attendance · Timetable · Lessons · Teachers. Both layers use `EduTabBar` with `isScrollable: true`.
- **Academics — Back button convention:** All push-route pages in academics (`GradeDetailPage`, `StudentGradePage`, `ExamDetailPage`, `PaperDetailPage`) use `Icons.chevron_left_rounded` (size 24), title `fontSize: 15, fontWeight: w500, color: cs.onSurface`, and `elevation: 0, scrolledUnderElevation: 0, backgroundColor: cs.surface`. **Exams shell back buttons:** All in-shell back buttons in `exams_grades_screen.dart` (`_ExamGroupDetailView` header and `_PaperDetailView` via `_SectionHeader`) use `Icons.chevron_left` (size 22) — not `Icons.arrow_back`. The `_SectionHeader` widget renders `leadingAction!.icon` at size 22. Do NOT use `Icons.arrow_back` anywhere in the exams shell.
- **Academics — FAB action sheet pattern:** `GradeDetailPage` FAB is context-sensitive per content tab. Single-action tabs (Students, Exams, Subjects, Timetable, Lessons) execute the action directly on tap. Multi-action tabs (Teachers: Assign Class Teacher + Assign Subject Teacher) open a bottom sheet action picker. Attendance tab has no FAB (marking is inline). FAB animates in/out via `ScaleTransition` when switching to/from Comparisons tab, and bounces subtly when switching between content tabs. Create Exam sheet (`_CreateExamFromGradeSheet`) pre-fills grade and stream from context.
- **Academics — Attendance marking is embedded:** Attendance marking lives exclusively in `AttendanceTab` inside the grade detail page. The standalone `AttendanceScreen` for teacher/owner roles redirects to `GradeDetailPage` with `initialContentTabIndex = 3`.
- **Academics — Shared widgets used:** `ThinProgressBar` (`ui/widgets/thin_progress_bar.dart`) for percentage bars, `TrajectoryIndicator` (`ui/widgets/trajectory_indicator.dart`) for stream/student trend display, `StatBadge` (`ui/widgets/stat_badge.dart`) for compact stat chips. Color utilities from `core/academic_utils.dart` (`percentageColor`, `masteryColor`, `formatPercent`, `formatScore`, `formatDateFromDays`).
- **Academics — Empty state consistency:** All tabs use centered column pattern: 52×52 rounded container (radius 10, `surfaceContainerHighest` alpha 0.5) + icon (22px, `onSurfaceVariant` alpha 0.3) + 18px gap + title (13.5px, w500, `onSurface`) + 5px gap + subtitle (12px, w400, `onSurfaceVariant` alpha 0.5). Loading states: centered 20×20 `CircularProgressIndicator(strokeWidth: 2)` with `onSurfaceVariant` alpha 0.4.
- **Academics — Ranking animation:** Comparisons tab ranking table uses staggered fade-in + slide animation (50ms delay per row, 200ms duration, `Curves.easeOut`). Top-ranked stream gets a subtle gold tint (`#FFA726` at 6–8% alpha).

## Last Updated
Task G2+G6 (Bug Sweep Round 2) — `academics/grade_detail_page.dart`: Two fixes. **(G2)** Timetable and Lessons FAB stubs removed. In `_actionsForContentTab`, logical indices 4 (Timetable) and 5 (Lessons) now fall through to the default `_ => []` case instead of returning stub `_FabAction` entries that called `_showStubSnackbar`. The FAB no longer appears on these tabs. `_showStubSnackbar` method removed (was only used by those two stubs). **(G6)** Empty streams guard added at top of `build()`. If `widget.grade.streams` is empty, returns a `Scaffold` with `EduEmptyState` (icon: `view_stream_outlined`, title: "No streams configured", subtitle: "No streams have been set up for this grade yet.") instead of proceeding to tab builders that would throw `StateError` on `streams.first`. New import: `edu_empty_state.dart`. No other files changed.

Task G5 (Bug Sweep) — `roles/school_roles_screen.dart`: Hid the Delete button from the role row actions. Both `_deleteRole` and `_purgeRole` called the same `_dao.deleteRole()` DAO method (hard delete), making the two buttons redundant. **Fix:** Removed Delete `EduDataTableAction` from actions list (was gated by `canDelete`). Removed unused `canDelete` variable. Only Purge remains, gated to `_isSuper` (`UserLevel.super_`). `_deleteRole` retained as empty stub with `// ignore: unused_element` and TODO for future soft-delete. Old TODO inside `_purgeRole` removed. No new imports or dependencies.

Task G8 (Bug Sweep Round 2) — Added "Next Class" countdown and "Upcoming Exams" sections to `guardian_overview.dart`, matching parity with `student_overview.dart`. **Changes in `student_overview.dart`:** Made `_StudentNextClass` → `StudentNextClass` and `_StudentUpcomingExams` → `StudentUpcomingExams` (removed `_` prefix to make public). Updated `_StudentUpcomingExamsState` to reference `StudentUpcomingExams` in `State<>`, `createState()`, and `didUpdateWidget()`. Internal usage in `StudentOverview.build()` updated to use the new public names. **Changes in `guardian_overview.dart`:** Added `StudentNextClass` widget after the ward attendance status section (section 1c), gated by `term != null && termContext.isCurrentTermActive`, passing `ward.adm` as `studentAdm`. Added `StudentUpcomingExams` widget after the "Recent Grades" section (section 6b), gated by `term != null`, with `SectionTitle` "Upcoming Exams" and `onViewAll` pointing to `'Progress'` tab. No new imports needed — `student_overview.dart` was already imported.

Task G10 (Bug Sweep Round 2) — Removed `announcements.read` permission guard from "Recent Announcements" section in `staff_overview.dart`. Previously the section was wrapped in `if (schoolContext.permissions.can(Resource.announcements, Action.read))`, hiding it from staff without that permission. But the Announcements nav tab is always visible for staff (per Task C6), creating an inconsistency. **Fix (part 1):** Removed the `if` condition wrapper, making the "Recent Announcements" section render unconditionally — matching the always-visible Announcements tab. Comment updated from "permission-gated" to "always visible — matches Announcements tab". **Fix (part 2):** Removed `perms.can(Resource.announcements, Action.read)` from the `hasAnyPermission` check at the top of `StaffOverview.build()`. Since announcements are shown unconditionally, having only `announcements.read` should not count as a "meaningful permission" that bypasses the "no roles assigned" empty state — the user can still navigate to the always-visible Announcements tab directly. No new imports or dependencies.

Task F1 (Bug Sweep Round 2) — Eliminated FutureBuilder-in-StreamBuilder anti-pattern in `owners_tab.dart`, `teachers_tab.dart`, `staff_tab.dart`. All three member tabs previously used `StreamBuilder` wrapping `FutureBuilder` for batch-loading user data. Every stream emission created a NEW Future, causing `FutureBuilder` to briefly enter `ConnectionState.waiting` → visual flicker during rapid sync. **Fix:** Replaced `FutureBuilder` entirely with a stale-while-revalidate pattern. Each tab state now holds `Map<String, UsersData> _userMap` and `Set<String> _lastUserIds` as cached state fields. A new `_refreshUsersIfNeeded()` (owners/staff) / `_refreshUsers()` (teachers) method compares the current member ID set against `_lastUserIds` — if unchanged, returns immediately (no re-fetch). If changed, fires an async `.get().then(setState)` query while continuing to render the stale `_userMap`. This eliminates the `ConnectionState.waiting` flicker entirely. The `_batchLoadUsers()` async method and `FutureBuilder` widget are removed from all three files. No new imports or dependencies added.

Task F2 (Bug Sweep Round 2) — Reactive enrolled students in `paper_detail_page.dart`. Replaced one-shot `_loadStudents()` Future (`_dao.getEnrolledStudents(...)`) with a reactive `StreamSubscription<List<StudentsData>>` (`_studentsSub`) that listens to the new `_dao.watchEnrolledStudents(...)` stream. If a student is enrolled/unenrolled during a multi-hour grading session, the spreadsheet now reflects the change automatically via `setState`. New state field: `_studentsSub` (`StreamSubscription<List<StudentsData>>?`). New method: `_subscribeStudents()` (replaces `_loadStudents()`). `_subscribeStudents()` called in `initState()`. `_studentsSub` cancelled in `dispose()`. New DAO method: `ExamsGradesDao.watchEnrolledStudents()` in `exams_grades_dao.dart` — mirrors `getEnrolledStudents()` but uses `.watch()` instead of `.get()`, returning `Stream<List<StudentsData>>`.

Task D1 (Bug Sweep Round 2) — Defense-in-depth permission guard added to `mpesa_config_screen.dart`. New `_hasPermission` getter checks `entry is OwnerEntry || perms.can(Resource.schools, Action.update)`. Applied in three places: (1) `build()` returns `RestrictedAccessState` for unauthorized users, (2) `_save()` early return, (3) `_delete()` early return. `material.dart` import updated to `hide Action`. New imports: `models/membership.dart` (`OwnerEntry`), `models/permissions.dart` (`Resource`, `Action`), `../exams/exams_shared.dart` (`RestrictedAccessState`).

Task D2 (Bug Sweep Round 2) — Defense-in-depth permission checks for Settings and Roles screens. **Settings:** `school_settings_screen.dart` `_save()` now has an early-return permission guard — checks `entry is! OwnerEntry && !perms.can(Resource.schools, Action.update)` before proceeding with the save. The `build()` method already had this guard (from Task E01); D2 extends it to the write path. **Roles:** `school_roles_screen.dart` `_SchoolRolesBodyState.build()` now has a defense-in-depth guard at the top — non-owner users without `Resource.roles` / `Action.read` permission see a centered lock icon + "Access restricted" + "You don't have permission to view roles." message instead of the roles list. Matches the pattern used in `SchoolSettingsScreen`. No new imports needed — `OwnerEntry`, `Resource`, `Action` were already imported in both files.

Task D3 (Bug Sweep Round 2) — Owner self-removal button hidden in members tab. `owners_tab.dart`: In `_OwnersTabState.build()`, added `currentUserId` lookup via `cache.currentUser?.user.id` and `isSelf` check (`owner.user == currentUserId`) per owner row. `_OwnerRow` now receives `canDelete: _canDelete && !isSelf` — when the owner is the current user, the Remove action is hidden from both the desktop inline actions and the mobile/desktop `_OwnerInfoSheet`. The mobile `Dismissible` wrapper is also skipped entirely for the current user's row (`|| isSelf` added to the guard condition). Previously, tapping "Remove" on yourself showed a confusing error snackbar on desktop and was silently swallowed on mobile. No new imports needed — `cache` was already imported via `client.dart`.


Previous:
Tasks C1+C8 (Round 2) — Timetable term reactivity fix + generation permission gaps in `timetable_screen.dart`. **C1 fix:** `_OwnerTimetableShellState` now tracks `_lastYear` and `_lastTerm` in state, set during `initState`. Added `didUpdateWidget` override that compares current term year/term against stored values — on change, cancels `_hasTimetableSub`, resets `_hasTimetable` to false, clears `_rules`, and re-calls `_subscribeHasTimetable()` + `_loadConfig()`. This prevents stale FAB state and wrong-term rule usage after term switch. `_ClassTimetableViewState.didUpdateWidget` (student/guardian view) expanded from checking only `studentAdm` to also checking `termContext.currentTerm?.year` and `termContext.currentTerm?.term` — on term change, re-calls `_loadData()` to re-query enrollment for the new term. **C8 fix:** Split `canManage` into three distinct permission flags: `canManage` (classes.update — for Rules/Edit Slots), `canGenerate` (classes.update AND classes.delete — for Generate Timetable FAB, since generation first clears all entries), `canGenerateLessons` (classes.update AND lessons.create — for Generate Lessons FAB, since it creates records in the lessons table). All three include OwnerEntry bypass. FAB visibility updated: Generate Timetable gated by `canGenerate`, Generate Lessons gated by `canGenerateLessons`. Overall FAB column visibility = `canGenerate || canManage || canDelete`. No new imports needed.

Previous:
Task C4 (Round 2) — Scope exam creation grades to teacher assignments in `exam_creation_page.dart`. Previously, `ExamCreationPage` built grade tabs from `widget.config.curricula`, showing every grade/stream in the school. A teacher with `exams.create` permission could create exams for ANY grade, including grades where they have zero assignments. **Fix:** Added `_teacherAssignedGrades` (`Set<int>?`) and `_loadingTeacherGrades` (`bool`) state fields. Refactored `initState` into two paths: for `TeacherEntry`, starts with a placeholder empty `TabController` and calls `_fetchTeacherGrades()` which does a one-shot read of `MembersDao.watchTeacherSubjectsForTerm()` to derive the set of grades the teacher is assigned to; for non-teacher entries, calls `_initGradeTabs()` directly (showing all grades). `_initGradeTabs()` (extracted from old `initState` logic) filters `config.curricula` by `_teacherAssignedGrades` when non-null. After teacher grades load, disposes placeholder controllers and rebuilds filtered tabs via `setState`. In `build()`, the CLASS section now shows: (1) a `CircularProgressIndicator` while `_loadingTeacherGrades` is true; (2) a centered empty state with school icon + "No assigned grades" + explanatory text when the teacher has zero subject assignments for the current term; (3) the normal grade tabs + stream sub-tabs + paper timetable otherwise. Owners and staff with `exams.create` continue to see all grades (unchanged behavior). New DAO dependency: `MembersDao.watchTeacherSubjectsForTerm()` (already imported). Pattern matches `ExamGroupDetailView._subscribeToTeacherSubjects()` for consistency.

Previous:
Task C5 (Round 2) — Cache teacher overview streams to prevent re-subscription churn in `teacher_overview.dart`. Five widgets had inline stream/DAO creation inside `build()`, causing new stream references (and StreamBuilder re-subscription) on every parent rebuild. **Fixes:** (1) Extracted the attendance status `StreamBuilder` from `TeacherOverview` (StatelessWidget) into a new `_TeacherAttendanceCard` StatefulWidget — caches `AttendanceDao`, the attendance stream, and the `date` (days-since-epoch) as state fields initialized in `initState()`. Date is recomputed in `didUpdateWidget` on prop changes (handles midnight boundary if widget is recreated with new term props). (2) `_TeacherNextClass`: added `late Stream<List<TimetableData>> _timetableStream` state field built via `_buildTimetableStream()` in `initState()`; `didUpdateWidget` now checks all four props (`schoolId`, `year`, `term`, `teacherUserId`) instead of only `schoolId`. (3) `_TeacherTodaySchedule`: same pattern — cached `_timetableStream` in state, `didUpdateWidget` checks all four props. (4) `_TeacherClassChips`: added `late Stream<List<ClassTeacher>> _classTeacherStream` cached in state via `_buildClassTeacherStream()`; removed inline `MembersDao(db)` creation from `build()`; `didUpdateWidget` checks `schoolId` and `userId`. (5) `_TeacherUpcomingExams`: added `late Stream<List<SubjectTeacher>> _subjectTeacherStream` and `late Stream<List<ExamWithPapers>> _examsStream` cached in state; removed inline `ExamsGradesDao(db)` and `MembersDao(db)` creation from `build()`; `didUpdateWidget` checks `schoolId`, `userId`, `term.year`, `term.term`. `_TeacherQuickStats` was already correct (uses manual stream subscriptions cached in `_subs`). No new imports needed.

Previous: Task C2 (Round 2) — Member tab swipe-to-dismiss silent failure fix in `owners_tab.dart`, `teachers_tab.dart`, `staff_tab.dart`. All three member tabs' `Dismissible.confirmDismiss` callbacks called `service.removeOwner()`/`removeTeacher()`/`removeStaff()` but discarded the `Result` return value. If the operation failed (e.g. `cannotRemoveSelf`, `permissionDenied`, `databaseError`), no error feedback was shown — the user tapped "Remove", confirmed, and nothing visibly happened. **Fix:** Captured the `Result` in all three `confirmDismiss` callbacks and added `if (result case Err(:final error) when context.mounted)` guard that shows a floating `SnackBar` with `'Failed to remove <role>: $error'`. Also fixed two info-sheet `_confirmRemove` methods that discarded the Result: `_TeacherInfoSheetState._confirmRemove` (now captures Result, shows error SnackBar on `Err`, only pops sheet on `Ok`) and `_StaffInfoSheet._confirmRemove` (same pattern). Error type is `MemberActionError` enum (not `GrpcError`), so `toFriendlyMessage()` is not applicable — uses `$error` string interpolation matching the existing pattern in `_OwnerRow._confirmRemoveOwner`, `_OwnerInfoSheet._confirmRemoveOwner`, `_TeacherRow._confirmRemove`, and `_StaffRow._confirmRemove` which already handled the Result correctly. No new imports needed — `models/result.dart` was already imported in all three files.

Previous: Task C3 (Round 2) — Cache `ExamsListView` Drift stream to prevent re-subscription churn in `exam_list_view.dart`. The `StreamBuilder` inside `_ExamsListViewState.build()` called `_buildStream()` inline, creating a new Drift stream object on every widget rebuild. Any `setState` call (search toggle, filter chip, typing) caused unsubscribe→resubscribe, briefly showing a loading spinner. **Fix:** Added `late Stream<List<ExamGroup>> _examStream` state field, initialized in `initState()` via `_buildStream()`. Added `didUpdateWidget()` override that rebuilds the stream only when `schoolId`, `year`, `term`, or `entry` change. Search query and type filter changes trigger `setState` but do NOT rebuild the stream — filtering is already applied client-side via `_applyFilters()` on the snapshot data. `StreamBuilder` now references the cached `_examStream` field instead of calling `_buildStream()` inline.

Previous: Task C7 (Round 2) — Staff Academics tab missing `students.read` as visibility gate in `school_dashboard_screen.dart`. The Academics nav item for `MembershipRole.staff` checked `classes.read` OR `attendance.read/mark` but NOT `students.read`. A staff member with only `students.read` permission could not access the Academics screen to see students organized by grade/stream. **Fix:** Added `perms.canAny(Resource.students, [Action.read])` as a third OR condition in the staff Academics tab visibility gate in `_itemsForRole()`. Single fix location — `_itemsForRole` is shared by both mobile and desktop layouts. No new imports needed.

Previous:
Task B4 (Round 2) — Grade detail content tabs permission gating in `grade_detail_page.dart`. Previously only the Students tab was gated (behind `students.read`); Exams, Subjects, Attendance, Timetable, Lessons, and Teachers tabs were always rendered regardless of permissions. A staff member reaching the Academics screen via `attendance.mark` alone could see all tab data. **Fix:** Added 4 new `late final bool` visibility fields (`_canSeeExams`, `_canSeeClasses`, `_canSeeLessons`, `_canSeeAttendance`) computed in `initState` from RBAC permissions. `_canSeeExams` = `exams.read || isOwner || isTeacher`. `_canSeeClasses` = `classes.read || isOwner || isTeacher` (gates Subjects, Timetable, Teachers tabs). `_canSeeLessons` = `lessons.read || isOwner || isTeacher`. `_canSeeAttendance` = `attendance.read || attendance.mark || isOwner || isTeacher`. `_canSeeStudents` updated to include `|| isOwner || isTeacher`. Teachers and owners always see all tabs (their membership implies full class-level visibility). `_contentTabs` list and `TabBarView` children now use `if` guards on all 7 tabs. Added `late final List<int> _visibleToLogical` mapping (parallel list mapping visible tab indices to logical indices 0–6). `_logicalContentIndex(visible)` updated from the old `_canSeeStudents ? visible : visible + 1` formula to `_visibleToLogical[visible]`, supporting arbitrary hidden tabs. `initialContentTabIndex` resolution updated to use `_visibleToLogical.indexOf(logicalInit)` instead of the old offset arithmetic. No new imports needed — `membership.dart` (`OwnerEntry`, `TeacherEntry`) and `permissions.dart` (`Resource`, `Action`) were already imported.

Previous: Task B6 — OwnerEntry FAB bypass in `members_page.dart`. `_canCreateForCurrentTab()` previously used strict `perms.can(...)` checks without any OwnerEntry bypass, meaning an owner without explicit role/scope assignments saw all 6 member tabs (via the existing `_computeVisibleTabs` OwnerEntry bypass) but no FAB to create members. Fixed by adding `final entry = widget.schoolContext.currentEntry.value; if (entry is OwnerEntry) return true;` at the top of `_canCreateForCurrentTab()`, before the permission switch. Consistent with the OwnerEntry bypass pattern used in finance, roles, and announcements screens. `OwnerEntry` was already available via the existing `models/membership.dart` import.

Previous: Task A3b — Privilege escalation fix in school role create/edit screens (`school_roles_screen.dart`, `school_role_detail_screen.dart`). Previously, any user with `Roles.Create` or `Roles.Update` could create or edit a role granting ALL permissions — then assign it to themselves for full privilege escalation. **Fix A (UI):** Permission toggles are now filtered against the editor's own permissions. `_RoleFormSheet` gains `required SchoolContext schoolContext` parameter; `_PermExpandedActions` gains optional `canEditPermission` callback (`bool Function(Resource, Action)?`). When the editor lacks a permission, the toggle is disabled (greyed out with 0.4 opacity, lock icon, `Switch.onChanged: null`). `_togglePermission`, `_toggleResourceAll`, and `_toggleSelectAll` all guard against toggling permissions the editor doesn't hold. Owners (`OwnerEntry`) bypass all checks. Same pattern applied to `_PermissionsTab` and `_ExpandedPermissions` in the detail screen — both gain `SchoolContext schoolContext` parameter, and `_ExpandedPermissions` gains `canEditPermission` callback. `_ResourceRow` forwards the callback. **Fix B (Save):** Both `_RoleFormSheetState._save()` and `_PermissionsTabState._save()` validate that the resulting permission map is a subset of the editor's own permissions before writing. Non-subset attempts show a snackbar/error message and abort. Owners skip the check.

Previous: Task A4 — Security fix in `student_detail_page.dart`: `StudentDetailPage` and `StudentDetailSheet` now require `SchoolContext schoolContext` parameter. Edit, Change Status, and Delete action icons in `_StudentDetailSheetState.build()` are gated behind permission booleans computed from `SchoolContext`: `canEdit` = `isOwner || perms.can(Resource.students, Action.update)`, `canDelete` = `isOwner || perms.can(Resource.students, Action.delete)`. Previously all three actions were rendered unconditionally for every user — any staff member with `students.read` (enough to see the Students tab) gained full edit/status-change/delete capabilities. `_StudentRow` in `students_tab.dart` updated to accept and forward `schoolContext` to `StudentDetailSheet` at both call sites (bottom sheet and side sheet). New imports in `student_detail_page.dart`: `models/membership.dart` (`OwnerEntry`), `models/permissions.dart` (`Resource`, `Action`), `models/school_context.dart` (`SchoolContext`). `material.dart` import changed to `hide Action` to avoid conflict with `permissions.dart`'s `Action` enum.

Previous: Task A2 — Security fix in `paper_detail_page.dart`: `_canGradeContent` no longer grants grade write access via `grades.read`. Previously the getter checked `perms.can(Resource.grades, Action.read) || perms.can(Resource.grades, Action.mark)` — meaning any user with only `grades.read` could enter scores, upload answer sheets, and trigger AI marking. Fixed to check only `perms.can(Resource.grades, Action.mark)`. Added `OwnerEntry` bypass (`if (widget.schoolContext.currentEntry.value is OwnerEntry) return true;`) to align with the existing bypass in `ExamGroupDetailView._canMarkGrades`. The teacher-specific fallback (exam creator / invigilator / subject teacher check) remains untouched.

Previous: Tasks C1, C3, C4, C6 — School dashboard nav fixes in `school_dashboard_screen.dart`:

**(C1 — Teacher core 4)** Moved `Academics` and `Exams` from permission-gated section into the always-visible core for `MembershipRole.teacher`. Teacher nav items are now: Overview | Academics | Exams | Timetable (core 4, always visible), followed by permission-gated Members, Finance, Announcements (always visible), Roles. Previously Exams required `exams.read` and Academics required `classes.read` — both are now unconditional. The `ExamsGradesScreen` and `AcademicsScreen` already contain teacher-specific filtering logic (teacher filter, assigned classes/subjects scoping) that handles teachers without explicit read permissions.

**(C3 — Staff Academics alt gate)** For `MembershipRole.staff`, the Academics nav item gate expanded from `perms.canAny(Resource.classes, [Action.read])` to also include `perms.canAny(Resource.attendance, [Action.read, Action.mark])`. Staff with attendance permissions but no `classes.read` can now navigate to the Academics section (which contains the attendance tab inside grade detail pages).

**(C4 — Staff Settings nav item)** Added a permission-gated Settings nav item for `MembershipRole.staff`: `if (perms.can(Resource.schools, Action.update))`. The `SchoolSettingsScreen` already handles non-owner access correctly (defense-in-depth guard from Task E01). Staff with `schools.update` permission now have a navigation path to school settings.

**(C6 — Always-visible Announcements)** Removed the `perms.canAny(Resource.announcements, [Action.read])` gate on the Announcements nav item for both `MembershipRole.teacher` and `MembershipRole.staff`. Announcements are now always visible for all roles (matching student/guardian treatment). The `AnnouncementsScreen` already handles routing internally: users with admin permissions get `_AdminFeed`, others get `_RoleFeed` filtered to their audience bit.

Previous: Task C5 — Staff/Teacher announcements routing fix. In `announcements_screen.dart`, added `Action.read` to the `canAny(Resource.announcements, [...])` check for both `StaffEntry` and `TeacherEntry` routing. Previously, a staff/teacher with only `announcements.read` permission was routed to `_RoleFeed` (audience-filtered) instead of `_AdminFeed` (all announcements). Now `Action.read` is included alongside `Action.create`, `Action.update`, `Action.delete` in the admin routing gate.

Previous: Task B5 — Owner RBAC bypass in Finance action buttons. In `finance_screen.dart`, added `isOwner` (`entry is OwnerEntry`) fallback to all permission checks within the three finance tabs: `_InvoiceListView.build()` (`canRecord`, `canEditInvoice`, `canDeleteInvoice`), `_PaymentsTab.build()` (`canApprove`, `canEdit`, `canDelete`), `_FeesTab.build()` (`canCreateFee`). Owners now see all action buttons regardless of scope assignments. No-op handlers for Invoice edit/delete and Payment approve/edit/delete replaced with "Coming soon" `SnackBar` + `// TODO: implement` comments. `OwnerEntry` was already imported via `models/membership.dart`.

Previous: Task B4 — Owner RBAC bypass in Roles screen CRUD. In `school_roles_screen.dart`, `canCreate`, `canEdit`, `canDelete` now include `isOwner` check (`entry is OwnerEntry`) so a freshly added owner with no scope assignments can still create/edit/delete roles (chicken-and-egg fix). Added `import '../../../../models/membership.dart'` for `OwnerEntry`.

Previous: Task B3 — Owner RBAC bypass in `MembersPage._computeVisibleTabs`. Added early return at the top of `_computeVisibleTabs()`: when the current entry is `OwnerEntry`, returns `_MemberTab.values` (all 6 tabs) without checking permissions.

Previous: Task B6 — Verified owner RBAC bypass in Announcements admin actions (already correctly implemented with `entry is OwnerEntry` checks on `canCreate`, `canEdit`, `canDelete`).

Previous: Task A3 — Remove guardian/student payment capability from `_GuardianFinanceView` (read-only finance enforcement).

Previous: Task G7 — Remove My Classes page from teacher dashboard.

**(G7 — Remove My Classes page)** `school_dashboard_screen.dart`: Removed `import 'my_classes/my_classes_screen.dart'`. Removed `'My Classes'` nav item from the teacher core items in `_itemsForRole()`. Teacher core items are now: Overview, Timetable (was 3 items, now 2). Removed `'My Classes'` from `_kAcademicNavLabels` set. Removed the `if (item.label == 'My Classes')` content panel mapping in `_buildContentPanel()`. Deleted the entire `lib/ui/screens/school_dashboard/my_classes/` directory (`my_classes_screen.dart`). The teacher's Overview page already displays class teacher assignments and subjects taught, making My Classes redundant.

Previous: Task G3 — Remove standalone Attendance page from all dashboard roles.

**(G3 — Remove standalone Attendance page)** `school_dashboard_screen.dart`: Removed `import 'attendance/attendance_screen.dart'`. Removed `Attendance` nav item from all 5 role branches in `_itemsForRole()`: Owner (hardcoded const), Teacher (core section), Staff (permission-gated `attendance.read`/`mark` block), Student (hardcoded const), Guardian (hardcoded const). Removed `'Attendance'` from `_kAcademicNavLabels` set. Removed the `if (item.label == 'Attendance')` content panel mapping in `_buildContentPanel()`. Deleted the entire `lib/ui/screens/school_dashboard/attendance/` directory (`attendance_screen.dart`). The `AttendanceTab` at `academics/tabs/attendance_tab.dart` is unaffected — it remains the primary attendance surface inside Grade Detail Page.

Previous: Task G5 — Show "no roles assigned" empty state for staff with no permissions.

**(G5 — Staff empty state)** `overview_screen.dart`: `_StaffOverview.build()` now checks `hasAnyPermission` at the top of the method — a union of `_hasAnyStatPermission(perms)` (7 read permissions on students, teachers, staff, fees, payments, exams, classes) plus `announcements.read`, `roles.read`, `attendance.read`, and `attendance.mark`. When `!hasAnyPermission`, returns a centered empty state with a shield icon (56×56, `surfaceContainerHighest` bg, `borderRadius: 14`), "No roles assigned" heading (15px w500), and explanatory body text (13px w300, `height: 1.4`) — replacing the previous minimal welcome card + italic hint. Staff WITH at least one permission see the existing overview unchanged (welcome card, permission-gated quick stats, announcements).

Previous: Task G1 — Force content area rebuild on role switch.

**(G1 — Role-dependent KeyedSubtree)** `school_dashboard_screen.dart`: In `_DashboardShellState.build()`, the `KeyedSubtree` wrapping the content area changed from `const ValueKey('dashboard-content')` to `ValueKey('dashboard-content-${currentEntry.role}')`. The constant key preserved widget element identity across role changes, preventing Flutter from rebuilding the subtree when the user switched roles (e.g. Owner → Teacher). The role-dependent key forces a full tear-down and rebuild of the content subtree on role change, while preserving state for same-role entry switches (e.g. Guardian ward A → ward B). `_UnifiedMobileTabBar` and `_PillTabStrip` are both `StatelessWidget` — they rebuild correctly with new items/controller and require no changes.

Previous: Tasks F04, F12 — Term null safety fix, dead code cleanup, and minor consistency fixes.

**(F04 — Term null safety)** `active_term_context.dart`: `ActiveTermContext` constructor now auto-selects `allTerms.first` as fallback when `initialTerm` is null but `allTerms` is non-empty. Previously, `currentTerm` could be null even when `hasTerms` was true (terms exist but none marked active and `getMostRecentTerm` returns null). This ensures academic sections always have a term to work with when terms exist.

**(F12 — Dead code cleanup in school_dashboard_screen.dart)** Removed dead `'Exams & Grades'` string from `_kAcademicNavLabels` set (nav label is always `'Exams'` after Task 01 rename). Simplified `_buildContentPanel` Exams branch from `item.label == 'Exams & Grades' || item.label == 'Exams'` to just `item.label == 'Exams'`.

**(F12 — my_classes_screen.dart assertion)** Added `assert(entry is TeacherEntry, 'My Classes should only be shown to teachers')` before the `userId` extraction. The screen already handles the non-teacher case gracefully (shows empty state), but the assertion documents the contract and catches routing bugs in debug mode.

**(F12 — overview_screen.dart cache.currentUser fix)** `_TeacherOverview.build()`: Changed `userId` from `cache.currentUser?.user.id ?? ''` to `entry.teacher.user`. The `entry` parameter already carries the teacher's user ID — using `cache.currentUser` was fragile (could theoretically differ in multi-account scenarios) and unnecessary since the `TeacherEntry` is available.

**(F12 — members_page.dart duplicate department check)** `_CreateDepartmentSheetState._save()`: Added duplicate name check via `widget.dao.getDepartment(schoolId, name)` before creating. If a department with the same name already exists, shows a SnackBar error and returns early instead of hitting a database constraint error.

Previous:
Task F03 — Fix `_RoleFeed` and `_AdminFeed` using `SchoolConfig.defaults()` instead of actual school config.

**(F03 — SchoolConfig fix in announcements)** `announcements_screen.dart`: Both `_AdminFeedState` and `_RoleFeedState` previously used `SchoolConfig.defaults()` (empty config) for grade/stream labels in announcement rows and detail sheets. Fixed by adding a shared top-level `_buildConfigFromStreams(List<SchoolStream>)` helper (same curriculum-detection heuristic used in attendance, exams, and timetable screens: grades ≥41 → 8-4-4, grades ≥9 → CBC, ambiguous 1–8 resolved by checking for 8-4-4-only grades). Both states now hold a `CatalogDao`, a `SchoolConfig _config` field, and a `StreamSubscription<List<SchoolStream>>? _configSub`. Config is loaded reactively in `initState` via `_catalogDao.watchAllStreamsForSchool(_schoolId).listen(...)` and disposed in `dispose()`. `_AdminFeedState._loadConfig()` changed from empty async stub to real reactive subscription. `_RoleFeedState._buildAnnouncementsFeed()` now passes `_config` instead of `SchoolConfig.defaults()`. New imports: `dart:async`, `database/daos/catalog_dao.dart`, `database/tables/curriculum_subjects.dart`.

Previous:
Task E02 — Defense-in-depth permission guards for AcademicsScreen and ExamsGradesScreen.

**(E02 — AcademicsScreen guard)** `academics_screen.dart`: Added defense-in-depth entry-type guard at the top of `AcademicsScreen.build()`. `StudentEntry` and `GuardianEntry` are now blocked with an `EduEmptyState` widget (lock icon + "Access restricted" + "This section is for school administrators."). Nav routing should already prevent these roles from reaching the screen, but the guard provides a safety net. New import: `ui/widgets/edu_empty_state.dart`.

**(E02 — ExamsGradesScreen staff guard)** `exams_grades_screen.dart`: Added staff permission guard after the existing student/guardian guard in `ExamsGradesScreen.build()`. Staff without `Resource.exams` / `Action.read` permission now see `_RestrictedAccessState` instead of the exam shell. The existing write-action gating within `_ExamsShell` was already properly implemented via `_canCreateExam` (checks `exams.create`), `_canEditExam` (checks `exams.update`), `_canDeleteExam` (checks `exams.delete`), and `_canMarkGrades` (checks `grades.mark`) — all enforce permission checks for staff and teachers before allowing mutations. No additional write-action gating needed.

Previous:
Tasks D01, E06 — Reactive permissions and DAO-based permission loading in school dashboard.

**(E06 — DAO-based permission loading)** `school_dashboard_screen.dart`: `_initializeSession()` no longer uses raw `db.select(db.scopes)` and `db.select(db.roles)` queries. Replaced with `SchoolScopesDao(db).getAggregatedPermissions(schoolId, userId, userLevel)` which encapsulates the full permission computation: loading school-scoped scopes, loading system-scoped scopes for elevated users (`UserLevel.system`/`super_`), parsing all role permission blobs via `parsePermissionsBlob`, and unioning them into a `SchoolPermissions` object. Removed imports: `package:drift/drift.dart`, `core/permission_parser.dart`, `database/tables/enums.dart`. Added import: `database/daos/school_scopes_dao.dart`.

**(D01 — Reactive permissions)** `school_dashboard_screen.dart`: `_SchoolDashboardScreenState` now mixes in `WidgetsBindingObserver`. After initial permission load, subscribes to `SchoolScopesDao.watchAggregatedPermissions(schoolId, userId, userLevel)` — a Drift reactive stream that re-emits whenever any matching scope or role row changes via sync deltas. On permission change: preserves the current `MembershipEntry`, disposes old `SchoolContext`, creates a new one with updated permissions, and calls `setState`. Also re-loads permissions on `AppLifecycleState.resumed` as a fallback via `_reloadPermissions()`. New fields: `_permissionsSub` (`StreamSubscription<SchoolPermissions>?`). `_DashboardShellState` gains `didUpdateWidget` override that detects when the `SchoolContext` object changes (identity check), re-wires the `currentEntry` listener from old to new context, and recomputes nav items with new permissions (resets `_tabController`, `_currentItems`, `_selectedIndex`).

Previous:
Tasks D05, E01 — N+1 batch query fix in members page, DAO-in-build fix in overview, permission guard in settings screen.

**(D05 — Fix N+1 queries in `_batchLoadUsers` and DAO creation in `build()`)** `members_page.dart`: All three `_batchLoadUsers` methods (`_OwnersTabState`, `_TeachersTabState`, `_StaffTabState`) replaced N+1 per-user `findUserById` loop with a single batch query: `db.select(db.users)..where((t) => t.id.isIn(userIds))`. Collects unique user IDs from the member list, runs one `SELECT … WHERE id IN (…)` query, and builds the `Map<String, UsersData>` from the result. Early-returns empty map when `userIds` is empty. `overview_screen.dart`: `_OwnerQuickStats` converted from `StatelessWidget` to `StatefulWidget`. DAO instances (`MembersDao`, `EnrollmentsDao`) moved from `build()` to `initState`-level `late final` fields (`_membersDao`, `_enrollmentsDao`), preventing new DAO allocation on every rebuild. All `StreamBuilder` references updated from `membersDao`/`enrollmentsDao` to `_membersDao`/`_enrollmentsDao` and from `schoolId`/`term` to `widget.schoolId`/`widget.term`.

**(E01 — Add permission guard to SchoolSettingsScreen)** `school_settings_screen.dart`: Defense-in-depth permission guard added at the top of `_SchoolSettingsScreenState.build()`. Reads `widget.schoolContext.permissions` and `widget.schoolContext.currentEntry.value`. If the entry is not `OwnerEntry` AND the user lacks `Resource.schools` / `Action.update` permission, returns a centered Column with lock icon (48px grey), "Access restricted" title, and "You don't have permission to view school settings." subtitle (13px w300 grey). New imports: `models/membership.dart` (`OwnerEntry`), `models/permissions.dart` (`Resource`, `Action`). `material.dart` import updated to `hide Action` to resolve naming conflict with `permissions.dart`. Reactive conversion of the school data form was considered but deferred — the form's text controllers and dirty-tracking logic make mid-edit baseline changes disruptive to UX; sync updates will be reflected on next screen entry.

Previous:
Tasks D02, D03, D04 — Reactivity fixes for entry-dependent screens.

**(D02 — AnnouncementsScreen reactive entry)** `announcements_screen.dart`: `AnnouncementsScreen.build()` now wraps its role-dispatch switch in `ValueListenableBuilder<MembershipEntry>` listening to `schoolContext.currentEntry`. Previously read `schoolContext.currentEntry.value` directly, meaning the screen did not rebuild when the user switched roles via the role switcher. The `entry` variable is now provided by the builder callback instead of a one-shot `.value` read.

**(D03 — TimetableScreen reactive entry)** `timetable_screen.dart`: `TimetableScreen.build()` now wraps its role-dispatch switch in `ValueListenableBuilder<MembershipEntry>` listening to `schoolContext.currentEntry`. Same fix as D02 — previously read `.value` directly, causing the timetable view to not update on role switch. The no-term early return (`_NoTermState`) remains outside the builder since it doesn't depend on the entry.

**(D04 — AttendanceTab ward-switch calendar reset)** `progress_screen.dart`: `_AttendanceTab` now accepts `super.key` in its constructor. The call site in `_ProgressScreenState.build()` passes `key: ValueKey('attendance_${student.adm}')`, forcing a full State rebuild (including `_calendarMonth` reset to current month) when the guardian switches wards. This is necessary because `_AttendanceTabState` stores `_calendarMonth` in local state and had no `didUpdateWidget` override to reset it.

Previous: Tasks B05, B06, B07, C02, C05 — Term-gating removal, teacher announcement scoping, finance tab permission gating, grade/stream filtering, View All links.

**(C02 — Remove term-gating for non-academic sections)** `announcements_screen.dart`: Removed the `termCtx.currentTerm == null` early return in `AnnouncementsScreen.build()`. Announcements are non-academic and do not require a term to display. The `termCtx` is still passed to child widgets for grade/stream enrollment resolution. Removed the now-unused `_NoTermState` class. `finance_screen.dart`: Removed the top-level `termCtx.currentTerm == null` early return in `FinanceScreen.build()`. In `_OwnerFinanceShellState.build()`, replaced the hard `_NoTermState` blocker with a friendly `_EmptyState` message: "No term selected / Select a term to view financial data" (when terms exist) or "No terms configured / Create a term to start managing finances" (when no terms exist). Uses `widget.termContext.hasTerms` to differentiate. In `_GuardianFinanceViewState.build()`, replaced the `_NoTermState` return with a softer `_EmptyState` message: "No term selected / Financial data will appear once a term is active". Removed the now-unused `_NoTermState` class from finance_screen.dart. `school_dashboard_screen.dart`: Verified that `_kAcademicNavLabels` does NOT contain `'Finance'` or `'Announcements'` — no change needed (already correct from previous task B5).

**(B05 — Fix teacher announcements — author-scoped edit/delete)** `announcements_screen.dart`: In `_AdminFeedState.build()`, the global `canEdit` and `canDelete` flags now exclude `TeacherEntry`. Added `final isTeacher = entry is TeacherEntry;` check. `canEdit` is now `!isTeacher && (entry is OwnerEntry || permissions.can(announcements, update))` and similarly for `canDelete`. This means the blanket edit/delete-on-all-rows flag is `false` for teachers. The per-row author check in `_AnnouncementRowState.build()` (which does `widget.canEdit || isAuthor`) still allows teachers to edit/delete their own announcements via the `isAuthor` branch. Owners and staff with permission retain blanket edit/delete on all announcements. The create FAB is unaffected.

**(B06 — Permission-gate staff finance tabs individually)** `finance_screen.dart`: `_OwnerFinanceShellState` now computes a `_visibleTabs` list via `_computeVisibleTabs()` instead of always showing all 4 tabs. Tab visibility rules: Overview requires any finance permission (fees.read or payments.read); Invoices requires fees.read; Payments requires payments.read; Fees requires fees.read. Owners bypass all checks (all 4 tabs visible). `TabController.length` uses `_visibleTabs.length` instead of hardcoded 4. `_onTabChanged` indexes into `_visibleTabs` instead of `_FinanceTab.values`. Added `_tabLabel()` helper and `_buildTabContent()` switch method for dynamic tab rendering. Added `didUpdateWidget` that recomputes visible tabs and recreates the `TabController` if the tab set changes (handles role switching within the same school session). Added static `_tabsEqual` helper for list comparison. Write actions within each tab (create fee, record payment, approve payment, edit/delete) were already individually permission-gated by prior task A09.

**(B07 — Filter announcements by grade/stream for students and guardians)** `announcements_screen.dart`: The `_RoleFeed` widget already resolved enrollment and passed `grade`/`stream` to `AnnouncementsDao.watchAnnouncementsForAudience()` — no changes needed there. `overview_screen.dart`: Added `studentAdm`, `termYear`, and `termNum` optional parameters to `_RecentAnnouncements`. When all three are provided, the widget wraps the announcement query in a `StreamBuilder<Enrollment?>` that resolves the student's enrollment via `EnrollmentsDao.watchStudentEnrollment()`, then passes `enrollment?.grade` and `enrollment?.stream` to the filtered query. Extracted `_buildList()` helper method to avoid code duplication. The `_StudentOverview` and `_GuardianOverview` now pass `studentAdm`, `termYear: term?.year`, and `termNum: term?.term` to their `_RecentAnnouncements` widgets.

**(C05 — Fix "View All" dead links on guardian/student overview)** `overview_screen.dart`: Added `onViewAll` callbacks to `_SectionTitle` widgets across all role overviews. **Student overview:** Today's Schedule → Timetable, Recent Grades → Grades, Attendance → Attendance, Recent Announcements → Announcements. **Guardian overview:** Already had all View All links wired (Timetable, Attendance, Progress, Finance, Announcements) — no changes needed. **Owner overview:** Recent Announcements → Announcements. **Teacher overview:** Today's Schedule → Timetable, My Classes → My Classes, Upcoming Exams → Exams, Recent Announcements → Announcements. **Staff overview:** Recent Announcements → Announcements. All use `DashboardNavigation.goToTab(context, label)`.

Previous:
Task B04 — Permission-gate teacher Timetable tab (admin/teacher view split).

**(B04 — timetable admin/teacher view split)** `timetable_screen.dart`: Narrowed the `TeacherEntry` routing in `TimetableScreen.build()` from `canAny(Resource.classes, [Action.create, Action.update, Action.delete])` to `can(Resource.classes, Action.update)`. Previously, teachers with *any* of create/update/delete on `classes` were routed to the full `_OwnerTimetableShell` (rules configuration, generation wizard, delete FAB, lessons tab). Now only `classes.update` grants the admin view — teachers without it see `_TeacherTimetableView` (read-only weekly schedule of their personal timetable). Inside `_OwnerTimetableShellState.build()`, the `canManage` variable (which gates the wizard FAB and generate-lessons FAB) was also changed from `Action.create` to `Action.update` for consistency. The `canDelete` check (`Action.delete`) and the `StaffEntry` routing (`can(Resource.classes, Action.update)`) were already correct and unchanged. Net effect: teacher view = read-only personal schedule; admin view (owner, or staff/teacher with `classes.update`) = full management UI with rules config, generation, and deletion.

Previous: Task B01 — Scope teacher Academics to assigned classes/subjects only.

**(B01 — teacher scoping in AcademicsScreen)** `academics_screen.dart`: When the current entry is a `TeacherEntry`, the grade/stream tree is now filtered to only show grades/streams where the teacher has assignments via `class_teachers` or `subject_teachers` for the active term. Implementation adds `teacherId` extraction (`entry is TeacherEntry ? entry.teacher.user : null`) after the existing permission checks. When `teacherId` is non-null, two nested `StreamBuilder` widgets reactively watch `db.classTeachers` and `db.subjectTeachers` (filtered by school/year/term/teacher). Distinct `(grade, stream)` pairs from both tables are collected into a `Set<String>` of `"$grade|$stream"` keys, and `allStreams` is filtered to `visibleStreams` containing only matching pairs. The tree body UI is extracted into a new `_buildTreeBody(visibleStreams, allStreams, cs, isDark, ...)` helper method — `visibleStreams` drives the grade tree display while `allStreams` (full school-wide list) is still passed to `_showAddGradeSheet` so that grade creation (if permitted) correctly detects which grades already exist. Non-teacher entries bypass filtering entirely and see all grades/streams. New import: `package:drift/drift.dart` (with `hide Column` to avoid Flutter `Column` conflict). Term context obtained via `ActiveTermProvider.of(context)` — when `currentTerm` is null, an empty tree is shown.

Previous: Tasks B02, C03, C04 — Attendance permission fix, owner routing, and GradeDetailPage tab leak fix.

**(B02 — attendance permission inversion fix)** `attendance_screen.dart`: Added permission guard for `TeacherEntry` in `AttendanceScreen.build()` — teachers without `attendance.mark` or `attendance.read` now see `_AccessDeniedState` instead of `_ClassPickerShell`. Fixed `_needsTeacherFilter` in `_loadClasses()`: changed from `!permissions.can(Resource.attendance, Action.mark)` to `!permissions.canAny(Resource.attendance, [Action.mark, Action.read])`. This preserves the BUG-017 fix (teachers with `attendance.mark` see ALL classes and can mark any of them) while also allowing teachers with only `attendance.read` to see ALL classes (read-only). Teachers without either permission are blocked entirely by the guard.

**(C03 — owner attendance routing)** `attendance_screen.dart`: Owners already see ALL classes (the `_needsTeacherFilter` check only applies to `TeacherEntry`). Made the header subtitle dynamic via a `Builder` widget — owners and staff see "Select a class to manage attendance" (or "…view attendance" if read-only); teachers see "Select a class to mark attendance" (or "…view attendance" if read-only). No structural changes to owner routing — `_ClassPickerShell` remains the shared component, which is functionally correct for now. A more thorough admin attendance dashboard is deferred to a future task.

**(C04 — GradeDetailPage tab leak fix)** `grade_detail_page.dart`: Added `final String? restrictToTab` parameter to `GradeDetailPage`. When set, the content tab bar (`EduTabBar` for `_contentTabs`) is hidden and the content `TabBarView` uses `NeverScrollableScrollPhysics()` to prevent swiping to other tabs. `attendance_screen.dart`: `_navigateToClass()` now passes `restrictToTab: 'Attendance'` when launching `GradeDetailPage`, preventing users from accessing Students, Exams, Subjects, Timetable, Lessons, or Teachers tabs they may not have permission to see. Stream tabs remain navigable (the user can still switch between streams within the grade).

Previous: Task B08 (verification only) — Confirmed grade access RBAC is fully permission-based with no `StaffEntry`/`OwnerEntry` bypass. `_canGradeContent` in `paper_detail_page.dart` uses `perms.can(Resource.grades, Action.read) || perms.can(Resource.grades, Action.mark)`. `_can()` in `grade_detail_page.dart` delegates to `widget.schoolContext.permissions.can(resource, action)`. Both were already fixed by A09; no code changes needed.

Task A09 — Removed `entry is OwnerEntry` blanket RBAC bypass from 5 files across the school dashboard:

**(members_page.dart)** Removed `entry is OwnerEntry ||` from 13 permission checks: `_computeVisibleTabs()` (6 tab visibility checks), `_canCreateForCurrentTab()` (6 create-action checks), `_DepartmentsTabState._canDelete`, `_OwnersTabState._canDelete`, `_TeachersTabState._canDelete`, `_TeachersTabState._canEdit`, `_StaffTabState._canDelete`, `_StaffTabState._canEdit`, `_StudentsTabState._canDelete`, `_GuardiansTabState._canEditGuardian`, `_GuardiansTabState._canUnlinkGuardian`, `_DepartmentDetailScreenState._canDeleteDept`, `_DepartmentDetailScreenState._canUpdateDept`. All now use only `perms.can(Resource.X, Action.Y)`. Removed unused `membership.dart` import.

**(school_roles_screen.dart)** Removed `|| entry is OwnerEntry` from `canCreate`, `canEdit`, `canDelete` in `_SchoolRolesBodyState.build()`. All now use only `perms.can(Resource.roles, Action.Y)`. Removed unused `membership.dart` import.

**(finance_screen.dart)** Removed `entry is OwnerEntry ||` from 7 permission checks: `_InvoiceListView.build()` (`canRecord`, `canEditInvoice`, `canDeleteInvoice`), `_PaymentsTab.build()` (`canApprove`, `canEdit`, `canDelete`), `_FeesTab.build()` (`canCreateFee`). All now use only `schoolContext.permissions.can(Resource.X, Action.Y)`. The `OwnerEntry()` case in the top-level `FinanceScreen.build()` switch statement is preserved — that's layout dispatch, not RBAC.

**(paper_detail_page.dart)** `_canProgressStatus`: removed `if (entry is OwnerEntry) return true` blanket bypass. Now uses `perms.can(Resource.exams, Action.update)` as the general check (covers any role including owners with proper permissions), plus teacher-specific logic (exam creator, invigilator, teaches subject) remains intact. `_canGradeContent`: simplified to `perms.can(Resource.grades, Action.read) || perms.can(Resource.grades, Action.mark)` — removed both `OwnerEntry` and `StaffEntry` blanket bypasses and teacher subject-matching (deeper data-scoping deferred to B08).

**(grade_detail_page.dart)** `_can()` helper: removed `entry is OwnerEntry ||` bypass. Now delegates directly to `widget.schoolContext.permissions.can(resource, action)`. Updated doc comment to reflect RBAC-only behavior.

Previous: Tasks A06, A07, A08 — Three fixes to `_itemsForRole()` in `school_dashboard_screen.dart` and one fix in `exams/exams_grades_screen.dart`:

**(A06)** Guardian Finance tab is now **always visible**. Previously gated by `perms.canAny(Resource.fees, [Action.read]) || perms.canAny(Resource.payments, [Action.read])`, which never passed because guardians typically have no roles/scopes and thus empty permissions. The entire guardian nav list is now `const` (no conditional items): `Overview | Progress | Timetable | Finance | Attendance | Announcements`.

**(A07)** Added `Attendance` tab (`Icons.fact_check_outlined`) to teacher always-visible core nav items. Teachers now see: `Overview | My Classes | Timetable | Attendance` as core items. Previously Attendance was missing entirely from the teacher nav.

**(A08 Part 1)** Teacher `Exams` tab moved from always-visible core to **permission-gated** section. Now requires `perms.canAny(Resource.exams, [Action.read])`. Teacher permission-gated items are now: `Exams | Academics | Members | Finance | Announcements | Roles`.

**(A08 Part 2)** `_canMarkGrades` getter in `_ExamGroupDetailViewState` (exams_grades_screen.dart) fixed. Previously: `entry is TeacherEntry || entry is OwnerEntry` — any teacher could mark grades regardless of permissions. Now: `OwnerEntry` still returns `true` (bypass), but all other roles check `permissions.can(Resource.grades, Action.mark)`. This ensures teachers without the `grades.mark` permission cannot edit grade data.

Task A05 — Fixed `StudentEntry` falling through to `_OwnerFinanceShell` in `finance_screen.dart`. The `FinanceScreen.build()` switch on `MembershipEntry` previously used a `_ =>` wildcard that routed unmatched entries (including `StudentEntry`) to the full admin finance shell. Replaced with an explicit `StudentEntry(:final student)` case that reuses `_GuardianFinanceView` with the student's own ADM and name, giving students a read-only view of their invoices and payments. The wildcard is removed entirely — the switch is now exhaustive over all 5 sealed subtypes (`OwnerEntry`, `StaffEntry`, `TeacherEntry`, `GuardianEntry`, `StudentEntry`), so adding a new subtype in the future will produce a compile error instead of a silent fallthrough.

Task D1 — Teacher "Academics" tab replaced with "My Classes" in core nav items. In `_itemsForRole()`, the teacher's always-visible "core 4" nav items changed from `Overview | Academics | Exams | Timetable` to `Overview | My Classes | Exams | Timetable`. `Academics` moved to the permission-gated section, visible only when `perms.canAny(Resource.classes, [Action.read])`. Icon changed from `Icons.menu_book_outlined` to `Icons.class_outlined` for the `My Classes` item. The `_buildContentPanel` method already handles both `'My Classes'` (routes to `MyClassesScreen`) and `'Academics'` (routes to `AcademicsScreen`), so no content routing changes were needed.

Tasks B1, B4, B5, E4, E5 — Five fixes to `school_dashboard_screen.dart` nav items and content panel routing:

**(B1)** Added `'Grades'` handler in `_buildContentPanel()` — routes to `ProgressScreen` (which already handles `StudentEntry` via `student.adm`). Students now see their grades instead of the "Coming soon" placeholder.

**(B4)** Added `'Finance'` nav item (`Icons.receipt_long_outlined`) to `MembershipRole.student` nav items. The existing `'Finance'` handler in `_buildContentPanel()` already routes to `FinanceScreen` for all roles. Note: `FinanceScreen` itself still falls through to `_OwnerFinanceShell` for `StudentEntry` via its wildcard `_ =>` case — a separate task should add a student-specific finance view there.

**(B5)** Removed `'Announcements'`, `'Finance'`, `'Members'`, and `'Students'` from `_kAcademicNavLabels`. The set now contains only truly academic items: `Academics`, `My Classes`, `Exams & Grades`, `Exams`, `Timetable`, `Attendance`, `Grades`, `Progress`. Non-academic sections (Announcements, Finance, Members) are no longer gated behind `NoTermsBlankState` when no terms exist.

**(E4)** Removed the redundant `'Students'` nav item from `MembershipRole.staff` nav items. The `'Members'` tab (which is already in the staff nav, permission-gated) includes a Students sub-tab, making a separate Students entry unnecessary and confusing.

**(E5)** Staff `'Announcements'` nav item changed from `const` (always visible) to permission-gated: `if (perms.canAny(Resource.announcements, [Action.read]))`. Matches the pattern used for teacher Announcements nav item.

Previous: Task A4 — `_initializeSession()` now merges system-scoped permissions (scopes where `school IS NULL`) with school-scoped permissions for `UserLevel.system` and `UserLevel.super_` users. This ensures System users who also hold school memberships get the union of both permission sets, per AGENT.md §17.

Previous: Task D1 — CONTEXT.md update for Staff Dashboard RBAC-Gating feature (Tasks A1, B1, C1).

Task A1 — Staff Members nav item condition expanded from checking only `Resource.teachers` and `Resource.students` to checking all 5 member resources: `departments`, `owners`, `teachers`, `staff`, `students`. This ensures a staff member with read permission on *any* member resource sees the Members nav item.

Task B1 — Permission-gated `_StaffOverview` and `_StaffQuickStats`. `_StaffQuickStats` now accepts `SchoolPermissions` and dynamically builds stat cards based on role permissions (students.read, teachers.read, staff.read, fees/payments.read, exams.read, classes.read). Added `_hasAnyStatPermission()` top-level helper. Entire Quick Stats section hidden when no stat permissions exist; "limited access" italic hint shown instead. New imports: `models/permissions.dart`, `models/school_permissions.dart`. `material.dart` import updated with `hide Action`.

Task C1 — Members page empty-tabs fallback removed and replaced with an empty state widget. `_tabController` (`TabController?`) and `_currentTab` (`_MemberTab?`) are now nullable to support the zero-tabs case. Empty state shows lock icon + "No member access" heading + explanation text. `_onTabChanged`, `_canCreateForCurrentTab`, and `dispose()` all guard against null.

Task 20 — Final CONTEXT.md sweep for question bank feature. Added `paper_pdf_viewer.dart` (`downloadAndOpenPdf`) entry. Updated `paper_generation_page.dart` from "Step 1 Complete" to ✅ Complete — all 3 steps (allocate, review & edit, finalize) now documented with `_ReviewQuestionCard`, `_EditPaperQuestionSheet`, regeneration support, and finalize flow. Updated `paper_detail_page.dart` description to document new **Generate Paper** button (pending status, `_PaperHeader`, navigates to `PaperGenerationPage`), **Print Paper** button (done/marked status, calls `downloadAndOpenPdf`, `_printBusy` loading state), `MarkingStatusIndicator` widget integration (shown above grades section, wired with `onComplete` callback to reload grades), and **View Breakdown** button in grade rows (`_GradeSpreadsheet` desktop + `_GradeList` mobile — opens `QuestionGradesSheet` via `showEduSheet` for AI-graded students). New imports: `marking_status_indicator.dart`, `paper_pdf_viewer.dart`, `question_grades_sheet.dart`.

Previous: Task 17 — Added `question_grades_sheet.dart` to `academics/`. `QuestionGradesSheet` is a self-contained bottom sheet (BUG-010 compliant via `EduSheet` wrapper) showing per-question AI marking breakdown for a student's paper. Fetches data via `questionBankService.getQuestionGrades(...)` in `initState`. Displays overall score bar (color-coded percentage badge + 4px progress bar) + list of `_QuestionCard` widgets (expandable question text, AI feedback, rubric results in data-table style with ✓/✗ icons). Loading/error/empty states handled. Private widgets: `_OverallScoreBar`, `_QuestionCard`, `_RubricTable`, `_RubricRow`. Helpers: `_pctColor`, `_fmtScore`.

Previous: Bug Group 1–3 fix batch — 7 tasks across 5 commits. **(1)** Teacher Attendance nav item removed from `_itemsForRole()` in `school_dashboard_screen.dart`. **(2)** `AttendanceScreen._ClassPickerShellState._loadConfig()` replaced from stub to real implementation using `CatalogDao.watchAllStreamsForSchool` + `_buildConfigFromStreams()`. `_gradeLabel()` now delegates to centralized `gradeLabel()` from `core/extensions.dart`. `_streamLabel()` fallback changed from `'Stream $streamCode'` to `gradeLabel(grade, config: config)`. **(3)** Raw stream-code fallbacks (`'Stream ${ct.stream}'`, `'Stream $stream'`, `'Stream ${key.stream}'`) replaced with empty strings in `my_classes_screen.dart` and `exam_detail_page.dart`. **(4)** `_AttendanceTabState` now overrides `didUpdateWidget` to re-resolve `_canMark` on stream/grade/year/term prop changes, with `debugPrint` diagnostic logging in `_resolveCanMark()` for OwnerEntry and TeacherEntry branches. **(5)** `_canManage` getter in `PaperDetailPage` split into `_canProgressStatus` (exam-level: exam creator, invigilator, exams.update permission, or teaches subject) and `_canGradeContent` (subject-specific: teaches subject to paper's grade/stream). **(6)** `_PaperHeader` gains `canGrade` prop — scheme button visibility gated by `canGrade` (subject-specific) instead of `canManage` (exam-level).

Task 2 (Bug 1) — ExamGroup consumers updated to use name-based grouping. `_examGroupName()` simplified to `group.name`. `_ExamGroupRowState.build()` `examName` simplified to `widget.group.name`. `_showEditExamName()` `currentName` simplified to `group.name`. `_ExamGroupDetailView` gains `onGroupKeyChanged: ValueChanged<String>?` callback — after a rename in `_showEditExamName`, the new group key (`$school|$year|$term|$newName`) is pushed to `_ExamsShellState` so the `StreamBuilder` matcher continues tracking the renamed group. `_CreatePaperSheetState._extendExamRange` call to `updateExamGroupDateRange` updated to pass `examName: group.name` instead of `type`/`oldStart`/`oldEnd`. DAO `updateExamGroupDateRange` signature changed: removed `type`, `oldStart`, `oldEnd` params; added `examName`; where clause now matches by `e.name.equals(examName)`.

Task 4 — `_ExamRow` in `exams_tab.dart` now shows `exam.name` as primary text (13.5px w500) above the date range, which was demoted to secondary (11px w400 muted with `cs.onSurfaceVariant` alpha 0.6).

UI polish pass — targeted updates across `school_dashboard_screen.dart`, `school_role_detail_screen.dart`, and `paper_detail_page.dart`. **(1)** `_UnifiedMobileTabBar` migrated from custom `GestureDetector` + `AnimatedContainer` tab indicators to `TabBar` + `TabController` with `BoxDecoration` indicator for a single smooth sliding animation; takes `TabController controller` parameter instead of `selectedIndex` + `onTabSelected`; `_buildTab` helper removed; dual-ripple issue eliminated. **(2)** `_initializeSession()` in `school_dashboard_screen.dart` now uses `parsePermissions` from `core/permission_parser.dart` instead of inline `jsonDecode` + `Permissions.fromJson` with a silent `catch`. **(3)** `school_role_detail_screen.dart`: diagnostic `debugPrint` logging added to `_PermissionsTabState._save()`, `_resetFromRole`, and `didUpdateWidget`; `updated` timestamps switched from seconds to milliseconds precision. **(4)** `paper_detail_page.dart`: `_MobileGradeEntrySheet` now wraps content in `EduSheet(title: student.name)` for BUG-010 compliance (self-contained sheet with background, handle, title, keyboard padding), layout compacted with tighter padding/gaps/compact input decorations, save button replaced with cancel + green save icon buttons per §21; `_openStudentActionSheet` now wraps content in `EduSheet(title: student.name)` + `SafeArea(top: false)` for BUG-010 compliance.

Previous:
Task 01 (unified-mobile-tab-bar) — Unified mobile tab bar across all dashboard roles. **Removed:** `_SimpleTabBar` widget (was teacher-only, solid `cs.primary` fill indicator) and `LoopingTabStrip` usage (was infinite-looping `ListView.builder` with icon+label Column for non-teacher roles). **Added:** `_UnifiedMobileTabBar` — a single text-only tab bar for all mobile roles, modeled on `_MobileDayStrip` from `timetable_screen.dart`. Design: outer container `height: 44`, `margin: EdgeInsets.symmetric(horizontal: 16, vertical: 6)`, `padding: EdgeInsets.all(3)`, `surfaceContainerHighest` background with alpha (0.6 dark / 0.45 light), `BorderRadius.circular(10)`, subtle box shadow. Each tab: `AnimatedContainer(duration: 180ms)`, selected `cs.surface` + box shadow, unselected transparent; text `fontSize: 12.5`, selected `w500 + cs.onSurface`, unselected `w400 + cs.onSurfaceVariant @ 0.7`. For ≤5 tabs: `Expanded` children in a `Row`; for >5 tabs: horizontally scrollable via `SingleChildScrollView` with 80px fixed-width tabs. Uses simple index callback (`onTabSelected`) that calls `_selectIndex` → `_tabController.animateTo(index)`. **Tab renames:** Owner `'Exams & Grades'` → `'Exams'`; Staff `'Exams & Grades'` → `'Exams'`. **Import removed:** `ui/widgets/looping_tab_strip.dart` no longer imported. The `LoopingTabStrip` widget file remains in `lib/ui/widgets/` (not deleted — may be used elsewhere or in the future).

Previous: 
Task 01 (paper-layout) — Four targeted layout fixes in `exams_grades_screen.dart`: (1) `_StatusChip` Container now has `constraints: BoxConstraints(minWidth: 76)` + child Text has `textAlign: TextAlign.center` so all status labels render at a consistent minimum width. (2) `_PaperSlotCard.build` overhauled — Container gains `constraints: BoxConstraints(minHeight: 68)`; left Column gains `mainAxisAlignment: MainAxisAlignment.center`; subject Text wrapped in `Expanded` with `maxLines: 1, overflow: ellipsis`; invigilator row always rendered with "—" fallback (no conditional guard); right Column gains `mainAxisAlignment: MainAxisAlignment.center`; chevron changed to `Icons.chevron_right_rounded`. (3) `_PaperSlotBox` Container changed from `BoxConstraints(minHeight: 56)` to `BoxConstraints(minHeight: 68, maxHeight: 68)`; subject Text `maxLines: 2` changed to `maxLines: 1`. (4) `_PaperEmptyCell` Container changed from `BoxConstraints(minHeight: 56)` to `BoxConstraints(minHeight: 68, maxHeight: 68)`. All paper card heights are now standardised to 68px min across both the desktop cross-table grid (`_PaperSlotBox`/`_PaperEmptyCell`) and the mobile list view (`_PaperSlotCard`). Previous: Task 02 (paper-layout) — Three targeted layout fixes in `exam_detail_page.dart`: (1) `_PaperStatusChip` now has `constraints: BoxConstraints(minWidth: 72)` + `textAlign: TextAlign.center` so all status labels ("Pending", "In Progress", "Done", "Marked") render at the same minimum width; (2) `_PaperTimetableCard` inner Container now has `constraints: BoxConstraints(minHeight: 60)` + subject Text changed to `maxLines: 1` for consistent mobile card heights; (3) `_PapersCrossTable` desktop cells (both empty placeholder and `_buildCell` filled cards) changed from `BoxConstraints(minHeight: 56)` to `BoxConstraints(minHeight: 68, maxHeight: 68)` for a fixed-height grid, and subject Text in `_buildCell` changed to `maxLines: 1`. Previous: Task 03 — Lessons tab redesigned in `timetable_screen.dart`. **`_LessonsTab` changed:** `StatelessWidget` → `StatefulWidget`, new `config: SchoolConfig` param added, call site updated with `config: _config ?? SchoolConfig.defaults()`. **New widgets:** `_LessonStatsBar`, `_LessonStatChip`, `_LessonViewSwitcher`, `_LessonViewChip`, `_LessonLogView`, `_LessonDayHeader`, `_LessonLogRow`, `_LessonTeachersView` / `_LessonTeachersViewState`, `_TeacherLessonSubRow`, `_LessonCoverageView`, `_LessonsEmptyState`. **New helpers:** `_lessonDayLabel(int)`, `_lessonClassLabel(int, int, SchoolConfig)`. **New enum:** `_LessonView { log, teachers, coverage }`. **Removed:** `_LessonDateHeader`, `_LessonRow`, `_EmptyLessonsState`. Previous: Task 02 — Generate Lessons UI feature added to `timetable_screen.dart`. **New widgets:** `_GenerateLessonsFab`, `_GeneratedLesson`, `showGenerateLessonsDialog`, `_GenerateLessonsDialog` / `_GenerateLessonsDialogState`, `_ScopeOptionCard` / `_ScopeOptionCardState`, `_PreviewDateHeader`, `_PreviewLessonItem`, `_showSubstitutePickerDialog`, `_SubstitutePickerDialog` / `_SubstitutePickerDialogState`. **Modified:** `_OwnerTimetableShellState.build()` FAB — now shows `_GenerateLessonsFab` + delete when `_hasTimetable`, `_GenerateFab` when no timetable. Added `_openGenerateLessonsDialog()` method. Previous: Task TW-09 — General polish: border radius audit, desktop height cap, icon fix, responsiveness verification.

**Changes in TW-09:** Desktop wizard height cap added (`ConstrainedBox maxHeight: screenHeight - 80` wrapping root `Column`). Close icon in wizard header fixed (`Icons.close` → `Icons.close_rounded`). Border radius audit confirmed clean — all dialog containers use `BorderRadius.circular(8)`, mobile sheet top-corners use `AppTheme.kModalRadius` (12). All three `ToggleButtons` groups confirmed wrapped in `SingleChildScrollView(scrollDirection: Axis.horizontal)`. Stage 3 `ReorderableListView.builder` confirmed `shrinkWrap: true` + `NeverScrollableScrollPhysics`. Full integration checklist passed.

**Changes in TW-08:** Added Stage 3 "Remainder Slots" (`_Stage3RemainderSlots`/`_Stage3RemainderSlotsState`) — collapsible grade → stream → subject priority reordering with `ReorderableListView`, `_RemainderSubjectTile` (drag handle + name + `+1` badge), `_computeRemainder` helper. Stage count changed from 4 → 5; old Stage 3 Generate is now Stage 4; step dots `totalSteps` updated to 5. `_buildStage` switch updated; `_goNext` triggers `_computeConflicts` at stage 3 (was 2). Stage labels updated to include `'Remainder Slots'`.

Tasks F05, F06, F07, F08 — UI polish and finance fixes in `finance_screen.dart`:

**(F05 — FAB border radius)** `finance_screen.dart`: Fees tab FAB `BorderRadius.circular(12)` changed to `BorderRadius.circular(AppTheme.kCardRadius)` (8.0) per UI guidelines §21.

**(F07 — Guardian payment date)** `finance_screen.dart`: `_GuardianPaymentSheetState._save()` now passes `date: DateTime.now().millisecondsSinceEpoch ~/ 86400000` (days since epoch) to `dao.recordPayment()`. Previously the date was omitted entirely.

**(F08 — Finance amount validation)** `finance_screen.dart`: Amount field validators in `_GuardianPaymentSheet`, `_CreateFeeSheet`, and `_RecordPaymentSheet` updated with more descriptive error messages: "Amount is required" (was "Required"), "Enter a valid number" (new — parse failure now separated from range check), "Amount must be greater than zero" (was "Must be > 0"). All three sheets already had basic validation; messages are now user-friendly and the invalid-number / non-positive cases are distinguished.
